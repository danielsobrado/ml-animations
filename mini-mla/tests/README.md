# mini-mla tests

The exercises keep tests next to each binary so learners can run one file at a time:

```bash
cargo test --bin 01_kv_cache_size
```

The full pack is:

```bash
cargo test --bins
```
