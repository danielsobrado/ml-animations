function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const registryRotation = stableHash(`bert:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const SPECS = [
  ['bert-001-name', 'Foundation', 'What does BERT stand for in this lesson?', 'Bidirectional Encoder Representations from Transformers', ['Basic Encoder for Recurrent Translation', 'Backward Estimator for Ranked Tokens'], 'The name highlights the two key ideas: encoder representations and bidirectional context from transformer layers.'],
  ['bert-002-purpose', 'Foundation', 'What is the central problem BERT is designed to solve?', 'Build contextual representations for language understanding tasks', ['Generate long text one token at a time', 'Store a fixed dictionary definition for every word'], 'BERT is strongest when the full input is available and the model needs a representation for classification, extraction, or token labeling.'],
  ['bert-003-family', 'Foundation', 'Which transformer family does BERT use?', 'Encoder-only transformer architecture', ['Decoder-only autoregressive architecture', 'Encoder-decoder translation architecture'], 'The lesson places BERT in the encoder-only family, unlike GPT-style decoders and T5-style encoder-decoder systems.'],
  ['bert-004-direction', 'Foundation', 'What does bidirectional context mean for BERT attention?', 'A token can use both left and right context in the same input', ['A token can only inspect earlier prefix tokens', 'A token is encoded without any surrounding words'], 'BERT self-attention is not causal during encoding, so each token representation can condition on surrounding tokens.'],
  ['bert-005-tokenization', 'Foundation', 'What tokenization style does the BERT lesson emphasize?', 'WordPiece tokenization with subword pieces', ['Byte-pair merges only for decoder sampling', 'Character-only encoding with no vocabulary ids'], 'The tokenization panel shows text moving from basic tokens into WordPiece subwords before vocabulary ids are used.'],
  ['bert-006-special-tokens', 'Foundation', 'Which special-token pattern represents a single BERT input sentence?', '[CLS] sentence tokens [SEP]', ['[BOS] sentence tokens [EOS] [GEN]', '[START] sentence tokens [STOP] [MASK]'], 'BERT adds [CLS] at the beginning and [SEP] at the end so the encoder receives task-aware boundaries.'],
  ['bert-007-pair-format', 'Foundation', 'How does BERT format a sentence-pair input?', '[CLS] sentence A [SEP] sentence B [SEP]', ['sentence A [MASK] sentence B [PAD] [PAD]', '[SEP] sentence A [CLS] sentence B [MASK]'], 'The pair format lets BERT encode both sentences together while segment embeddings mark which tokens belong to each sentence.'],
  ['bert-008-input-embeddings', 'Foundation', 'Which three components are summed to form BERT input embeddings?', 'Token, segment, and position embeddings', ['Query, key, and value embeddings', 'Word, label, and decoder embeddings'], 'The embeddings panel shows the final input as an elementwise sum of token identity, sentence segment, and learned position vectors.'],
  ['bert-009-position-embeddings', 'Foundation', 'What kind of position embeddings does the BERT lesson describe?', 'Learned position embeddings up to a fixed maximum length', ['Sinusoidal-only positions with unlimited extrapolation', 'No position signal because attention is bidirectional'], 'BERT uses learned position vectors, and the practice panel notes the common 512-position limit.'],
  ['bert-010-segment-embeddings', 'Foundation', 'What do segment embeddings tell BERT?', 'Whether a token belongs to sentence A or sentence B', ['Whether a token should be masked during softmax', 'Whether a token is a noun or a verb'], 'Segment embeddings support sentence-pair tasks and NSP-style pretraining by distinguishing the two input parts.'],
  ['bert-011-base-size', 'Foundation', 'What is the BERT-Base shape shown in the lesson?', '12 layers, 768 hidden size, and 12 attention heads', ['6 layers, 512 hidden size, and 8 attention heads', '24 layers, 1024 hidden size, and 16 attention heads'], 'BERT-Base is the 110M-parameter configuration used throughout the architecture and calculator panels.'],
  ['bert-012-large-size', 'Foundation', 'What is the BERT-Large shape referenced by the practice check?', '24 layers, 1024 hidden size, and 16 attention heads', ['12 layers, 768 hidden size, and 12 attention heads', '48 layers, 2048 hidden size, and 32 attention heads'], 'The larger configuration doubles the layer count and increases hidden size and heads, raising quality potential and cost.'],
  ['bert-013-head-dim', 'Foundation', 'In BERT-Base, why do 12 heads of 64 dimensions match the hidden size?', '12 heads times 64 dimensions equals 768 hidden channels', ['12 heads times 12 layers equals 144 vocabulary ids', '64 layers times 768 heads equals the sequence length'], 'The attention panel shows each head using 64 dimensions, then concatenating heads back to the 768-dimensional stream.'],
  ['bert-014-mlm', 'Foundation', 'What is masked language modeling in BERT pretraining?', 'Predicting original tokens at selected masked positions from context', ['Predicting only the next token after a prefix', 'Classifying the final deployed task label from scratch'], 'MLM trains BERT to infer hidden tokens using both sides of the sentence.'],
  ['bert-015-mask-rate', 'Foundation', 'What fraction of tokens are selected for BERT MLM prediction?', '15 percent of tokens are selected', ['Half of every sentence is selected', 'Every token is selected at every step'], 'The pretraining panel states that 15 percent of tokens are chosen for the MLM target.'],
  ['bert-016-mask-mix', 'Foundation', 'What happens to selected MLM tokens under the BERT masking recipe?', '80 percent become [MASK], 10 percent become random, and 10 percent stay unchanged', ['All selected tokens become [MASK] with no exceptions', 'Selected tokens are always deleted from the sequence'], 'The 80/10/10 mixture reduces the mismatch between pretraining inputs and fine-tuning inputs.'],
  ['bert-017-nsp', 'Foundation', 'What does next sentence prediction ask BERT to classify?', 'Whether sentence B follows sentence A or is a random sentence', ['Which token should be generated after sentence B', 'Which attention head has the largest norm'], 'NSP uses paired inputs and the [CLS] representation to learn a sentence relationship signal.'],
  ['bert-018-cls', 'Foundation', 'Why is the [CLS] token important for BERT classification tasks?', 'Its final hidden state can summarize the input for a task head', ['It replaces every unknown WordPiece token', 'It stores the vocabulary id for the answer span'], 'The tasks and fine-tuning panels use the [CLS] output for sequence-level decisions.'],
  ['bert-019-finetune', 'Foundation', 'What is the basic BERT fine-tuning pattern?', 'Load pretrained weights, add a task head, and train on labeled task data', ['Throw away the encoder and train only a tokenizer', 'Use the MLM objective at inference for every user request'], 'Fine-tuning adapts a pretrained encoder to a task such as sentiment, NER, QA, or sentence-pair classification.'],
  ['bert-020-task-fit', 'Foundation', 'Which task family is the most natural first fit for BERT?', 'Natural language understanding with a complete input available', ['Open-ended autoregressive chat generation', 'Image denoising without text tokens'], 'BERT is an understanding encoder; generation can be built around encoders, but BERT itself is not a decoder-first text generator.'],
  ['bert-021-basic-tokenization', 'Mechanism', 'What happens before WordPiece tokenization in the lesson flow?', 'Raw text is split by whitespace and punctuation into basic tokens', ['Every character is immediately assigned a class label', 'The encoder layers choose token boundaries from attention weights'], 'The tokenization panel first shows basic token splitting, then subword handling, special tokens, and ids.'],
  ['bert-022-wordpiece-unknown', 'Mechanism', 'Why does WordPiece break rare words into subwords?', 'It lets the model represent unseen or rare surface forms with known pieces', ['It makes every sentence exactly 512 tokens long', 'It prevents the model from using position embeddings'], 'Subword tokenization balances vocabulary size with coverage of rare and morphologically varied words.'],
  ['bert-023-continuation-mark', 'Mechanism', 'What does a continuation marker such as ## usually indicate in WordPiece output?', 'The piece continues a word that began with an earlier token', ['The token starts a new segment embedding', 'The token should be ignored by every task head'], 'The lesson shows words such as playing becoming a stem plus a continuation piece.'],
  ['bert-024-vocab-ids', 'Mechanism', 'What is the final output of tokenization before embeddings?', 'Vocabulary ids for the token sequence', ['Attention probabilities for each head', 'Fine-tuned class logits for the task'], 'After special tokens are added, BERT maps each token to an integer id so embedding lookup can begin.'],
  ['bert-025-embedding-sum', 'Mechanism', 'Why are token, segment, and position embeddings added instead of run as separate inputs?', 'The encoder consumes one vector per token position', ['The decoder requires three independent output streams', 'The MLM loss can only read position vectors'], 'Adding the components creates a single hidden vector that carries identity, sentence membership, and location information.'],
  ['bert-026-single-segment', 'Mechanism', 'What segment setting is typical for a one-sentence BERT input?', 'All real tokens can use the sentence A segment id', ['Tokens alternate A and B at every position', 'Every punctuation mark receives a new segment id'], 'Segment embeddings still exist for single-sentence inputs, but they do not need to separate two sentences.'],
  ['bert-027-qkv-projection', 'Mechanism', 'What do the Q, K, and V projections do in a BERT attention head?', 'They map hidden vectors into query, key, and value spaces for one head', ['They choose which WordPiece tokens enter the vocabulary', 'They compute the final sentiment label directly'], 'Each head projects the 768-dimensional stream into smaller attention subspaces before scoring relationships.'],
  ['bert-028-attention-score', 'Mechanism', 'Which formula describes the attention score step in the BERT panel?', 'QK^T divided by sqrt(d_k)', ['Q plus K plus V divided by vocabulary size', 'Softmax of labels divided by sequence length'], 'Scaled dot-product attention compares queries with keys and scales by the head dimension.'],
  ['bert-029-softmax-weights', 'Mechanism', 'What does softmax do to attention scores for one query token?', 'It converts scores into weights that sum to one across attended tokens', ['It removes every right-context token from the row', 'It turns subword ids back into plain English words'], 'After scoring, BERT uses normalized weights to decide how much information to aggregate from each value vector.'],
  ['bert-030-value-sum', 'Mechanism', 'What is produced by the weighted sum of values in self-attention?', 'A context-mixed vector for the query token', ['A new vocabulary table for the whole model', 'A binary NSP label with no further layers'], 'The value aggregation step combines information from other positions according to attention weights.'],
  ['bert-031-head-concat', 'Mechanism', 'What happens after BERT-Base computes outputs from its 12 attention heads?', 'The head outputs are concatenated and projected back to hidden size', ['Only the largest head is kept and all others are discarded', 'The heads are averaged into a single token id'], 'Multi-head attention keeps several relationship views, then merges them back into the main hidden stream.'],
  ['bert-032-attention-patterns', 'Mechanism', 'Why does the attention panel show different representative heads?', 'Different heads can specialize in position, [CLS] focus, syntax, or self patterns', ['All heads are required to learn identical diagonal matrices', 'Heads exist only to make the parameter count easier to divide'], 'The visualization teaches that multiple heads can capture different token relationships in parallel.'],
  ['bert-033-residual-norm-one', 'Mechanism', 'What follows multi-head self-attention inside a BERT encoder layer?', 'A residual addition and layer normalization step', ['A tokenizer reset and vocabulary rebuild', 'A decoder cross-attention block'], 'The encoder layer uses add-and-normalize after attention before moving to the feed-forward network.'],
  ['bert-034-ffn', 'Mechanism', 'What does the BERT feed-forward network do at each position?', 'Applies two linear layers with GELU, expanding then returning hidden size', ['Mixes tokens across positions with a causal mask', 'Chooses the final answer span before attention'], 'The lesson shows the BERT-Base FFN as 768 to 3072 to 768 with GELU activation.'],
  ['bert-035-residual-norm-two', 'Mechanism', 'What follows the feed-forward network in the encoder block?', 'Another residual addition and layer normalization step', ['A random masking pass over 15 percent of inputs', 'A switch from encoder mode into decoder-only mode'], 'BERT repeats the attention sublayer and feed-forward sublayer pattern with residual paths and normalization.'],
  ['bert-036-stack', 'Mechanism', 'What does stacking encoder layers give BERT?', 'Repeated refinement of contextual token representations', ['A longer learned vocabulary after every layer', 'A guarantee that no downstream fine-tuning is needed'], 'BERT-Base stacks 12 encoder blocks and BERT-Large stacks 24, deepening contextual representation.'],
  ['bert-037-output-per-token', 'Mechanism', 'What does BERT output after encoding a sequence?', 'A contextual hidden vector for every input token position', ['Only the next generated token id', 'Only a single attention matrix with no hidden states'], 'Task heads decide how to use the final token states, such as [CLS] for classification or per-token states for NER.'],
  ['bert-038-pooler', 'Mechanism', 'What role does a pooled [CLS] representation play?', 'It gives sequence-level heads a compact input', ['It replaces the WordPiece vocabulary during pretraining', 'It forces all tasks to predict token spans'], 'The [CLS] output is commonly transformed and passed to sequence-level classifiers.'],
  ['bert-039-mlm-context', 'Mechanism', 'Why can MLM use both sides of a masked word?', 'The masked position is encoded with bidirectional surrounding context', ['The model secretly reads the original token id as an input', 'The decoder generates the sentence from right to left only'], 'BERT masks selected tokens and predicts them from the other visible tokens in the sequence.'],
  ['bert-040-mlm-loss-scope', 'Mechanism', 'Where is the MLM cross-entropy loss computed?', 'Only at the selected prediction positions', ['At every padding token whether selected or not', 'Only on the [CLS] token for every sequence'], 'The pretraining panel explicitly notes that MLM loss is for masked or selected positions, not every input token.'],
  ['bert-041-mask-mismatch', 'Mechanism', 'Why does BERT leave some selected MLM tokens unchanged or random?', 'To reduce pretrain-finetune mismatch from always showing [MASK]', ['To make NSP impossible for sentence pairs', 'To convert BERT into a causal generator'], 'Fine-tuning inputs usually do not contain [MASK], so the 80/10/10 rule makes pretraining less dependent on that marker.'],
  ['bert-042-nsp-sampling', 'Mechanism', 'How are NSP training pairs sampled in the lesson?', 'Half consecutive pairs and half random second sentences', ['All pairs are translations of each other', 'Only pairs with identical words are used'], 'The panel describes 50 percent IsNext and 50 percent NotNext examples for sentence relationship classification.'],
  ['bert-043-nsp-classifier', 'Mechanism', 'Which representation is used to classify the NSP relationship?', 'The [CLS] token output from the paired input', ['The last [SEP] token id before embedding lookup', 'The largest WordPiece continuation token'], 'NSP is a sequence-pair classification task, so it uses the sequence-level [CLS] representation.'],
  ['bert-044-finetune-head', 'Mechanism', 'What does a task-specific fine-tuning head change?', 'It maps BERT hidden states to the task output format', ['It changes WordPiece into byte-pair encoding', 'It removes all pretrained encoder weights'], 'Different heads turn the same encoder into a classifier, token labeler, answer-span predictor, or sentence-pair scorer.'],
  ['bert-045-end-to-end', 'Mechanism', 'What does the fine-tuning panel mean by training end to end?', 'The task head and pretrained BERT layers are updated together', ['Only segment embeddings are updated while all other weights vanish', 'The model is evaluated without any labeled examples'], 'BERT fine-tuning usually updates the whole model with a small learning rate, not only the new output layer.'],
  ['bert-046-qa-head', 'Mechanism', 'How does extractive question answering use BERT outputs?', 'Predict start and end positions inside the context passage', ['Generate an answer that can ignore the passage tokens', 'Classify every token as sentence A or sentence B'], 'The QA task panel frames BERT as selecting an answer span from the provided context.'],
  ['bert-047-ner-head', 'Mechanism', 'How does named entity recognition use BERT outputs?', 'Attach a token-level classifier to contextual states', ['Pool [CLS] and discard all token states before labeling', 'Run NSP between every pair of adjacent words'], 'NER needs a label per token or subword, so it uses per-position representations rather than only sequence pooling.'],
  ['bert-048-sentence-pair', 'Mechanism', 'What input design supports NLI or sentence similarity with BERT?', 'Encode both sentences in one sequence with [SEP] boundaries and segment ids', ['Encode sentence B with a separate decoder and ignore sentence A', 'Mask every token in sentence A and train only on sentence B'], 'Joint encoding lets tokens in both sentences attend to each other while segment embeddings preserve sentence identity.'],
  ['bert-049-padding-mask', 'Mechanism', 'Why does a BERT batch need padding masks?', 'So padding positions do not contribute as meaningful context', ['So real tokens cannot attend bidirectionally', 'So [CLS] is removed from classification tasks'], 'Padding makes sequences equal length for batching, but attention should ignore padded positions.'],
  ['bert-050-param-drivers', 'Mechanism', 'Which parts dominate BERT parameter count in the calculator?', 'Embedding tables plus repeated attention and feed-forward layer weights', ['Only the visible quiz questions in the practice panel', 'Only the final softmax over two NSP labels'], 'The calculator sums embeddings, QKV and output projections, feed-forward weights, layer norms, and pooler parameters.'],
  ['bert-051-sentiment-fit', 'Application', 'A team needs sentiment classification for complete customer reviews. Why is BERT a good candidate?', 'It can encode the full review and classify the [CLS] representation', ['It is optimized to stream each next word to the user', 'It requires no labeled validation set for deployment'], 'Sentiment is a sequence-level understanding task, which matches BERT plus a classification head.'],
  ['bert-052-ner-fit', 'Application', 'A support system must identify product names and locations in tickets. Which BERT setup fits?', 'Token classification over contextual token states', ['NSP over random ticket pairs only', 'A single decoder-only next-token head'], 'Named entity recognition needs per-token labels and benefits from context around each entity mention.'],
  ['bert-053-extractive-qa', 'Application', 'A search product needs answers copied from a supplied paragraph. Which BERT output should it train?', 'Start and end span logits over the context tokens', ['A free-form continuation sampled until [SEP]', 'A vocabulary id for the document title only'], 'Extractive QA with BERT chooses a span inside the encoded context rather than generating arbitrary new text.'],
  ['bert-054-generation-choice', 'Application', 'A product needs open-ended multi-turn text generation. What should the architecture choice note?', 'A decoder-style model is the more natural baseline', ['BERT is automatically best because it is bidirectional', 'NSP is enough to generate every reply token'], 'BERT can support understanding components, but decoder-only models are built for autoregressive generation.'],
  ['bert-055-long-docs', 'Application', 'A legal document is much longer than 512 tokens. What is the safest BERT plan?', 'Chunk or use a long-context variant before relying on vanilla BERT', ['Send the whole document because learned positions extrapolate forever', 'Delete all [SEP] tokens so more words fit without tradeoffs'], 'Standard BERT position embeddings have a fixed maximum length, so long documents need segmentation or a model designed for longer contexts.'],
  ['bert-056-similarity', 'Application', 'For semantic similarity between two sentences, how should the input usually be formatted?', 'Joint sentence-pair format with [CLS] and [SEP] tokens', ['Two unrelated batches with no shared encoder pass', 'Only the second sentence followed by [MASK] tokens'], 'Sentence-pair tasks are one of the reasons BERT has segment embeddings and paired-input formatting.'],
  ['bert-057-domain-data', 'Application', 'A hospital wants clinical entity extraction with limited labels. What makes BERT useful?', 'Pretraining supplies language features that can be adapted with supervised fine-tuning', ['The base model already guarantees clinical labels with no evaluation', 'The tokenizer removes the need to inspect domain errors'], 'Transfer learning can reduce label needs, but domain-specific validation is still required.'],
  ['bert-058-bank-example', 'Application', 'In the bank disambiguation example, why does BERT handle both meanings better than static embeddings?', 'The representation changes with the surrounding sentence context', ['The word bank always receives one fixed vector no matter the sentence', 'The model chooses a meaning from the answer choices before encoding'], 'BERT builds contextual representations, so river bank and money bank contexts can yield different hidden states.'],
  ['bert-059-latency-choice', 'Application', 'A mobile app has tight latency and acceptable accuracy with the smaller model. What choice follows the lesson tradeoff?', 'Prefer BERT-Base or a compressed variant after validation', ['Always deploy BERT-Large because it has more layers', 'Ignore latency because encoder-only models have no cost'], 'Model size changes latency and memory; the larger configuration is not automatically the best deployment choice.'],
  ['bert-060-cls-classifier', 'Application', 'A topic classifier uses one label for the whole document chunk. Which representation is the natural input?', 'The final hidden state of [CLS]', ['The raw id of the first WordPiece after [SEP]', 'The attention score from one arbitrary head'], 'Sequence classification commonly uses the [CLS] representation as the pooled input to a classifier head.'],
  ['bert-061-subword-labels', 'Application', 'A NER dataset labels words, but BERT splits Hawthorne into Haw and ##thorne. What must the pipeline handle?', 'Align word labels to subword pieces consistently', ['Drop every split word from the training set', 'Train NSP labels for the two subwords'], 'Token classification needs a policy for first pieces, continuation pieces, and label aggregation back to words.'],
  ['bert-062-no-answer', 'Application', 'A QA example asks for information absent from the passage. What should an extractive BERT system check?', 'Whether the task design includes a no-answer option or rejection rule', ['Whether the answer can be invented outside the context', 'Whether [MASK] can replace the missing evidence at inference'], 'Extractive QA predicts spans from context; production systems need handling for unanswerable questions.'],
  ['bert-063-pair-classification', 'Application', 'For entailment between two claims, what BERT design is most aligned?', 'Use sentence-pair classification over the joint encoded pair', ['Use token classification on only the second claim', 'Use MLM to predict every hidden label word'], 'NLI is a sentence-pair classification task, so joint encoding and a [CLS] classifier fit naturally.'],
  ['bert-064-heldout-eval', 'Application', 'After fine-tuning BERT on a task dataset, what evidence is needed before deployment?', 'Held-out evaluation on examples that match the target use case', ['A high score on the built-in lesson quiz only', 'The fact that the pretrained model has 110M parameters'], 'Fine-tuning can overfit or miss domain-specific failures, so held-out task evaluation is required.'],
  ['bert-065-small-data', 'Application', 'Why can BERT fine-tuning work with less labeled data than training from scratch?', 'The pretrained encoder already captures broad language patterns', ['The classifier head contains all world knowledge before training', 'The [SEP] token automatically supplies labels'], 'Pretraining on unlabeled text gives useful representations that supervised fine-tuning can adapt.'],
  ['bert-066-learning-rate', 'Application', 'Why does the fine-tuning panel recommend small learning rates?', 'Large updates can damage pretrained representations during adaptation', ['Small learning rates make tokenization unnecessary', 'The model cannot update more than one layer at a time'], 'Fine-tuning updates many pretrained weights, so careful optimization helps preserve useful features.'],
  ['bert-067-search-rerank', 'Application', 'A search engine reranks candidate passages for a query. Why might BERT help?', 'It can jointly encode query and passage to judge relevance', ['It can decode an unlimited stream of search results', 'It guarantees retrieval recall before ranking starts'], 'BERT-style cross-encoding is strong for understanding relationships between two text sequences.'],
  ['bert-068-padding-batch', 'Application', 'A training batch contains sentences of different lengths. What implementation detail matters?', 'Pad to a common length and mask padding during attention', ['Remove [CLS] from short examples only', 'Treat [PAD] as a high-confidence answer span'], 'Batched encoders need padding for shape consistency, but padding should not become semantic context.'],
  ['bert-069-task-head-choice', 'Application', 'Which task head should be chosen for per-token part-of-speech labels?', 'A linear classifier applied to each token representation', ['A single pooled classifier on [CLS] only', 'An NSP classifier over sentence order'], 'Part-of-speech tagging is token classification, so each token state needs a label distribution.'],
  ['bert-070-span-boundary', 'Application', 'A QA model predicts the right answer text but with one extra token. What should error analysis inspect?', 'Start and end boundary logits around the context span', ['Only the segment id assigned to [CLS]', 'Only whether the sentence pair was random'], 'Extractive QA errors often come from boundary selection, not from a missing generated vocabulary item.'],
  ['bert-071-wordpiece-domain', 'Application', 'A domain corpus has many rare chemical names. What tokenization issue should be monitored?', 'Long subword splits can make terms harder to label and truncate', ['WordPiece will always create one token per chemical name', 'Rare words bypass the embedding table entirely'], 'Subword coverage prevents unknown words, but excessive splitting can affect sequence length and token-level labeling.'],
  ['bert-072-base-large', 'Application', 'Validation shows BERT-Large is slightly better but too slow. What is the practical next step?', 'Compare the accuracy gain against latency, memory, and cost budgets', ['Ship the larger model because parameter count settles the decision', 'Remove validation because speed is an implementation detail'], 'The model-size table is meant to connect architecture scale to real deployment tradeoffs.'],
  ['bert-073-mlm-limits', 'Application', 'A team expects MLM pretraining alone to make an instruction-following assistant. What should you tell them?', 'MLM learns representations, while instruction following needs different supervised or preference signals', ['MLM is identical to chat fine-tuning once [MASK] is hidden', 'NSP automatically teaches multi-turn assistant behavior'], 'The previous objective lesson and BERT lesson separate representation pretraining from later behavior-shaping objectives.'],
  ['bert-074-head-reuse', 'Application', 'A pretrained sentiment BERT must become a QA model. What usually changes?', 'Replace the sequence classifier with a span-prediction head and fine-tune', ['Keep the sentiment labels and relabel [MASK] tokens as spans', 'Delete the encoder layers and keep only WordPiece'], 'Task heads determine the output format, so a QA system needs start and end predictions rather than one sentiment class.'],
  ['bert-075-domain-gap', 'Application', 'When is generic BERT likely insufficient without adaptation?', 'When the target domain has specialized language and failure costs', ['When the task has any complete input sentence', 'When the sequence contains both [CLS] and [SEP]'], 'Pretrained BERT is a strong starting point, but domain language and risk level decide whether further fine-tuning or evaluation is needed.'],
  ['bert-076-trap-left-only', 'Tricky', 'Which BERT statement is false after the attention lesson?', 'BERT reads only left-to-right like a causal decoder', ['BERT builds contextual token representations', 'BERT uses an encoder-only transformer stack'], 'BERT uses bidirectional self-attention over the available input, unlike a causal decoder.'],
  ['bert-077-trap-decoder', 'Tricky', 'Which architecture claim should be rejected for BERT?', 'BERT is a decoder-only model for prefix generation', ['BERT belongs to the encoder-only family', 'BERT is suited to many understanding tasks'], 'The lesson repeatedly frames BERT as an encoder-only model, not a GPT-style decoder.'],
  ['bert-078-trap-two-pass', 'Tricky', 'Which bidirectionality explanation is misleading?', 'BERT needs two separate recurrent passes to combine directions', ['BERT self-attention can see tokens on both sides', 'BERT representations depend on surrounding context'], 'Bidirectional context comes from non-causal self-attention inside transformer encoder layers.'],
  ['bert-079-trap-cls-magic', 'Tricky', 'Which [CLS] interpretation is unsafe?', '[CLS] contains the final label before the encoder runs', ['[CLS] receives a contextual hidden state after encoding', '[CLS] can feed a sequence-level classifier'], 'The [CLS] token is an input position whose final representation is learned through the encoder, not a prefilled answer.'],
  ['bert-080-trap-mlm-all', 'Tricky', 'What is wrong about saying MLM trains on every token equally?', 'The MLM loss is computed only for selected prediction positions', ['Visible tokens provide context for selected predictions', 'Masked positions are predicted with cross-entropy'], 'Unselected tokens help form context, but they are not all direct MLM targets in the loss.'],
  ['bert-081-trap-all-mask', 'Tricky', 'Which masking statement misses the BERT pretraining detail?', 'Every selected MLM token is replaced by [MASK]', ['Most selected tokens become [MASK]', 'Some selected tokens stay unchanged or become random'], 'The actual recipe uses 80 percent [MASK], 10 percent random, and 10 percent unchanged among selected positions.'],
  ['bert-082-trap-nsp-next-token', 'Tricky', 'Which NSP claim confuses objectives?', 'NSP is the same as next-token prediction', ['NSP is a binary sentence relationship task', 'NSP uses paired sentences with [SEP] separators'], 'Next sentence prediction classifies whether B follows A, while next-token prediction trains autoregressive continuation.'],
  ['bert-083-trap-segment-position', 'Tricky', 'Which embedding claim should be corrected?', 'Segment embeddings are the same thing as position embeddings', ['Segment embeddings mark sentence membership', 'Position embeddings mark token order'], 'BERT sums both components, but they encode different information.'],
  ['bert-084-trap-head-only', 'Tricky', 'Which fine-tuning shortcut is usually wrong for this lesson?', 'Fine-tuning always trains only the new task head', ['Fine-tuning can update BERT and the task head together', 'A task head maps hidden states to task outputs'], 'The panel describes end-to-end fine-tuning with a small learning rate.'],
  ['bert-085-trap-generator', 'Tricky', 'Which product assumption is dangerous?', 'BERT is naturally a free-form autoregressive generator', ['BERT can support classification and extraction', 'Decoder-style models fit open-ended generation better'], 'BERT is an encoder for understanding; autoregressive generation is not its native design.'],
  ['bert-086-trap-length', 'Tricky', 'Which length claim is false for standard BERT?', 'BERT can process any length because attention is bidirectional', ['Vanilla BERT has a learned maximum position range', 'Long documents need chunking or a long-context variant'], 'Bidirectional attention does not remove the learned position and compute constraints.'],
  ['bert-087-trap-heads-same', 'Tricky', 'Which attention-head claim misses the point of the panel?', 'All BERT attention heads must learn the same pattern', ['Different heads can focus on different relationships', 'Head outputs are merged after parallel attention'], 'The attention visualization shows representative heads with different behaviors.'],
  ['bert-088-trap-subword-labels', 'Tricky', 'Which NER pipeline assumption should be rejected?', 'Word-level labels automatically align to subword pieces with no policy', ['Subword splits require label alignment choices', 'Token classifiers operate on BERT token positions'], 'A token classification pipeline must decide how to train and evaluate labels for split words.'],
  ['bert-089-trap-large', 'Tricky', 'Which deployment rule is too simplistic?', 'BERT-Large is always the best deployment choice', ['Model size should be weighed against latency and memory', 'BERT-Base can be preferred when it meets quality targets'], 'The model-size comparison is a tradeoff, not a universal ranking.'],
  ['bert-090-trap-objective', 'Tricky', 'Which pretraining claim overstates the lesson?', 'The pretraining objective alone determines every downstream metric', ['Fine-tuning data and task heads affect downstream behavior', 'Pretraining creates reusable language representations'], 'Downstream quality depends on pretraining, task data, architecture choices, and evaluation.'],
  ['bert-091-define', 'Interview', 'In one sentence, how should you define BERT?', 'BERT is an encoder-only transformer that learns bidirectional contextual representations for language understanding', ['BERT is a recurrent model that generates text from a prefix', 'BERT is a tokenizer that replaces all transformer layers'], 'A good interview definition names the family, context direction, representation role, and task fit.'],
  ['bert-092-bert-vs-gpt', 'Interview', 'How would you compare BERT and GPT in an interview?', 'BERT encodes full inputs bidirectionally; GPT decodes prefixes autoregressively', ['BERT and GPT differ only by vocabulary size', 'BERT uses no attention while GPT uses attention'], 'The architecture-family distinction explains why BERT fits understanding tasks and GPT fits generation.'],
  ['bert-093-token-flow', 'Interview', 'What is the BERT input preparation sequence?', 'Tokenize text, apply WordPiece, add [CLS]/[SEP], map ids, then add embeddings', ['Run attention first, then decide token boundaries', 'Fine-tune labels first, then build the vocabulary'], 'The tokenization and embeddings panels show a staged path from raw text to encoder-ready vectors.'],
  ['bert-094-embedding-equation', 'Interview', 'How should you state the BERT embedding equation?', 'Input vector equals token embedding plus segment embedding plus position embedding', ['Input vector equals query plus key plus value after softmax', 'Input vector equals class label plus answer span plus NSP target'], 'The three embedding components are summed before the first encoder layer.'],
  ['bert-095-layer-trace', 'Interview', 'Trace one BERT encoder layer at a high level.', 'Self-attention, add and normalize, feed-forward with GELU, then add and normalize again', ['Tokenize, mask every token, classify NSP, then sample text', 'Decode from prefix, cross-attend to images, and stop at [SEP]'], 'This sequence matches the encoder-layer panel and separates token mixing from per-position transformation.'],
  ['bert-096-mlm-explain', 'Interview', 'How would you explain BERT masked language modeling precisely?', 'Select 15 percent of tokens, apply the 80/10/10 replacement mix, and predict originals at those positions', ['Mask every token and train the model to copy the visible sentence', 'Predict only whether sentence B follows sentence A'], 'The exact masking setup matters because it defines the pretraining signal and avoids answer leakage from the original token.'],
  ['bert-097-nsp-explain', 'Interview', 'How would you explain BERT next sentence prediction from the lesson?', 'Classify whether the second sentence is consecutive or random using the paired input representation', ['Generate the next word after sentence A until sentence B appears', 'Label each WordPiece with a named entity tag'], 'NSP is a sentence-pair binary classification objective, not a generation or token-labeling task.'],
  ['bert-098-finetune-pipeline', 'Interview', 'What fine-tuning pipeline should you describe for BERT?', 'Start from pretrained BERT, add the right task head, format labeled data, train carefully, and evaluate held-out examples', ['Start from random weights, skip validation, and use the same head for every task', 'Use only [MASK] predictions at inference for classification and QA'], 'This answer follows the fine-tuning panel and keeps task format plus validation in the loop.'],
  ['bert-099-task-map', 'Interview', 'How should you map BERT task heads to common applications?', 'Use [CLS] classifiers for sequence tasks, token classifiers for NER, and span heads for extractive QA', ['Use NSP for NER, [PAD] for sentiment, and WordPiece ids for answer spans', 'Use one fixed classifier head for every task output type'], 'A strong answer connects output structure to the correct head over BERT representations.'],
  ['bert-100-takeaway', 'Interview', 'What is the production-ready takeaway for BERT?', 'Treat BERT as a powerful understanding encoder whose fit depends on task format, length limits, fine-tuning data, and evaluation', ['Treat BERT as a universal generator that needs no task-specific checks', 'Treat the built-in pretraining objectives as deployment metrics'], 'The lesson is not just memorizing BERT facts; it is matching architecture, objective, and task constraints.'],
];

export const BERT_QUIZ = SPECS.map((spec) => q(...spec));
