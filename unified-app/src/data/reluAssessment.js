function q(id, level, prompt, correct, distractors, explanation) {
  return {
    id,
    level,
    choices: [correct, ...distractors],
    answerIndex: 0,
    prompt,
    explanation,
  };
}

const RELU_SPECS = [
  ['relu-001-purpose-c', 'Foundation', 'What problem does the ReLU lesson focus on first?', 'Adding a simple nonlinear gate after a linear pre-activation', ['Estimating calibrated class probabilities directly', 'Replacing the optimizer with a fixed rule'], 'ReLU is an activation function: it gates a pre-activation so stacked layers are not just linear maps.'],
  ['relu-002-name', 'Foundation', 'What does ReLU stand for?', 'Rectified linear unit', ['Recursive likelihood update', 'Regularized label utility'], 'The name points to a linear positive branch and a rectified zeroed negative branch.'],
  ['relu-003-formula', 'Foundation', 'Which formula defines the plain ReLU activation?', 'f(z) = max(0, z)', ['f(z) = 1 / (1 + exp(-z))', 'f(z) = z squared for every z'], 'Plain ReLU keeps the larger of zero and the input value.'],
  ['relu-004-negative-forward', 'Foundation', 'What does ReLU output when the pre-activation is -3?', 'Zero output for that unit', ['Negative three unchanged', 'A normalized probability'], 'Any negative input falls on the blocked side and is clipped to zero.'],
  ['relu-005-positive-forward', 'Foundation', 'What does ReLU output when the pre-activation is 2.5?', 'Two point five passes through unchanged', ['Zero because all activations are clipped', 'One because outputs are probabilities'], 'Positive inputs are on the active side, so ReLU behaves like the identity there.'],
  ['relu-006-zero-forward', 'Foundation', 'What output should you expect at exactly z = 0?', 'The activation value is zero', ['The activation must become one', 'The activation must become negative'], 'The two branches meet at zero, so the forward output is zero.'],
  ['relu-007-preactivation', 'Foundation', 'In a neural layer, what is the input to ReLU usually called?', 'The pre-activation from the linear step', ['The final held-out accuracy', 'The shuffled label vector'], 'A layer commonly computes z = Wx + b first, then applies ReLU to z.'],
  ['relu-008-activation', 'Foundation', 'After ReLU is applied to z, what do we usually call the result?', 'The post-activation value', ['The validation split rule', 'The optimizer momentum term'], 'The activation is the value passed onward to the next layer.'],
  ['relu-009-gate-intuition', 'Foundation', 'What is the simplest intuition for ReLU?', 'It opens for positive signal and closes for negative signal', ['It sorts examples by target label', 'It averages every feature in a batch'], 'ReLU is a one-sided gate: positive values pass, negative values are blocked.'],
  ['relu-010-nonlinearity', 'Foundation', 'Why does a network need a nonlinear activation such as ReLU?', 'Stacked linear layers alone collapse into one linear map', ['Linear layers cannot store weights', 'Labels cannot be compared without probabilities'], 'Without nonlinearities between linear maps, depth does not add nonlinear decision power.'],
  ['relu-011-local-slope-positive', 'Foundation', 'What is the local slope of ReLU for z greater than zero?', 'Slope one on the active branch', ['Slope zero on every branch', 'Slope equal to the class count'], 'On the positive side, ReLU is the identity function, so its derivative is one.'],
  ['relu-012-local-slope-negative', 'Foundation', 'What is the local slope of ReLU for z less than zero?', 'Slope zero on the blocked branch', ['Slope one on the blocked branch', 'Slope equals the input magnitude'], 'The negative branch is flat at zero, so the local derivative is zero.'],
  ['relu-013-kink', 'Foundation', 'What special feature does ReLU have at z = 0?', 'A sharp kink where the slope changes', ['A smooth maximum probability', 'A trainable running average'], 'The formula is continuous at zero but the slope jumps between branches.'],
  ['relu-014-subgradient-basic', 'Foundation', 'How do implementations usually handle the derivative at exactly zero?', 'They choose a conventional subgradient such as zero', ['They stop the whole training run', 'They use the test label as the slope'], 'The exact zero case is a kink; frameworks pick a practical convention.'],
  ['relu-015-piecewise-linear', 'Foundation', 'What kind of function is ReLU?', 'Piecewise linear with two regions', ['A periodic sine wave', 'A softmax over classes'], 'ReLU is linear on each side of zero, with different slopes.'],
  ['relu-016-sparsity-basic', 'Foundation', 'Why can ReLU activations be sparse?', 'Many negative pre-activations become exact zeros', ['Every positive activation becomes zero', 'The optimizer removes random labels'], 'Zeroing the negative side means only some units are active for an example.'],
  ['relu-017-hidden-layer-use', 'Foundation', 'Where is plain ReLU most commonly used?', 'Hidden layers that need a cheap nonlinearity', ['As the only metric on a test set', 'As a replacement for data cleaning'], 'ReLU is a common hidden-layer activation because it is simple and computationally cheap.'],
  ['relu-018-output-caution', 'Foundation', 'Why is ReLU not automatically the right final output activation?', 'Its range and zeroing behavior may not match the prediction target', ['It cannot be used inside any hidden layer', 'It always returns calibrated probabilities'], 'Output layers must match the task; ReLU is not a universal final activation.'],
  ['relu-019-dead-unit-basic', 'Foundation', 'What is a dead ReLU unit?', 'A unit stuck inactive for most or all inputs', ['A unit with perfect validation accuracy', 'A unit that stores batch labels'], 'A dead unit spends nearly all examples on the zero-output side.'],
  ['relu-020-foundation-summary', 'Foundation', 'What should you remember before moving beyond the basics?', 'ReLU is max(0, z): positives pass, negatives zero out, and gradients follow the branch', ['ReLU is a probability normalizer for every layer', 'ReLU makes all networks train perfectly'], 'This summary captures the forward rule, the gate intuition, and the gradient consequence.'],

  ['relu-021-forward-order', 'Mechanism', 'What is the correct forward order for a dense layer followed by ReLU?', 'Compute z = Wx + b first, then apply max(0, z)', ['Apply max to labels first, then choose weights', 'Normalize test accuracy before computing z'], 'ReLU gates the linear pre-activation, not the labels or metrics.'],
  ['relu-022-elementwise', 'Mechanism', 'How is ReLU applied to a vector of pre-activations?', 'Independently to each element', ['Only to the largest element in the vector', 'By averaging all elements into one value'], 'Every coordinate gets its own max(0, z) gate.'],
  ['relu-023-matrix-tensor', 'Mechanism', 'How is ReLU applied to a matrix or tensor activation?', 'Element by element with the same threshold rule', ['Only once per batch dimension', 'Only after converting values to labels'], 'The ReLU rule is broadcast across the activation tensor.'],
  ['relu-024-backward-mask', 'Mechanism', 'What does the ReLU backward pass need from the forward pass?', 'A mask of which pre-activations were positive', ['The validation set ordering', 'A new random target for each unit'], 'Backprop needs to know which entries were active so it can pass or block gradient.'],
  ['relu-025-upstream-active', 'Mechanism', 'If z was positive, how does ReLU transform the upstream gradient locally?', 'It passes the upstream gradient through unchanged', ['It multiplies the gradient by zero', 'It turns the gradient into a probability'], 'The local derivative is one on the active branch, so multiplication by one preserves the signal.'],
  ['relu-026-upstream-blocked', 'Mechanism', 'If z was negative, how does ReLU transform the upstream gradient locally?', 'It blocks that path with a zero multiplier', ['It doubles the upstream gradient', 'It averages gradients across classes'], 'The local derivative is zero on the blocked branch, so no gradient passes through that route.'],
  ['relu-027-weight-gradient-active', 'Mechanism', 'When a ReLU unit is active for an example, what happens to the weights feeding it?', 'They can receive gradient from that example', ['They are removed from the model graph', 'They become output labels'], 'An active branch has nonzero local derivative, so upstream loss signal can reach incoming weights.'],
  ['relu-028-weight-gradient-inactive', 'Mechanism', 'When a ReLU unit is inactive for an example, what happens to its incoming-weight gradient from that path?', 'That example contributes zero through the ReLU gate', ['That example always contributes a larger update', 'That example changes the batch split'], 'A negative pre-activation makes the local ReLU derivative zero for that example.'],
  ['relu-029-bias-effect', 'Mechanism', 'How does the bias before ReLU affect activation?', 'It shifts which inputs cross the zero threshold', ['It changes the final metric name', 'It removes the need for weights'], 'The bias moves z, so it can make a unit easier or harder to activate.'],
  ['relu-030-threshold', 'Mechanism', 'What is the threshold in plain ReLU?', 'Zero on the pre-activation axis', ['The number of classes', 'The validation loss minimum'], 'Plain ReLU switches branch at z = 0.'],
  ['relu-031-geometric-view', 'Mechanism', 'Geometrically, what does a ReLU unit add after a linear score?', 'A half-space gate for that score', ['A random forest split tree', 'A calibrated posterior table'], 'The sign of the linear pre-activation determines whether that feature is active.'],
  ['relu-032-composition', 'Mechanism', 'Why can stacked ReLU layers represent complex piecewise-linear functions?', 'Each gate changes which linear region is active', ['Every layer becomes exactly the same line', 'All hidden units output probabilities'], 'Different activation patterns select different linear pieces of the network.'],
  ['relu-033-positive-nonsaturation', 'Mechanism', 'Why does ReLU often help gradient flow compared with saturating activations on the positive side?', 'Its positive branch keeps derivative one instead of flattening', ['Its negative branch has derivative one', 'It removes the chain rule from training'], 'For positive z, ReLU avoids the tiny-slope plateau that some activations have at extremes.'],
  ['relu-034-negative-flat', 'Mechanism', 'What is the tradeoff of the flat negative branch?', 'Inactive units pass no local gradient for those examples', ['Inactive units always learn faster', 'Negative values become calibrated class scores'], 'The same flat branch that creates zeros can also stop learning signal locally.'],
  ['relu-035-unbounded-positive', 'Mechanism', 'What is true about ReLU outputs on the positive side?', 'They are unbounded above', ['They are capped at one', 'They are forced to sum to one'], 'If z grows, ReLU(z) grows with it; there is no upper cap in the activation.'],
  ['relu-036-numerical-scale', 'Mechanism', 'Why can very large positive ReLU activations still be a concern?', 'They can contribute to unstable activation and gradient scales', ['They guarantee better generalization', 'They make the model nontrainable by definition'], 'ReLU avoids positive-side saturation, but it does not automatically control magnitude.'],
  ['relu-037-zero-is-not-missing', 'Mechanism', 'What does a zero ReLU output mean mechanically?', 'The pre-activation was at or below the gate threshold', ['The input example was deleted', 'The target class was unknown'], 'Zero is the value produced by the blocked branch; it is not a missing-data marker.'],
  ['relu-038-activation-pattern', 'Mechanism', 'What is an activation pattern in a ReLU network?', 'The set of units that are active for an input', ['The order of files in a dataset', 'The number of optimizer epochs'], 'The active or inactive state of gates determines which local linear path the input follows.'],
  ['relu-039-local-linearity', 'Mechanism', 'Inside one fixed ReLU activation pattern, how does the network behave locally?', 'Like a linear map within that region', ['Like a random label sampler', 'Like a probability table with no weights'], 'When all gates stay fixed, the remaining operations are linear or affine.'],
  ['relu-040-boundary-crossing', 'Mechanism', 'What happens when an input change pushes z across zero?', 'The unit changes branch and the local slope changes', ['The loss function disappears', 'The data split is recomputed'], 'Crossing the threshold switches between blocked and active behavior.'],
  ['relu-041-initialization-link', 'Mechanism', 'Why does initialization matter for ReLU layers?', 'Initial weight scale affects how many units start active and how signals propagate', ['Initialization chooses the class labels', 'Initialization replaces the activation function'], 'Bad initial scale can make too many units silent or create oversized activations.'],
  ['relu-042-he-init', 'Mechanism', 'Which initialization idea is commonly paired with ReLU hidden layers?', 'He or Kaiming scaling for rectified activations', ['Sorting labels alphabetically', 'Setting every weight to the same constant'], 'He-style scaling accounts for ReLU zeroing roughly part of the signal.'],
  ['relu-043-xavier-caution', 'Mechanism', 'What is a reasonable caution for deep ReLU stacks using a generic small initialization?', 'Signals may shrink if scale ignores ReLU gating', ['The network will become a decision tree', 'The labels will automatically rebalance'], 'Deep ReLU networks still need variance-aware initialization.'],
  ['relu-044-learning-rate', 'Mechanism', 'How can an overly large learning rate contribute to dead ReLU behavior?', 'Updates can push pre-activations far into the negative region', ['Updates make negative slopes positive', 'Updates turn ReLU into softmax'], 'Large steps can move a unit into a region where it rarely activates and receives little gradient.'],
  ['relu-045-input-scale', 'Mechanism', 'Why does input scale matter before ReLU layers?', 'Large or shifted inputs change how often gates open', ['Input scale changes the answer option order', 'Input scale removes the need for loss'], 'The sign and magnitude of z depend on upstream data scale and learned weights.'],
  ['relu-046-normalization-context', 'Mechanism', 'How can normalization before a ReLU affect training behavior?', 'It can change the distribution of values entering the gate', ['It makes ReLU output probabilities', 'It deletes the positive branch'], 'Changing the scale and center of z changes the active fraction.'],
  ['relu-047-inplace-risk', 'Mechanism', 'Why can in-place ReLU operations be risky in automatic differentiation code?', 'They may overwrite values needed for backward computation', ['They always improve validation accuracy', 'They force every gradient to one'], 'Backprop may need forward values or masks; overwriting tensors can break gradient computation.'],
  ['relu-048-monitor-active', 'Mechanism', 'What diagnostic summarizes whether ReLU gates are mostly open or closed?', 'The fraction of activations greater than zero', ['The alphabetical order of feature names', 'The number of rows in the test report'], 'Tracking active fraction shows whether units are firing or mostly silent.'],
  ['relu-049-monitor-zeros', 'Mechanism', 'What does an unusually high fraction of zero activations suggest?', 'Many units or examples are on the blocked branch', ['The model has already converged perfectly', 'The loss no longer uses labels'], 'Zero activations are expected, but too many can signal inactive representation.'],
  ['relu-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide ReLU debugging?', 'Forward values choose a gate, and the same gate decides local gradient flow', ['Only the optimizer decides whether gradients pass', 'Zero outputs always mean the data is corrupted'], 'The branch selected during the forward pass controls the backward mask.'],

  ['relu-051-compute-vector', 'Application', 'For z = [-2, 0, 3], what is ReLU(z)?', '[0, 0, 3]', ['[-2, 0, 3]', '[1, 1, 1]'], 'Apply max(0, z) element by element.'],
  ['relu-052-compute-shift', 'Application', 'If z = x - 1 and x is 0.4, what is the ReLU output?', '0 because the pre-activation is negative', ['0.4 because x was positive', '1.4 because the bias is added after ReLU'], 'Compute z first: 0.4 - 1 = -0.6, then ReLU clips it to zero.'],
  ['relu-053-compute-positive', 'Application', 'If z = 1.7 before ReLU and the upstream gradient is 4, what local gradient reaches z?', '4 because the active derivative is one', ['0 because every ReLU blocks gradients', '6.8 because ReLU multiplies by z'], 'For positive z, the local derivative is one, so the upstream gradient is unchanged.'],
  ['relu-054-compute-negative-grad', 'Application', 'If z = -1.7 before ReLU and the upstream gradient is 4, what local gradient reaches z?', '0 because the blocked derivative is zero', ['4 because every branch passes gradients', '-6.8 because gradients multiply by z'], 'For negative z, the local derivative is zero, so this path contributes no gradient.'],
  ['relu-055-crossing-zero', 'Application', 'A unit moves from z = -0.2 to z = 0.2. What changed?', 'Its output became positive and its local slope changed to one', ['Its output stayed zero with no slope change', 'It became a normalized class distribution'], 'Crossing zero moves the unit from the blocked branch to the active branch.'],
  ['relu-056-dead-diagnosis', 'Application', 'During training, one hidden unit outputs zero for nearly every batch. What is the first ReLU-specific concern?', 'The unit may be stuck inactive with little gradient', ['The unit is definitely overfitting by itself', 'The unit is computing calibrated probabilities'], 'Persistent zero outputs are a dead-unit warning, especially if the incoming weights receive little signal.'],
  ['relu-057-negative-bias-debug', 'Application', 'A new layer has a large negative bias before ReLU and almost no active units. What should you inspect?', 'Whether the bias is pushing pre-activations below zero', ['Whether the output labels are alphabetized', 'Whether the route title includes the word activation'], 'A negative bias shift can close many gates before learning begins.'],
  ['relu-058-high-lr-debug', 'Application', 'A model suddenly develops many dead units after unstable updates. Which setting is a likely contributor?', 'A learning rate that is too aggressive', ['A test-set name that is too short', 'A final metric that is logged twice'], 'Large updates can push gates into a region where they rarely reopen.'],
  ['relu-059-init-debug', 'Application', 'A very deep ReLU network has shrinking activations from the first step. What should you check early?', 'Whether initialization scale is appropriate for ReLU', ['Whether ReLU has a trainable alpha parameter', 'Whether the validation labels are sorted'], 'Variance-aware initialization is an early guardrail for ReLU signal scale.'],
  ['relu-060-input-scaling-debug', 'Application', 'Input features are shifted far negative before the first ReLU layer. What risk does that create?', 'The first gates may close too often', ['The first gates must output probabilities', 'The first gates will ignore weights entirely'], 'A negative shift can make pre-activations fall below zero more frequently.'],
  ['relu-061-hidden-choice', 'Application', 'For a standard hidden layer in a feedforward network, why might you choose ReLU?', 'It is cheap and gives a useful nonlinear gate', ['It directly computes cross-entropy loss', 'It guarantees calibrated uncertainty'], 'ReLU is popular in hidden layers because it is simple, fast, and effective when used with sound training setup.'],
  ['relu-062-final-logits', 'Application', 'Why should you be cautious about putting ReLU directly on final classification logits?', 'It restricts negative logit values before the class loss sees them', ['It makes hidden layers impossible', 'It removes all matrix multiplication'], 'Classification heads typically need flexible logits before the final loss or probability transform.'],
  ['relu-063-nonnegative-output', 'Application', 'When can a ReLU output head make sense?', 'When the target must be nonnegative and zero clipping matches the task', ['Whenever the task has more than two classes', 'Whenever training loss is noisy'], 'A ReLU head can be appropriate for constrained nonnegative targets, but it is a task-specific choice.'],
  ['relu-064-monitoring-plan', 'Application', 'Which monitoring plan is useful for ReLU-heavy training?', 'Track activation sparsity and gradient norms by layer', ['Track only the route URL string', 'Track answer option letters by batch'], 'Activation and gradient diagnostics reveal whether gates are healthy or mostly closed.'],
  ['relu-065-sparse-not-bad', 'Application', 'A ReLU layer has some zero activations. What is the best interpretation?', 'Some sparsity is expected and only becomes suspicious when excessive or persistent', ['Any zero proves the model is broken', 'Zeros mean labels were removed'], 'ReLU naturally creates zeros; the concern is pathological silence, not every zero.'],
  ['relu-066-large-positive-debug', 'Application', 'A ReLU layer has very large positive activations. What should you check?', 'Activation scale, initialization, normalization, and learning rate', ['Whether negative outputs are being hidden', 'Whether ReLU has stopped being piecewise linear'], 'Positive-side ReLU does not cap values, so large scales can still destabilize training.'],
  ['relu-067-batch-example', 'Application', 'In a batch, a unit is active for some examples and inactive for others. How should you reason about gradients?', 'Each example follows its own gate state', ['The whole batch must share one ReLU derivative', 'Only the labels decide the derivative'], 'ReLU is elementwise per example and unit, so gradient masks can differ across the batch.'],
  ['relu-068-vectorized-code', 'Application', 'In vectorized code, what is the correct ReLU operation on an array z?', 'Apply maximum with zero elementwise', ['Take the mean of z and copy it everywhere', 'Divide z by the number of classes'], 'Vectorized ReLU uses an elementwise max operation.'],
  ['relu-069-autograd-check', 'Application', 'A custom ReLU backward pass returns one for every input. What bug does that introduce?', 'It lets gradients through the negative branch incorrectly', ['It removes all gradients from active units', 'It converts outputs into probabilities'], 'The derivative should be zero when z is negative and one when z is positive.'],
  ['relu-070-custom-forward-check', 'Application', 'A custom ReLU forward pass returns z for negative values. What bug does that introduce?', 'It no longer clips negative pre-activations', ['It clips positive values to zero', 'It computes the cross-entropy loss'], 'Plain ReLU must zero negative inputs.'],
  ['relu-071-visual-lab', 'Application', 'What should the ReLU visual lab help you predict before moving the input slider?', 'Whether output and slope will be active or blocked', ['Which class label the dataset contains', 'Which deployment branch will run'], 'The core interaction is crossing the zero threshold and watching output plus local slope.'],
  ['relu-072-feature-selectivity', 'Application', 'Why can ReLU make hidden representations more selective?', 'Only units with positive evidence activate for a given input', ['Every unit outputs the same constant', 'The loss chooses random features'], 'Different inputs open different subsets of gates.'],
  ['relu-073-gradient-check', 'Application', 'How would you check whether a suspected dead unit is learning?', 'Inspect its activation frequency and incoming-weight gradients', ['Read only the final accuracy title', 'Count the number of answer choices'], 'Dead-unit diagnosis needs both forward activity and backward signal evidence.'],
  ['relu-074-recovery-caveat', 'Application', 'Why might a dead-looking ReLU sometimes recover?', 'Other parameter updates can shift its pre-activation back above zero', ['The blocked branch itself sends a large gradient', 'The validation set changes its labels automatically'], 'A unit can reopen if upstream or bias changes move z positive, but a fully isolated inactive path has no local signal.'],
  ['relu-075-application-summary', 'Application', 'What practical habit prevents many ReLU mistakes?', 'Compute z first, apply max(0, z), then use the chosen branch for gradients', ['Treat every activation as a probability', 'Assume zero outputs are always data errors'], 'A disciplined forward-and-backward trace catches most ReLU bugs.'],

  ['relu-076-false-probability-trap', 'Tricky', 'Which ReLU claim is false?', 'ReLU turns any hidden value into a calibrated probability', ['ReLU zeros negative pre-activations', 'ReLU has slope one for positive pre-activations'], 'ReLU outputs nonnegative activations, not calibrated probabilities.'],
  ['relu-077-false-negative-trap', 'Tricky', 'Which negative-side ReLU claim is false?', 'Plain ReLU preserves negative inputs unchanged', ['Plain ReLU preserves positive inputs unchanged', 'Plain ReLU outputs zero for negative inputs'], 'The defining rectification step clips negative values to zero.'],
  ['relu-078-false-gradient-trap', 'Tricky', 'Which ReLU backward claim is false?', 'The negative branch has local derivative one', ['The positive branch has local derivative one', 'The negative branch has local derivative zero'], 'The blocked branch is flat, so its derivative is zero.'],
  ['relu-079-false-dead-trap', 'Tricky', 'Which dead-unit claim is false?', 'A dead ReLU is always harmless because zeros are sparse', ['A dead unit may receive almost no gradient', 'Dead units can waste capacity'], 'Sparsity can be useful, but persistent inactivity can reduce model capacity.'],
  ['relu-080-false-magic-trap', 'Tricky', 'Which broad ReLU claim is false?', 'Using ReLU guarantees deep networks will avoid all gradient problems', ['ReLU can help positive-side gradient flow', 'ReLU still needs sensible initialization and learning rates'], 'ReLU helps with some saturation issues but does not solve every training instability.'],
  ['relu-081-false-bounded-trap', 'Tricky', 'Which output-range claim is false?', 'ReLU is bounded above by one', ['ReLU is bounded below by zero', 'Positive ReLU outputs can grow with z'], 'Plain ReLU has no upper cap on positive values.'],
  ['relu-082-false-smooth-trap', 'Tricky', 'Which smoothness claim is false?', 'ReLU is differentiable with one smooth derivative at zero', ['ReLU is continuous at zero', 'ReLU has a kink at zero'], 'The value is continuous, but the derivative is not uniquely defined at the kink.'],
  ['relu-083-false-final-layer-trap', 'Tricky', 'Which output-layer claim is false?', 'ReLU is always the correct final activation for classification', ['Final activations should match the prediction target', 'Hidden ReLU use does not imply final ReLU use'], 'Classification heads usually need logits or probabilities with task-specific handling.'],
  ['relu-084-false-zero-trap', 'Tricky', 'Which zero-output claim is false?', 'Every zero ReLU output proves a data pipeline bug', ['Zero can simply mean z was negative', 'Many persistent zeros can be a diagnostic signal'], 'Zeros are normal ReLU outputs, but their frequency matters.'],
  ['relu-085-false-batch-trap', 'Tricky', 'Which batch-level claim is false?', 'All examples in a batch must share the same ReLU mask', ['Each element has its own active or blocked state', 'Different examples can activate different units'], 'ReLU masks are elementwise, so they can differ across examples and units.'],
  ['relu-086-false-bias-trap', 'Tricky', 'Which bias claim is false?', 'Bias has no effect on whether a ReLU gate opens', ['Bias shifts the pre-activation before ReLU', 'A large negative bias can close gates'], 'Because ReLU thresholds z, any shift in z can change activation.'],
  ['relu-087-false-init-trap', 'Tricky', 'Which initialization claim is false?', 'Every initialization scale works equally well with ReLU', ['Weight scale affects activation statistics', 'ReLU layers often use rectifier-aware scaling'], 'Initialization influences both active fractions and signal scale.'],
  ['relu-088-false-inplace-trap', 'Tricky', 'Which implementation claim is false?', 'In-place ReLU is always safe in every autograd graph', ['Some graphs need saved forward values', 'In-place operations can break gradients if they overwrite needed tensors'], 'In-place ReLU can be fine in controlled cases, but it is not universally safe.'],
  ['relu-089-false-sparsity-trap', 'Tricky', 'Which sparsity claim is false?', 'More zero activations are always better', ['Some sparsity can be useful', 'Excessive silence can indicate dead capacity'], 'Useful sparsity has a limit; too much inactivity can hurt learning.'],
  ['relu-090-tricky-summary', 'Tricky', 'Which ReLU claim is unsafe?', 'ReLU is simple, so it cannot be the source of training bugs', ['ReLU bugs often come from branch, scale, or mask mistakes', 'ReLU should be debugged with forward and backward traces'], 'Simple operations can still fail through wrong masks, poor scales, dead units, or task-mismatched outputs.'],

  ['relu-091-interview-definition', 'Interview', 'In an interview, how should you define ReLU in one sentence?', 'ReLU applies max(0, z), passing positives and zeroing negatives', ['ReLU averages labels into a probability', 'ReLU is a full optimizer for neural nets'], 'A concise definition should include the formula and the branch behavior.'],
  ['relu-092-interview-derivative', 'Interview', 'What derivative explanation should you give for ReLU?', 'Derivative one for z > 0, zero for z < 0, with a convention at zero', ['Derivative zero for z > 0 and one for z < 0', 'Derivative is always equal to the output value'], 'This is the key backward-pass fact behind ReLU gates.'],
  ['relu-093-interview-why-used', 'Interview', 'Why is ReLU widely used in hidden layers?', 'It is cheap, nonlinear, and keeps positive-side gradients simple', ['It guarantees perfect accuracy', 'It replaces the need for data preprocessing'], 'The practical appeal is simplicity plus useful gradient behavior on the active side.'],
  ['relu-094-interview-dead', 'Interview', 'How would you explain the dead ReLU problem?', 'A unit can stay on the zero branch and receive little or no gradient', ['A unit becomes dead when it outputs a large positive value', 'A unit dies because the loss has no labels'], 'Dead ReLUs are about persistent inactivity and blocked local gradients.'],
  ['relu-095-interview-debug', 'Interview', 'What ReLU debugging checks would you mention?', 'Activation sparsity, gradient flow, bias shifts, initialization, and learning rate', ['Only final accuracy after deployment', 'Only whether the route loads'], 'Good debugging connects zero patterns to the parameters and scales that create them.'],
  ['relu-096-interview-output', 'Interview', 'How would you answer whether ReLU belongs on the output layer?', 'Only when the output constraint matches the task', ['Always, because hidden layers use it', 'Never, because it cannot output zero'], 'Output activations are task-dependent; hidden-layer habits do not automatically transfer.'],
  ['relu-097-interview-saturation', 'Interview', 'How would you contrast ReLU with saturating activations without overclaiming?', 'ReLU avoids positive-side saturation but has a zero-gradient negative side', ['ReLU has no gradient problems anywhere', 'ReLU is smooth and bounded like a probability curve'], 'The balanced answer names both the benefit and the dead-zone tradeoff.'],
  ['relu-098-interview-init', 'Interview', 'What initialization note pairs well with a ReLU answer?', 'Use rectifier-aware scaling to preserve signal through depth', ['Set every weight to zero for stable gates', 'Choose initialization from the test labels'], 'ReLU networks benefit from initialization that accounts for gated activations.'],
  ['relu-099-interview-implementation', 'Interview', 'What implementation detail can make or break a custom ReLU?', 'The backward mask must match which z values were positive in forward', ['The answer choices must be sorted alphabetically', 'The loss must be computed before the layer exists'], 'A forward/backward mismatch silently creates wrong gradients.'],
  ['relu-100-interview-summary', 'Interview', 'What is a production-ready ReLU takeaway?', 'Use it as a simple hidden-layer gate, but monitor dead units, scale, and task fit', ['Use it everywhere without diagnostics', 'Avoid it because zero outputs are always wrong'], 'A production answer combines the core rule with the practical caveats.'],
];

export const RELU_QUIZ = RELU_SPECS.map(([id, level, prompt, correct, distractors, explanation]) => (
  q(id, level, prompt, correct, distractors, explanation)
));
