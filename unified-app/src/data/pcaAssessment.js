function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash;
}

function q(id, level, prompt, correct, distractors, explanation) {
  const match = /^pca-(\d{3})/.exec(id);
  const number = Number(match?.[1] || 1);
  const registryRotation = stableHashNumber(`pca:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return Object.freeze({
    id,
    level,
    prompt,
    choices: Object.freeze(choices),
    answerIndex,
    explanation,
    questionHash: stableHash(`${prompt}\n${choices.join('\n')}`),
  });
}

export const PCA_QUIZ = Object.freeze([
  q('pca-001-main-purpose', 'Foundation', 'What is the main purpose of PCA?', 'Find lower-dimensional directions that preserve as much input variance as possible', ['Predict class labels from a target column', 'Randomly remove columns until the table is smaller'], 'PCA is an unsupervised projection method that keeps high-variance directions in the input data.'),
  q('pca-002-unsupervised', 'Foundation', 'Why is PCA called an unsupervised method?', 'It does not use target labels when it finds components', ['It never uses numeric feature values', 'It only works after a classifier is trained'], 'PCA looks at the geometry of the feature matrix, not at labels or outcomes.'),
  q('pca-003-numeric-inputs', 'Foundation', 'What kind of data does the basic PCA animation assume?', 'Numeric feature coordinates that can be centered and projected', ['Free-text labels with no numeric representation', 'A single class name for each example only'], 'PCA works on numeric vectors because it uses means, covariance, dot products, and projections.'),
  q('pca-004-centering', 'Foundation', 'Why does PCA center the data first?', 'So directions describe variation around the feature means', ['So every target label becomes zero', 'So the first row is always selected as PC1'], 'Centering subtracts feature means so covariance measures spread around the cloud center.'),
  q('pca-005-feature-mean', 'Foundation', 'What is subtracted during centering?', 'The mean of each feature column', ['The largest eigenvalue in each row', 'The final prediction error for each label'], 'Each feature column is shifted by its own average value.'),
  q('pca-006-variance-idea', 'Foundation', 'In PCA, what does variance describe?', 'How spread out the data is along a direction', ['How many classes the labels contain', 'Whether the model has a bias term'], 'A direction with high variance separates points more widely after projection.'),
  q('pca-007-covariance-idea', 'Foundation', 'What does covariance capture for two centered features?', 'Whether the features tend to move together or in opposite directions', ['Whether one feature is the target label', 'Whether the rows are sorted alphabetically'], 'Positive or negative covariance describes joint movement around the means.'),
  q('pca-008-positive-covariance', 'Foundation', 'What does positive covariance suggest in a two-feature scatter plot?', 'The features often increase together', ['One feature must be useless', 'The second component always has more variance'], 'Positive covariance means centered values often have the same sign.'),
  q('pca-009-negative-covariance', 'Foundation', 'What does negative covariance suggest?', 'One feature tends to increase as the other decreases', ['Both features are constant', 'The data has already been classified'], 'Negative covariance means centered values often have opposite signs.'),
  q('pca-010-component-direction', 'Foundation', 'What is a principal component direction?', 'A unit direction in feature space used as a new axis', ['A row chosen as the training label', 'A random test split for evaluation'], 'A component is an axis onto which centered data can be projected.'),
  q('pca-011-pc1', 'Foundation', 'Which direction becomes PC1?', 'The direction with the largest projected variance', ['The direction with the smallest raw feature name', 'The direction chosen by the target labels'], 'PC1 is the maximum-variance axis through the centered data.'),
  q('pca-012-pc2', 'Foundation', 'In ordinary two-dimensional PCA, how is PC2 related to PC1?', 'It is the orthogonal remaining variance direction', ['It is always identical to PC1', 'It is selected from the labels'], 'For a symmetric covariance matrix, component directions are orthogonal.'),
  q('pca-013-eigenvector-role', 'Foundation', 'What role do covariance eigenvectors play in PCA?', 'They give the principal component axes', ['They store the original row order', 'They count the number of classes'], 'The eigenvectors of the covariance matrix are the component directions.'),
  q('pca-014-eigenvalue-role', 'Foundation', 'What does an eigenvalue represent in PCA?', 'The variance captured along its component direction', ['The number of missing values in a feature', 'The probability that a point is positive'], 'Larger eigenvalues correspond to more variance explained by that component.'),
  q('pca-015-score', 'Foundation', 'What is a PCA score for one point?', 'The coordinate of the centered point along a component', ['The class probability assigned by PCA', 'The amount of label leakage in the row'], 'A score is usually computed with a dot product between a centered row and a component.'),
  q('pca-016-projection', 'Foundation', 'What does projecting onto PC1 do in the animation?', 'Moves each point onto the strongest one-dimensional axis', ['Changes each point into a class label', 'Deletes the feature means permanently'], 'Projection keeps the coordinate along PC1 and drops the perpendicular part.'),
  q('pca-017-reconstruction', 'Foundation', 'What is a one-component reconstruction?', 'An approximate point rebuilt from its PC1 score and the mean', ['The original point copied with no compression', 'A new target value created from labels'], 'With one component, PCA rebuilds each point from only its coordinate on PC1.'),
  q('pca-018-explained-variance', 'Foundation', 'What does explained variance percentage tell you?', 'How much total input variance the kept components account for', ['How accurate a classifier is on test labels', 'How many rows were removed as outliers'], 'Explained variance is about retained feature spread, not supervised accuracy.'),
  q('pca-019-component-count', 'Foundation', 'What happens when you keep fewer components than original dimensions?', 'You compress the data and lose some variation off the kept axes', ['You always improve every downstream metric', 'You convert regression into classification'], 'Dropping components removes information outside the retained subspace.'),
  q('pca-020-not-feature-selection', 'Foundation', 'Why is PCA not the same as selecting original columns?', 'Its components are usually weighted combinations of features', ['It only works on one original column', 'It uses labels to rank raw features'], 'PCA creates new axes, often mixing several original features.'),

  q('pca-021-centered-matrix', 'Mechanism', 'What does X_centered contain?', 'Each row after subtracting the training feature means', ['Each label after subtracting prediction error', 'Only the rows that have the largest variance'], 'The centered matrix is the input to covariance or SVD-based PCA.'),
  q('pca-022-covariance-matrix', 'Mechanism', 'What does the covariance matrix summarize?', 'Pairwise feature variances and covariances after centering', ['The train/test accuracy for each class', 'The order in which rows were collected'], 'Diagonal entries are feature variances; off-diagonal entries are covariances.'),
  q('pca-023-symmetric', 'Mechanism', 'Why are PCA covariance eigenvectors convenient to work with?', 'The covariance matrix is symmetric, giving orthogonal component directions', ['The covariance matrix contains label names', 'The covariance matrix is always the identity matrix'], 'Real symmetric covariance matrices have orthogonal eigenvectors.'),
  q('pca-024-ordering', 'Mechanism', 'How are PCA components ordered?', 'By descending eigenvalue or explained variance', ['By original column position only', 'By the number of characters in each feature name'], 'The first component has the largest eigenvalue, then the next largest, and so on.'),
  q('pca-025-score-matrix', 'Mechanism', 'How do you compute component scores for many rows?', 'Multiply centered data by the component matrix', ['Multiply labels by the confusion matrix', 'Sort rows by their original index'], 'Scores are projected coordinates, commonly X_centered times V.'),
  q('pca-026-dot-product', 'Mechanism', 'Why does a dot product appear in PCA projection?', 'It measures how much of a centered row lies along a component direction', ['It counts unique target labels', 'It guarantees no variance is lost'], 'Projection onto a unit vector is computed with a dot product.'),
  q('pca-027-rebuild-formula', 'Mechanism', 'What is the basic reconstruction step after keeping components?', 'Map scores back through the kept directions and add the mean', ['Replace scores with target labels and shuffle rows', 'Drop the mean and treat scores as probabilities'], 'Reconstruction returns from component coordinates to the original feature space.'),
  q('pca-028-rank-k', 'Mechanism', 'What does a rank-k PCA approximation keep?', 'The first k component directions and their scores', ['The k rows with the largest labels', 'The last k columns before centering'], 'Keeping k components gives the best linear rank-k approximation under squared reconstruction error.'),
  q('pca-029-sign-arbitrary', 'Mechanism', 'Why can the sign of a PCA component flip between implementations?', 'Both a direction and its negative span the same axis', ['The data became supervised during fitting', 'The eigenvalue changed into a probability'], 'If v is an eigenvector, -v is also an eigenvector for the same axis.'),
  q('pca-030-component-axis', 'Mechanism', 'If PC1 points down-left instead of up-right, what usually changes?', 'Only the sign of the scores, not the represented one-dimensional subspace', ['The explained variance must become negative', 'The labels are now used in the projection'], 'A sign flip changes coordinate signs but not the line being used.'),
  q('pca-031-covariance-vs-correlation', 'Mechanism', 'When does correlation-based PCA differ from covariance-based PCA?', 'When features have different scales and are standardized first', ['When there are no target labels', 'When the data has more than one row'], 'Standardizing makes PCA depend on correlations rather than raw units.'),
  q('pca-032-scale-impact', 'Mechanism', 'Why can feature scale dominate covariance PCA?', 'A large-unit feature can contribute much larger variance', ['Feature names become longer in the matrix', 'Labels are automatically upweighted'], 'PCA follows variance, so units and scaling matter.'),
  q('pca-033-standardization', 'Mechanism', 'When is standardizing before PCA often appropriate?', 'When features use different units and comparable influence is desired', ['When labels must be included in the covariance matrix', 'When every feature is already constant'], 'Standardization puts features on a common variance scale.'),
  q('pca-034-not-always-standardize', 'Mechanism', 'Why might you avoid standardizing before PCA?', 'Raw variance may be meaningful when features share units', ['Standardization makes centering impossible', 'PCA cannot run on centered data'], 'If units are comparable, preserving raw variance can be the intended analysis.'),
  q('pca-035-svd-link', 'Mechanism', 'How is SVD related to PCA?', 'Right singular vectors of centered data give principal directions', ['SVD uses labels to create supervised components', 'SVD only works for square covariance matrices'], 'PCA can be computed directly from the SVD of the centered data matrix.'),
  q('pca-036-singular-values', 'Mechanism', 'What do singular values of centered data connect to?', 'Component variances after appropriate scaling', ['The number of target classes', 'The alphabetical order of feature names'], 'Squared singular values are proportional to covariance eigenvalues.'),
  q('pca-037-scree-plot', 'Mechanism', 'What does a scree plot show?', 'Explained variance by component in order', ['Prediction residuals by target value', 'A random order of training examples'], 'A scree plot helps reveal diminishing returns from later components.'),
  q('pca-038-cumulative-variance', 'Mechanism', 'What does cumulative explained variance help decide?', 'How many components meet a retention threshold', ['Which target label is most common', 'Whether row order is sorted correctly'], 'Teams often choose k by the cumulative fraction of variance retained.'),
  q('pca-039-reconstruction-error', 'Mechanism', 'What creates reconstruction error in PCA compression?', 'Discarded variation outside the kept component subspace', ['The presence of an intercept in linear regression', 'The fact that labels were shuffled'], 'Dropped components cannot contribute to the rebuilt point.'),
  q('pca-040-loss-view', 'Mechanism', 'What objective does standard PCA optimize for projection?', 'Minimize squared reconstruction error among linear subspaces of that size', ['Maximize target-label precision directly', 'Minimize the number of original rows'], 'Variance maximization and squared reconstruction-error minimization are equivalent for standard PCA.'),
  q('pca-041-noise-effect', 'Mechanism', 'In the animation, what does increasing noise usually do to PC1 explained variance?', 'It lowers the share captured by one dominant axis', ['It makes PC1 use target labels', 'It guarantees a perfect one-component reconstruction'], 'More spread away from PC1 leaves more variance for other directions.'),
  q('pca-042-correlation-effect', 'Mechanism', 'What happens when points lie close to a slanted line?', 'One component can explain most of the variance', ['All components explain exactly equal variance', 'PCA becomes a supervised classifier'], 'Strong linear structure makes a one-dimensional projection effective.'),
  q('pca-043-zero-variance', 'Mechanism', 'What does a zero-variance feature contribute after centering?', 'No spread for PCA to preserve', ['A guaranteed first principal component', 'A hidden target label'], 'A constant centered feature is all zeros and contributes no variance.'),
  q('pca-044-sample-covariance', 'Mechanism', 'Why do implementations often divide covariance by n - 1?', 'It is the usual sample covariance scaling', ['It makes labels disappear from the dataset', 'It forces every eigenvalue to equal one'], 'The scaling affects eigenvalue magnitudes but not component directions when applied uniformly.'),
  q('pca-045-loading-vs-score', 'Mechanism', 'What is the difference between a loading and a score?', 'A loading describes a feature weight in a component; a score describes a row coordinate', ['A loading is a target label; a score is a train/test split', 'They are always identical arrays'], 'Loadings belong to component directions, while scores belong to examples.'),
  q('pca-046-rotation-view', 'Mechanism', 'A helpful way to visualize PCA is as what transformation?', 'A rotation to new axes followed by optional truncation', ['A nonlinear tree split followed by voting', 'A relabeling of examples by accuracy'], 'PCA changes coordinates to variance-ordered axes; compression drops later axes.'),
  q('pca-047-negative-correlation-axis', 'Mechanism', 'For a strong negative two-feature relationship, where does PC1 point?', 'Along the downward-sloping direction of greatest spread', ['Along the target-label axis', 'Always exactly horizontal'], 'PC1 follows the major axis of the point cloud, regardless of sign.'),
  q('pca-048-whitening', 'Mechanism', 'What does PCA whitening add after projection?', 'It rescales component scores by their variances', ['It restores all discarded components', 'It inserts target labels into the scores'], 'Whitening produces decorrelated scores with normalized variance, but changes scale.'),
  q('pca-049-fit-transform', 'Mechanism', 'What should be stored after fitting PCA for later data?', 'Training means and component directions', ['The future test labels', 'The shuffled row order from training'], 'New data must be centered and projected using the fitted training transform.'),
  q('pca-050-outlier-sensitivity', 'Mechanism', 'Why can outliers strongly affect PCA?', 'They can contribute large variance and pull component directions', ['They are automatically ignored by covariance', 'They turn PCA into a decision tree'], 'Covariance and squared reconstruction error are sensitive to extreme points.'),

  q('pca-051-visualization', 'Application', 'How is PCA often used for visualizing high-dimensional data?', 'Project many features into two or three component scores', ['Turn all features into a single class label', 'Guarantee that clusters become separable'], 'A low-dimensional score plot can reveal broad structure, though it is not proof of separability.'),
  q('pca-052-compression', 'Application', 'How can PCA compress image-like vectors?', 'Keep leading components and store lower-dimensional scores', ['Keep only the image file names', 'Use labels to erase background pixels'], 'Many images can be approximated by a smaller number of component coordinates.'),
  q('pca-053-denoising', 'Application', 'When can PCA help with denoising?', 'When signal lies in leading components and noise is spread across weaker directions', ['When noise is the largest source of variance', 'When labels are missing from the test set'], 'Dropping low-variance components can reduce some noise, but only under a suitable signal assumption.'),
  q('pca-054-redundant-features', 'Application', 'What does PCA do when two features are highly redundant?', 'It can combine their shared variation into a leading component', ['It always deletes both original features', 'It uses the target to choose the better feature'], 'Correlated features often produce one strong shared direction and one smaller contrast direction.'),
  q('pca-055-preprocessing-pipeline', 'Application', 'In a train/test workflow, where should PCA be fit?', 'On the training data only, then applied to validation or test data', ['On the full dataset including held-out rows', 'Separately on each test example'], 'Fitting PCA on held-out data leaks information about its feature distribution.'),
  q('pca-056-choose-k-threshold', 'Application', 'How might a team choose k using explained variance?', 'Keep the smallest k that reaches a chosen cumulative retention target', ['Keep the component with the longest feature name', 'Always keep exactly one component'], 'A threshold such as 90 percent or 95 percent is a common starting rule, not a guarantee of task performance.'),
  q('pca-057-choose-k-model', 'Application', 'How should k be chosen for a downstream predictive model?', 'Validate candidate k values on held-out performance and constraints', ['Choose the largest eigenvalue as the number of rows', 'Include labels while fitting PCA on test data'], 'Explained variance is useful, but downstream validation decides the practical tradeoff.'),
  q('pca-058-anomaly-caveat', 'Application', 'How can reconstruction error be used in anomaly detection?', 'Large error can flag points poorly represented by the learned subspace', ['Small error proves the point is fraudulent', 'PCA labels every anomaly automatically'], 'A point far from the retained subspace may be unusual, but thresholds need domain validation.'),
  q('pca-059-loadings-interpretation', 'Application', 'How do you interpret a component with large positive weights on several features?', 'Those features move together along that component direction', ['Those features are proven causes of the label', 'Those features must be removed from the dataset'], 'Loadings describe a pattern of variation, not causality.'),
  q('pca-060-opposite-loadings', 'Application', 'What can opposite signs in a component loading pattern indicate?', 'The component contrasts features that vary in opposite directions', ['The component has negative explained variance', 'The PCA fit failed because signs are illegal'], 'Opposite signs can describe a tradeoff axis in the centered data.'),
  q('pca-061-standardize-example', 'Application', 'A dataset has income in dollars and age in years. What preprocessing question matters before PCA?', 'Whether to standardize so dollars do not dominate by unit scale', ['Whether to convert the target into an eigenvalue', 'Whether to sort rows by income first'], 'Different measurement units can change covariance PCA substantially.'),
  q('pca-062-same-units-example', 'Application', 'A dataset has sensor channels measured in the same calibrated units. Why might raw covariance PCA be reasonable?', 'Larger raw variance may represent genuinely stronger signal', ['The covariance matrix no longer needs centering', 'The method becomes supervised automatically'], 'When units are comparable, preserving variance magnitude can be meaningful.'),
  q('pca-063-missing-values', 'Application', 'What should you do about missing numeric values before basic PCA?', 'Handle or impute them before fitting the transform', ['Leave them as text strings inside covariance', 'Treat missingness as the target label'], 'Standard PCA implementations need numeric arrays without missing entries.'),
  q('pca-064-categorical-values', 'Application', 'Why do raw categorical strings not go directly into PCA?', 'PCA requires numeric feature vectors with meaningful arithmetic', ['PCA requires every feature to be a label', 'Strings always have zero variance'], 'Categorical data must be encoded carefully before PCA can use it.'),
  q('pca-065-one-hot-caveat', 'Application', 'What is a caveat when applying PCA to one-hot encoded categories?', 'Distances and variance may reflect encoding choices rather than natural geometry', ['One-hot features make centering impossible', 'PCA will recover the original category names as labels'], 'Encoding choices affect the geometry that PCA sees.'),
  q('pca-066-monitoring', 'Application', 'In production, what PCA quantity can help monitor drift?', 'Changes in score distributions or reconstruction error', ['Changes in hidden target labels only', 'Whether component names become shorter'], 'A shifted input distribution may move scores or increase reconstruction error.'),
  q('pca-067-data-leakage', 'Application', 'What is a leakage risk with PCA preprocessing?', 'Fitting centering and components before the train/test split', ['Using only training rows to compute means', 'Applying the saved transform to validation rows'], 'Using all rows lets held-out distribution information influence training features.'),
  q('pca-068-regression-features', 'Application', 'When PCA scores feed a regression model, what should you remember?', 'The scores are unsupervised features, so validation still determines usefulness', ['The regression target was used to choose components', 'The model no longer needs a test set'], 'PCA can reduce collinearity or dimension, but it does not optimize prediction directly.'),
  q('pca-069-classification-features', 'Application', 'Why might PCA hurt a classifier even with high explained variance?', 'Low-variance directions can still contain label-separating information', ['High explained variance means labels were leaked', 'Classifiers cannot consume numeric scores'], 'PCA preserves input variance, not necessarily discriminative signal.'),
  q('pca-070-cluster-plot', 'Application', 'What is a safe interpretation of separated groups in a PC1-PC2 plot?', 'The first two variance directions show visible grouping worth investigating', ['The groups are guaranteed causal classes', 'The remaining components contain no information'], 'A PCA plot is exploratory and can omit important variation.'),
  q('pca-071-feature-names', 'Application', 'Why can PCA reduce interpretability compared with raw features?', 'Components mix original features into new axes', ['Components cannot be plotted', 'Scores are always random numbers'], 'A component may need loading analysis before stakeholders can interpret it.'),
  q('pca-072-budget-tradeoff', 'Application', 'How does PCA support storage or latency constraints?', 'It can replace many features with fewer component scores', ['It removes the need to collect training data', 'It makes all downstream models exact'], 'Lower-dimensional features can reduce memory and computation while losing some information.'),
  q('pca-073-reconstruction-review', 'Application', 'What should you inspect after choosing a small number of components for compression?', 'Whether reconstruction error is acceptable for the use case', ['Whether every target label was included in covariance', 'Whether PC signs match a preferred color palette'], 'Compression decisions should be checked against the quality needed by the application.'),
  q('pca-074-domain-meaning', 'Application', 'Why should PCA components be reviewed with domain context?', 'High-variance patterns may reflect artifacts, batch effects, or real signals', ['PCA automatically removes every artifact', 'Domain context changes eigenvectors after fitting'], 'Variance alone does not tell you whether a pattern is useful or spurious.'),
  q('pca-075-after-scaling-fit', 'Application', 'If you standardize before PCA, what must be saved for deployment?', 'Training feature means, standard deviations, and PCA components', ['Only the final test-set labels', 'The random screen coordinates from the plot'], 'The same fitted preprocessing steps must be applied consistently to new rows.'),

  q('pca-076-high-variance-trap', 'Tricky', 'What is the trap in assuming PC1 is always the most predictive feature?', 'High input variance does not guarantee target relevance', ['PC1 never contains any signal', 'PC1 is computed from test labels only'], 'PCA is unsupervised, so prediction value must be checked separately.'),
  q('pca-077-causality-trap', 'Tricky', 'What should you not conclude from a large PCA loading?', 'That the feature causally drives an outcome', ['That the feature contributes to a variance direction', 'That the component can be inspected'], 'Loadings are descriptive patterns in input variation, not causal estimates.'),
  q('pca-078-sign-trap', 'Tricky', 'Why is comparing raw signs of components across two PCA runs risky?', 'An equivalent component axis may flip sign without changing meaning', ['Eigenvectors must always be positive', 'Explained variance becomes invalid after a flip'], 'Sign orientation is arbitrary, so compare axes or aligned scores carefully.'),
  q('pca-079-scale-trap', 'Tricky', 'What can happen if one feature is measured in thousands and another in decimals?', 'The large-scale feature can dominate the covariance directions', ['PCA automatically detects the better unit', 'The smaller-scale feature becomes the target label'], 'Raw variance depends on measurement scale.'),
  q('pca-080-explained-accuracy-trap', 'Tricky', 'What does 95 percent explained variance not promise?', 'High supervised accuracy on the final task', ['Retention of most input variance', 'A low-dimensional representation of the features'], 'Explained variance is not an accuracy metric.'),
  q('pca-081-all-components', 'Tricky', 'In two original dimensions, what happens if you keep both PCA components?', 'The transform can be lossless apart from numerical precision', ['All labels become perfectly predicted', 'The covariance matrix stops being centered'], 'Keeping all components is a rotation of the centered feature space, not compression.'),
  q('pca-082-linear-limit', 'Tricky', 'What kind of structure does standard PCA capture best?', 'Linear directions of variation', ['Arbitrary curved manifolds without modification', 'Decision-tree splits based on labels'], 'Standard PCA uses linear subspaces.'),
  q('pca-083-kernel-note', 'Tricky', 'Why might someone use kernel PCA instead of ordinary PCA?', 'To capture nonlinear structure through a kernel-defined feature space', ['To make PCA supervised by default', 'To avoid all numeric input requirements'], 'Kernel PCA changes the representation so nonlinear patterns can become linear in feature space.'),
  q('pca-084-outlier-trap', 'Tricky', 'What is risky about fitting PCA before checking extreme points?', 'Outliers can rotate components toward themselves', ['Outliers are guaranteed to become PC2 only', 'Outliers are ignored when computing covariance'], 'Extreme observations can have large influence on variance directions.'),
  q('pca-085-small-sample-trap', 'Tricky', 'Why can PCA components be unstable with few rows?', 'Estimated covariance directions can change a lot with small sample variation', ['Eigenvectors cannot exist with fewer than one million rows', 'Centering stops working on small datasets'], 'Small samples make variance directions noisy.'),
  q('pca-086-collinearity', 'Tricky', 'What happens when features are nearly collinear?', 'One large component may capture the shared direction and a small one the difference', ['Every eigenvalue must be equal', 'PCA refuses to compute scores'], 'Collinearity concentrates variance into fewer directions.'),
  q('pca-087-equal-eigenvalues', 'Tricky', 'Why are component directions less stable when eigenvalues are nearly equal?', 'Small data changes can rotate directions within a similar-variance subspace', ['The data becomes supervised', 'The covariance diagonal must be negative'], 'When variance is tied, there is no strongly preferred axis.'),
  q('pca-088-sparse-trap', 'Tricky', 'Why might ordinary PCA be hard to interpret in sparse high-dimensional data?', 'Components can be dense combinations of many original features', ['PCA cannot produce more than one score', 'Sparse inputs always have no variance'], 'Dense loadings may blur simple feature-level explanations.'),
  q('pca-089-mean-shift', 'Tricky', 'What happens if new production data has a shifted mean?', 'Scores and reconstruction errors can shift because centering uses training means', ['The saved components automatically refit without notice', 'Explained variance turns into classification accuracy'], 'A fitted PCA transform is anchored to the training preprocessing statistics.'),
  q('pca-090-pc-origin', 'Tricky', 'Where does a PCA component line pass in the original feature plot?', 'Through the training data mean after centering is undone', ['Through the first raw row by definition', 'Through the point with the largest target value'], 'In original coordinates, PCA axes are centered at the mean.'),

  q('pca-091-whiteboard-pipeline', 'Interview', 'How would you summarize a standard PCA pipeline?', 'Split data, fit preprocessing on training rows, center or standardize, compute components, project, and validate k', ['Fit PCA on all rows, include labels, and skip validation', 'Sort features alphabetically and keep the first k columns'], 'A production PCA pipeline must avoid leakage and validate the dimension choice.'),
  q('pca-092-derive-core', 'Interview', 'What core derivation connects PCA to eigenvectors?', 'Maximizing projected variance under a unit-length constraint leads to covariance eigenvectors', ['Minimizing class error directly leads to target eigenvectors', 'Counting rows under a threshold leads to random axes'], 'The constrained variance objective produces the covariance eigenproblem.'),
  q('pca-093-debug-poor-results', 'Interview', 'A PCA-preprocessed model performs worse than raw features. What is a strong first diagnosis?', 'The discarded low-variance directions may contain predictive signal or scaling was inappropriate', ['The model must have used too many labels during PCA', 'PCA cannot be used before any supervised model'], 'PCA can remove information that matters for the task, especially when variance and labels are misaligned.'),
  q('pca-094-explain-to-stakeholder', 'Interview', 'How would you explain explained variance to a non-specialist?', 'It is the fraction of feature variation retained by the compressed coordinates', ['It is the final business accuracy of the model', 'It is the probability that each prediction is correct'], 'Explained variance describes information retained from inputs, not outcome correctness.'),
  q('pca-095-pca-vs-feature-selection', 'Interview', 'How would you contrast PCA with feature selection?', 'PCA creates new mixed features, while feature selection keeps chosen original columns', ['PCA keeps labels and feature selection removes labels', 'They are identical whenever data is centered'], 'This distinction matters for interpretability and deployment.'),
  q('pca-096-pca-vs-autoencoder', 'Interview', 'How would you compare PCA with a linear autoencoder?', 'A linear autoencoder with squared error can learn a similar principal subspace', ['A linear autoencoder always uses labels but PCA never can', 'They are unrelated because PCA has no reconstruction view'], 'Both can be viewed through low-dimensional reconstruction, though neural autoencoders can be extended nonlinearly.'),
  q('pca-097-high-dimensional-choice', 'Interview', 'For many more features than rows, why might SVD be preferred for PCA?', 'It can compute the principal subspace without explicitly forming a huge covariance matrix', ['It avoids centering entirely', 'It turns unsupervised PCA into a classifier'], 'SVD on centered data is often numerically and computationally practical.'),
  q('pca-098-production-contract', 'Interview', 'What should a production PCA transform document?', 'Feature order, preprocessing statistics, component matrix, component count, and training data scope', ['Only the chart color used for PC1', 'Only the final labels predicted by another model'], 'PCA transforms are sensitive to feature order and fitted preprocessing state.'),
  q('pca-099-monitor-drift', 'Interview', 'How would you monitor a deployed PCA step?', 'Track input statistics, score distributions, explained-variance assumptions, and reconstruction error', ['Check only whether component signs are positive', 'Refit on every request without versioning'], 'Monitoring should catch distribution shift and transform misuse.'),
  q('pca-100-core-misconception-review', 'Interview', 'What is the most important PCA misconception to correct in review?', 'PCA preserves high input variance, not necessarily label signal, causality, or fairness', ['PCA always selects the best original feature for prediction', 'PCA cannot be used for dimensionality reduction'], 'A production-ready explanation separates unsupervised variance preservation from downstream goals.'),
]);
