function q(id, level, prompt, correct, distractors, explanation) {
  const questionNumber = Number(id.match(/^cv-(\d{3})-/)?.[1] || 1);
  const desiredFinalIndex = (questionNumber - 1) % 3;
  const registryRotation = stableHash(`cross-validation:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    prompt,
    choices,
    answerIndex,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

export const CROSS_VALIDATION_QUIZ = Object.freeze([
  q('cv-001-purpose', 'Foundation', 'What is the main purpose of cross-validation?', 'Estimate model performance by rotating which held-out fold provides feedback', ['Train once on all rows with no evaluation boundary', 'Guarantee the chosen model cannot overfit'], 'Cross-validation reuses limited labeled data for repeated train/validation evaluations.'),
  q('cv-002-k-fold', 'Foundation', 'What does k mean in k-fold cross-validation?', 'The number of parts the dataset is split into for rotation', ['The number of features selected before splitting', 'The number of final test sets used for tuning'], 'Each fold becomes validation once while the remaining folds train.'),
  q('cv-003-fold-role', 'Foundation', 'What is the role of the held-out fold in one CV round?', 'It evaluates a model trained without those rows', ['It supplies preprocessing statistics before training', 'It is copied into every training batch'], 'The held-out fold plays the validation role for that round.'),
  q('cv-004-training-folds', 'Foundation', 'In one CV round, what happens to the other folds?', 'They are combined to fit the model for that round', ['They are discarded permanently', 'They become the final production test set'], 'Every round trains on k minus one folds.'),
  q('cv-005-average-score', 'Foundation', 'Why average scores across folds?', 'To summarize performance across several held-out slices', ['To hide every weak fold from the report', 'To make validation labels unavailable'], 'The mean CV score is a compact estimate across rotations.'),
  q('cv-006-score-spread', 'Foundation', 'What should you inspect besides the mean CV score?', 'The spread of scores across folds', ['Only the highest fold score', 'Only the number of model parameters'], 'Wide fold spread can reveal instability or split mismatch.'),
  q('cv-007-not-test', 'Foundation', 'Why is cross-validation not a replacement for a final test boundary?', 'Repeated model choices can still adapt to CV feedback', ['CV never uses validation labels', 'A final test boundary is only for unlabeled data'], 'CV is development feedback, so a final untouched estimate can still matter.'),
  q('cv-008-pipeline', 'Foundation', 'Where should learned preprocessing be fitted during CV?', 'Inside each training fold only', ['Once on the full dataset before folding', 'On the validation fold before fitting'], 'Preprocessing must be refit inside each fold to avoid leakage.'),
  q('cv-009-stratified', 'Foundation', 'Why use stratified folds for classification?', 'To keep label proportions closer across fold rotations', ['To make every fold contain the same duplicate users', 'To remove the need for a metric'], 'Stratification reduces accidental label imbalance in held-out folds.'),
  q('cv-010-grouped', 'Foundation', 'Why use grouped folds for repeated entities?', 'To keep all rows from one entity in the same fold', ['To scatter each user across every fold', 'To choose features with the validation labels'], 'Grouped folds block entity-level memorization leakage.'),
  q('cv-011-time-series', 'Foundation', 'Why is ordinary shuffled CV risky for forecasting?', 'Training may see future patterns before validating on earlier rows', ['Time order always removes class imbalance', 'Forecasting never needs validation'], 'Temporal tasks need fold designs that respect chronology.'),
  q('cv-012-random-seed', 'Foundation', 'Why record the fold random seed?', 'To reproduce the exact row-to-fold assignment', ['To improve the score without retraining', 'To make folds independent of the data'], 'Fold assignments are part of the experimental protocol.'),
  q('cv-013-loocv', 'Foundation', 'What is leave-one-out cross-validation?', 'A CV design where each validation fold contains one example', ['A design that leaves out all labels', 'A final test set used after deployment'], 'LOOCV is an extreme k-fold case with k equal to the number of examples.'),
  q('cv-014-repeated', 'Foundation', 'What is repeated k-fold CV?', 'Running k-fold CV multiple times with different fold assignments', ['Repeating the best fold until it improves', 'Using the test set as every fold'], 'Repeats can reduce dependence on one random partition.'),
  q('cv-015-small-data', 'Foundation', 'Why is CV useful on small datasets?', 'Each example can contribute to training in most rounds and validation in one round', ['It creates new independent labels', 'It removes all variance from evaluation'], 'CV uses data more efficiently than one small holdout.'),
  q('cv-016-fold-size', 'Foundation', 'What happens as k gets larger?', 'Each validation fold gets smaller while each training fit uses more rows', ['Every validation fold gets larger', 'The final test set disappears automatically'], 'Large k changes the bias-variance tradeoff of the estimate.'),
  q('cv-017-model-selection', 'Foundation', 'What can CV guide during development?', 'Hyperparameter and model-family choices', ['Final test labels during every experiment', 'Production traffic without labels'], 'CV is often used as the validation feedback loop.'),
  q('cv-018-refit', 'Foundation', 'After choosing settings with CV, what is a common next step?', 'Refit the chosen pipeline on the full development set', ['Refit preprocessing on the final test labels', 'Keep only the best validation fold model'], 'The selected configuration is often retrained on all non-test data.'),
  q('cv-019-metric', 'Foundation', 'When should the CV metric be chosen?', 'Before comparing model variants', ['After finding the best score', 'Only after opening the test set'], 'Changing metrics after seeing scores is another selection path.'),
  q('cv-020-core-warning', 'Foundation', 'What is the core warning for CV?', 'Every operation that learns from data must respect the fold boundary', ['Fold boundaries matter only for neural networks', 'Validation rows should fit scalers for stability'], 'Leakage-safe CV is about preserving each held-out fold as unseen data.'),

  q('cv-021-rotation', 'Mechanism', 'How does k-fold CV rotate validation?', 'Each fold is held out once while the remaining folds train that round', ['The same fold is always used for validation', 'Every row is validation in every round'], 'Rotation gives k held-out evaluations.'),
  q('cv-022-train-count', 'Mechanism', 'With 5-fold CV, how much data trains each round?', 'Four folds train while one fold validates', ['One fold trains while four folds validate', 'All five folds train and validate simultaneously'], 'A single round holds out one fifth.'),
  q('cv-023-estimate', 'Mechanism', 'What does the mean CV score estimate?', 'Expected performance under the fold protocol and data distribution represented by the sample', ['The exact deployment score for all future time', 'The score after tuning on the final test set'], 'The estimate is conditional on the protocol and sample.'),
  q('cv-024-fold-variance', 'Mechanism', 'What can high fold-to-fold variance indicate?', 'Performance depends strongly on which rows are held out', ['The model is guaranteed to be calibrated', 'The folds have no effect on training'], 'Large spread is a warning about instability, small data, or segments.'),
  q('cv-025-selection-bias', 'Mechanism', 'Why can picking the best of many CV scores be optimistic?', 'The selected result may exploit noise in the fold assignments', ['CV scores are never random', 'Averaging folds removes all model-selection bias'], 'Trying many variants overfits the validation protocol.'),
  q('cv-026-nested-purpose', 'Mechanism', 'What problem does nested CV address?', 'Separating hyperparameter tuning from performance estimation', ['Removing the need to fit models', 'Making every fold chronological'], 'Inner folds select; outer folds estimate.'),
  q('cv-027-inner-loop', 'Mechanism', 'In nested CV, what does the inner loop do?', 'Choose settings using only the outer-training portion', ['Report the final untouched test score', 'Train on the outer validation fold'], 'The inner loop must stay inside the outer training data.'),
  q('cv-028-outer-loop', 'Mechanism', 'In nested CV, what does the outer loop estimate?', 'Performance of the selection procedure on held-out outer folds', ['The best inner-fold score only', 'The preprocessing mean of all rows'], 'The outer fold tests the whole tuning recipe.'),
  q('cv-029-pipeline-clone', 'Mechanism', 'Why clone the pipeline for each fold?', 'Each fold needs fresh fitted state learned only from its training rows', ['The validation fold should reuse fitted state from another validation fold', 'Cloning changes the target labels'], 'State sharing across folds can leak information.'),
  q('cv-030-scaler-leak', 'Mechanism', 'How does fitting a scaler before CV leak?', 'Validation-fold statistics influence the transformed training rows', ['The scaler deletes validation labels', 'The model sees fewer training rows'], 'Full-data preprocessing lets held-out data shape the training recipe.'),
  q('cv-031-imputer-leak', 'Mechanism', 'How can imputation leak in CV?', 'Fill values are computed with rows that should be held out', ['Missing values are always labels', 'Imputation cannot be used with folds'], 'Fold-local imputation keeps validation distribution unseen.'),
  q('cv-032-feature-selection', 'Mechanism', 'Why must feature selection be inside the CV pipeline?', 'Selecting features with all labels lets validation labels guide the feature set', ['Feature selection never uses targets', 'Only neural networks can leak through features'], 'Label-informed feature choice is model selection.'),
  q('cv-033-target-encoding', 'Mechanism', 'Why is target encoding especially leak-prone in CV?', 'Category encodings can contain validation target averages unless fitted fold-locally', ['Target encoding removes categories from validation', 'It prevents stratified folds'], 'Target-dependent transforms need strict fold boundaries.'),
  q('cv-034-early-stopping', 'Mechanism', 'How should early stopping be handled with CV?', 'Use fold-local validation feedback or an inner split without touching outer held-out rows', ['Monitor the outer fold while still tuning that round', 'Monitor the final test set for every epoch'], 'Stopping rules are model-selection decisions.'),
  q('cv-035-threshold', 'Mechanism', 'Where should a classification threshold be tuned in a CV workflow?', 'Inside the development protocol, not on the final test boundary', ['On the final test labels after each model update', 'On unlabeled rows with no metric'], 'Thresholds are part of the selected model behavior.'),
  q('cv-036-refit-risk', 'Mechanism', 'What must stay frozen before final refit and test?', 'Model family, hyperparameters, preprocessing recipe, and metric', ['Only the random seed name', 'Nothing because CV already tested everything'], 'The final refit should not reopen selection decisions.'),
  q('cv-037-group-leak', 'Mechanism', 'Why do grouped folds change the unit of splitting?', 'The independent evaluation unit is the entity, not each row', ['Groups make labels unnecessary', 'Groups guarantee higher accuracy'], 'Rows from the same entity can share hidden information.'),
  q('cv-038-time-folds', 'Mechanism', 'What makes rolling-origin CV different from shuffled CV?', 'Training windows occur before their validation windows', ['Validation rows are sampled from the far past only', 'Every fold ignores chronology'], 'Rolling-origin protocols mimic forecasting deployment.'),
  q('cv-039-blocked-folds', 'Mechanism', 'Why use blocked folds for autocorrelated data?', 'Nearby rows can be too similar for independent random validation', ['Blocks remove all seasonality', 'Blocks make the metric irrelevant'], 'Temporal or spatial correlation can leak neighbor information.'),
  q('cv-040-gap', 'Mechanism', 'Why add a gap between train and validation in time-based CV?', 'To reduce leakage from near-neighbor or lookahead effects', ['To increase the number of labels', 'To make validation rows part of training'], 'A gap can mimic latency or information delay.'),
  q('cv-041-repeat-variance', 'Mechanism', 'What does repeated CV help diagnose?', 'Sensitivity of the estimate to random fold assignment', ['Whether the final test labels can be reused', 'The number of output classes automatically'], 'Repeats expose partition dependence.'),
  q('cv-042-best-fold', 'Mechanism', 'Why not report only the best fold?', 'It cherry-picks an unusually favorable validation slice', ['The best fold has no labels', 'The best fold is always the deployment distribution'], 'Single-fold selection hides variance and optimism.'),
  q('cv-043-weighted-average', 'Mechanism', 'When might a weighted fold average be needed?', 'When fold sizes differ and each row should contribute equally', ['When the worst fold should be ignored', 'When metrics are unrelated to predictions'], 'Unequal fold sizes can make a simple mean misrepresent row-level performance.'),
  q('cv-044-non-decomposable', 'Mechanism', 'What is a caution with metrics like F1 across CV folds?', 'Averaging fold F1 may differ from computing F1 on pooled out-of-fold predictions', ['F1 cannot be used with labels', 'Every fold must have identical predictions'], 'Some metrics do not decompose linearly across folds.'),
  q('cv-045-oof', 'Mechanism', 'What are out-of-fold predictions?', 'Predictions for each row made by a model that did not train on that row', ['Predictions made after training on every row', 'Predictions from the final test set only'], 'OOF predictions support diagnostics and stacking without in-row training leakage.'),
  q('cv-046-stacking', 'Mechanism', 'Why use OOF predictions for stacking?', 'The meta-model sees base-model predictions produced without training on that row', ['The meta-model should train on leaked in-sample predictions', 'Stacking removes the need for validation'], 'OOF predictions keep the meta-training signal honest.'),
  q('cv-047-random-state', 'Mechanism', 'Why control random states in CV experiments?', 'To separate real model changes from partition or initialization noise', ['To guarantee the best possible score', 'To make leakage impossible'], 'Reproducibility helps debug score changes.'),
  q('cv-048-parallel-folds', 'Mechanism', 'Why can folds often run in parallel?', 'Each fold fit is independent once the fold assignments are fixed', ['Each fold requires the previous fold prediction as a label', 'Parallelism makes validation data become training data'], 'Fold models do not depend on one another.'),
  q('cv-049-compute-cost', 'Mechanism', 'What is the compute cost tradeoff of CV?', 'More folds or repeats require more model fits', ['CV always costs less than one model fit', 'CV removes training entirely'], 'Each round trains a separate model.'),
  q('cv-050-protocol-doc', 'Mechanism', 'What should a CV protocol document?', 'Fold design, metric, pipeline steps, tuning scope, and final-test boundary', ['Only the best score screenshot', 'Only the filename of the notebook'], 'A documented protocol prevents accidental reuse and leakage.'),

  q('cv-051-scenario-scaler', 'Application', 'A notebook fits StandardScaler before GridSearchCV. What is the issue?', 'Validation-fold statistics have already shaped preprocessing', ['The scaler must be fit on the final test set', 'Grid search cannot use numerical features'], 'Put scaling inside the estimator pipeline.'),
  q('cv-052-scenario-users', 'Application', 'Rows contain multiple purchases per customer. Which CV design is safer?', 'GroupKFold or equivalent entity-level splitting', ['Random row KFold with customers scattered everywhere', 'Using the best fold as the test set'], 'Customers should not cross train and validation folds.'),
  q('cv-053-scenario-rare-class', 'Application', 'A rare positive class disappears from some validation folds. What helps?', 'Stratified fold construction for the label distribution', ['Removing the validation metric', 'Sorting rows by prediction after training'], 'Stratification keeps class proportions more stable.'),
  q('cv-054-scenario-time', 'Application', 'You forecast next month from previous months. Which CV style fits best?', 'Rolling or expanding time-based validation windows', ['Random shuffling across all months', 'Leave-one-out on individual transactions without dates'], 'Forecasting validation should occur after training windows.'),
  q('cv-055-scenario-gap', 'Application', 'Labels arrive seven days after prediction time. What fold detail may be needed?', 'A temporal gap between training and validation windows', ['Duplicating validation rows into training', 'Choosing the highest scoring fold only'], 'The gap models information latency.'),
  q('cv-056-scenario-feature-select', 'Application', 'A team selects top features on all labels, then cross-validates. What happened?', 'Feature selection leaked validation label information', ['The model became unsupervised', 'The folds no longer need labels'], 'Feature selection belongs inside the fold pipeline.'),
  q('cv-057-scenario-target-encode', 'Application', 'A category encoder uses full-data target means before CV. What is wrong?', 'Validation targets are embedded in the encoded features', ['Target encoding cannot represent categories', 'The training fold has too many rows'], 'Target encoders must be trained without the held-out fold.'),
  q('cv-058-scenario-one-bad-fold', 'Application', 'One fold performs far worse than the rest. What should you do?', 'Inspect whether that fold contains a segment or split issue that matters for deployment', ['Delete it from the mean without explanation', 'Assume the best fold is the only real result'], 'A bad fold can reveal a real generalization risk.'),
  q('cv-059-scenario-many-models', 'Application', 'You tried hundreds of models on the same CV setup. What risk grows?', 'Overfitting to the CV feedback itself', ['The final test set trains automatically', 'The labels become independent of features'], 'Model selection can adapt to validation noise.'),
  q('cv-060-scenario-nested', 'Application', 'You need an unbiased estimate of a hyperparameter search on small data. What helps?', 'Nested cross-validation with inner tuning and outer estimation', ['Reporting the best inner score only', 'Using the final test set for every candidate'], 'Nested CV evaluates the selection procedure.'),
  q('cv-061-scenario-refit', 'Application', 'After CV selects C for logistic regression, what should final training usually do?', 'Fit the chosen pipeline on all development data before final testing', ['Keep only the fold model with the highest score', 'Change C after seeing final test labels'], 'Refit uses all non-test data once choices are fixed.'),
  q('cv-062-scenario-threshold', 'Application', 'A team tunes probability thresholds after CV using test labels. What is wrong?', 'The final boundary became part of model selection', ['Thresholds do not affect model behavior', 'CV cannot produce probabilities'], 'Threshold tuning needs development feedback, not final-test peeking.'),
  q('cv-063-scenario-oof', 'Application', 'You want a confusion matrix using every training row without in-sample predictions. What should you use?', 'Out-of-fold predictions collected across CV rounds', ['Predictions from a model trained on all rows', 'Only the predictions from the best validation fold'], 'OOF predictions evaluate each row with a model that did not fit it.'),
  q('cv-064-scenario-stacking', 'Application', 'A stacked model trains on base predictions from models that saw the same rows. What is the issue?', 'The meta-model receives over-optimistic in-sample base predictions', ['The base models used too few feature columns', 'Stacking requires no validation protocol'], 'Use OOF predictions for the meta-training set.'),
  q('cv-065-scenario-fold-size', 'Application', 'A 3-fold CV estimate is very noisy. What change might help if compute allows?', 'More folds or repeated CV to assess partition sensitivity', ['Reporting only one favorable fold', 'Fitting preprocessing before all folds'], 'More rotations or repeats can stabilize the estimate.'),
  q('cv-066-scenario-loocv', 'Application', 'LOOCV is selected for a very unstable model. What concern remains?', 'The estimate can have high variance and high compute cost', ['LOOCV uses no labels', 'LOOCV trains zero models'], 'LOOCV is not automatically the best protocol.'),
  q('cv-067-scenario-metric', 'Application', 'A team switches the CV metric after seeing which model wins. What is the issue?', 'The metric choice became part of the tuning loop', ['Metrics cannot be used in CV', 'The folds become larger'], 'Evaluation criteria should be fixed before comparison.'),
  q('cv-068-scenario-imbalanced-regression', 'Application', 'A regression target has rare extreme values. What should fold design check?', 'Whether extremes are represented consistently or grouped by the right unit', ['Whether every fold has identical feature names only', 'Whether validation rows are fitted by the scaler'], 'Fold composition affects the relevance of the estimate.'),
  q('cv-069-scenario-duplicate-images', 'Application', 'Augmented image crops from the same original appear in different folds. What is wrong?', 'Near-duplicate information crosses the fold boundary', ['Augmentation cannot be used in training', 'The validation fold has no images'], 'Split originals or groups before augmentation.'),
  q('cv-070-scenario-spatial', 'Application', 'Nearby geographic samples are highly correlated. What fold design should you consider?', 'Spatial blocks or grouped regions held out together', ['Randomly scattering neighboring points everywhere', 'Using alphabetical feature order as the split'], 'Spatial dependence can make random folds too optimistic.'),
  q('cv-071-scenario-report', 'Application', 'A report shows only mean CV accuracy. What additional detail improves it?', 'Fold spread, protocol, metric, and leakage controls', ['The color of the chart only', 'The notebook execution time only'], 'Production readers need uncertainty and protocol details.'),
  q('cv-072-scenario-holdout', 'Application', 'A large dataset already has a stable validation and test split. When might simple holdout be enough?', 'When the estimate is stable and compute or protocol simplicity matters', ['When leakage is acceptable', 'When no metric exists'], 'CV is useful but not mandatory for every large-data workflow.'),
  q('cv-073-scenario-online', 'Application', 'Offline CV is strong but online performance drops. What should you suspect?', 'The fold protocol may not match deployment traffic or time drift', ['CV guarantees online behavior exactly', 'The final test boundary was too untouched'], 'Validation design must match the deployment question.'),
  q('cv-074-scenario-cache', 'Application', 'Embeddings are fit on all rows before CV and cached. What is the concern?', 'The cached representation may contain validation-fold information', ['Caching always prevents leakage', 'Embeddings cannot be evaluated'], 'Any learned representation must obey fold boundaries.'),
  q('cv-075-scenario-final-test', 'Application', 'After nested CV estimates a selection procedure, why keep a final test set in high-stakes work?', 'To provide one untouched confirmation after all protocol choices are frozen', ['To tune one more round of hyperparameters', 'To refit fold scalers on test rows'], 'High-stakes workflows often reserve a final external check.'),

  q('cv-076-trap-preprocess', 'Tricky', 'Which claim about preprocessing before CV is false?', 'It is always safe because validation rows are not directly trained on', ['Fold-local fitting is the leakage-safe default', 'Full-data fitting can leak distribution information'], 'Indirect use of validation rows can still shape training features.'),
  q('cv-077-trap-best-fold', 'Tricky', 'Which CV reporting habit is misleading?', 'Selecting the best fold score as the headline estimate', ['Reporting the mean and fold spread', 'Explaining the fold design'], 'Best-fold reporting is cherry-picking.'),
  q('cv-078-trap-no-test', 'Tricky', 'Which statement overclaims what CV provides?', 'Cross-validation removes any need for an untouched final evaluation in all projects', ['CV can guide development on limited data', 'Repeated CV feedback can be overfit'], 'CV is not magic immunity from selection bias.'),
  q('cv-079-trap-random', 'Tricky', 'When is random KFold the wrong default?', 'When rows share entities, time order, or nearby correlated structure', ['When rows are independent enough for the task', 'When stratification preserves a classification label mix'], 'The split unit must match the generalization unit.'),
  q('cv-080-trap-nested', 'Tricky', 'What is a common nested-CV mistake?', 'Letting the outer held-out fold influence inner hyperparameter choices', ['Tuning only inside the outer training portion', 'Reporting outer-fold scores'], 'Outer folds must remain unseen by the inner selection process.'),
  q('cv-081-trap-repeat', 'Tricky', 'What does repeating CV not fix by itself?', 'A fold design that leaks entities or future information', ['Sensitivity to one random partition', 'Uncertainty from small fold assignments'], 'Repeats of a flawed protocol are still flawed.'),
  q('cv-082-trap-loocv', 'Tricky', 'Which LOOCV statement is too absolute?', 'Leave-one-out is always the most reliable CV choice', ['LOOCV can be expensive', 'LOOCV may have high variance for unstable models'], 'Protocol choice depends on model, data, and compute.'),
  q('cv-083-trap-oof', 'Tricky', 'Which out-of-fold prediction is invalid?', 'A prediction from a model trained on the same row it predicts', ['A prediction from the fold model that held the row out', 'A prediction used for leakage-safe stacking'], 'OOF means the row was out of the training fold.'),
  q('cv-084-trap-metric', 'Tricky', 'Which metric practice leaks judgment?', 'Choosing the metric after inspecting which candidate wins', ['Predefining the metric from the product goal', 'Reporting secondary metrics as diagnostics'], 'Post-hoc metric switching is selection.'),
  q('cv-085-trap-group-stratified', 'Tricky', 'Why can group and stratification requirements conflict?', 'Keeping groups intact may make exact label proportions impossible', ['Groups remove the target labels', 'Stratification always splits each group across folds'], 'Real fold design often balances constraints.'),
  q('cv-086-trap-time-shuffle', 'Tricky', 'Why can shuffled CV look excellent on time series but fail later?', 'It may train on future regimes that would not exist at prediction time', ['It uses too little arithmetic averaging', 'It prevents the model from seeing old rows'], 'Time leakage creates optimistic validation.'),
  q('cv-087-trap-refit', 'Tricky', 'Which final-refit action is unsafe?', 'Changing hyperparameters after seeing the final test result', ['Training the frozen chosen pipeline on all development data', 'Saving the fold protocol for audit'], 'The final test should not reopen development.'),
  q('cv-088-trap-variance', 'Tricky', 'Which interpretation of low fold variance is unsafe?', 'It proves the deployment distribution cannot shift', ['It suggests the sampled folds behaved similarly', 'It is still conditional on the split protocol'], 'Low variance under one protocol does not rule out drift.'),
  q('cv-089-trap-cache', 'Tricky', 'Which cached artifact is dangerous for CV?', 'A learned representation fit using every row before fold creation', ['A fixed raw input file with no fitted state', 'A fold manifest saved before training'], 'Cached fitted state can hide leakage.'),
  q('cv-090-trap-production', 'Tricky', 'What is the subtle production risk of a clean CV score?', 'The fold protocol may not match the real deployment unit or timing', ['Clean CV guarantees operational success', 'A good score means monitoring is unnecessary'], 'Validation must answer the same question production will ask.'),

  q('cv-091-interview-define', 'Interview', 'How should you define cross-validation in a technical interview?', 'A rotation-based validation protocol that estimates performance while preserving held-out boundaries', ['A way to train on the test set safely', 'A guarantee that hyperparameter search is unbiased'], 'A strong definition names rotation and the boundary.'),
  q('cv-092-interview-leakage', 'Interview', 'What leakage warning should you mention after defining CV?', 'Preprocessing, feature selection, and encoders must be fitted inside each fold', ['Only the model weights can leak', 'Validation labels should fit scalers'], 'Interviewers often probe fold-boundary discipline.'),
  q('cv-093-interview-protocol', 'Interview', 'How would you choose a CV protocol for a new dataset?', 'Match folds to the generalization unit, time structure, label balance, metric, and compute budget', ['Always use random 10-fold regardless of data', 'Choose the split that gives the best score'], 'Protocol choice is a modeling decision.'),
  q('cv-094-interview-nested', 'Interview', 'When would you bring up nested CV?', 'When tuning and performance estimation must be separated on limited data', ['When no hyperparameters exist and data is huge', 'When the test set should tune every candidate'], 'Nested CV is about honest estimation of a selection process.'),
  q('cv-095-interview-tradeoff', 'Interview', 'What tradeoff should you state for larger k or repeated CV?', 'Potentially better data use or stability at the cost of more fits and sometimes higher estimate variance', ['Less compute and less variance in every case', 'No need for preprocessing pipelines'], 'CV settings affect both statistics and compute.'),
  q('cv-096-interview-bad-fold', 'Interview', 'How should you discuss one unusually bad fold?', 'Treat it as a diagnostic signal about segments, leakage, or deployment mismatch before dismissing it', ['Remove it automatically from the report', 'Replace it with the best fold'], 'Bad folds can be the most useful evidence.'),
  q('cv-097-interview-refit', 'Interview', 'How do you explain final refit after CV?', 'Once choices are frozen, train the selected pipeline on all development data and evaluate only once on final test data', ['Refit after every test-score inspection', 'Keep the single best fold model because it saw validation data'], 'Final refit uses data efficiently without reopening selection.'),
  q('cv-098-interview-oof', 'Interview', 'How would you explain out-of-fold predictions?', 'Each training row gets a prediction from a model that did not train on that row', ['Every prediction comes from the final all-data model', 'Only test-set rows receive predictions'], 'OOF predictions are useful for diagnostics and stacking.'),
  q('cv-099-interview-challenge', 'Interview', 'What design-review claim about CV should you challenge?', 'The CV score is high, so leakage, drift, and deployment mismatch are impossible', ['The fold unit should match the product question', 'The metric should be fixed before tuning'], 'Strong reviews challenge absolute claims unsupported by the protocol.'),
  q('cv-100-interview-success', 'Interview', 'What is interview-ready cross-validation mastery?', 'Choosing and defending a leakage-safe fold protocol under data, metric, time, and compute constraints', ['Memorizing that k usually equals ten', 'Reporting only the highest fold score'], 'Mastery is protocol judgment, not just the formula.'),
]);
