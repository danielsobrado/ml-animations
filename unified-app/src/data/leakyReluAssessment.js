function q(id, level, prompt, correct, distractors, explanation) {
  const questionNumber = Number(id.match(/^lrelu-(\d{3})-/)?.[1] || 1);
  const desiredFinalIndex = (questionNumber - 1) % 3;
  const registryRotation = stableHash(`leaky-relu:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const LEAKY_RELU_SPECS = [
  ['lrelu-001-purpose', 'Foundation', 'What problem does Leaky ReLU modify from plain ReLU?', 'It reduces the fully flat negative-side dead zone', ['It turns hidden activations into probabilities', 'It removes the need for a linear layer'], 'Leaky ReLU keeps ReLU simplicity but gives the negative side a small nonzero slope.'],
  ['lrelu-002-prereq', 'Foundation', 'What ReLU fact should you know before Leaky ReLU?', 'Plain ReLU has zero slope for negative pre-activations', ['Plain ReLU outputs negative values unchanged', 'Plain ReLU sums all classes to one'], 'Leaky ReLU is easiest to understand as changing the negative branch of ReLU.'],
  ['lrelu-003-formula', 'Foundation', 'Which formula describes Leaky ReLU?', 'f(z) = z for z >= 0 and alpha z for z < 0', ['f(z) = max(0, z) for every branch', 'f(z) = softmax(z) across classes'], 'The positive branch stays linear with slope one; the negative branch is scaled by alpha.'],
  ['lrelu-004-alpha-meaning', 'Foundation', 'What does alpha control in Leaky ReLU?', 'The slope of the negative branch', ['The number of output classes', 'The batch size used for training'], 'Alpha is the small multiplier applied when z is below zero.'],
  ['lrelu-005-negative-forward', 'Foundation', 'If alpha = 0.1 and z = -2, what does Leaky ReLU output?', '-0.2', ['0 exactly', '2.1'], 'Negative values are multiplied by alpha, so 0.1 times -2 is -0.2.'],
  ['lrelu-006-positive-forward', 'Foundation', 'If alpha = 0.1 and z = 3, what does Leaky ReLU output?', '3 unchanged on the positive side', ['0.3 after alpha scaling', '1 because positive outputs are capped'], 'Leaky ReLU matches plain ReLU on positive pre-activations.'],
  ['lrelu-007-zero-forward', 'Foundation', 'What is the Leaky ReLU output at z = 0?', 'Zero at the branch meeting point', ['Alpha by itself', 'One by convention'], 'Both branches meet at zero, so the forward value is zero.'],
  ['lrelu-008-positive-slope', 'Foundation', 'What is the Leaky ReLU derivative when z is positive?', 'One on the active branch', ['Alpha on the active branch', 'Zero on every branch'], 'The positive side is identical to the identity function.'],
  ['lrelu-009-negative-slope', 'Foundation', 'What is the Leaky ReLU derivative when z is negative?', 'Alpha on the negative branch', ['Zero on the negative branch', 'One divided by alpha'], 'The negative branch is a line with slope alpha.'],
  ['lrelu-010-alpha-zero', 'Foundation', 'What happens when alpha is set to 0?', 'Leaky ReLU becomes plain ReLU', ['The output becomes one for every input', 'The positive branch disappears'], 'With alpha zero, negative inputs are multiplied to zero just like ReLU.'],
  ['lrelu-011-small-alpha', 'Foundation', 'Why is alpha usually small?', 'It preserves mostly rectified behavior while keeping a small gradient path', ['It makes the function a probability distribution', 'It forces every unit to stay active'], 'A small alpha keeps negative responses damped but not completely flat.'],
  ['lrelu-012-negative-sign', 'Foundation', 'For positive alpha and negative z, what sign does the output keep?', 'It remains negative but scaled down', ['It becomes positive evidence', 'It must become exactly zero'], 'Leaky ReLU does not flip negative values; it shrinks them.'],
  ['lrelu-013-nonlinearity', 'Foundation', 'Why is Leaky ReLU still nonlinear?', 'It has two different slopes joined at zero', ['It uses a different random label per branch', 'It averages all hidden units'], 'The slope changes at zero, so the function is piecewise linear rather than one global line.'],
  ['lrelu-014-piecewise', 'Foundation', 'What kind of function is Leaky ReLU?', 'Piecewise linear with a small negative-side slope', ['A normalized categorical distribution', 'A quadratic curve on both sides'], 'Both branches are lines, but their slopes differ.'],
  ['lrelu-015-dead-zone-basic', 'Foundation', 'Why can Leaky ReLU reduce dead-unit behavior?', 'Negative pre-activations still pass a small local gradient', ['Negative pre-activations become positive automatically', 'All units receive the same update no matter z'], 'A nonzero negative-side derivative gives incoming weights some learning signal.'],
  ['lrelu-016-not-magic', 'Foundation', 'What limitation should you remember early?', 'It reduces a dead-zone risk but does not fix every training problem', ['It guarantees perfect convergence', 'It removes initialization concerns'], 'Learning rate, initialization, scale, and architecture still matter.'],
  ['lrelu-017-hidden-use', 'Foundation', 'Where is Leaky ReLU typically considered?', 'Hidden layers where ReLU-style gating is useful', ['As the only evaluation metric', 'As a data split algorithm'], 'It is an activation choice for hidden representations, not a whole training pipeline.'],
  ['lrelu-018-alpha-default', 'Foundation', 'What does a common small alpha such as 0.01 mean?', 'Negative values keep one percent of their magnitude', ['Positive values are capped at 0.01', 'All outputs become binary'], 'Alpha = 0.01 makes the negative branch shallow but nonzero.'],
  ['lrelu-019-lab', 'Foundation', 'What should the Leaky ReLU lab make visible?', 'How alpha changes negative outputs and negative-side gradients', ['How class labels are shuffled', 'How the optimizer removes layers'], 'The key interaction is changing alpha and observing forward and backward consequences.'],
  ['lrelu-020-foundation-summary', 'Foundation', 'What foundation summary should guide the rest of the lesson?', 'Leaky ReLU keeps positives unchanged and maps negatives to alpha times z', ['Leaky ReLU converts all activations into probabilities', 'Leaky ReLU deletes the need for backpropagation'], 'The whole lesson builds on the two branches and the alpha-controlled negative slope.'],

  ['lrelu-021-forward-order', 'Mechanism', 'What is the correct forward order for a layer followed by Leaky ReLU?', 'Compute z first, then apply the branch rule using alpha', ['Apply alpha to labels before the layer', 'Choose alpha from validation accuracy after each example'], 'Leaky ReLU gates the pre-activation z produced by the previous affine step.'],
  ['lrelu-022-elementwise', 'Mechanism', 'How is Leaky ReLU applied to vector or tensor activations?', 'Element by element with the same alpha rule', ['Only to the largest activation', 'Only once per training epoch'], 'Each pre-activation independently chooses the positive or negative branch.'],
  ['lrelu-023-backward-mask', 'Mechanism', 'What backward information does Leaky ReLU need?', 'Which entries were positive and which were negative', ['The alphabetical order of labels', 'The final route URL'], 'The local derivative is one or alpha depending on the sign of z.'],
  ['lrelu-024-positive-backward', 'Mechanism', 'If z was positive and upstream gradient is g, what local gradient reaches z?', 'g multiplied by one', ['g multiplied by alpha', 'zero for every positive z'], 'Positive Leaky ReLU behaves like the identity in both forward and backward passes.'],
  ['lrelu-025-negative-backward-a', 'Mechanism', 'If z was negative and upstream gradient is g, what local gradient reaches z?', 'alpha times g', ['zero no matter alpha', 'g divided by the number of classes'], 'The negative branch derivative is alpha, so backprop scales the upstream gradient by alpha.'],
  ['lrelu-026-chain-rule', 'Mechanism', 'How does Leaky ReLU fit into backpropagation?', 'It contributes a local derivative of one or alpha to the chain rule', ['It replaces the loss derivative', 'It stops the chain rule from applying'], 'Backprop multiplies upstream gradients by the local activation derivative.'],
  ['lrelu-027-alpha-gradient-size', 'Mechanism', 'What happens to negative-side gradients when alpha is larger?', 'More gradient magnitude passes through negative pre-activations', ['Less gradient passes through positive pre-activations', 'The derivative becomes exactly zero'], 'Increasing alpha raises the negative branch slope.'],
  ['lrelu-028-alpha-forward-size', 'Mechanism', 'What happens to negative outputs when alpha is larger?', 'They become less strongly damped', ['They become exactly zero', 'They all become positive'], 'A larger multiplier keeps more negative magnitude in the activation.'],
  ['lrelu-029-alpha-small-tradeoff', 'Mechanism', 'What is the tradeoff of a very small alpha?', 'It behaves close to ReLU but may still pass only tiny negative gradients', ['It makes the positive branch vanish', 'It turns the model into a classifier by itself'], 'Small alpha preserves strong gating but weakens the recovery signal.'],
  ['lrelu-030-alpha-large-tradeoff', 'Mechanism', 'What is the tradeoff of a very large alpha near one?', 'The activation becomes close to linear and loses much of the gating effect', ['The activation becomes exactly softmax', 'The negative branch becomes flat again'], 'If both slopes are nearly one, the nonlinearity becomes weak.'],
  ['lrelu-031-kink', 'Mechanism', 'What happens at z = 0 in Leaky ReLU?', 'The branches meet with different slopes', ['The function jumps to alpha', 'The output becomes a class probability'], 'The function is continuous at zero but the derivative changes.'],
  ['lrelu-032-subgradient', 'Mechanism', 'How do implementations handle the derivative exactly at zero?', 'They choose a convention because the slope is not unique', ['They use the test label as the derivative', 'They must stop training'], 'As with ReLU, the exact kink point needs a practical convention.'],
  ['lrelu-033-relu-contrast-forward', 'Mechanism', 'How does Leaky ReLU differ from ReLU for z below zero?', 'It returns alpha z instead of exactly zero', ['It returns z squared', 'It returns a normalized probability'], 'The negative branch is the only forward difference from plain ReLU.'],
  ['lrelu-034-relu-contrast-backward', 'Mechanism', 'How does Leaky ReLU differ from ReLU during backward pass for z below zero?', 'It uses alpha instead of zero as the local derivative', ['It uses one for every negative input', 'It ignores upstream gradients'], 'This nonzero derivative is the main reason it can reduce dead-unit behavior.'],
  ['lrelu-035-sign-preservation', 'Mechanism', 'Why does Leaky ReLU preserve the sign of negative evidence?', 'Positive alpha times negative z is still negative', ['The function takes an absolute value', 'The function adds one to every output'], 'Leaky ReLU dampens negative values without flipping their sign.'],
  ['lrelu-036-sparsity', 'Mechanism', 'How does Leaky ReLU affect exact activation sparsity compared with ReLU?', 'It produces fewer exact zeros for negative inputs', ['It produces more exact zeros by default', 'It forces every positive value to zero'], 'Negative inputs become small negative numbers rather than zeros.'],
  ['lrelu-037-representation', 'Mechanism', 'What representation tradeoff follows from fewer exact zeros?', 'The model keeps more negative-side information but may be less sparse', ['The model stops learning weights', 'The labels are encoded in alpha'], 'Leaky ReLU trades hard silence for a small continuous negative signal.'],
  ['lrelu-038-initialization', 'Mechanism', 'Why does initialization still matter with Leaky ReLU?', 'Weight scale still controls activation and gradient magnitude through depth', ['Alpha chooses all initial weights automatically', 'The negative branch removes all scale concerns'], 'A nonzero negative slope helps but does not make signal scale irrelevant.'],
  ['lrelu-039-ledger-row', 'Mechanism', 'What should a forward and backward ledger show for a negative row?', 'Leaky output alpha z, local slope alpha, and upstream gradient scaled by alpha', ['Positive output z, local slope one, and unchanged upstream gradient', 'A probability for each class and no local slope'], 'The lesson ledger ties the negative forward value directly to the backward multiplier.'],
  ['lrelu-040-learning-rate', 'Mechanism', 'Why can learning rate still create problems with Leaky ReLU?', 'Large updates can still destabilize pre-activations and weights', ['Alpha prevents all unstable updates', 'The positive branch has no parameters'], 'A small negative slope does not protect against oversized optimizer steps.'],
  ['lrelu-041-bias-shift', 'Mechanism', 'How does a bias shift affect Leaky ReLU behavior?', 'It changes which branch each input falls on', ['It changes alpha into a class label', 'It removes the positive branch'], 'Bias moves z relative to the zero kink.'],
  ['lrelu-042-alpha-fixed', 'Mechanism', 'In standard Leaky ReLU, how is alpha usually treated?', 'As a chosen hyperparameter rather than a learned weight', ['As a target label', 'As the output probability'], 'Plain Leaky ReLU typically uses a fixed negative slope selected before training.'],
  ['lrelu-043-upstream-control', 'Mechanism', 'What does the upstream-gradient control change in the lesson?', 'The amount of gradient arriving before Leaky ReLU applies its local slope', ['The alpha value stored in the activation formula', 'The sign of every negative pre-activation'], 'Backprop multiplies the incoming gradient by the local derivative, so upstream scale matters.'],
  ['lrelu-044-numerical-scale', 'Mechanism', 'Why can positive activations still grow large under Leaky ReLU?', 'The positive branch is unbounded like ReLU', ['Alpha caps the positive branch at one', 'The kink normalizes the whole vector'], 'Leaky ReLU changes the negative branch, not the positive-side upper range.'],
  ['lrelu-045-alpha-one', 'Mechanism', 'What happens if alpha equals 1?', 'The activation becomes the identity function', ['The activation becomes plain ReLU', 'The negative branch becomes zero'], 'Both branches would have slope one, so there is no kinked gating.'],
  ['lrelu-046-alpha-negative', 'Mechanism', 'Why is a negative alpha usually not the intended Leaky ReLU setting?', 'It would flip signs on the negative branch', ['It would make the positive branch disappear', 'It would create a valid probability vector'], 'Leaky ReLU is normally defined with a small positive negative-side slope.'],
  ['lrelu-047-active-fraction', 'Mechanism', 'Why is active fraction less complete as a diagnostic for Leaky ReLU than for ReLU?', 'Negative units still pass a small signal instead of being fully silent', ['Positive units no longer matter', 'The function has no backward pass'], 'Counting positive values is useful, but negative-side gradient is not exactly zero.'],
  ['lrelu-048-gradient-histogram', 'Mechanism', 'What does a gradient histogram show in a Leaky ReLU network?', 'Whether negative-side paths are tiny, healthy, or unstable', ['Whether labels were shuffled alphabetically', 'Whether alpha is a route name'], 'Gradient distributions reveal how much signal actually flows through branches.'],
  ['lrelu-049-alpha-selection', 'Mechanism', 'How should alpha generally be selected?', 'As a modeling hyperparameter validated with training behavior and metrics', ['By copying the number of classes', 'By setting it larger than every activation'], 'Alpha affects representation and optimization, so it should be chosen deliberately.'],
  ['lrelu-050-mechanism-summary', 'Mechanism', 'What mechanism summary should you remember?', 'Leaky ReLU replaces ReLU zero-gradient negatives with alpha-scaled negatives', ['Leaky ReLU removes the need for derivatives', 'Leaky ReLU normalizes logits into probabilities'], 'Both the forward negative value and backward negative derivative are controlled by alpha.'],

  ['lrelu-051-compute-negative', 'Application', 'For alpha = 0.01 and z = -5, what is the Leaky ReLU output?', 'negative 0.05', ['0', 'positive 5.01'], 'Multiply the negative input by alpha: 0.01 times -5 equals -0.05.'],
  ['lrelu-052-compute-positive', 'Application', 'For alpha = 0.01 and z is positive five, what is the Leaky ReLU output?', 'Positive five passes through unchanged', ['It becomes alpha times five', 'It is capped at one'], 'Positive values pass through unchanged.'],
  ['lrelu-053-compute-vector', 'Application', 'For alpha = 0.1 and z = [-2, 0, 3], what is Leaky ReLU(z)?', '[-0.2, 0, 3]', ['[0, 0, 3]', '[2, 0, 0.3]'], 'Apply alpha z on negatives, zero at zero, and identity on positives.'],
  ['lrelu-054-negative-gradient', 'Application', 'If alpha = 0.05, z = -2, and upstream gradient is 10, what gradient reaches z?', '0.5', ['0', '10'], 'The negative local derivative is alpha, so 10 times 0.05 equals 0.5.'],
  ['lrelu-055-positive-gradient', 'Application', 'If alpha = 0.05, z = 2, and upstream gradient is ten, what gradient reaches z?', 'The upstream value passes through unchanged', ['It is scaled by alpha', 'It becomes zero'], 'The positive local derivative is one.'],
  ['lrelu-056-alpha-compare-output', 'Application', 'For z = -4, which alpha gives the less clipped output?', 'alpha = 0.2 gives -0.8', ['alpha = 0 gives -0.8', 'alpha = 0.01 gives -4'], 'A larger positive alpha keeps more negative magnitude.'],
  ['lrelu-057-alpha-compare-gradient', 'Application', 'For a negative z and the same upstream gradient, which alpha sends more gradient back?', 'The larger positive negative-side slope', ['The smaller positive negative-side slope', 'A zero negative-side slope'], 'The negative-side gradient multiplier is alpha.'],
  ['lrelu-058-dead-unit-debug', 'Application', 'A ReLU model has many dead units. What is a reasonable activation experiment?', 'Try a small positive negative-side slope and monitor gradients', ['Remove the loss function', 'Make alpha equal to the number of labels'], 'Leaky ReLU can be tested as a mitigation while measuring whether signal improves.'],
  ['lrelu-059-too-linear-debug', 'Application', 'A Leaky ReLU network with alpha near 1 underfits like a mostly linear stack. What should you suspect?', 'Alpha may be too large to provide enough gating', ['Alpha is too small to pass any gradient', 'The positive branch is capped at one'], 'When alpha approaches one, the activation approaches the identity.'],
  ['lrelu-060-too-small-debug', 'Application', 'A Leaky ReLU network with alpha extremely close to 0 still has weak negative-side recovery. What should you suspect?', 'The negative-side slope may be too tiny to matter much', ['The positive branch is being multiplied by alpha', 'The function has become softmax'], 'Very small alpha behaves almost like plain ReLU.'],
  ['lrelu-061-bias-debug', 'Application', 'A layer has most pre-activations below zero but still small nonzero gradients. What explains that under Leaky ReLU?', 'The negative branch passes alpha-scaled gradients', ['The negative branch became positive', 'The loss stopped using backpropagation'], 'Leaky ReLU does not fully block negative pre-activations.'],
  ['lrelu-062-output-head-caution', 'Application', 'Why be cautious using Leaky ReLU as a final output head?', 'Its signed range and kink must match the target semantics', ['It cannot be used inside hidden layers', 'It always outputs calibrated probabilities'], 'Output activations should be selected for the prediction target, not copied from hidden layers.'],
  ['lrelu-063-monitor-alpha', 'Application', 'What should you monitor when changing alpha?', 'Validation behavior, activation distribution, and gradient scale', ['Only the order of answer options', 'Only the file name of the lesson'], 'Alpha affects both representation and optimization, so metrics plus internals matter.'],
  ['lrelu-064-sparsity-application', 'Application', 'If a model benefits from exact sparse zeros, what Leaky ReLU tradeoff matters?', 'Leaky ReLU reduces exact zero sparsity on negative inputs', ['Leaky ReLU caps all positives at zero', 'Leaky ReLU removes all negative signs'], 'The negative branch is nonzero, so it is less sparse than ReLU.'],
  ['lrelu-065-scale-application', 'Application', 'If large positive activations appear after switching to Leaky ReLU, what should you remember first?', 'Alpha does not cap the positive branch', ['Alpha turns positives into probabilities', 'Negative slope forces all positives to zero'], 'The lesson changes the negative branch; positive values still pass through unchanged.'],
  ['lrelu-066-custom-forward', 'Application', 'A custom Leaky ReLU returns zero for all negative z. What bug is present?', 'It accidentally implemented plain ReLU on the negative branch', ['It made alpha too large', 'It changed positives into probabilities'], 'Leaky ReLU should return alpha z below zero, not exactly zero.'],
  ['lrelu-067-custom-backward', 'Application', 'A custom Leaky ReLU backward uses zero derivative for negative z. What bug is present?', 'It drops the alpha gradient path', ['It doubles positive gradients', 'It normalizes outputs across classes'], 'The negative branch derivative should be alpha.'],
  ['lrelu-068-framework-param', 'Application', 'In a library call, what parameter name should you check for Leaky ReLU behavior?', 'The negative_slope or alpha setting', ['The class_count setting', 'The validation_split_name setting'], 'Frameworks often name the negative-side slope `negative_slope` or alpha.'],
  ['lrelu-069-alpha-default-check', 'Application', 'Why check the framework default alpha?', 'Different defaults change negative output and gradient scale', ['Defaults always disable backpropagation', 'Defaults always equal the batch size'], 'A slope of 0.01 and 0.2 can behave noticeably differently.'],
  ['lrelu-070-mixed-stack', 'Application', 'When comparing ReLU and Leaky ReLU in an experiment, what should stay controlled?', 'Other settings such as initialization, learning rate, and data split', ['The answer letters only', 'The route title only'], 'A fair activation comparison holds unrelated training factors stable.'],
  ['lrelu-071-lab-prediction', 'Application', 'Before moving alpha in the lab, what should you predict for a negative z?', 'Output and gradient magnitude both scale with alpha', ['Only the positive branch changes', 'The output becomes a probability'], 'For negative pre-activations, alpha controls both forward and backward values.'],
  ['lrelu-072-alpha-zero-case', 'Application', 'In the lab, why is alpha = 0 a useful comparison?', 'It shows the plain ReLU baseline', ['It shows the identity function', 'It shows calibrated probabilities'], 'Alpha zero removes the leak and recreates the fully flat negative branch.'],
  ['lrelu-073-alpha-one-case', 'Application', 'In the lab, why is alpha = 1 a useful boundary case?', 'It shows the identity-function limit', ['It shows the zero-leak comparison case', 'It shows all outputs capped at one'], 'Alpha one makes both sides have slope one.'],
  ['lrelu-074-production-check', 'Application', 'What production check is useful after choosing Leaky ReLU?', 'Confirm the configured alpha is saved and loaded consistently', ['Confirm labels are stored in alpha', 'Confirm every activation is exactly zero'], 'A deployment mismatch in slope can change model behavior.'],
  ['lrelu-075-application-summary', 'Application', 'What practical Leaky ReLU habit prevents mistakes?', 'Trace sign, alpha, output, and local derivative together', ['Treat alpha as a probability threshold', 'Assume negative outputs are impossible'], 'Forward and backward reasoning share the same branch decision and alpha.'],

  ['lrelu-076-false-positive-trap', 'Tricky', 'Which Leaky ReLU claim is false?', 'Leaky ReLU multiplies positive inputs by alpha', ['Leaky ReLU leaves positive inputs unchanged', 'Leaky ReLU uses alpha on negative inputs'], 'Alpha belongs to the negative branch, not the positive branch.'],
  ['lrelu-077-false-zero-trap', 'Tricky', 'Which negative-branch claim is false?', 'Leaky ReLU outputs exactly zero for every negative input', ['Leaky ReLU outputs alpha z for negative z', 'Leaky ReLU keeps a small negative-side slope'], 'Exact zero for every negative input describes plain ReLU, not Leaky ReLU.'],
  ['lrelu-078-false-sign-trap', 'Tricky', 'Which sign claim is false?', 'Leaky ReLU turns negative evidence positive', ['A positive alpha keeps negative outputs negative', 'The negative branch scales values down'], 'A small positive multiplier preserves the sign of a negative input.'],
  ['lrelu-079-false-gradient-trap', 'Tricky', 'Which backward claim is false?', 'The negative branch always blocks all gradient', ['The negative branch passes alpha-scaled gradient', 'The positive branch passes gradient with slope one'], 'Leaky ReLU was introduced specifically to avoid a fully zero negative-side derivative.'],
  ['lrelu-080-false-alpha-trap', 'Tricky', 'Which alpha claim is false?', 'Larger alpha always improves the model', ['Larger alpha passes more negative-side signal', 'Too-large alpha can weaken gating'], 'Alpha is a tradeoff, not a monotonic guarantee.'],
  ['lrelu-081-false-magic-trap', 'Tricky', 'Which training claim is false?', 'Leaky ReLU eliminates the need for good initialization', ['Initialization still affects signal scale', 'Learning rate can still destabilize training'], 'A leak helps one gradient path but does not replace training fundamentals.'],
  ['lrelu-082-false-sparsity-trap', 'Tricky', 'Which sparsity claim is false?', 'Leaky ReLU is as exactly sparse as ReLU by default', ['Leaky ReLU usually has fewer exact zeros', 'Reduced sparsity is part of the tradeoff'], 'Negative inputs become nonzero when alpha is positive.'],
  ['lrelu-083-false-probability-trap', 'Tricky', 'Which output claim is false?', 'Leaky ReLU converts activations into probabilities between zero and one', ['Leaky ReLU can output negative values', 'Leaky ReLU can output values greater than one'], 'It is not a probability normalizer and its output is not restricted to [0, 1].'],
  ['lrelu-084-false-alpha-one-trap', 'Tricky', 'Which boundary claim is false?', 'Alpha = 1 gives plain ReLU', ['Alpha = 0 gives plain ReLU', 'Alpha = 1 gives the identity function'], 'Alpha one makes the negative branch match the positive branch.'],
  ['lrelu-085-false-upstream-trap', 'Tricky', 'Which upstream-gradient claim is false?', 'Changing upstream gradient changes the forward Leaky ReLU output', ['Changing upstream gradient changes the backward value passed through the local slope', 'Forward output depends on z and alpha'], 'The upstream-gradient control affects backprop, while forward output is set by the pre-activation and alpha.'],
  ['lrelu-086-false-final-trap', 'Tricky', 'Which final-layer claim is false?', 'Leaky ReLU is always the correct final activation', ['Output activations should match the target', 'Hidden-layer success does not guarantee output-layer fit'], 'The final layer has task-specific constraints.'],
  ['lrelu-087-false-kink-trap', 'Tricky', 'Which kink claim is false?', 'Leaky ReLU has one identical derivative on both sides for every alpha', ['The slopes differ when alpha is not one', 'The branches meet at zero'], 'The derivative changes across the kink unless alpha equals one.'],
  ['lrelu-088-false-diagnosis-trap', 'Tricky', 'Which diagnostic claim is false?', 'A nonzero negative slope means diagnostics are unnecessary', ['Gradient histograms can still reveal scale problems', 'Activation distributions still matter'], 'Leaky ReLU reduces one failure mode but still needs inspection.'],
  ['lrelu-089-false-default-trap', 'Tricky', 'Which framework claim is false?', 'Every framework uses the same default negative slope', ['You should check the configured negative slope', 'Different defaults can change training behavior'], 'Defaults vary by API or project convention, so inspect the actual setting.'],
  ['lrelu-090-tricky-summary', 'Tricky', 'Which claim is unsafe?', 'Leaky ReLU fixes dead ReLUs without any tradeoff', ['Leaky ReLU trades hard zeroing for a small negative branch', 'Alpha changes both optimization and representation'], 'The right mental model is a tradeoff controlled by alpha.'],

  ['lrelu-091-interview-definition', 'Interview', 'How should you define Leaky ReLU in one sentence?', 'It is ReLU with a small alpha slope for negative pre-activations', ['It is softmax with a different class axis', 'It is an optimizer that removes dead units'], 'A good definition names the ReLU baseline and the modified negative branch.'],
  ['lrelu-092-interview-formula', 'Interview', 'What formula should you give for Leaky ReLU?', 'f(z) = z if z >= 0, otherwise alpha z', ['f(z) = 0 for all z below and above zero', 'f(z) = exp(z) divided by a class sum'], 'This piecewise formula is the cleanest interview answer.'],
  ['lrelu-093-interview-derivative', 'Interview', 'What derivative should you give for Leaky ReLU?', 'One for positive z and alpha for negative z', ['Alpha for positive z and zero for negative z', 'The output value for every branch'], 'The derivative statement explains the backward-pass benefit.'],
  ['lrelu-094-interview-why', 'Interview', 'Why use Leaky ReLU instead of plain ReLU?', 'To keep a small gradient path when units are negative', ['To guarantee calibrated probabilities', 'To avoid all need for validation'], 'The main motivation is reducing fully blocked negative-side learning.'],
  ['lrelu-095-interview-alpha', 'Interview', 'How should you explain alpha?', 'A hyperparameter controlling negative-side slope and leak strength', ['The number of hidden layers', 'The final class probability'], 'Alpha controls both negative output magnitude and negative gradient magnitude.'],
  ['lrelu-096-interview-tradeoff', 'Interview', 'What tradeoff should an interview answer include?', 'Too small acts like ReLU; too large weakens nonlinear gating', ['Larger alpha is always better', 'Alpha only affects display color'], 'This shows you understand alpha as a tuning choice.'],
  ['lrelu-097-interview-debug', 'Interview', 'What debugging signals would you mention for Leaky ReLU?', 'Activation distributions, gradient histograms, alpha, scale, and validation metrics', ['Only final accuracy after deployment', 'Only the lesson route string'], 'Debugging should connect branch behavior to training outcomes.'],
  ['lrelu-098-interview-relu-contrast', 'Interview', 'What is the crisp contrast with plain ReLU?', 'ReLU has zero negative slope; Leaky ReLU has alpha negative slope', ['ReLU and Leaky ReLU are identical for negative z', 'Leaky ReLU removes the positive branch'], 'The contrast is entirely about the negative branch.'],
  ['lrelu-099-interview-implementation', 'Interview', 'What implementation bug should you watch for?', 'Using ReLU backward or forward rules while intending Leaky ReLU', ['Sorting labels before applying alpha', 'Using matrix multiplication before labels exist'], 'The forward output and backward derivative must both include alpha on negative z.'],
  ['lrelu-100-interview-summary', 'Interview', 'What production-ready takeaway should you give?', 'Use Leaky ReLU when a small negative gradient path helps, but validate alpha and monitor scale', ['Use it everywhere because it has no downside', 'Avoid it because negative outputs are impossible'], 'A strong answer includes the benefit, the hyperparameter, and the diagnostics.'],
];

export const LEAKY_RELU_QUIZ = LEAKY_RELU_SPECS.map(([id, level, prompt, correct, distractors, explanation]) => (
  q(id, level, prompt, correct, distractors, explanation)
));
