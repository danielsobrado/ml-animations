function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/-(\d{3})-/)?.[1] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const registryRotation = stableHash(`gpt2-comprehensive:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const SPECS = [
  ['gpt2-001-family', 'Foundation', 'What transformer family does GPT-2 use?', 'Decoder-only transformer architecture', ['Encoder-only bidirectional representation model', 'Encoder-decoder sequence translation stack'], 'GPT-2 is a decoder-only language model trained to continue a prefix.'],
  ['gpt2-002-objective', 'Foundation', 'What is GPT-2 trained to predict?', 'The next token given previous tokens', ['A masked token using both left and right context', 'A sentence-pair IsNext label'], 'The lesson frames GPT-2 as an autoregressive model that learns next-token prediction.'],
  ['gpt2-003-tokenizer', 'Foundation', 'Which tokenization method does the GPT-2 lesson use?', 'Byte-Pair Encoding with subword tokens', ['WordPiece with sentence segment ids', 'Character labels without a vocabulary'], 'Step 1 states that GPT-2 uses BPE to break text into token ids.'],
  ['gpt2-004-vocab', 'Foundation', 'What vocabulary size is shown for GPT-2 BPE?', '50,257 tokens', ['30,522 tokens', '1,024 tokens'], 'The tokenization step lists GPT-2 vocabulary size as 50,257.'],
  ['gpt2-005-embedding', 'Foundation', 'What happens after text is tokenized?', 'Token ids are mapped to learned embedding vectors', ['The model directly samples words from raw text', 'Layer norm chooses the vocabulary'], 'GPT-2 needs numeric token ids before it can look up embedding vectors.'],
  ['gpt2-006-hidden-small', 'Foundation', 'What embedding dimension does the lesson use for GPT-2 Small?', '768 hidden channels', ['512 hidden channels', '1024 hidden channels'], 'The token embedding panel uses 768 dimensions for GPT-2 Small.'],
  ['gpt2-007-position', 'Foundation', 'Why does GPT-2 add positional embeddings?', 'Self-attention alone does not know token order', ['BPE removes the need for order information', 'The causal mask stores every absolute position'], 'Step 2 explains that attention is permutation-invariant without a position signal.'],
  ['gpt2-008-context', 'Foundation', 'What maximum context length is shown for GPT-2 positional embeddings?', '1024 positions', ['512 positions', '4096 positions'], 'The positional step defines maxSeqLen as 1024 for GPT-2.'],
  ['gpt2-009-mask', 'Foundation', 'What does the causal mask prevent during training?', 'Looking at future tokens', ['Looking at earlier prefix tokens', 'Using token embeddings'], 'Causal masking stops the model from cheating by attending to tokens to the right.'],
  ['gpt2-010-attention', 'Foundation', 'What is multi-head self-attention doing in GPT-2?', 'Letting each token mix information from allowed earlier positions', ['Replacing the FFN with a tokenizer rule', 'Reading the whole future answer before prediction'], 'GPT-2 attention gathers prefix context while respecting the causal mask.'],
  ['gpt2-011-heads', 'Foundation', 'How many attention heads does GPT-2 Small use in the lesson?', '12 parallel heads', ['8 parallel heads', '16 parallel heads'], 'The attention and architecture steps both show 12 heads for GPT-2 Small.'],
  ['gpt2-012-ffn', 'Foundation', 'What does the feed-forward network do after attention?', 'Processes each token position independently', ['Chooses BPE merges for the next batch', 'Lets future tokens overwrite prefix states'], 'Step 4 emphasizes that the FFN is position-wise after attention has mixed token information.'],
  ['gpt2-013-ffn-width', 'Foundation', 'For d_model 768, what FFN hidden width does the exercise expect?', '3072 hidden units', ['768 hidden units', '1536 hidden units'], 'The lesson says the internal FFN dimension is typically four times the embedding dimension.'],
  ['gpt2-014-residual', 'Foundation', 'Why are residual connections important in GPT-2?', 'They provide direct paths for information and gradients', ['They remove the need for token embeddings', 'They make the model bidirectional'], 'Step 5 links residual paths to stable training of deep networks.'],
  ['gpt2-015-preln', 'Foundation', 'Where does GPT-2 place layer normalization in the block?', 'Before the attention or FFN sublayer', ['Only after the final softmax', 'Only inside the tokenizer'], 'The lesson calls GPT-2 style normalization Pre-LN.'],
  ['gpt2-016-small-config', 'Foundation', 'What is the GPT-2 Small configuration shown in the architecture step?', '12 layers, 12 heads, 768 hidden size, about 124M parameters', ['24 layers, 16 heads, 1024 hidden size, about 355M parameters', '48 layers, 25 heads, 1600 hidden size, about 1.5B parameters'], 'The configurator identifies GPT-2 Small as the 124M model with 12 layers and 12 heads.'],
  ['gpt2-017-weight-tying', 'Foundation', 'What is weight tying in GPT-2?', 'Sharing the input embedding matrix with the output projection', ['Sharing the attention mask with the optimizer state', 'Sharing every layer norm parameter across layers'], 'Step 7 shows the input embedding matrix reused as the output unembedding matrix.'],
  ['gpt2-018-training-opt', 'Foundation', 'Which optimization simulates a larger batch on limited memory?', 'Gradient accumulation', ['Top-k sampling', 'Weight tying'], 'Step 8 explains accumulating gradients over small batches before an optimizer step.'],
  ['gpt2-019-kv-cache', 'Foundation', 'What does KV caching save during generation?', 'Previously computed key and value vectors', ['The BPE merge table after every token', 'The final sampled text only'], 'Step 9 states that KV caching avoids recomputing keys and values for tokens already processed.'],
  ['gpt2-020-sampling', 'Foundation', 'Which sampling methods are shown in the inference step?', 'Greedy, top-k, and nucleus top-p sampling', ['NSP, MLM, and span extraction', 'Bagging, boosting, and pruning'], 'The lesson closes by comparing next-token selection strategies for generation.'],
  ['gpt2-021-bpe-rare', 'Mechanism', 'Why does BPE help GPT-2 handle rare words?', 'Rare words can be represented as known subword pieces', ['Rare words are skipped before training begins', 'Every rare word receives a new layer norm'], 'BPE avoids a strict word-level unknown-token bottleneck by composing pieces.'],
  ['gpt2-022-common-words', 'Mechanism', 'Why can BPE keep common words as single tokens?', 'Frequent character sequences are merged into larger units', ['The causal mask promotes common words into heads', 'The FFN copies common words from labels'], 'BPE learns merges so common strings can become compact tokens.'],
  ['gpt2-023-token-id', 'Mechanism', 'What does a token id index in GPT-2?', 'A row in the learned token embedding table', ['A fixed answer choice in the quiz', 'A future attention column'], 'Token ids are used for embedding lookup before transformer blocks process the sequence.'],
  ['gpt2-024-position-add', 'Mechanism', 'How are position embeddings combined with token embeddings?', 'They are added to token embeddings before the blocks', ['They replace token embeddings after softmax', 'They are concatenated after every output logit'], 'Step 2 shows token embedding plus positional embedding as the block input.'],
  ['gpt2-025-learned-absolute', 'Mechanism', 'What kind of positional signal does this GPT-2 lesson describe?', 'Learned absolute positional embeddings', ['Rotary relative embeddings', 'No positional embeddings'], 'The step contrasts learned positions with the need to encode order explicitly.'],
  ['gpt2-026-order-example', 'Mechanism', 'Why does the dog-bites-man example matter?', 'It shows that word order changes meaning', ['It shows BPE cannot split punctuation', 'It proves layer norm is optional'], 'Without position information, attention would not distinguish reordered tokens.'],
  ['gpt2-027-causal-upper', 'Mechanism', 'Which part of the attention matrix is blocked by a causal mask?', 'Future columns above the allowed prefix triangle', ['All previous columns below the diagonal', 'Only the [PAD] row at the bottom'], 'The visualization masks positions where a query would see tokens to its right.'],
  ['gpt2-028-train-cheat', 'Mechanism', 'Why is causal masking required during next-token training?', 'Without it, the model could see the target token directly', ['Without it, BPE would forget rare words', 'Without it, residual connections would disappear'], 'Next-token prediction must use only prefix context, not the answer token itself.'],
  ['gpt2-029-qkv', 'Mechanism', 'What do Q, K, and V matrices create in self-attention?', 'Queries, keys, and values used to score and aggregate context', ['Token ids, position ids, and layer ids', 'Gradients, losses, and optimizer steps'], 'Attention projects hidden states into query, key, and value spaces.'],
  ['gpt2-030-formula', 'Mechanism', 'What is the scaled dot-product attention formula shown in the lesson?', 'softmax(QK^T / sqrt(d_k)) V', ['GELU(xW1 + b1)W2 + b2', 'Layer(Input) + Input'], 'The attention step displays the standard scaled dot-product attention equation.'],
  ['gpt2-031-head-purpose', 'Mechanism', 'Why use multiple attention heads?', 'Each head can learn different relationships such as syntax, semantics, or position', ['Each head owns a different vocabulary file', 'Each head disables causal masking for one token'], 'Multi-head attention gives parallel views of allowed context.'],
  ['gpt2-032-ffn-order', 'Mechanism', 'What is the FFN computation sequence?', 'Expand, apply GELU, then project back to d_model', ['Mask, sample, then update the tokenizer', 'Normalize, delete, then append future tokens'], 'The FFN widens the representation and returns it to the model dimension.'],
  ['gpt2-033-ffn-not-mixing', 'Mechanism', 'What does position-wise FFN mean?', 'The same MLP transforms each position separately', ['The FFN attends to every token pair', 'The FFN runs only on the final token id'], 'Attention mixes across tokens; the FFN transforms each token vector independently.'],
  ['gpt2-034-gelu', 'Mechanism', 'Why is GELU mentioned in the FFN step?', 'It is the nonlinear activation between the two linear projections', ['It is the tokenizer merge score', 'It is the sampling probability cutoff'], 'The FFN math includes GELU after the first linear layer.'],
  ['gpt2-035-residual-add', 'Mechanism', 'What does a residual connection add?', 'The sublayer input to the sublayer output', ['The vocabulary size to the context length', 'The future target token to every prefix'], 'Residual addition lets the network preserve and refine information.'],
  ['gpt2-036-gradient-flow', 'Mechanism', 'How do residual connections help optimization?', 'They give gradients a shorter route through deep stacks', ['They increase BPE vocabulary size every layer', 'They remove the need for mixed precision'], 'The exercise in Step 5 points to gradient flow as the key reason.'],
  ['gpt2-037-preln-stability', 'Mechanism', 'Why does GPT-2 use pre-layer normalization?', 'Normalizing before sublayers improves deep training stability', ['It makes the model encoder-only', 'It replaces attention with word counts'], 'The lesson says Pre-LN differs from the original Post-LN transformer and improves stability.'],
  ['gpt2-038-stack-repeat', 'Mechanism', 'What is repeated in the full GPT-2 architecture?', 'Transformer blocks containing attention, FFN, residuals, and layer norms', ['A separate tokenizer for every layer', 'An NSP classifier after every token'], 'The architecture step stacks a fixed number of transformer blocks.'],
  ['gpt2-039-size-scale', 'Mechanism', 'What changes from GPT-2 Small to XL in the configurator?', 'Layer count, head count, hidden size, and parameter count increase', ['Only the BPE vocabulary size changes', 'Only the sampling method changes'], 'The configurations scale depth, width, heads, and total parameters.'],
  ['gpt2-040-medium-config', 'Mechanism', 'Which configuration describes GPT-2 Medium in the lesson?', '24 layers, 16 heads, 1024 hidden size, about 355M parameters', ['12 layers, 12 heads, 768 hidden size, about 124M parameters', '36 layers, 20 heads, 1280 hidden size, about 774M parameters'], 'The Step 6 table lists the medium model with 24 layers and 355M parameters.'],
  ['gpt2-041-large-config', 'Mechanism', 'Which configuration describes GPT-2 Large in the lesson?', '36 layers, 20 heads, 1280 hidden size, about 774M parameters', ['24 layers, 16 heads, 1024 hidden size, about 355M parameters', '48 layers, 25 heads, 1600 hidden size, about 1.5B parameters'], 'The Step 6 table lists large as 36 layers with 774M parameters.'],
  ['gpt2-042-xl-config', 'Mechanism', 'Which configuration describes GPT-2 XL in the lesson?', '48 layers, 25 heads, 1600 hidden size, about 1.5B parameters', ['12 layers, 12 heads, 768 hidden size, about 124M parameters', '36 layers, 20 heads, 1280 hidden size, about 774M parameters'], 'The largest shown configuration has 48 layers and 1.5B parameters.'],
  ['gpt2-043-tying-math', 'Mechanism', 'How does the lesson express tied input and output matrices?', 'The output projection reuses the transpose of the input embedding matrix', ['The causal mask is reused as the optimizer matrix', 'The FFN hidden matrix is reused as position ids'], 'Weight tying connects token lookup and unembedding weights.'],
  ['gpt2-044-tying-benefit', 'Mechanism', 'What is the primary benefit of weight tying?', 'It reduces parameters and memory while sharing token geometry', ['It lets the model attend to future targets', 'It removes the need for next-token loss'], 'The lesson highlights parameter savings and regularization from reuse.'],
  ['gpt2-045-embedding-share', 'Mechanism', 'Why is weight tying especially meaningful in GPT-2 Small?', 'The embedding matrix is a large fraction of total parameters', ['The model has no attention matrices', 'The vocabulary has only 1024 entries'], 'Step 7 notes that the embedding matrix accounts for about 30 percent of GPT-2 Small parameters.'],
  ['gpt2-046-grad-accum', 'Mechanism', 'What happens during gradient accumulation?', 'Several small-batch gradients are summed before one optimizer update', ['Several sampled tokens are merged into one BPE token', 'Several layer norms are deleted before training'], 'Accumulation simulates a larger effective batch under memory limits.'],
  ['gpt2-047-mixed-precision', 'Mechanism', 'What does mixed precision do in the training step?', 'Uses FP16 activations or gradients while keeping stable FP32 master weights', ['Uses string tokens instead of numbers', 'Samples only from top-p tokens during training'], 'The lesson frames mixed precision as a memory and speed optimization with stability safeguards.'],
  ['gpt2-048-clipping', 'Mechanism', 'What does gradient clipping prevent?', 'Exploding gradients that destabilize training', ['Causal masks from hiding future tokens', 'BPE merges from becoming too common'], 'Clipping caps gradient norm to keep updates controlled.'],
  ['gpt2-049-warmup-decay', 'Mechanism', 'Why use warmup and cosine decay?', 'To ramp learning rate safely and then reduce it over training', ['To change the maximum context from 1024 to 2048', 'To replace greedy decoding with top-k sampling'], 'The training optimization step includes cosine decay with warmup as a schedule for stable optimization.'],
  ['gpt2-050-kv-mechanism', 'Mechanism', 'How does KV caching speed autoregressive decoding?', 'It stores previous keys and values so only the new token needs fresh projections', ['It stores final text so no logits are computed', 'It trains the model with larger effective batches'], 'Generation appends one token at a time; cached K/V tensors avoid recomputing prefix attention state.'],
  ['gpt2-051-chat-fit', 'Application', 'A product needs to continue user text one token at a time. Why does GPT-2 fit the core modeling pattern?', 'Its decoder-only objective predicts continuations from prefixes', ['Its bidirectional encoder reads both sides of a missing word', 'Its NSP head classifies sentence pairs'], 'Autoregressive continuation is exactly the decoder-only GPT-2 setup.'],
  ['gpt2-052-classifier-mismatch', 'Application', 'A team wants a compact sentiment classifier on fixed complete inputs. What should they remember?', 'GPT-2 can be adapted, but encoder-style models may be a more direct baseline', ['GPT-2 is impossible to fine-tune for any label task', 'Causal masking makes labels appear without training data'], 'GPT-2 is generation-first; classification is possible but not always the natural first choice.'],
  ['gpt2-053-rare-domain', 'Application', 'A dataset contains rare product names and typos. What GPT-2 component helps avoid unknown-word failure?', 'BPE subword tokenization', ['Pre-layer normalization only', 'Greedy decoding only'], 'BPE can represent uncommon strings as pieces instead of a single unknown word.'],
  ['gpt2-054-order-critical', 'Application', 'A prompt changes from dog bites man to man bites dog. Which component lets GPT-2 distinguish them?', 'Learned positional embeddings added to token embeddings', ['Weight tying alone', 'Gradient clipping alone'], 'The positional step exists because token order changes meaning.'],
  ['gpt2-055-leakage-eval', 'Application', 'During training, validation loss looks unrealistically low after disabling the mask. What is the likely issue?', 'The model can attend to future answer tokens', ['The vocabulary size is too small', 'The FFN hidden size is four times too large'], 'Removing causal masking leaks targets and invalidates next-token training.'],
  ['gpt2-056-context-limit', 'Application', 'A prompt is 1500 tokens for vanilla GPT-2. What should the engineer handle?', 'The prompt exceeds the 1024-position context shown in the lesson', ['The model will automatically add more learned positions', 'The tokenizer will reduce every prompt to one token'], 'The lesson uses learned absolute positions up to 1024, so longer prompts need truncation or a different model.'],
  ['gpt2-057-head-debug', 'Application', 'One attention head tracks punctuation while another tracks syntax. How should this be interpreted?', 'Different heads can specialize in different relationships', ['The model is broken because all heads should match', 'The tokenizer has become bidirectional'], 'The multi-head section explicitly describes heads learning different aspects.'],
  ['gpt2-058-ffn-width', 'Application', 'If a GPT-style block has d_model 1024, what FFN width follows the lesson rule?', '4096 hidden units', ['2048 hidden units', '1024 hidden units'], 'The lesson uses an FFN hidden size of four times d_model.'],
  ['gpt2-059-deep-training', 'Application', 'A 48-layer model trains unstably with weak gradient flow. Which block feature is most relevant?', 'Residual connections plus pre-layer normalization', ['Removing token embeddings', 'Turning off BPE merges'], 'Step 5 focuses on residuals and Pre-LN as training-stability tools for deep GPT-2 variants.'],
  ['gpt2-060-size-choice', 'Application', 'A deployment can afford 124M parameters but not 774M. Which GPT-2 configuration matches?', 'GPT-2 Small', ['GPT-2 Large', 'GPT-2 XL'], 'The architecture table lists GPT-2 Small as the 124M model.'],
  ['gpt2-061-parameter-budget', 'Application', 'A model is too large mostly because of vocabulary embeddings. Which optimization from the lesson targets this?', 'Weight tying between input embeddings and output projection', ['Causal masking of future tokens', 'Cosine learning-rate decay'], 'Weight tying saves parameters in the large vocabulary projection.'],
  ['gpt2-062-oom-batch', 'Application', 'GPU memory cannot hold batch size 32, but that effective batch is desired. What should be used?', 'Gradient accumulation over smaller microbatches', ['Greedy decoding during training', 'Removing the causal mask'], 'Accumulation trades extra steps for lower memory per step.'],
  ['gpt2-063-fp16-speed', 'Application', 'Training is memory-bound and hardware supports half precision. Which optimization fits?', 'Mixed precision with stability safeguards', ['Nucleus sampling in the training loop', 'Turning BPE into word-level tokens'], 'Step 8 describes FP16 activations and gradients as a memory and speed tool.'],
  ['gpt2-064-exploding', 'Application', 'Loss spikes because gradients sometimes become huge. Which Step 8 technique is relevant?', 'Gradient clipping by norm', ['Top-k sampling', 'Weight untying'], 'Clipping caps gradient magnitude to avoid destabilizing updates.'],
  ['gpt2-065-schedule', 'Application', 'A run diverges early with a high learning rate. Which scheduler pattern from the lesson helps?', 'Warmup followed by cosine decay', ['Immediate maximum learning rate forever', 'Sampling only from the top token'], 'Warmup avoids harsh early updates, and decay lowers the rate later.'],
  ['gpt2-066-generation-latency', 'Application', 'Generation recomputes the whole prefix at every new token. Which fix is directly taught?', 'Enable KV caching for previous tokens', ['Increase the BPE vocabulary every step', 'Train NSP on the generated output'], 'KV caching is designed to prevent redundant prefix attention computation.'],
  ['gpt2-067-greedy-risk', 'Application', 'A generated paragraph is deterministic but repetitive. Which sampling note explains this?', 'Greedy decoding always picks the highest-probability token', ['Top-p always chooses the lowest-probability token', 'Causal masking disables all repetition'], 'The inference step warns that greedy sampling can be repetitive.'],
  ['gpt2-068-topk', 'Application', 'A team wants randomness but only among a fixed number of likely tokens. Which method matches?', 'Top-k sampling', ['Gradient clipping', 'Position embedding lookup'], 'Top-k samples from the k most likely next-token options.'],
  ['gpt2-069-topp', 'Application', 'A team wants a dynamic candidate set based on cumulative probability. Which method matches?', 'Nucleus top-p sampling', ['BPE merge ranking', 'Pre-layer normalization'], 'Top-p uses the smallest set whose cumulative probability exceeds p.'],
  ['gpt2-070-cache-memory', 'Application', 'What tradeoff should be considered when enabling KV caching?', 'It speeds decoding but stores growing key-value tensors', ['It removes all inference memory needs', 'It makes training labels unnecessary'], 'KV caching saves compute by spending memory on cached prefix states.'],
  ['gpt2-071-prefix-only', 'Application', 'A prompt contains a future answer hidden after the target position. What should correct GPT-2 training do?', 'Mask that future text from the current query position', ['Let the current token attend to every later answer token', 'Use sentence-pair segment embeddings to reveal it'], 'Autoregressive training must preserve prefix-only visibility.'],
  ['gpt2-072-weight-tying-change', 'Application', 'If input and output vocabularies differ, what happens to weight tying?', 'It may no longer be directly applicable', ['It becomes mandatory for causal masking', 'It doubles the context length'], 'Tying assumes compatible token embedding and output projection spaces.'],
  ['gpt2-073-quality-vs-cost', 'Application', 'A larger GPT-2 variant improves validation loss but misses latency targets. What decision is most aligned?', 'Compare quality gain against memory and latency budgets', ['Ship the largest model because parameter count always wins', 'Ignore validation because sampling fixes architecture cost'], 'The configurator teaches model scale as a tradeoff, not a universal deployment rule.'],
  ['gpt2-074-train-vs-infer', 'Application', 'Which distinction matters when debugging a slow generation service?', 'Training optimizations and inference optimizations solve different bottlenecks', ['Gradient accumulation is the same as KV caching', 'Top-k sampling reduces training memory directly'], 'Step 8 targets training efficiency; Step 9 targets autoregressive decoding efficiency.'],
  ['gpt2-075-end-to-end', 'Application', 'What is the right high-level trace for a GPT-2 token prediction?', 'BPE ids plus positions enter masked transformer blocks, logits are produced, and a next token is selected', ['Raw text enters NSP, the encoder reads both sides, and a span is copied', 'The FFN chooses token merges before embeddings exist'], 'A production trace should connect tokenization, positions, causal blocks, logits, and sampling.'],
  ['gpt2-076-trap-bidirectional', 'Tricky', 'Which GPT-2 architecture claim is false?', 'GPT-2 uses bidirectional encoder attention like BERT', ['GPT-2 is decoder-only', 'GPT-2 predicts next tokens from prefixes'], 'GPT-2 is a causal decoder, not a bidirectional encoder.'],
  ['gpt2-077-trap-word-only', 'Tricky', 'Which tokenization claim should be rejected?', 'GPT-2 uses word-level tokenization with one id per dictionary word', ['GPT-2 uses BPE subword units', 'BPE can split rare words into pieces'], 'The lesson says GPT-2 uses Byte-Pair Encoding, not pure word-level tokenization.'],
  ['gpt2-078-trap-no-position', 'Tricky', 'Which positional-encoding claim is unsafe?', 'GPT-2 can ignore positions because attention already knows order', ['GPT-2 adds learned positional embeddings', 'Word order can change sentence meaning'], 'Self-attention by itself is permutation-invariant, so positions are needed.'],
  ['gpt2-079-trap-future', 'Tricky', 'Which masking statement is wrong?', 'The causal mask lets each token attend to future target tokens', ['The causal mask blocks future positions', 'Prefix tokens remain visible to later queries'], 'Causal masking exists to prevent target leakage.'],
  ['gpt2-080-trap-heads', 'Tricky', 'Which multi-head attention claim is misleading?', 'All GPT-2 attention heads must learn identical relationships', ['Different heads can learn different aspects', 'Heads use separate Q, K, and V projections'], 'Multiple heads are useful because they can specialize.'],
  ['gpt2-081-trap-ffn-mixes', 'Tricky', 'Which FFN interpretation is false?', 'The FFN is the sublayer that mixes information across token positions', ['The FFN processes each position independently', 'Attention is the cross-token mixing sublayer'], 'The FFN is position-wise; attention performs token mixing.'],
  ['gpt2-082-trap-residual', 'Tricky', 'Which residual-connection claim should be corrected?', 'Residual connections are only decorative diagram arrows', ['Residuals help information and gradients flow', 'Residuals add a sublayer input back to its output'], 'Residual paths are functional training-stability mechanisms.'],
  ['gpt2-083-trap-postln', 'Tricky', 'Which layer-norm claim contradicts the GPT-2 lesson?', 'GPT-2 applies layer norm only after each sublayer in Post-LN style', ['GPT-2 uses Pre-LN in the lesson', 'Pre-LN improves training stability'], 'The lesson explicitly describes GPT-2 as using pre-layer normalization.'],
  ['gpt2-084-trap-size', 'Tricky', 'Which model-size rule is too simplistic?', 'The largest GPT-2 variant is always the best deployment choice', ['Scale should be balanced against latency and memory', 'Small can be appropriate under tight budgets'], 'Model scale improves capacity but also raises operational cost.'],
  ['gpt2-085-trap-tying', 'Tricky', 'Which weight-tying claim is wrong?', 'Weight tying doubles the embedding parameters', ['Weight tying reuses a matrix', 'Weight tying reduces parameter count'], 'Tying saves parameters by sharing input and output token matrices.'],
  ['gpt2-086-trap-accumulation', 'Tricky', 'Which gradient-accumulation claim is false?', 'Gradient accumulation reduces the number of examples needed for learning', ['Gradient accumulation simulates a larger effective batch', 'Gradient accumulation can help with memory limits'], 'Accumulation changes memory scheduling, not the amount of data required.'],
  ['gpt2-087-trap-fp16', 'Tricky', 'Which mixed-precision claim is unsafe?', 'FP16 always improves stability without any FP32 safeguards', ['Mixed precision can save memory and speed compute', 'A stable implementation may keep FP32 master weights'], 'The lesson notes stability safeguards, not blind half-precision everywhere.'],
  ['gpt2-088-trap-cache-training', 'Tricky', 'Which KV-cache statement should be rejected?', 'KV caching is a training optimizer for larger effective batch size', ['KV caching is an inference optimization', 'KV caching avoids recomputing prefix keys and values'], 'KV caching helps autoregressive generation; gradient accumulation addresses effective batch size.'],
  ['gpt2-089-trap-greedy', 'Tricky', 'Which decoding claim is too strong?', 'Greedy decoding is always the highest-quality generation method', ['Greedy decoding can be repetitive', 'Top-k and top-p add controlled sampling'], 'Greedy is deterministic, but it can produce repetitive or low-diversity text.'],
  ['gpt2-090-trap-topk-topp', 'Tricky', 'Which sampling comparison is wrong?', 'Top-k and top-p define candidate sets in exactly the same way', ['Top-k uses a fixed count of likely tokens', 'Top-p uses a cumulative probability threshold'], 'The inference step distinguishes fixed-k candidates from nucleus probability mass.'],
  ['gpt2-091-define', 'Interview', 'How should you define GPT-2 in one interview sentence?', 'GPT-2 is a decoder-only transformer trained with causal next-token prediction for text generation', ['GPT-2 is an encoder-only model trained with masked token prediction', 'GPT-2 is a tokenizer without transformer blocks'], 'A concise definition should include architecture family, mask direction, objective, and use case.'],
  ['gpt2-092-input-trace', 'Interview', 'How would you trace GPT-2 input preparation?', 'BPE creates token ids, token embeddings are looked up, and learned positions are added', ['Layer norm creates raw text, then BPE samples logits', 'KV caching creates the training labels before tokenization'], 'The first two lesson steps move from raw text to token-plus-position vectors.'],
  ['gpt2-093-attention-trace', 'Interview', 'How would you explain GPT-2 attention?', 'Each head computes scaled dot-product attention over allowed prefix positions under a causal mask', ['Each head reads the whole future sequence to predict the current token', 'Each head classifies whether two sentences are consecutive'], 'The attention explanation must include QKV scoring and prefix-only visibility.'],
  ['gpt2-094-block-trace', 'Interview', 'What is the high-level GPT-2 block sequence?', 'Pre-LN, masked self-attention with residual, then Pre-LN, FFN with residual', ['Tokenize, classify NSP, extract span, and stop', 'Sample top-p, train BPE, delete positions, and normalize'], 'This combines the attention, FFN, residual, and normalization steps.'],
  ['gpt2-095-configs', 'Interview', 'What should you remember about GPT-2 model sizes?', 'Small through XL scale layers, heads, hidden size, and parameters from 124M to 1.5B', ['All GPT-2 sizes keep the same layers and change only sampling', 'Only the vocabulary size changes across model sizes'], 'The configurator teaches concrete scaling dimensions and their cost implications.'],
  ['gpt2-096-weight-tying', 'Interview', 'How should you explain weight tying?', 'Reuse the token embedding matrix for output projection to reduce parameters and share token geometry', ['Use the same causal mask as the optimizer state', 'Tie every residual connection into one layer'], 'A strong answer names the shared matrices and the parameter-saving reason.'],
  ['gpt2-097-training', 'Interview', 'Name the training optimizations covered in the lesson.', 'Gradient accumulation, mixed precision, gradient clipping, and warmup with cosine decay', ['Greedy, top-k, top-p, and KV caching', 'MLM, NSP, span extraction, and segment ids'], 'Step 8 is about efficient and stable training, not decoding.'],
  ['gpt2-098-inference', 'Interview', 'Name the inference optimizations and choices covered in the lesson.', 'KV caching plus greedy, top-k, and top-p token selection', ['Gradient accumulation plus FP16 master weights', 'BPE merges plus learned absolute position ids only'], 'Step 9 focuses on fast generation and how to choose the next token.'],
  ['gpt2-099-debug', 'Interview', 'If generation is slow and repetitive, what diagnosis should you give?', 'Use KV caching for redundant prefix compute and revisit greedy decoding for repetition', ['Use NSP labels and remove the causal mask', 'Increase BPE vocabulary and delete residuals'], 'The likely issues map to Step 9: compute reuse and sampling strategy.'],
  ['gpt2-100-takeaway', 'Interview', 'What is the production-ready GPT-2 takeaway?', 'Match decoder-only generation, causal masking, scale, training stability, and inference caching to the task constraints', ['Treat GPT-2 as a bidirectional encoder that needs no deployment tradeoffs', 'Treat the largest configuration and greedy decoding as automatic defaults'], 'The lesson is about tracing the whole architecture and choosing the right tradeoffs, not memorizing a name.'],
];

export const GPT2_COMPREHENSIVE_QUIZ = SPECS.map((spec) => q(...spec));
