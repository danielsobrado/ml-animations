function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const rotation = stableHash(`attention-mechanism:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + rotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const ATTENTION_MECHANISM_SPECS = [
  ['attn-001-purpose', 'Foundation', 'What problem does attention solve?', 'It selects useful context instead of averaging everything equally', ['It deletes sequence order from every model', 'It replaces training data with labels'], 'Attention routes information toward the parts of context that match the current query.'],
  ['attn-002-query-role', 'Foundation', 'What is the role of a query vector?', 'It represents what information is being sought', ['It stores the mixed output directly', 'It is the final loss value'], 'A query is compared with keys to decide where to read from.'],
  ['attn-003-key-role', 'Foundation', 'What is the role of a key vector?', 'It represents what a position can be matched against', ['It is always the predicted token', 'It deletes the value vector'], 'Keys are addresses or descriptors used for compatibility scoring.'],
  ['attn-004-value-role', 'Foundation', 'What is the role of a value vector?', 'It carries the content that gets mixed into the output', ['It chooses the softmax axis by itself', 'It is only a route name'], 'Attention weights are applied to values to form the context vector.'],
  ['attn-005-score-meaning', 'Foundation', 'What does a larger query-key score usually mean?', 'That key is more compatible with the query', ['That value is deleted before mixing', 'That training is already complete'], 'Higher scores usually receive larger softmax weights.'],
  ['attn-006-softmax-role', 'Foundation', 'Why does attention apply softmax to scores?', 'To turn competing scores into weights over keys', ['To convert vectors into raw text', 'To remove all value information'], 'Softmax normalizes each query row into a distribution over readable positions.'],
  ['attn-007-weighted-sum', 'Foundation', 'What do attention weights do after softmax?', 'They form a weighted sum of value vectors', ['They sort examples into train and test sets', 'They overwrite the tokenizer vocabulary'], 'The output is a mixture of values, with larger weights copying more content.'],
  ['attn-008-context-vector', 'Foundation', 'What is an attention context vector?', 'The output mixture produced for a query', ['A fixed dictionary definition', 'A scalar learning rate'], 'Each query receives its own mixed representation from values.'],
  ['attn-009-query-specific', 'Foundation', 'Why can two queries attend differently?', 'Each query produces its own scores against the keys', ['All queries share one permanent heatmap', 'Values choose weights without scores'], 'Attention is dynamic and depends on current query-key compatibility.'],
  ['attn-010-not-global-importance', 'Foundation', 'What misconception should you avoid?', 'Treating attention weights as permanent global importance', ['Tracing a high score into a high weight', 'Checking which values are mixed'], 'Weights are contextual for one query, layer, and input, not universal explanations.'],
  ['attn-011-seq2seq-link', 'Foundation', 'Why did attention help sequence-to-sequence models?', 'It let decoders read relevant source positions directly', ['It forced every input into one fixed vector only', 'It removed the need for hidden states'], 'Attention reduced the fixed-bottleneck problem in long sequences.'],
  ['attn-012-transformer-link', 'Foundation', 'Why is attention central to transformers?', 'It lets token representations mix context through learned routing', ['It replaces every MLP and residual connection', 'It makes embeddings unnecessary'], 'Transformers repeatedly use attention to exchange information across positions.'],
  ['attn-013-cross-self-preview', 'Foundation', 'What is the broad difference between self-attention and cross-attention?', 'Self reads the same sequence; cross reads another source', ['Self uses no values; cross uses no keys', 'They are unrelated to Q/K/V'], 'Both use the attention primitive, but their Q, K, and V sources differ.'],
  ['attn-014-mask-preview', 'Foundation', 'What does an attention mask control?', 'Which key positions a query is allowed to read', ['Which optimizer is installed', 'Which values become token ids'], 'Masks block illegal or padded positions before softmax.'],
  ['attn-015-lab-goal', 'Foundation', 'What should the attention lab help you predict?', 'Which value receives the largest weight after a query changes', ['Which CSS class is longest', 'Which answer letter repeats'], 'The lab connects query-key scores to softmax weights and value mixing.'],
  ['attn-016-high-weight-effect', 'Foundation', 'What happens when one value gets most of the attention weight?', 'The output moves close to that value vector', ['The key vector becomes the output text', 'The model skips the weighted sum'], 'A dominant weight makes the mixture mostly copy one value.'],
  ['attn-017-uniform-weights', 'Foundation', 'What do uniform attention weights produce?', 'An average-like mixture of the values', ['A guaranteed correct explanation', 'A deleted context vector'], 'If weights are equal, no value is selected more strongly than another.'],
  ['attn-018-soft-alignment', 'Foundation', 'How can attention behave like soft alignment?', 'A query can spread weight across related positions', ['It assigns one hard pointer by definition', 'It sorts tokens alphabetically'], 'Attention can distribute credit over several possible sources.'],
  ['attn-019-content-addressing', 'Foundation', 'Why is attention called content-based addressing?', 'Matches depend on vector content rather than fixed slot number alone', ['Only token position ever matters', 'Values ignore the query completely'], 'The query finds compatible keys by their learned features.'],
  ['attn-020-foundation-summary', 'Foundation', 'What foundation summary should you remember?', 'Attention scores keys, softmaxes weights, and mixes values for each query', ['Attention is a static importance chart', 'Attention removes all compute cost'], 'This captures the core Q/K/V pipeline and its main caveat.'],

  ['attn-021-formula', 'Mechanism', 'What is the standard scaled dot-product attention formula?', 'softmax(QK^T / sqrt(d_k))V', ['softmax(VK^T) divided by labels', 'accuracy times the query length'], 'Queries score keys, softmax normalizes rows, and weights multiply values.'],
  ['attn-022-score-matrix', 'Mechanism', 'What does QK^T produce?', 'A matrix of query-key compatibility scores', ['A list of final class labels', 'A copied value table'], 'Each row corresponds to a query and each column to a key.'],
  ['attn-023-scaling-factor', 'Mechanism', 'Why divide dot-product scores by sqrt(d_k)?', 'To keep large key dimensions from saturating softmax', ['To remove the value vectors from the model', 'To make all tokens alphabetical'], 'Unscaled high-dimensional dot products can become too large and overly sharp.'],
  ['attn-024-softmax-axis', 'Mechanism', 'Which axis should softmax use in basic attention scoring?', 'Across keys for each query row', ['Across queries for each key column', 'Across model checkpoints'], 'Each query needs a distribution over the keys it can read.'],
  ['attn-025-weight-sum', 'Mechanism', 'What should attention weights sum to for one unmasked query row?', '1 across the allowed keys', ['The number of layers', 'The embedding dimension'], 'Softmax creates a normalized row distribution over allowed positions.'],
  ['attn-026-output-multiply', 'Mechanism', 'What does weights @ V compute?', 'One weighted value mixture per query', ['A new tokenizer vocabulary', 'The validation split'], 'The value matrix supplies the content being combined.'],
  ['attn-027-shapes', 'Mechanism', 'If Q has n_q rows and K has n_k rows, what shape are the scores?', 'n_q by n_k', ['n_k by value_dim by labels', 'One scalar for the whole batch only'], 'Every query scores every key, so the matrix has query rows and key columns.'],
  ['attn-028-value-shape', 'Mechanism', 'Why must the number of value rows match the number of keys?', 'Each key weight must select a corresponding value row', ['Values must be wider than queries', 'Keys and values are never paired'], 'The softmax column for key j weights value j.'],
  ['attn-029-mask-timing', 'Mechanism', 'Where should illegal positions be masked?', 'Before softmax, by lowering their scores', ['After values are averaged', 'After the loss is reported'], 'Masked positions need near-zero probability in the softmax output.'],
  ['attn-030-padding-mask', 'Mechanism', 'What does a padding mask prevent?', 'Reading value content from pad positions', ['Training without gradients', 'Using keys in the score matrix'], 'Pad tokens are structural placeholders and should not contribute content.'],
  ['attn-031-causal-mask', 'Mechanism', 'What does a causal mask prevent?', 'A position reading future keys', ['The model reading any previous token', 'The value vectors from being learned'], 'Autoregressive decoders must not see future positions during prediction.'],
  ['attn-032-cross-attention', 'Mechanism', 'In cross-attention, where can queries and keys come from?', 'Queries from one sequence and keys/values from another', ['All from random labels only', 'Values from the optimizer state only'], 'Decoder-to-encoder attention is a common cross-attention pattern.'],
  ['attn-033-self-attention', 'Mechanism', 'In self-attention, where do Q, K, and V usually originate?', 'From the same sequence representations', ['From three unrelated datasets', 'From only the final loss'], 'Different projections can be made from the same token states.'],
  ['attn-034-projections', 'Mechanism', 'Why use learned Q, K, and V projections?', 'They let the model form matching and content spaces suited to the task', ['They make vectors nonnumeric', 'They remove all trainable parameters'], 'Projection matrices learn what should be queried, keyed, and carried as value content.'],
  ['attn-035-multihead-preview', 'Mechanism', 'What does multi-head attention preview conceptually?', 'Several attention operations run in parallel with different projections', ['One attention score copied forever', 'A tokenizer with no embeddings'], 'Multiple heads can learn different routing patterns.'],
  ['attn-036-softmax-competition', 'Mechanism', 'What happens to other weights if one unmasked score becomes much larger?', 'They usually shrink because softmax mass is limited', ['They all increase independently', 'They become raw token strings'], 'Within a row, attention weights compete for total probability mass.'],
  ['attn-037-temperature', 'Mechanism', 'How does softer scoring affect attention weights?', 'It spreads mass more evenly across keys', ['It deletes the value matrix', 'It changes keys into labels'], 'Lower sharpness or higher temperature makes the distribution less peaked.'],
  ['attn-038-sharpness', 'Mechanism', 'What does a very sharp attention row indicate?', 'Most weight concentrates on a small number of keys', ['Every value is ignored equally', 'The score matrix has no rows'], 'Sharp rows copy strongly from selected values.'],
  ['attn-039-uniform-mechanism', 'Mechanism', 'When can attention become average-like?', 'When scores are equal across allowed keys', ['When all masks are illegal', 'When values have no rows'], 'Equal scores softmax to equal weights.'],
  ['attn-040-entropy-diagnostic', 'Mechanism', 'What does attention entropy help diagnose?', 'How concentrated or spread a row of weights is', ['Whether the dataset is alphabetized', 'Whether routes are SEO-ready'], 'Entropy summarizes how selective a query row is.'],
  ['attn-041-complexity', 'Mechanism', 'Why can full attention become expensive on long sequences?', 'All query-key pairs are scored', ['Only one token is ever compared', 'It never uses matrix multiplication'], 'Dense all-pairs scoring grows quickly with sequence length.'],
  ['attn-042-memory', 'Mechanism', 'What memory object can become large in naive attention?', 'The full attention score or weight matrix', ['The list of class names only', 'A single scalar loss'], 'Storing all pairwise scores can dominate long-sequence memory.'],
  ['attn-043-numerical-stability', 'Mechanism', 'Why do implementations use stable softmax tricks?', 'Large scores can overflow without stabilization', ['Softmax requires text strings', 'Values must be negative'], 'Subtracting row maxima is a common stable softmax step.'],
  ['attn-044-gradient-flow', 'Mechanism', 'Where can gradients flow through attention?', 'Through scores, weights, projections, and values', ['Only through route names', 'Only through nonnumeric labels'], 'The whole Q/K/V computation is differentiable apart from mask choices.'],
  ['attn-045-value-content', 'Mechanism', 'Why is a high score not enough by itself?', 'The selected value content determines what enters the output', ['Scores are never used in softmax', 'Values are always empty'], 'Scores route information, but values carry the information being routed.'],
  ['attn-046-interpretation-limit', 'Mechanism', 'Why are attention maps limited explanations?', 'They show one routing pattern, not the whole model reason', ['They are exact causal proofs', 'They replace validation metrics'], 'Downstream behavior also depends on values, projections, layers, and residual paths.'],
  ['attn-047-position-role', 'Mechanism', 'Why can position information still matter with attention?', 'Content matching alone does not encode order by itself', ['Attention always sorts tokens correctly', 'Values delete all positions'], 'Models add positional signals so attention can use order-sensitive patterns.'],
  ['attn-048-residual-context', 'Mechanism', 'How does attention usually fit inside a transformer block?', 'Its output is combined with residual, norm, and MLP components', ['It is the entire transformer by itself', 'It replaces every nonlinearity'], 'Attention is a mixing sublayer inside a larger architecture.'],
  ['attn-049-debug-checks', 'Mechanism', 'What implementation checks matter for attention?', 'Shapes, masks, softmax axis, scaling, and value pairing', ['Only icon names and colors', 'Only route ordering'], 'Most attention bugs come from mismatched shapes, axes, or masking.'],
  ['attn-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide debugging?', 'Trace QK scores to row softmax weights to weighted values', ['Treat the heatmap as a permanent dictionary', 'Ignore keys once values exist'], 'The score-softmax-mix pipeline is the central causal chain.'],

  ['attn-051-query-change', 'Application', 'If a query vector moves closer to one key, what should you expect?', 'That key may receive a larger attention weight', ['The value matrix must disappear', 'Every key becomes masked'], 'Changing query-key compatibility changes the score row.'],
  ['attn-052-key-change', 'Application', 'If one key vector changes but values stay fixed, what can change?', 'The weights assigned to value rows', ['Only the final route title', 'The number of examples in the dataset'], 'Keys influence routing even when values carry the same content.'],
  ['attn-053-value-change', 'Application', 'If attention weights stay fixed but one value changes, what changes?', 'The output mixture can change', ['The score matrix must change first', 'The mask becomes invalid by definition'], 'Values determine the content copied by existing weights.'],
  ['attn-054-wrong-axis', 'Application', 'Softmax is accidentally applied across queries instead of keys. What breaks?', 'Each query no longer has its own distribution over readable keys', ['The model gains perfect explanations', 'Values become labels automatically'], 'The row-wise interpretation depends on normalizing over keys.'],
  ['attn-055-missing-padding-mask', 'Application', 'A padded batch attends strongly to pad tokens. What is likely missing?', 'A padding mask before softmax', ['A larger learning rate only', 'A new CSS layout'], 'Pad positions must be blocked from receiving useful attention mass.'],
  ['attn-056-missing-causal-mask', 'Application', 'A decoder can read future target tokens during training. What is the bug?', 'The causal mask is missing or applied incorrectly', ['The values are too informative', 'The query has too many rows'], 'Future leakage makes autoregressive training and evaluation misleading.'],
  ['attn-057-uniform-output', 'Application', 'All allowed scores are equal for one query. What output should you expect?', 'A roughly average mixture of allowed values', ['A guaranteed correct source alignment', 'A deleted value vector'], 'Equal weights mean no selective routing.'],
  ['attn-058-dominant-score', 'Application', 'One allowed score is far above the rest. What should happen after softmax?', 'Most weight should concentrate on that key', ['All keys should receive equal mass', 'The mask should become all zero'], 'Softmax turns a large score gap into a peaked distribution.'],
  ['attn-059-long-context-cost', 'Application', 'Why can naive attention struggle on very long context?', 'The number of query-key comparisons grows rapidly', ['The formula stops using vectors', 'Long context removes all masks'], 'All-pairs scoring and storage are costly at long lengths.'],
  ['attn-060-rag-context', 'Application', 'In a RAG answer, retrieved chunks become extra context. How is attention relevant?', 'Queries can read useful retrieved tokens through key-value routing', ['Retrieval quality is proven by attention alone', 'Chunks bypass all model layers'], 'Attention uses provided context, but retrieval quality still matters.'],
  ['attn-061-cross-modal', 'Application', 'A text query reads image patch features. What attention pattern is this?', 'Cross-modal attention over aligned keys and values', ['A train/test split', 'A scalar-only loss calculation'], 'Cross-attention can route information between modalities when representations are aligned.'],
  ['attn-062-translation-alignment', 'Application', 'In translation, a target token weights several source tokens. What is this like?', 'A soft alignment between target query and source keys', ['A hard-coded dictionary lookup only', 'A proof that the translation is correct'], 'Attention can distribute source evidence across multiple positions.'],
  ['attn-063-heatmap-review', 'Application', 'What should you remember when reading an attention heatmap?', 'It is query-specific and not a full explanation', ['It proves global token importance', 'It replaces error analysis'], 'Heatmaps are useful diagnostics but not complete causal stories.'],
  ['attn-064-duplicate-keys', 'Application', 'Two keys are nearly identical for one query. What may happen?', 'Attention mass may split between them', ['The output must become zero', 'One key is deleted automatically'], 'Softmax can divide probability across similarly scored keys.'],
  ['attn-065-all-values-same', 'Application', 'All value rows are identical. What does attention selectivity change?', 'Little in the output, even if weights differ', ['The score formula becomes invalid', 'The softmax axis switches'], 'Different weights matter less when the content being mixed is the same.'],
  ['attn-066-nan-scores', 'Application', 'A score row contains NaN values. What should be done?', 'Fix or reject invalid scores before softmax', ['Treat NaN as best attention', 'Convert NaN into a token id'], 'Invalid numeric values can poison the weight distribution.'],
  ['attn-067-mask-all-keys', 'Application', 'A query has every key masked. What is the concern?', 'Softmax has no valid position to normalize over', ['The output is guaranteed perfect', 'The value matrix becomes larger'], 'Implementations need a defined fallback for all-masked rows.'],
  ['attn-068-scale-omitted', 'Application', 'Scaling by sqrt(d_k) is omitted in a large head dimension. What risk appears?', 'Softmax can become overly sharp and unstable', ['Attention becomes cross-validation', 'Values stop carrying content'], 'Large dot products can saturate the distribution.'],
  ['attn-069-low-confidence', 'Application', 'A row has diffuse weights over many unrelated keys. What might you inspect?', 'Query/key quality, masking, and representation fit', ['Only the favicon file', 'Only the number of answer choices'], 'Diffuse attention can indicate uncertainty or poor matching.'],
  ['attn-070-production-logging', 'Application', 'What logs help debug an attention-based failure?', 'Shapes, mask counts, score ranges, and selected high-weight positions', ['Only button colors', 'Only package names'], 'These logs expose common implementation and data issues.'],
  ['attn-071-bias-risk', 'Application', 'Why can attention still route biased context?', 'Weights depend on representations learned from biased data', ['Softmax guarantees fairness', 'Keys remove all social information'], 'Attention can amplify problematic correlations in the representations.'],
  ['attn-072-explainability-risk', 'Application', 'A stakeholder treats one attention map as proof of why the model answered. What is wrong?', 'Attention maps are incomplete evidence for model reasoning', ['Attention maps have no numeric values', 'The value vectors are always labels'], 'A single map should be paired with tests, ablations, and error analysis.'],
  ['attn-073-routing-vs-retrieval', 'Application', 'How is attention routing different from external retrieval?', 'Attention mixes provided context; retrieval chooses what context enters', ['They are always the same database call', 'Retrieval replaces softmax weights'], 'RAG quality depends on both retrieved content and model attention over it.'],
  ['attn-074-shape-debug', 'Application', 'An implementation errors at weights @ V. What shape issue is likely?', 'The key dimension in weights does not match the value rows', ['The prompt is too short', 'The answer index is invalid'], 'The number of columns in weights must equal the number of value rows.'],
  ['attn-075-application-summary', 'Application', 'What practical habit prevents attention mistakes?', 'Check scores, masks, axes, values, heatmaps, and task outcomes', ['Trust one bright cell as truth', 'Ignore invalid numeric rows'], 'Reliable attention work connects implementation traces to behavior.'],

  ['attn-076-false-query-trap', 'Tricky', 'Which query claim is false?', 'The query is already the final mixed output', ['The query scores keys', 'Different queries can route differently'], 'Queries ask where to read; values form the mixed output.'],
  ['attn-077-false-key-trap', 'Tricky', 'Which key claim is false?', 'Keys are summed directly into the attention output', ['Keys are compared with queries', 'Keys help determine weights'], 'Values, not keys, are mixed into the output.'],
  ['attn-078-false-value-trap', 'Tricky', 'Which value claim is false?', 'Values decide their own attention weights without scores', ['Values carry mixed content', 'Weights multiply value vectors'], 'Scores from queries and keys determine the weights.'],
  ['attn-079-false-softmax-trap', 'Tricky', 'Which softmax claim is false?', 'Softmax should normalize across query rows for each key column', ['Each query needs weights over keys', 'Softmax turns scores into a row distribution'], 'The usual axis is over keys for each query.'],
  ['attn-080-false-mask-trap', 'Tricky', 'Which mask claim is false?', 'Masks should be applied only after values are fully mixed', ['Masks block illegal positions before softmax', 'Padding and causal rules use masks'], 'Masks must affect scores before probabilities are assigned.'],
  ['attn-081-false-importance-trap', 'Tricky', 'Which interpretation claim is false?', 'One attention heatmap proves permanent global token importance', ['A heatmap can be a useful diagnostic', 'Weights are contextual to the query and layer'], 'Attention maps are local routing views, not universal explanations.'],
  ['attn-082-false-cost-trap', 'Tricky', 'Which cost claim is false?', 'Dense attention is free at million-token scale', ['All-pairs scoring can be expensive', 'Long contexts motivate efficient attention methods'], 'Full attention has real compute and memory costs.'],
  ['attn-083-false-scaling-trap', 'Tricky', 'Which scaling claim is false?', 'The sqrt(d_k) scale removes the need for softmax', ['Scaling keeps scores in a usable range', 'Softmax still produces weights'], 'Scaling and softmax solve different parts of the computation.'],
  ['attn-084-false-cross-trap', 'Tricky', 'Which cross-attention claim is false?', 'Cross-attention means queries, keys, and values must all come from one sequence', ['Cross-attention can read another sequence', 'Decoder queries can attend to encoder memory'], 'Cross-attention separates query source from key/value source.'],
  ['attn-085-false-self-trap', 'Tricky', 'Which self-attention claim is false?', 'Self-attention means the model uses no Q/K/V projections', ['Self-attention can derive Q, K, and V from the same sequence', 'Self-attention still uses the attention primitive'], 'Self-attention changes the source of Q/K/V, not the underlying roles.'],
  ['attn-086-false-output-trap', 'Tricky', 'Which output claim is false?', 'Attention output is the raw score matrix itself', ['Attention output is a weighted value mixture', 'Scores are an intermediate routing signal'], 'Scores route; weighted values are the output.'],
  ['attn-087-false-uniform-trap', 'Tricky', 'Which uniform-weight claim is false?', 'Uniform attention proves the model selected the best key', ['Uniform weights are average-like', 'Equal scores produce equal weights'], 'Uniform rows show little selectivity, not proof of correctness.'],
  ['attn-088-false-position-trap', 'Tricky', 'Which position claim is false?', 'Attention automatically knows sequence order without any position signal', ['Position information can be added separately', 'Order-sensitive tasks need positional cues'], 'Content matching alone does not encode absolute or relative order.'],
  ['attn-089-false-transformer-trap', 'Tricky', 'Which transformer claim is false?', 'A transformer block is only attention and nothing else', ['Attention often sits with residual, norm, and MLP layers', 'Attention is a key transformer sublayer'], 'Transformers combine attention with other components.'],
  ['attn-090-tricky-summary', 'Tricky', 'Which attention belief should you reject?', 'Attention weights are complete causal explanations by themselves', ['Attention weights show contextual routing', 'Mechanism and behavior still need validation'], 'Interpretability requires more evidence than one set of weights.'],

  ['attn-091-interview-definition', 'Interview', 'How should you define attention in one sentence?', 'A differentiable way to score keys and mix values for each query', ['A fixed dictionary of word importance', 'A replacement for all model layers'], 'A concise definition should mention query, keys, values, and mixing.'],
  ['attn-092-interview-qkv', 'Interview', 'How should you explain Q, K, and V?', 'Queries ask, keys match, and values carry the content to copy', ['Queries are labels, keys are losses, and values are routes', 'All three are always identical outputs'], 'This wording captures the roles without overcomplicating the mechanism.'],
  ['attn-093-interview-formula', 'Interview', 'How should you describe scaled dot-product attention?', 'Compute QK^T, scale, softmax over keys, then multiply by V', ['Average V before scoring Q and K', 'Sort tokens alphabetically then choose a label'], 'This follows the actual score-softmax-value pipeline.'],
  ['attn-094-interview-mask', 'Interview', 'What mask detail should you mention?', 'Masks block illegal or padded keys before softmax', ['Masks are decorative heatmap colors', 'Masks are applied after final logits only'], 'Mask placement is a common correctness issue.'],
  ['attn-095-interview-axis', 'Interview', 'What softmax-axis detail should you state?', 'Normalize over keys separately for each query', ['Normalize over all batches at once', 'Normalize over optimizer names'], 'The row-wise distribution gives each query its own routing.'],
  ['attn-096-interview-scaling', 'Interview', 'Why mention sqrt(d_k) scaling?', 'It prevents large dot products from making softmax too sharp', ['It removes the need for values', 'It makes every attention map causal'], 'Scaling is core to stable dot-product attention.'],
  ['attn-097-interview-interpretation', 'Interview', 'What caveat should you give about attention weights?', 'They are contextual routing signals, not complete explanations', ['They prove permanent token importance', 'They guarantee fairness'], 'A mature answer avoids overstating heatmaps.'],
  ['attn-098-interview-debugging', 'Interview', 'What debugging checklist should you name?', 'Shapes, masks, score ranges, softmax axis, and value pairing', ['Only CSS class names', 'Only the number of route entries'], 'These checks catch the most common implementation failures.'],
  ['attn-099-interview-applications', 'Interview', 'What applications should you connect to attention?', 'Seq2seq alignment, transformers, RAG context use, and multimodal fusion', ['Only train/test splitting', 'Only static glossary pages'], 'Attention is a general routing primitive used across architectures.'],
  ['attn-100-interview-summary', 'Interview', 'What production-ready attention takeaway should you give?', 'Validate the whole path from scores to weights to mixed values and outcomes', ['Trust one heatmap as a final explanation', 'Skip masks because softmax fixes everything'], 'Production readiness combines correct mechanics with behavioral validation.'],
];

export const ATTENTION_MECHANISM_QUIZ = ATTENTION_MECHANISM_SPECS.map(([id, level, prompt, correct, distractors, explanation]) => (
  q(id, level, prompt, correct, distractors, explanation)
));
