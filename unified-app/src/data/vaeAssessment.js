function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/-(\d{3})-/)?.[1] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const registryRotation = stableHash(`vae:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return { id, level, choices, answerIndex, prompt, explanation };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const SPECS = [
  ['vae-001-purpose', 'Foundation', 'What is the main purpose of the VAE lesson?', 'Learning a generative latent variable model that can reconstruct data and sample new data', ['Training a classifier that only outputs labels', 'Sorting pixels with no learned representation'], 'The lesson frames a VAE as a generative model built from an encoder, latent distribution, and decoder.'],
  ['vae-002-big-picture', 'Foundation', 'Which pipeline matches the VAE architecture overview?', 'Input x goes to encoder, latent sample z, decoder, then reconstruction x_hat', ['Input x goes directly to labels with no decoder', 'Noise is added after the final reconstruction only'], 'The overview shows input, encoder distribution, latent sample, decoder, and reconstructed output.'],
  ['vae-003-encoder-role', 'Foundation', 'What does the encoder produce in a VAE?', 'Parameters of an approximate posterior distribution q(z|x)', ['A final generated image with no decoder', 'A hard class label used as the latent vector'], 'The encoder maps data into distribution parameters rather than one fixed code.'],
  ['vae-004-decoder-role', 'Foundation', 'What does the decoder represent in the VAE diagram?', 'A conditional model p(x|z) that maps a latent sample back to data space', ['A sampler that chooses optimizer learning rates', 'A preprocessing step that deletes latent variables'], 'The decoder turns latent samples into reconstructions or generated examples.'],
  ['vae-005-regular-ae', 'Foundation', 'How is a VAE different from a regular autoencoder at the bottleneck?', 'A VAE encodes a distribution while a regular autoencoder usually encodes a deterministic vector', ['A VAE has no bottleneck but a regular autoencoder does', 'A VAE cannot reconstruct inputs but a regular autoencoder can'], 'The comparison table contrasts probabilistic latent distributions with deterministic codes.'],
  ['vae-006-mu-logvar', 'Foundation', 'Which two heads does the encoder commonly output?', 'One head for mu and one head for log-variance', ['One head for labels and one head for accuracy', 'One head for pixels and one head for optimizer state'], 'The encoder panel sends the hidden representation to separate mean and log-variance outputs.'],
  ['vae-007-logvar-reason', 'Foundation', 'Why does the lesson use log-variance instead of raw variance?', 'Log-variance can be any real number while variance remains positive after exponentiation', ['Log-variance removes the need for a decoder', 'Log-variance makes the KL term exactly zero for every input'], 'The panel notes that sigma comes from exp(0.5 * logvar), which keeps scale positive.'],
  ['vae-008-reparameterization', 'Foundation', 'What is the reparameterization trick written in the lesson?', 'z = mu + sigma * epsilon with epsilon sampled from N(0,I)', ['z = decoder(x) + optimizer_state', 'z = argmax over class probabilities'], 'The trick separates random epsilon from the learned mu and sigma path.'],
  ['vae-009-epsilon', 'Foundation', 'What is epsilon in the sampling step?', 'Standard normal noise used outside the learned encoder parameters', ['The target image after reconstruction', 'A trainable class embedding chosen by the decoder'], 'Epsilon supplies randomness while gradients still flow through mu and sigma.'],
  ['vae-010-generation', 'Foundation', 'How does a trained VAE generate a new sample?', 'Sample z from the prior N(0,I) and pass it through the decoder', ['Run the encoder on a label and skip the decoder', 'Copy the nearest training image exactly'], 'After training, generation can begin from the prior without an input image.'],
  ['vae-011-prior', 'Foundation', 'What prior does the lesson use for the latent space?', 'A standard normal distribution N(0,I)', ['A sorted list of class names', 'A uniform table of optimizer steps'], 'The KL term encourages q(z|x) to stay close to this simple prior.'],
  ['vae-012-reconstruction-loss', 'Foundation', 'What does reconstruction loss measure?', 'How well x_hat matches the original input x', ['How many latent dimensions are unused', 'How many tabs the lesson contains'], 'Reconstruction loss compares the decoder output with the data that entered the encoder.'],
  ['vae-013-kl-loss', 'Foundation', 'What does the KL divergence term encourage?', 'The learned q(z|x) should stay close to the prior distribution', ['The decoder should ignore every latent sample', 'The model should maximize pixel error'], 'KL regularization makes the latent space smoother and easier to sample from.'],
  ['vae-014-elbo-name', 'Foundation', 'What does ELBO stand for in the loss panel?', 'Evidence Lower Bound', ['Expected Label Batch Output', 'Encoder Linear Bias Offset'], 'The loss panel introduces the VAE objective as the Evidence Lower Bound.'],
  ['vae-015-total-loss', 'Foundation', 'In the simulation, how is total VAE loss displayed?', 'Reconstruction loss plus beta times KL loss', ['Only KL loss with no reconstruction term', 'Only decoder parameter count'], 'The loss panel uses totalLoss = reconLoss + beta * klLoss for the minimized form.'],
  ['vae-016-beta-one', 'Foundation', 'What does beta = 1 mean in the beta slider explanation?', 'The standard VAE balance between reconstruction and regularization', ['A deterministic autoencoder with no KL pressure', 'A model that never decodes samples'], 'The lesson labels beta = 1 as the standard VAE setting.'],
  ['vae-017-beta-high', 'Foundation', 'What is the high-level effect of beta greater than 1?', 'Stronger regularization with more disentanglement pressure and blurrier reconstructions', ['No regularization and perfectly sharp samples', 'A decoder that stops using latent variables'], 'The beta-VAE note says larger beta can improve disentanglement but often hurts reconstruction sharpness.'],
  ['vae-018-latent-interpolation', 'Foundation', 'What does latent interpolation demonstrate?', 'Smooth movement between latent points can decode into gradual output changes', ['Changing option letters alters the model weights', 'The decoder works only for training examples'], 'The latent panel shows a path between latent points and decoded outputs along the way.'],
  ['vae-019-posterior-collapse', 'Foundation', 'What is posterior collapse in VAE practice?', 'The encoder ignores input and outputs a distribution close to the prior', ['The decoder produces larger images than requested', 'The optimizer stores every generated sample'], 'The practice quiz names the failure where mu is near zero and sigma near one regardless of input.'],
  ['vae-020-blurry-images', 'Foundation', 'Why can VAE reconstructions look blurry?', 'Pixel-wise likelihood objectives can average over uncertain outputs', ['The model is restricted to grayscale images forever', 'The encoder is forbidden to learn hidden layers'], 'The practice panel contrasts likelihood-style averaging with sharper adversarial objectives.'],
  ['vae-021-input-flattening', 'Mechanism', 'In the encoder example, what happens to a 28 by 28 grayscale image first?', 'It is flattened into a 784-dimensional vector', ['It becomes a 20-class one-hot label', 'It is decoded before any encoding'], 'The encoder walkthrough starts by flattening the image into one feature vector.'],
  ['vae-022-hidden-compression', 'Mechanism', 'What do the 784 to 400 to 200 encoder layers illustrate?', 'Progressive compression before predicting latent distribution parameters', ['Expansion into a larger pixel grid before loss', 'A separate classifier that replaces the latent space'], 'The panel uses hidden layers to compress the input before the mean and log-variance heads.'],
  ['vae-023-two-heads', 'Mechanism', 'Why are mu and logvar produced by separate linear heads?', 'The same hidden summary must define both the center and spread of q(z|x)', ['The decoder requires two different image sizes', 'The optimizer cannot update a shared hidden layer'], 'A Gaussian posterior needs both location and scale information.'],
  ['vae-024-sigma-compute', 'Mechanism', 'How is sigma recovered from logvar in the code pattern?', 'sigma = exp(0.5 * logvar)', ['sigma = softmax(mu)', 'sigma = decoder(logvar)'], 'The square root of variance is implemented by multiplying log-variance by 0.5 before exponentiating.'],
  ['vae-025-gradient-path', 'Mechanism', 'Why does reparameterization help backpropagation?', 'Randomness is isolated in epsilon while z remains differentiable with respect to mu and sigma', ['It makes sampling deterministic for every training row', 'It removes the reconstruction loss from the objective'], 'Gradients can flow through the arithmetic that combines mu, sigma, and epsilon.'],
  ['vae-026-sampling-location', 'Mechanism', 'Where does randomness enter during latent sampling?', 'Through epsilon rather than through a non-differentiable draw from q(z|x)', ['Through the final answerIndex of the quiz', 'Through deleting the decoder weights'], 'The trick rewrites sampling so the stochastic source is separated from learned parameters.'],
  ['vae-027-kl-closed-form', 'Mechanism', 'Which terms appear in the Gaussian KL expression shown in the lesson?', '1 plus logvar minus mu squared minus exp(logvar)', ['Only reconstruction pixels and class labels', 'Only decoder weights and learning rate'], 'The code uses -0.5 * sum(1 + logvar - mu.pow(2) - logvar.exp()).'],
  ['vae-028-kl-minimum', 'Mechanism', 'When is the KL term small for a latent dimension?', 'When mu is near 0 and variance is near 1', ['When mu is huge and variance is near 0', 'When reconstruction loss is manually hidden'], 'The standard normal prior has mean 0 and variance 1.'],
  ['vae-029-bce-mse', 'Mechanism', 'Why does the loss panel mention BCE or MSE?', 'They are common reconstruction losses depending on the data likelihood assumption', ['They are routing losses for expert selection', 'They replace the need to decode x_hat'], 'Binary cross entropy fits normalized binary-like pixels, while MSE fits Gaussian-style errors.'],
  ['vae-030-sigmoid-output', 'Mechanism', 'Why does the decoder example use sigmoid on the output layer?', 'It constrains normalized pixel predictions to the [0,1] range', ['It forces q(z|x) to equal the prior exactly', 'It makes the hidden dimension equal to 784'], 'The decoder panel connects sigmoid with normalized image pixels and BCE.'],
  ['vae-031-decoder-expansion', 'Mechanism', 'What does the decoder path 20 to 200 to 400 to 784 show?', 'Expansion from a compact latent vector back to the original data shape', ['Compression of images after reconstruction', 'A top-k router choosing experts'], 'The decoder mirrors the encoder by turning a small z into pixel logits or values.'],
  ['vae-032-reshape', 'Mechanism', 'After the decoder outputs 784 values in the example, what happens?', 'The vector is reshaped into a 28 by 28 image', ['The vector becomes the next optimizer step', 'The vector is used as the KL prior directly'], 'The decoder output has the same number of values as the flattened input image.'],
  ['vae-033-reconstruction-mode', 'Mechanism', 'What happens in reconstruction mode?', 'An input is encoded to z and decoded back into x_hat for comparison with x', ['A random prior sample is labeled without decoding', 'The KL term is computed from class accuracy'], 'Reconstruction mode uses both encoder and decoder on an observed input.'],
  ['vae-034-generation-mode', 'Mechanism', 'What happens in generation mode?', 'The model samples from N(0,I) and decodes without needing an input image', ['The encoder must receive the exact original training image', 'The reconstruction loss is minimized at inference time'], 'The generation panel explicitly removes the encoder from the sampling path after training.'],
  ['vae-035-latent-gaussian', 'Mechanism', 'What does the Gaussian distribution mode in the latent panel emphasize?', 'KL pressure organizes latent codes around a smooth standard normal space', ['Latent vectors must be integers only', 'Class labels are the only valid latent coordinates'], 'A smooth Gaussian latent space supports interpolation and prior sampling.'],
  ['vae-036-class-clusters', 'Mechanism', 'What does the class clusters view illustrate?', 'Similar inputs tend to occupy nearby regions of latent space', ['Every example must map to the same point', 'The decoder deletes class structure'], 'The latent panel shows structure where related examples cluster together.'],
  ['vae-037-interp-formula', 'Mechanism', 'Which formula describes the linear interpolation shown?', 'z_interp = (1 - t) * z_start + t * z_end', ['z_interp = argmax(decoder_logits)', 'z_interp = beta / reconstruction_loss'], 'The latent panel uses this expression to walk between two latent vectors.'],
  ['vae-038-prior-match', 'Mechanism', 'Why does matching q(z|x) to N(0,I) matter for generation?', 'Random prior samples are more likely to land in regions the decoder learned to use', ['It guarantees reconstruction loss is always zero', 'It prevents the decoder from seeing any latent vector'], 'Without prior matching, decoded prior samples may fall into holes between training codes.'],
  ['vae-039-holes', 'Mechanism', 'What are holes in latent space?', 'Regions sampled from the prior that the decoder did not learn to decode well', ['Extra tabs in the user interface', 'Weights that are too small to store on disk'], 'The loss panel warns that weak KL can leave gaps that hurt generation.'],
  ['vae-040-high-recon-low-kl', 'Mechanism', 'What tradeoff appears with high reconstruction quality but weak KL regularization?', 'Sharp reconstructions but possible holes and poor generation', ['No reconstructions and perfect prior sampling', 'A classifier with no latent variables'], 'The tradeoff section says minimizing reconstruction alone can break latent smoothness.'],
  ['vae-041-low-recon-high-kl', 'Mechanism', 'What tradeoff appears with strong KL pressure?', 'Smoother latent space with blurrier reconstructions', ['Sharper reconstructions with no regularization', 'A decoder that exactly memorizes every pixel'], 'The lesson frames stronger KL as improving continuity while limiting information.'],
  ['vae-042-beta-zero', 'Mechanism', 'What does beta = 0 reduce the objective toward?', 'A regular autoencoder-like objective with no KL constraint', ['A beta-VAE with maximum disentanglement', 'A model that optimizes only the prior'], 'The slider text says beta zero removes the KL constraint.'],
  ['vae-043-beta-via-loss', 'Mechanism', 'How does beta change the minimized VAE loss?', 'It multiplies the KL term before adding it to reconstruction loss', ['It changes the number of output pixels', 'It sorts latent dimensions alphabetically'], 'The simulation exposes beta as the weight on KL regularization.'],
  ['vae-044-elbo-sign', 'Mechanism', 'How does maximizing ELBO relate to the displayed loss?', 'Minimizing reconstruction plus KL is the common negative-ELBO training form', ['Maximizing ELBO means increasing pixel error only', 'ELBO is unrelated to the VAE objective'], 'The lesson presents ELBO as reconstruction log-likelihood minus KL, while code often minimizes the negative form.'],
  ['vae-045-info-bottleneck', 'Mechanism', 'Why can high KL act like an information bottleneck?', 'It limits how much input-specific information z can carry away from the prior', ['It expands z until it equals the input dimension', 'It removes all noise from the model'], 'Strong regularization pulls q(z|x) toward N(0,I), reducing encoded detail.'],
  ['vae-046-posterior-collapse-mechanism', 'Mechanism', 'How can posterior collapse happen mechanistically?', 'A powerful decoder learns to model outputs while the encoder distribution stays near the prior', ['The encoder emits negative variance values', 'The reconstruction term becomes the only logged metric'], 'Collapse means z carries little information because q(z|x) looks like the prior for many inputs.'],
  ['vae-047-latent-dim', 'Mechanism', 'What does latent dimension control?', 'The capacity of z to carry compressed factors before decoding', ['The number of answer choices in the assessment', 'The total number of training epochs already completed'], 'The practice panel lets learners compare small visualization dimensions with practical latent sizes.'],
  ['vae-048-typical-mnist-dim', 'Mechanism', 'What latent size range does the practice quiz call typical for MNIST-like VAEs?', 'Roughly 10 to 50 dimensions', ['Exactly 784 dimensions for every useful VAE', 'Exactly one dimension for all images'], 'The local quiz notes 2D is mainly for visualization, while practical settings often use larger z.'],
  ['vae-049-param-heads', 'Mechanism', 'Why do the mu and logvar heads double the latent-output parameters?', 'Each latent coordinate needs both a mean parameter and a log-variance parameter', ['Each pixel is decoded twice for display only', 'The optimizer stores two copies of every batch'], 'The parameter calculator counts separate projection heads from hidden state to latent statistics.'],
  ['vae-050-param-mirror', 'Mechanism', 'What does the decoder parameter calculator mirror?', 'Hidden expansion from latent dimension back toward input dimension', ['The quiz scoring distribution', 'A second encoder that replaces generation'], 'The calculator counts decoder layers that reverse the compression direction.'],
  ['vae-051-choose-generation', 'Application', 'You need novel digit-like samples after training. Which path should you use?', 'Draw z from N(0,I) and run the decoder', ['Feed an empty tensor through only the encoder', 'Increase reconstruction loss at test time'], 'Generation uses prior samples because the decoder was trained to map latent points back to data.'],
  ['vae-052-choose-reconstruction', 'Application', 'You need to denoise or reconstruct a given input image. Which path is appropriate?', 'Encode the input, sample or use its latent representation, then decode x_hat', ['Sample a random z and ignore the input entirely', 'Use KL divergence as the output image'], 'Reconstruction starts from the observed x and compares the decoder result against it.'],
  ['vae-053-loss-for-normalized', 'Application', 'For normalized binary-like pixels in the decoder example, which reconstruction loss is natural?', 'Binary cross entropy paired with sigmoid outputs', ['Hinge loss on class ranks', 'Top-k routing loss'], 'The lesson pairs sigmoid [0,1] outputs with BCE for image pixels.'],
  ['vae-054-loss-for-continuous', 'Application', 'For continuous-valued reconstructions, which alternative reconstruction loss does the panel mention?', 'Mean squared error under a Gaussian-style assumption', ['PageRank teleportation loss', 'Vocabulary cross entropy only'], 'The loss panel lists MSE as an option when BCE is not the right data likelihood.'],
  ['vae-055-debug-holes', 'Application', 'A VAE reconstructs training images well but prior samples look invalid. What should you inspect first?', 'Whether KL regularization is too weak and the latent space has holes', ['Whether the tab labels are sorted alphabetically', 'Whether the decoder output has no pixels'], 'Good reconstruction with poor generation is the classic weak-prior-match warning.'],
  ['vae-056-debug-blur', 'Application', 'A VAE produces smooth but blurry reconstructions after increasing beta. What is the likely tradeoff?', 'The stronger KL term is limiting detail carried through z', ['The encoder is now outputting raw class labels', 'The prior has been removed from the model'], 'Large beta strengthens regularization and can sacrifice reconstruction fidelity.'],
  ['vae-057-debug-collapse', 'Application', 'You observe mu near 0 and sigma near 1 for many different inputs. What failure is likely?', 'Posterior collapse where z carries little input information', ['Perfect disentanglement with guaranteed sharp images', 'A successful replacement of the decoder'], 'Those statistics match the prior, which suggests the encoder is not distinguishing inputs.'],
  ['vae-058-tune-beta', 'Application', 'If samples are incoherent because latent space is poorly regularized, which beta change is most plausible?', 'Increase beta carefully to strengthen KL pressure', ['Set beta permanently to zero', 'Delete the reconstruction term entirely'], 'More KL pressure can improve prior matching, but the lesson warns about blur if it is too strong.'],
  ['vae-059-tune-sharpness', 'Application', 'If reconstructions are too blurry and KL dominates, what adjustment fits the lesson tradeoff?', 'Reduce beta or otherwise ease KL pressure while monitoring generation quality', ['Increase beta without limit', 'Remove the decoder sigmoid from all possible data types'], 'Less KL pressure can allow z to carry more detail, but may reopen latent holes.'],
  ['vae-060-latent-visualization', 'Application', 'Why might a teaching demo use a 2D latent space?', 'It makes interpolation and clusters visible even if it is smaller than practical settings', ['It is the only latent size that can train', 'It prevents the need for KL divergence'], 'The practice quiz distinguishes visualization convenience from realistic capacity.'],
  ['vae-061-latent-too-large', 'Application', 'What risk appears if latent dimension is as large as the input?', 'The model may behave like a weak bottleneck and memorize details instead of learning compact factors', ['The decoder cannot accept any latent vector', 'KL divergence becomes syntactically impossible'], 'A bottleneck is useful because it forces compression and factor learning.'],
  ['vae-062-latent-too-small', 'Application', 'What risk appears if latent dimension is extremely small?', 'Important information may be lost, increasing reconstruction blur or errors', ['The prior stops being normal', 'The model automatically becomes a classifier'], 'Too little capacity limits how much the decoder can recover.'],
  ['vae-063-use-mu-vs-sample', 'Application', 'For a stable reconstruction preview, why might a demo decode mu instead of a random z?', 'It removes sampling noise while showing the center of q(z|x)', ['It changes the model into a supervised classifier', 'It guarantees KL divergence is zero'], 'Training samples z, but visualization often uses the posterior mean for a stable display.'],
  ['vae-064-sample-diversity', 'Application', 'How can you get varied reconstructions for the same input?', 'Sample multiple epsilon values from the same mu and sigma', ['Change only the answer-choice order', 'Run the decoder with no latent input'], 'A posterior distribution supports multiple latent samples around the encoded input.'],
  ['vae-065-prior-outlier', 'Application', 'If a sampled z is far in the tail of N(0,I), what should you expect?', 'The decoded output may be less reliable because training concentrated near typical prior regions', ['The decoder must exactly reproduce the training input', 'The KL term becomes a class label'], 'The prior is standard normal, but extreme tail samples are less represented.'],
  ['vae-066-compare-ae', 'Application', 'A teammate says a plain autoencoder is enough for generation. What is the VAE-based response?', 'A deterministic autoencoder may leave gaps that make random latent sampling unreliable', ['A plain autoencoder always samples perfectly from N(0,I)', 'Generation never uses latent representations'], 'The VAE adds distribution matching so sampling between codes is meaningful.'],
  ['vae-067-code-review-logvar', 'Application', 'In a code review, a model outputs variance directly with no positivity constraint. What concern matches the lesson?', 'Variance could become invalid unless the implementation enforces positivity', ['The decoder will receive too many input pixels', 'The ELBO no longer contains reconstruction information'], 'The lesson uses log-variance because it can be unconstrained while variance stays positive after exp.'],
  ['vae-068-code-review-kl', 'Application', 'A VAE implementation computes only reconstruction loss. What behavior should you question?', 'Whether the latent space will support smooth prior sampling', ['Whether the output vector can be reshaped', 'Whether the optimizer can read file paths'], 'Without KL, the model is closer to a regular autoencoder and may generate poorly from random z.'],
  ['vae-069-code-review-sigmoid', 'Application', 'When should you question a sigmoid decoder output?', 'When target data is not normalized to a bounded [0,1] range', ['Whenever the encoder has two output heads', 'Whenever beta equals one'], 'Sigmoid is appropriate for normalized pixel probabilities, not every data modality.'],
  ['vae-070-param-count', 'Application', 'For an encoder hidden size of 200 and latent size 20, how many weights and biases are in one latent head?', '200 * 20 + 20 parameters', ['20 * 784 + 784 parameters', '200 + 20 parameters only'], 'A linear head from 200 features to 20 outputs has 200 times 20 weights plus 20 biases.'],
  ['vae-071-two-head-count', 'Application', 'With mu and logvar heads from 200 to 20, what happens to the one-head count?', 'It is doubled because the two heads have separate parameters', ['It is divided by the batch size', 'It disappears because sampling has no parameters'], 'The calculator separately counts the mean and log-variance projections.'],
  ['vae-072-decoder-output-count', 'Application', 'For a decoder hidden size of 400 and output size 784, what is the final linear layer count?', '400 * 784 + 784 parameters', ['784 * 20 + 20 parameters', '400 + 20 parameters'], 'The output layer maps 400 hidden units to every flattened pixel and adds one bias per pixel.'],
  ['vae-073-interpolation-check', 'Application', 'You interpolate between two latent points and decoded outputs jump abruptly. What does that suggest?', 'The latent space may not be smooth along that path', ['The encoder has too many visible tabs', 'The reconstruction loss is definitely zero'], 'A well-regularized VAE should often decode gradual latent walks into gradual changes.'],
  ['vae-074-use-downstream', 'Application', 'Why does VAE understanding matter before latent diffusion lessons?', 'It explains how images can be compressed to latents and decoded back to pixels', ['It proves diffusion never needs a decoder', 'It replaces denoising with classification'], 'Later diffusion lessons build on the idea that a learned latent space can carry visual structure.'],
  ['vae-075-check-objective', 'Application', 'A training log reports falling reconstruction loss and rising KL loss. What should you evaluate?', 'The balance between fidelity and latent regularization rather than one metric alone', ['Only the title of the experiment', 'Whether answer choices are evenly distributed'], 'The lesson presents VAE training as a tradeoff between reconstruction and prior matching.'],
  ['vae-076-false-deterministic', 'Tricky', 'Which statement is false after learning the VAE bottleneck?', 'A VAE is just a deterministic autoencoder with a different name', ['A VAE uses a distribution in latent space', 'A regular autoencoder usually outputs a fixed code'], 'The core distinction is probabilistic encoding with mean and variance parameters.'],
  ['vae-077-misleading-generation', 'Tricky', 'Which generation claim is misleading?', 'The encoder must process a real input every time a trained VAE generates', ['A trained decoder can map prior samples into data space', 'The prior is used when sampling new outputs'], 'Generation starts from z drawn from N(0,I), then decodes.'],
  ['vae-078-wrong-kl', 'Tricky', 'Which KL belief is wrong?', 'KL divergence is only a reconstruction sharpness booster', ['KL encourages q(z|x) to match the prior', 'KL can improve sampling by smoothing latent space'], 'KL primarily regularizes the latent distribution; stronger KL may reduce sharpness.'],
  ['vae-079-dangerous-beta', 'Tricky', 'Which beta-VAE conclusion is dangerous?', 'Larger beta is always better because disentanglement has no cost', ['Larger beta can increase regularization pressure', 'Too much KL can blur reconstructions'], 'The lesson explicitly frames beta as a tradeoff.'],
  ['vae-080-false-logvar', 'Tricky', 'Which log-variance statement is false?', 'Log-variance means the actual variance is allowed to be negative', ['Exponentiating log-variance recovers a positive variance scale', 'Log-variance is convenient for unconstrained network outputs'], 'The raw log value can be negative, but exp(logvar) is positive.'],
  ['vae-081-misleading-reparam', 'Tricky', 'Which reparameterization claim is misleading?', 'Reparameterization removes all randomness from VAE training', ['Epsilon still provides random noise', 'The learned path through mu and sigma remains differentiable'], 'The trick relocates randomness; it does not make sampling disappear.'],
  ['vae-082-wrong-decoder', 'Tricky', 'Which decoder belief is wrong?', 'The decoder only reconstructs and can never generate new data', ['The decoder maps latent samples back to data space', 'Prior samples can be decoded after training'], 'The same decoder supports reconstruction and generation.'],
  ['vae-083-false-sigmoid', 'Tricky', 'Which output-layer claim is false?', 'Sigmoid is the correct final activation for every possible VAE dataset', ['Sigmoid fits normalized [0,1] pixel outputs', 'Other data likelihoods can require different output choices'], 'The lesson example uses image pixels, not a universal rule for every modality.'],
  ['vae-084-misleading-collapse', 'Tricky', 'Which posterior-collapse interpretation is misleading?', 'If q(z|x) matches the prior for every input, the encoder is definitely using rich input information', ['Collapse can mean z carries little input-specific signal', 'A powerful decoder can contribute to collapse'], 'Near-prior outputs for all inputs are a warning that the latent code may be ignored.'],
  ['vae-085-wrong-elbo', 'Tricky', 'Which ELBO statement is wrong?', 'The ELBO has no connection to the reconstruction and KL terms', ['The VAE objective combines a data-fit term with a KL regularizer', 'Minimizing the displayed loss is related to maximizing a lower bound'], 'The loss panel directly links ELBO to reconstruction likelihood and KL divergence.'],
  ['vae-086-false-prior', 'Tricky', 'Which prior-sampling claim is false?', 'The prior can be ignored during training and still guarantee good random samples', ['Prior matching is part of why random z values decode well', 'Weak KL can leave bad regions between encoded examples'], 'Generation depends on the decoder seeing latent regions compatible with the prior.'],
  ['vae-087-dangerous-dim', 'Tricky', 'Which latent-dimension rule is dangerous?', 'Always set latent dimension equal to the input dimension for the best VAE', ['Dimension controls bottleneck capacity', 'Too small a latent can harm reconstruction'], 'A useful latent space usually balances compression, fidelity, and sampling behavior.'],
  ['vae-088-misleading-interpolation', 'Tricky', 'Which interpolation claim is misleading?', 'Smooth latent interpolation is guaranteed even without KL regularization', ['KL helps make latent neighborhoods more continuous', 'Decoded walks are a useful diagnostic for latent structure'], 'A plain autoencoder can create gaps and abrupt changes between codes.'],
  ['vae-089-false-loss-only', 'Tricky', 'Which training-log conclusion is false?', 'The lowest reconstruction loss alone proves the best generative VAE', ['Generation quality also depends on latent regularization', 'KL and reconstruction must be interpreted together'], 'A model can reconstruct well while sampling poorly from the prior.'],
  ['vae-090-wrong-blur', 'Tricky', 'Which blur explanation is wrong?', 'VAE blur happens because VAEs are mathematically unable to use neural networks', ['Likelihood averaging can blur uncertain outputs', 'Strong regularization can reduce detail in z'], 'The lesson attributes blur to objective and tradeoff effects, not an absence of neural networks.'],
  ['vae-091-interview-summary', 'Interview', 'In an interview, how would you summarize a VAE?', 'A probabilistic autoencoder trained to reconstruct data while regularizing latent distributions for sampling', ['A supervised classifier that stores one label per image', 'A nearest-neighbor table with no learned decoder'], 'A concise answer should include probabilistic encoding, reconstruction, KL, and generation.'],
  ['vae-092-interview-reparam', 'Interview', 'How would you explain the reparameterization trick clearly?', 'Sample fixed-form noise epsilon, then compute z from mu and sigma so gradients can flow', ['Backpropagate through a discrete argmax over image classes', 'Remove the latent distribution and train only the decoder'], 'The interview-level explanation should connect noise isolation to differentiability.'],
  ['vae-093-interview-elbo', 'Interview', 'What is the practical meaning of the VAE ELBO?', 'It trades off explaining the data with keeping the approximate posterior near the prior', ['It measures only inference latency', 'It ranks class labels after decoding'], 'The bound combines reconstruction likelihood and KL regularization.'],
  ['vae-094-interview-beta', 'Interview', 'How should you describe beta in beta-VAE?', 'A knob on KL strength that can improve disentanglement while reducing reconstruction detail', ['A switch that always makes every output sharper', 'A replacement for the encoder network'], 'Beta scales the regularizer and therefore changes the fidelity-structure tradeoff.'],
  ['vae-095-interview-collapse', 'Interview', 'What should you say when asked about posterior collapse?', 'The decoder ignores z because q(z|x) stays close to the prior and carries little input signal', ['The encoder learns negative variances on purpose', 'The model samples from a classifier instead of a prior'], 'A good answer names both the symptom and the loss/architecture interaction.'],
  ['vae-096-interview-ae-vs-vae', 'Interview', 'What is the key regular autoencoder versus VAE distinction?', 'Deterministic compression optimizes reconstruction, while VAE compression also shapes a sampleable latent distribution', ['Regular autoencoders have decoders and VAEs do not', 'VAEs remove reconstruction from training'], 'The comparison should focus on the probabilistic bottleneck and generative use.'],
  ['vae-097-interview-blur', 'Interview', 'How would you explain why VAEs can be blurrier than GAN-style models?', 'Likelihood-based reconstruction can average plausible outputs instead of choosing one sharp mode', ['They are forced to train on lower-resolution files', 'They cannot use nonlinear activations'], 'The lesson points to pixel-wise likelihood averaging under uncertainty.'],
  ['vae-098-interview-production', 'Interview', 'What production concern follows from using a VAE latent representation?', 'Compression quality and latent regularization set a ceiling on decoded detail and sample reliability', ['The decoder can be removed after deployment', 'The prior no longer matters once training starts'], 'Downstream systems inherit the strengths and limits of the learned latent space.'],
  ['vae-099-interview-diagnostics', 'Interview', 'What quick diagnostics would you run on a trained VAE?', 'Inspect reconstructions, prior samples, latent interpolations, KL scale, and collapse statistics', ['Only count browser tabs and final answer letters', 'Only inspect optimizer names with no outputs'], 'The lesson gives multiple observable signals: fidelity, sampling, smoothness, beta/KL balance, and posterior use.'],
  ['vae-100-interview-takeaway', 'Interview', 'What is the strongest VAE takeaway from the lesson?', 'Useful VAEs balance reconstruction fidelity, differentiable sampling, and a smooth prior-aligned latent space', ['The best VAE ignores KL and uses only pixel loss', 'Generation requires copying a stored training image'], 'The final synthesis is the tension between accurate reconstructions and a latent space that can generate.'],
];

export const VAE_QUIZ = Object.freeze(SPECS.map((spec) => q(...spec)));
