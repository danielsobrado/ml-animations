function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const rotation = stableHash(`transformer-token-generation:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + rotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const TRANSFORMER_TOKEN_GENERATION_SPECS = [
  ['ttg-001-purpose', 'Foundation', 'What problem does the token generation loop solve?', 'Turning a trained transformer into text one next-token decision at a time', ['Training a tokenizer from raw bytes', 'Replacing attention with a single fixed answer'], 'The lesson focuses on inference: context becomes logits, one token is selected, appended, and used for the next step.'],
  ['ttg-002-loop', 'Foundation', 'What is the basic loop during autoregressive generation?', 'Read context, score next tokens, choose one, append it, and repeat', ['Read all future answer tokens, copy them, and stop', 'Train a new vocabulary before every output word'], 'Autoregressive text is built incrementally rather than emitted as one prewritten sentence.'],
  ['ttg-003-step-output', 'Foundation', 'What does the model produce at one decoding step?', 'A distribution over possible next tokens', ['A complete final answer with no token choices', 'A new set of model weights'], 'The output head produces scores for vocabulary items, then selection turns those scores into one next token.'],
  ['ttg-004-logits', 'Foundation', 'What are logits in the generation loop?', 'Raw vocabulary scores before probability normalization', ['Already sampled final words with no ranking', 'Cached source documents from retrieval'], 'Softmax converts logits into probabilities, often after temperature adjustment.'],
  ['ttg-005-softmax', 'Foundation', 'What role does softmax play before selection?', 'It converts adjusted scores into a probability distribution', ['It changes the trained weights permanently', 'It decides which prompt the user typed'], 'Sampling and greedy selection operate on scores or probabilities derived from logits.'],
  ['ttg-006-temperature', 'Foundation', 'What does temperature control in this lesson?', 'How sharp or flat the next-token distribution becomes before selection', ['How many layers the transformer has', 'Whether tokens are stored in UTF-8'], 'Lower temperature makes high-scoring tokens dominate more; higher temperature spreads probability more broadly.'],
  ['ttg-007-top-k', 'Foundation', 'What does top-k filtering do at a step?', 'Keep only the k highest-scoring candidate tokens before sampling', ['Always keep every token in the vocabulary', 'Change the context into training labels'], 'Top-k limits the candidate set to a fixed number of strong options.'],
  ['ttg-008-top-p', 'Foundation', 'What does nucleus filtering do at a step?', 'Keep the smallest high-probability set whose cumulative mass reaches p', ['Always keep exactly p tokens', 'Search every possible completed sequence'], 'Top-p changes the number of candidates depending on the distribution shape.'],
  ['ttg-009-selection', 'Foundation', 'What happens after candidate filtering?', 'A token is selected by greedy choice or sampling', ['The model retrains on the prompt', 'The cache changes the vocabulary size'], 'Selection turns a distribution or filtered set into the next concrete token.'],
  ['ttg-010-append', 'Foundation', 'Why is the selected token appended?', 'It becomes part of the context for the following decision', ['It is discarded before the next step', 'It becomes a new model parameter'], 'Every sampled token changes what the model conditions on next.'],
  ['ttg-011-repeat', 'Foundation', 'Why does generation repeat the same process?', 'Each new token requires a fresh next-token decision from the updated context', ['The whole answer was already generated at the first step', 'The tokenizer must be rebuilt for each word'], 'The model repeatedly scores the next position until stopping.'],
  ['ttg-012-not-full-sentence', 'Foundation', 'What beginner mistake should this lesson prevent?', 'Thinking the model generates the whole answer in one fixed prediction', ['Tracing how one token changes the next context', 'Comparing deterministic and sampled selection'], 'The learning card explicitly warns that generation is not a single full-sentence prediction.'],
  ['ttg-013-greedy', 'Foundation', 'What is greedy decoding?', 'Always choose the highest-scoring next token', ['Always sample from the lowest-scoring token', 'Always train with a preference pair'], 'Greedy decoding is deterministic for fixed logits and context.'],
  ['ttg-014-sampling', 'Foundation', 'What is sampling in generation?', 'Draw a token according to the probability distribution after controls', ['Choose only the alphabetically first token', 'Load a new checkpoint at every step'], 'Sampling can keep multiple plausible continuations alive.'],
  ['ttg-015-determinism', 'Foundation', 'What setting is more deterministic in the lesson lab?', 'Low temperature with greedy selection', ['High temperature random sampling', 'Changing the prompt after every hidden layer'], 'Low temperature and greedy mode reduce variability in the next token choice.'],
  ['ttg-016-diversity', 'Foundation', 'What setting usually keeps more alternatives alive?', 'Higher temperature with sampling', ['Greedy decoding at zero-like temperature', 'A cache that rewrites probabilities'], 'Sampling from a flatter distribution can choose tokens beyond the top candidate.'],
  ['ttg-017-cache-role', 'Foundation', 'What is the KV cache role in this lesson?', 'Reuse prior key and value rows while decoding the next token', ['Change the model distribution for the same context', 'Store a complete human-written answer'], 'The cache is a compute optimization for prior context, not a different probability model.'],
  ['ttg-018-selected-token-cache', 'Foundation', 'What should learners trace after one token is selected?', 'How the selected token becomes part of context and cache for the next step', ['How the chosen token deletes earlier prompt tokens', 'How the final answer appears before decoding'], 'The check understanding card asks learners to trace the selected token into the following step.'],
  ['ttg-019-stop', 'Foundation', 'How does generation usually know when to stop?', 'It reaches a stop token, stop sequence, or maximum token limit', ['It always stops after exactly one token', 'It stops when softmax changes the weights'], 'Stopping is part of the decoding loop and product contract.'],
  ['ttg-020-foundation-summary', 'Foundation', 'What foundation summary is correct?', 'Context becomes logits, logits become controlled probabilities, one token is selected, appended, and repeated', ['The model writes the whole response before seeing the prompt', 'The cache replaces all next-token scoring'], 'This is the core loop the lesson wants learners to internalize.'],

  ['ttg-021-prefill', 'Mechanism', 'What is the prefill phase in serving?', 'Process the prompt context to create hidden states and cache entries', ['Sample all future tokens without logits', 'Train the model on user feedback'], 'Prefill handles the existing prompt before incremental decoding begins.'],
  ['ttg-022-decode-step', 'Mechanism', 'What is the decode phase after prefill?', 'Process the newest token and produce logits for the next position', ['Recompute the tokenizer vocabulary from scratch', 'Read future target tokens with no mask'], 'Decoding advances one generated position at a time.'],
  ['ttg-023-last-position', 'Mechanism', 'Which logits usually drive the next token in a decoder-only model?', 'The vocabulary logits at the current final position', ['The logits for every padding slot equally', 'The hidden states before the prompt is read'], 'The next token is predicted from the final visible context position.'],
  ['ttg-024-vocab-size', 'Mechanism', 'What is the size of the raw score vector at a step?', 'One score per vocabulary token', ['One score per attention layer only', 'One score per training example only'], 'The output projection maps hidden states to vocabulary-sized logits.'],
  ['ttg-025-temperature-math', 'Mechanism', 'How is temperature commonly applied?', 'Divide logits by temperature before softmax', ['Multiply token ids by vocabulary size', 'Delete all low-ranked tokens before scoring'], 'Temperature reshapes probabilities by changing logit scale.'],
  ['ttg-026-low-temp', 'Mechanism', 'What does a lower temperature do mechanically?', 'Sharpen the probability distribution around high-logit tokens', ['Make all tokens exactly equally likely', 'Increase context length without memory cost'], 'Lower temperature increases the dominance of strong candidates.'],
  ['ttg-027-high-temp', 'Mechanism', 'What does a higher temperature do mechanically?', 'Flatten the distribution so weaker candidates receive more probability', ['Force the top token every time', 'Skip softmax and use raw strings'], 'Higher temperature makes sampling more varied.'],
  ['ttg-028-filter-order', 'Mechanism', 'Where do top-k or top-p filters fit?', 'After logits are converted or ranked and before final token selection', ['After the final answer is already complete', 'Before the model reads the prompt'], 'Filters restrict the eligible candidate set for selection.'],
  ['ttg-029-renormalize', 'Mechanism', 'Why are probabilities often renormalized after filtering?', 'The remaining candidate probabilities must sum to one for sampling', ['The model needs new weights after every token', 'The prompt must become a hidden label'], 'Filtering removes candidates, so the remaining mass is redistributed.'],
  ['ttg-030-randomness', 'Mechanism', 'Where does randomness enter when sampling?', 'In drawing a token from the eligible probability distribution', ['In changing trained parameters during inference', 'In deciding the tokenizer vocabulary order'], 'The same distribution can yield different sampled tokens unless the selection is deterministic.'],
  ['ttg-031-seed', 'Mechanism', 'What does a random seed control?', 'The repeatability of stochastic token draws under the same settings', ['The factual correctness of every answer', 'The number of layers in the checkpoint'], 'Seeds can make sampling reproducible, but they do not change the underlying probabilities.'],
  ['ttg-032-causal-mask', 'Mechanism', 'Why does the decoder use causal visibility during generation?', 'The next step can condition only on tokens already in the context', ['The model should inspect future unsampled tokens', 'The vocabulary must be hidden from softmax'], 'Autoregressive generation cannot depend on tokens that have not been generated yet.'],
  ['ttg-033-cache-invariance', 'Mechanism', 'What should a correct KV cache preserve?', 'The same next-token distribution as recomputing the full context', ['A different answer distribution for speed', 'A smaller vocabulary with altered token meanings'], 'Caching should improve efficiency without changing outputs for identical settings.'],
  ['ttg-034-cache-growth', 'Mechanism', 'How does the cache change as generation proceeds?', 'New key and value rows are appended for generated tokens', ['Old tokens are deleted from all attention immediately', 'The cache stores only final sampled text'], 'Each new token contributes additional cached attention data.'],
  ['ttg-035-cache-limits', 'Mechanism', 'What still grows even with a KV cache?', 'Attention over visible cached positions and cache memory', ['The model weight count at every step', 'The tokenizer vocabulary after every sample'], 'A cache avoids repeated projection for old tokens, but the context still has to be attended over.'],
  ['ttg-036-no-prob-change', 'Mechanism', 'What should the cache not do?', 'Change next-token probabilities for the same context and model', ['Avoid recomputing prior key and value projections', 'Store previous token attention data'], 'If enabling cache changes probabilities, the implementation likely has a bug.'],
  ['ttg-037-logprobs', 'Mechanism', 'What are token log probabilities useful for?', 'Inspecting how likely selected and alternative tokens were', ['Training a tokenizer during decoding', 'Increasing factuality by themselves'], 'Logprobs help debug confidence and candidate competition at each step.'],
  ['ttg-038-eos', 'Mechanism', 'What is an end-of-sequence token used for?', 'Signal that the model has selected a learned stopping point', ['Force the model to ignore all previous text', 'Replace the sampling distribution'], 'EOS is one way generated text can terminate naturally.'],
  ['ttg-039-stop-sequence', 'Mechanism', 'How does a stop sequence differ from EOS?', 'It is an external decoding rule that halts output when a string pattern appears', ['It is always a trained vocabulary probability', 'It changes the transformer layer order'], 'Products can add stopping rules on top of model token choices.'],
  ['ttg-040-max-tokens', 'Mechanism', 'Why set a maximum output length?', 'To bound cost and prevent unbounded generation', ['To make the first token contain the whole response', 'To remove the need for a prompt'], 'Max-token limits are operational safeguards in the loop.'],
  ['ttg-041-streaming', 'Mechanism', 'Why can generation be streamed to users?', 'Tokens become available incrementally as each step completes', ['The full answer is known before the first logit', 'The browser predicts future tokens without the model'], 'One-token-at-a-time decoding naturally supports incremental display.'],
  ['ttg-042-batch-decoding', 'Mechanism', 'What makes batched generation more complex?', 'Different sequences can finish at different lengths and need separate masks', ['All sequences always choose identical tokens', 'Batches remove the need for caches'], 'Serving must track active sequences, padding, and stop conditions.'],
  ['ttg-043-repetition', 'Mechanism', 'What can repetition penalties or no-repeat rules affect?', 'The candidate scores or eligibility before selection', ['The pretrained weights stored on disk', 'The prompt tokens already sent by the user'], 'Decoding controls can discourage repeated tokens or phrases.'],
  ['ttg-044-context-truncation', 'Mechanism', 'What happens when context exceeds the window?', 'Old or selected text may need truncation according to the serving policy', ['The model can attend to unlimited tokens for free', 'The vocabulary automatically shrinks to one token'], 'The loop is constrained by context length and application policy.'],
  ['ttg-045-prompt-plus-output', 'Mechanism', 'What forms the context after several generated steps?', 'The original prompt plus the tokens generated so far', ['Only the first token of the prompt', 'Only the final answer after stopping'], 'Generated tokens are not separate from context; they condition later choices.'],
  ['ttg-046-sampling-vs-training', 'Mechanism', 'What is the distinction between decoding controls and training?', 'Controls change inference-time selection, not the model weights', ['Controls always fine-tune the model automatically', 'Controls replace the need for logits'], 'Temperature and filters shape selection for a fixed model.'],
  ['ttg-047-greedy-risk', 'Mechanism', 'What is a possible risk of purely greedy decoding?', 'It can get stuck in bland or locally likely continuations', ['It always explores all possible answers', 'It changes the learned distribution to be more factual'], 'Greedy selection optimizes the immediate top choice at each step, not global quality.'],
  ['ttg-048-sampling-risk', 'Mechanism', 'What is a possible risk of high-randomness sampling?', 'It can choose low-quality or incoherent continuations more often', ['It forces deterministic repetition every time', 'It removes all candidate tokens from the distribution'], 'More diversity can come with more variance and errors.'],
  ['ttg-049-lab-comparison', 'Mechanism', 'What should the lab comparison explain?', 'Which setting is more deterministic and which keeps more alternatives alive', ['Which model architecture family has no logits', 'Which tokenizer was trained from scratch'], 'The lab asks learners to contrast low-temperature greedy behavior with higher-temperature sampling.'],
  ['ttg-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide implementation?', 'Prefill context, compute next-token logits, apply controls, select, append, update cache, and stop when rules fire', ['Generate every token without updating context', 'Use the cache to replace scoring entirely'], 'This captures the practical inference loop from prompt to final output.'],

  ['ttg-051-too-random', 'Application', 'A generated answer is erratic across runs. What setting should you inspect first?', 'Temperature and stochastic sampling settings', ['The number of training epochs in a past run only', 'Whether the output head has a route icon'], 'High randomness can make outputs vary even when the prompt is unchanged.'],
  ['ttg-052-too-bland', 'Application', 'A model always gives the same dull completion. What decoding choice may be responsible?', 'Greedy or very low-temperature selection', ['A cache that stores previous keys correctly', 'A stop sequence that appears after the answer'], 'Very deterministic settings can reduce variety.'],
  ['ttg-053-cache-bug', 'Application', 'Enabling KV cache changes outputs with the same seed and settings. What should you suspect?', 'A cache implementation bug that changed the effective context or positions', ['Expected behavior because caches should alter probabilities', 'A top-p rule that always keeps exactly p tokens'], 'A correct cache should match full recomputation for the same context and settings.'],
  ['ttg-054-not-stopping', 'Application', 'A model keeps producing text long after a useful answer. What should you check?', 'EOS handling, stop sequences, and maximum token limits', ['Whether logits exist for the first prompt token', 'Whether the tokenizer was displayed in the sidebar'], 'Stopping behavior needs both model targets and decoding rules.'],
  ['ttg-055-cuts-off', 'Application', 'A response ends too early in the middle of a useful answer. What is a likely cause?', 'A stop sequence or max-token limit fired too aggressively', ['The cache correctly reused prior values', 'The distribution had more than one candidate'], 'External stop rules can terminate generation even when more content would be useful.'],
  ['ttg-056-repetition-loop', 'Application', 'A model repeats the same phrase many times. What controls might help?', 'Repetition penalties, no-repeat constraints, or adjusted sampling settings', ['Treating padding as a required output', 'Changing the route order in the catalog'], 'Decoding controls can reduce repetitive local loops, though they are not a full quality guarantee.'],
  ['ttg-057-streaming-ui', 'Application', 'Why can a chat UI show partial output before completion?', 'Each selected token can be displayed as soon as that decoding step finishes', ['The final response is known before inference starts', 'The browser guesses all future tokens without the model'], 'Streaming follows naturally from incremental generation.'],
  ['ttg-058-logprob-debug', 'Application', 'A team wants to explain why a token was chosen. What trace helps?', 'Step-level logits, filtered candidates, probabilities, and selected token', ['Only the final rendered paragraph', 'Only the package-lock file'], 'Debugging generation requires inspecting the candidate set and selection path.'],
  ['ttg-059-topk-small', 'Application', 'A top-k value is set extremely low. What behavior may appear?', 'Many plausible alternatives are removed before sampling', ['Every vocabulary item remains eligible', 'The context window becomes infinite'], 'Small top-k can make sampling much more constrained.'],
  ['ttg-060-topp-low', 'Application', 'A low nucleus threshold is used. What risk should you consider?', 'The candidate set may become too narrow for diverse output', ['The model will always sample from all tokens', 'The cache will no longer store key rows'], 'Low top-p keeps only a small amount of probability mass.'],
  ['ttg-061-temperature-high', 'Application', 'A high temperature is used for factual answers. What risk increases?', 'Less likely tokens may be selected more often', ['The model becomes permanently fine-tuned', 'The prompt stops conditioning the output'], 'Higher temperature can increase diversity but may hurt precision.'],
  ['ttg-062-temperature-low', 'Application', 'A low temperature is used for creative writing. What tradeoff appears?', 'Output can become more predictable and less varied', ['The model no longer produces logits', 'Every token becomes equally likely'], 'Low temperature favors the strongest candidates and reduces exploration.'],
  ['ttg-063-seed-repro', 'Application', 'A bug report needs reproducible sampled output. What should you record?', 'Prompt, model version, decoding settings, random seed, and stop rules', ['Only the final answer text', 'Only the number of UI tabs open'], 'Sampling reproducibility depends on both model state and decoding configuration.'],
  ['ttg-064-context-change', 'Application', 'Why can one early token change the rest of an answer?', 'It is appended and conditions every later next-token decision', ['It deletes the original prompt from memory by definition', 'It trains a new model for the later tokens'], 'Autoregressive generation compounds earlier choices through context updates.'],
  ['ttg-065-prefix-edit', 'Application', 'A user edits the prompt by one word and gets a different answer. Why is that expected?', 'The entire distribution can shift because the context changed', ['The model ignored the prompt in both runs', 'The cache rewrote the checkpoint weights'], 'Next-token probabilities are conditional on the visible context.'],
  ['ttg-066-batch-stop', 'Application', 'In a batch, one sequence finishes before the others. What should serving handle?', 'Mark it stopped while continuing active sequences safely', ['Force every sequence to keep generating the same tokens', 'Delete all cache entries for the whole batch'], 'Batched decoding needs per-sequence stop tracking.'],
  ['ttg-067-long-output-cost', 'Application', 'Why can long generated answers be expensive?', 'Each added token triggers another decode step and grows context work', ['The whole answer was computed in one free step', 'Softmax removes all memory cost'], 'Output length directly affects serving latency and compute.'],
  ['ttg-068-cache-memory', 'Application', 'Why can many concurrent generations stress memory?', 'Each active sequence can hold KV-cache data for visible tokens and layers', ['Caching stores only one scalar per model', 'All users share the same generated tokens'], 'KV cache improves compute but consumes memory.'],
  ['ttg-069-same-settings', 'Application', 'Two systems claim equivalent decoding. What must match?', 'Model, prompt, tokenizer, logits processing, seed, and stopping rules', ['Only the visible UI title', 'Only the final number of words'], 'Small differences in preprocessing or controls can change token choices.'],
  ['ttg-070-safety-filter', 'Application', 'Where can a product policy intervene during generation?', 'Before prompting, during candidate processing, after token selection, or after completion', ['Only by changing trained weights at every step', 'Only by ignoring the generated context'], 'Safety systems can wrap the loop at multiple points, but should be evaluated carefully.'],
  ['ttg-071-json-output', 'Application', 'A model must produce valid JSON. What decoding support may help?', 'Constrained decoding or stop rules aligned with the required syntax', ['Randomly increasing temperature until braces match', 'Disabling context updates after the first token'], 'Structured outputs often need constraints beyond ordinary free-form sampling.'],
  ['ttg-072-tool-call', 'Application', 'A model must emit a tool-call token format. What should you verify?', 'The decoding rules and training format make valid tool tokens likely and allowed', ['The KV cache changes probabilities to force tools', 'Top-p always keeps exactly one tool token'], 'Generation controls and model training both affect structured action outputs.'],
  ['ttg-073-monitoring', 'Application', 'What should production monitoring track for generation behavior?', 'Latency, stop reasons, output length, refusal or error patterns, and quality samples', ['Only whether the first prompt token was cached', 'Only the color of streamed text'], 'Generation issues often show up as cost, stopping, repetition, or quality problems.'],
  ['ttg-074-eval-decoding', 'Application', 'Why evaluate multiple decoding settings?', 'Different settings trade off determinism, diversity, cost, and task quality', ['All settings produce identical outputs by definition', 'Evaluation is unnecessary after choosing a seed'], 'The best decoding configuration depends on the product goal.'],
  ['ttg-075-application-summary', 'Application', 'What application summary is safe?', 'Tune decoding by task, inspect the token loop, and verify cache and stopping behavior', ['Assume one temperature works for every product', 'Trust the cache to fix output quality'], 'Production generation quality is a combination of model, context, controls, and serving correctness.'],

  ['ttg-076-false-whole-answer', 'Tricky', 'Which generation claim is false?', 'The model generates the entire final answer as one fixed vector before token selection', ['The model produces one next-token distribution at a time', 'Selected tokens become context for later steps'], 'The false claim contradicts the one-token-at-a-time loop.'],
  ['ttg-077-false-cache-prob', 'Tricky', 'Which KV-cache belief should you reject?', 'The cache is supposed to change next-token probabilities for the same context', ['The cache can reuse prior key and value rows', 'A correct cache should match full recomputation'], 'The cache is an efficiency mechanism, not a sampling policy.'],
  ['ttg-078-false-future', 'Tricky', 'Which causal-generation claim is wrong?', 'The decoder should read future unsampled tokens during normal generation', ['The decoder conditions on the prompt and generated prefix', 'Causal visibility matches autoregressive inference'], 'Future generated tokens do not exist yet.'],
  ['ttg-079-false-temperature', 'Tricky', 'Which temperature claim is false?', 'Temperature permanently changes the model weights during inference', ['Temperature reshapes logits before selection', 'Temperature can affect output diversity'], 'Temperature is an inference-time control for a fixed model.'],
  ['ttg-080-false-topk', 'Tricky', 'Which top-k claim should be rejected?', 'Top-k always keeps every token in the vocabulary eligible', ['Top-k keeps a fixed number of high-ranked candidates', 'Very small top-k can remove plausible alternatives'], 'Top-k is explicitly a candidate-limiting rule.'],
  ['ttg-081-false-topp', 'Tricky', 'Which top-p misconception is unsafe?', 'Top-p always keeps exactly p tokens', ['Top-p keeps tokens by cumulative probability mass', 'The kept token count can change by context'], 'Nucleus sampling uses probability mass, not a fixed token count.'],
  ['ttg-082-false-greedy', 'Tricky', 'Which greedy-decoding claim is wrong?', 'Greedy decoding explores many possible continuations at each step', ['Greedy decoding picks the highest-scoring token', 'Greedy decoding is deterministic for fixed logits'], 'Greedy decoding does not sample alternatives.'],
  ['ttg-083-false-sampling', 'Tricky', 'Which sampling claim is false?', 'Sampling guarantees the highest-quality answer on every run', ['Sampling draws from eligible probabilities', 'Sampling can vary outputs across runs'], 'Sampling adds stochasticity; it is not a quality guarantee.'],
  ['ttg-084-false-append', 'Tricky', 'Which append-step belief should be rejected?', 'The selected token is discarded before the next decision', ['The selected token becomes part of context', 'Earlier choices can influence later tokens'], 'Discarding selected tokens would break autoregressive conditioning.'],
  ['ttg-085-false-softmax', 'Tricky', 'Which softmax claim is wrong?', 'Softmax trains new model facts during generation', ['Softmax normalizes scores into probabilities', 'Softmax can be applied after temperature scaling'], 'Softmax is part of inference scoring, not a training update.'],
  ['ttg-086-false-stop', 'Tricky', 'Which stopping claim is unsafe?', 'Generation always stops correctly without EOS, stop rules, or length limits', ['Stop behavior should be tested', 'Max-token limits can bound runaway output'], 'Stopping must be designed and verified.'],
  ['ttg-087-false-seed', 'Tricky', 'Which seed claim is false?', 'A random seed guarantees factual correctness of sampled text', ['A seed can help reproduce stochastic choices', 'A seed does not change model knowledge'], 'Reproducibility is not the same as correctness.'],
  ['ttg-088-false-streaming', 'Tricky', 'Which streaming belief should you reject?', 'Streaming proves the whole answer was known before decoding began', ['Streaming can show tokens as they are selected', 'Incremental decoding supports partial display'], 'Streaming reflects stepwise availability, not precomputed final text.'],
  ['ttg-089-false-controls', 'Tricky', 'Which decoding-controls claim is wrong?', 'Temperature, top-k, and top-p replace the need for task evaluation', ['Controls trade off diversity and determinism', 'Controls should be tested against task quality'], 'Decoding settings are product choices that need evaluation.'],
  ['ttg-090-tricky-summary', 'Tricky', 'Which token-generation trap should you reject?', 'Treat the output as a single prediction and ignore context updates, filters, cache, and stop rules', ['Trace each selected token into the next context', 'Verify cache invariance and stopping behavior'], 'The common trap is forgetting that generation is an iterative controlled loop.'],

  ['ttg-091-interview-definition', 'Interview', 'How would you define transformer token generation in an interview?', 'It is autoregressive inference where each context produces next-token logits, one token is selected, appended, and the loop repeats', ['It is one full-answer prediction with no intermediate probabilities', 'It is only tokenizer training after deployment'], 'A strong definition names logits, selection, append, and repetition.'],
  ['ttg-092-interview-logits', 'Interview', 'How would you explain logits and probabilities?', 'The model outputs vocabulary scores, then scaling, filtering, and softmax prepare a distribution for selection', ['The model outputs already verified facts with no ranking', 'The model deletes all candidates before softmax'], 'This answer connects model output to decoding controls.'],
  ['ttg-093-interview-controls', 'Interview', 'How would you compare greedy and sampling modes?', 'Greedy chooses the top candidate deterministically, while sampling draws from eligible probabilities', ['Both modes always explore all continuations', 'Both modes retrain the model weights'], 'The distinction is selection policy, not model architecture.'],
  ['ttg-094-interview-temperature', 'Interview', 'How would you discuss temperature tradeoffs?', 'Lower values make output more deterministic, while higher values increase diversity and risk', ['Temperature only changes serving memory', 'Temperature is unrelated to candidate probabilities'], 'This is the practical knob users most often notice.'],
  ['ttg-095-interview-filters', 'Interview', 'How would you summarize top-k and top-p?', 'Top-k keeps a fixed count of strong candidates; top-p keeps enough candidates to reach probability mass', ['Both always keep exactly the same token count', 'Both are training objectives for base models'], 'This contrast avoids the common nucleus-sampling misconception.'],
  ['ttg-096-interview-cache', 'Interview', 'How would you describe KV-cache correctness?', 'It should reuse prior attention data without changing the distribution for the same context', ['It should change probabilities to make output faster', 'It should remove the need to append tokens'], 'Cache correctness is about output equivalence plus serving efficiency.'],
  ['ttg-097-interview-debug', 'Interview', 'How would you debug unexpected generation?', 'Inspect prompt, logits, filters, selected token, context append, cache use, seed, and stop reason', ['Only read the final answer and guess', 'Only change model size before checking settings'], 'Generation bugs can occur at multiple points in the loop.'],
  ['ttg-098-interview-serving', 'Interview', 'What serving concerns should you mention?', 'Latency per token, KV-cache memory, batching, streaming, and stop conditions', ['Only the number of static page routes', 'Only whether the first token is capitalized'], 'Production generation is both a modeling and serving problem.'],
  ['ttg-099-interview-evaluation', 'Interview', 'What evaluation answer shows mature decoding judgment?', 'Compare settings on task quality, diversity, determinism, cost, safety, and reproducibility', ['Pick the highest temperature for every task', 'Assume greedy decoding is always best'], 'Decoding settings should be selected from measured product behavior.'],
  ['ttg-100-interview-takeaway', 'Interview', 'What is the production-ready token-generation takeaway?', 'A trained transformer writes text through a controlled next-token loop whose context, cache, filters, and stops must be verified', ['The model emits a finished answer before any token selection', 'Decoding controls replace model evaluation'], 'The durable lesson is to reason step by step through the inference loop.'],
];

export const TRANSFORMER_TOKEN_GENERATION_QUIZ = TRANSFORMER_TOKEN_GENERATION_SPECS.map(
  ([id, level, prompt, correct, distractors, explanation]) => q(id, level, prompt, correct, distractors, explanation),
);
