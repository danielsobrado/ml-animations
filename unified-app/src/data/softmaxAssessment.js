function q(index, level, prompt, correct, distractors, explanation) {
  return {
    id: `softmax-${String(index).padStart(3, '0')}`,
    level,
    choices: [correct, ...distractors],
    answerIndex: 0,
    prompt,
    explanation,
  };
}

export const SOFTMAX_QUIZ = [
  q(1, 'Foundation', 'What is the main job of softmax?', 'Convert a vector of logits into a probability distribution', ['Choose the largest logit and discard the rest', 'Normalize each feature independently before training'], 'Softmax turns raw scores into nonnegative probabilities that sum to one.'),
  q(2, 'Foundation', 'What are logits in the softmax lesson?', 'Raw unnormalized scores before the probability transform', ['Already calibrated probabilities after evaluation', 'Integer class labels copied from the dataset'], 'The model or previous layer emits logits; softmax transforms them.'),
  q(3, 'Foundation', 'Which formula describes one softmax output?', 'p_i = exp(z_i) divided by the sum of exp(z_j) over all classes', ['p_i = z_i divided by the number of classes', 'p_i = max(0, z_i) for each class'], 'Each numerator is an exponentiated logit and the denominator is shared across the vector.'),
  q(4, 'Foundation', 'Why does softmax exponentiate logits?', 'Exponentiation makes each numerator positive and emphasizes score gaps', ['Exponentiation makes every output exactly equal', 'Exponentiation removes the need for a denominator'], 'Positive numerators allow probability normalization, and logit differences become probability ratios.'),
  q(5, 'Foundation', 'What is the denominator in softmax?', 'The sum of all exponentiated logits in the same vector', ['The maximum probability after normalization', 'The number of training examples in the batch'], 'The shared denominator couples all output probabilities.'),
  q(6, 'Foundation', 'What must be true of the softmax outputs for one example?', 'They are nonnegative and sum to one', ['They can be negative when logits are negative', 'They always sum to the number of classes'], 'The outputs form a categorical probability distribution.'),
  q(7, 'Foundation', 'If one logit is larger than another, what happens after softmax?', 'The larger logit gets the larger probability', ['The smaller logit always becomes the larger probability', 'Both logits must get identical probability'], 'Softmax preserves the ranking of logits while changing their scale.'),
  q(8, 'Foundation', 'What distribution comes from equal logits?', 'A uniform distribution over the classes', ['A one-hot distribution on the first class', 'A distribution with negative probabilities'], 'Equal logits have equal exponentials, so the normalized probabilities are equal.'),
  q(9, 'Foundation', 'What beginner mistake does the lesson warn against?', 'Treating each probability as if it depends only on its own logit', ['Checking that probabilities sum to one', 'Comparing logits before applying softmax'], 'Every probability uses the same denominator, so all logits affect all outputs.'),
  q(10, 'Foundation', 'What happens when you increase one logit while the others stay fixed?', 'That class probability rises while the others fall', ['Only that class changes and all other probabilities stay fixed', 'All probabilities rise together above one'], 'The denominator changes for every class, so probability mass is redistributed.'),
  q(11, 'Foundation', 'What does temperature control in the softmax lab?', 'How sharp or diffuse the probability distribution is', ['Which logits are allowed to be positive', 'Whether exponentials are computed at all'], 'Lower temperature sharpens differences; higher temperature spreads probability mass.'),
  q(12, 'Foundation', 'What is the effect of lower temperature?', 'It makes the distribution more peaked around high logits', ['It makes the distribution exactly uniform', 'It deletes the largest logit'], 'Dividing logits by a small temperature increases effective score gaps.'),
  q(13, 'Foundation', 'What is the effect of higher temperature?', 'It makes the distribution softer and more even', ['It guarantees the top class reaches probability one', 'It changes logits into labels'], 'Higher temperature reduces effective score gaps before exponentiation.'),
  q(14, 'Foundation', 'What does entropy summarize for a softmax distribution?', 'How spread out the probability mass is', ['The raw number of classes before scoring', 'The order of class indices before scoring'], 'High entropy means diffuse probabilities; low entropy means concentrated mass.'),
  q(15, 'Foundation', 'What does the margin metric compare in the softmax page?', 'The top probability against the second-highest probability', ['The first logit against the batch size', 'The denominator against the learning rate'], 'A larger margin means the leader is more separated from the runner-up.'),
  q(16, 'Foundation', 'What is translation invariance for softmax?', 'Adding the same constant to every logit leaves probabilities unchanged', ['Multiplying one class by any constant leaves probabilities unchanged', 'Permuting logits without tracking classes preserves meaning'], 'A common exponential factor cancels from every numerator and the denominator.'),
  q(17, 'Foundation', 'Why do implementations subtract the largest logit before exponentiating?', 'To avoid overflow without changing the probabilities', ['To make the largest class probability zero', 'To make negative logits impossible'], 'Subtracting a shared constant uses translation invariance for numerical stability.'),
  q(18, 'Foundation', 'Where does softmax commonly appear?', 'Multiclass classifiers, attention weights, and token sampling distributions', ['Only in regression residual penalties', 'Only in train-validation split choices'], 'Many ML systems need a normalized distribution over mutually exclusive options.'),
  q(19, 'Foundation', 'What should you not conclude from a high softmax probability alone?', 'That the model is automatically calibrated or correct', ['That the selected class has the largest probability', 'That probabilities are nonnegative'], 'Softmax produces confidence-like numbers, but calibration must be evaluated separately.'),
  q(20, 'Foundation', 'What foundation summary should guide the rest of the lesson?', 'Softmax normalizes all logits together into a coupled probability distribution', ['Softmax independently rescales each score without interaction', 'Softmax replaces the model that produced the logits'], 'The key idea is shared normalization over the whole vector.'),

  q(21, 'Mechanism', 'What is the correct computation order for softmax?', 'Start with logits, exponentiate, sum exponentials, then divide each exponential by the sum', ['Divide logits by class labels, then exponentiate the answer', 'Pick the largest class before computing any denominator'], 'The animation shows logits flowing through exp, sum, and normalization.'),
  q(22, 'Mechanism', 'For logits [0, 0, 0], what are the softmax probabilities?', '[1/3, 1/3, 1/3]', ['[0, 0, 0]', '[1, 1, 1]'], 'Equal exponentials normalize to equal shares.'),
  q(23, 'Mechanism', 'For two logits [1, 1], what distribution should softmax return?', '[0.5, 0.5]', ['[1, 1]', '[0, 0]'], 'The two equal exponentials split the denominator evenly.'),
  q(24, 'Mechanism', 'For logits [2, 1, 0], which class gets the largest probability?', 'The class with logit 2', ['The class with logit 0', 'All classes tie because there are three options'], 'Softmax preserves logit order while converting scores into probabilities.'),
  q(25, 'Mechanism', 'For logits [2, 1], what is the ratio p1 / p2?', 'exp(1), because the logit difference is one', ['1/2, because there are two classes', '0, because probabilities must cancel'], 'Softmax probability ratios equal exponentials of logit differences.'),
  q(26, 'Mechanism', 'Why does adding 5 to every logit not change softmax?', 'The factor exp(5) appears in every numerator and cancels in the denominator', ['The largest logit is deleted before normalization', 'The probabilities are rounded before exponentiation'], 'This is the mechanism behind translation invariance.'),
  q(27, 'Mechanism', 'What happens if you add 5 to only one logit?', 'That class receives more probability mass', ['The distribution is unchanged by translation invariance', 'Every class receives the same added probability'], 'Translation invariance requires adding the same constant to every logit.'),
  q(28, 'Mechanism', 'What does multiplying all logits by a positive factor usually do?', 'It changes sharpness by changing score gaps', ['It leaves probabilities unchanged like adding a constant', 'It always makes the distribution uniform'], 'Scaling logits changes differences, so softmax probabilities usually change.'),
  q(29, 'Mechanism', 'How does temperature enter the softmax computation?', 'Logits are divided by temperature before exponentiation', ['Probabilities are multiplied by temperature after summing', 'Class labels are divided by temperature'], 'The model code computes softmax on scaled logits z / temperature.'),
  q(30, 'Mechanism', 'What does temperature below 1 do to effective logit gaps?', 'It widens them before exponentiation', ['It sets every gap to zero', 'It swaps the top and bottom logits'], 'Dividing by a number below one increases the scaled differences.'),
  q(31, 'Mechanism', 'What does temperature above 1 do to effective logit gaps?', 'It narrows them before exponentiation', ['It makes exponentials negative', 'It removes the denominator'], 'Narrower scaled gaps produce a softer distribution.'),
  q(32, 'Mechanism', 'Why does the largest logit dominate when gaps are large?', 'Its exponential can be much larger than the others', ['The denominator ignores smaller logits', 'Softmax manually selects only the maximum'], 'All classes remain in the denominator, but exponentials amplify large gaps.'),
  q(33, 'Mechanism', 'Can a non-top class receive nonzero probability?', 'Yes, finite logits give every class a positive probability', ['No, softmax always outputs a hard argmax', 'Only if its logit is positive'], 'Exponentials of finite values are positive.'),
  q(34, 'Mechanism', 'What happens to softmax if there is only one class in the vector?', 'The single probability is 1', ['The probability is 0 because there is no comparison', 'The result is undefined for every implementation'], 'The only exponential is also the whole denominator.'),
  q(35, 'Mechanism', 'How is softmax applied to a batch of examples?', 'Usually row by row across the class dimension', ['Once across every number in the whole dataset', 'Only to the first example in the batch'], 'Each example needs its own distribution over classes or options.'),
  q(36, 'Mechanism', 'Why is the softmax denominator called shared normalization?', 'Every class probability divides by the same sum', ['Each class has a separate denominator', 'The denominator is copied from the target label'], 'The shared sum creates coupling among output probabilities.'),
  q(37, 'Mechanism', 'What does the graph panel display after softmax?', 'Bars whose heights are class probabilities from 0 to 1', ['Bars whose heights are raw unbounded logits only', 'Bars that must all have the same height'], 'The graph turns the probability vector into comparable bars.'),
  q(38, 'Mechanism', 'How does increasing one logit affect the denominator?', 'It increases the denominator by increasing that class exponential', ['It decreases the denominator for every class', 'It leaves the denominator fixed'], 'The denominator is the sum of exponentials, so changing one exponential changes the sum.'),
  q(39, 'Mechanism', 'Why do the other class probabilities fall when one logit rises?', 'Their numerators stay fixed while the shared denominator grows', ['Their logits are directly reduced by the formula', 'They become invalid classes'], 'Probability mass must still sum to one.'),
  q(40, 'Mechanism', 'What does subtracting max(logits) do to the largest scaled logit?', 'It turns the largest value into zero', ['It turns the largest value into one', 'It removes the class from the vector'], 'After max subtraction, all shifted logits are at most zero.'),
  q(41, 'Mechanism', 'Why does max subtraction help with exponentials?', 'It keeps exponentials at or below one for the shifted largest value', ['It changes the target class into the denominator', 'It forces every probability to be equal'], 'This avoids very large exponentials while preserving ratios.'),
  q(42, 'Mechanism', 'What is the Jacobian intuition for softmax?', 'Changing one logit affects its own probability and the others', ['Each probability has an independent derivative only', 'The derivative is always zero'], 'The coupled denominator creates off-diagonal effects.'),
  q(43, 'Mechanism', 'What is the own-class derivative sign for softmax probability p_i with respect to logit z_i?', 'Positive, because raising z_i raises p_i', ['Negative, because raising z_i lowers p_i', 'Always zero because probabilities are fixed'], 'The own probability increases as its logit increases.'),
  q(44, 'Mechanism', 'What is the cross-class derivative sign for p_j when z_i rises and j differs from i?', 'Negative, because probability mass shifts away from other classes', ['Positive for every other class', 'Always undefined'], 'The shared denominator makes non-target probabilities fall.'),
  q(45, 'Mechanism', 'How does softmax connect to cross-entropy in classifiers?', 'Softmax produces class probabilities that cross-entropy can compare with the target', ['Softmax replaces the loss with a data split', 'Cross-entropy turns logits into images'], 'The next lesson often uses softmax probabilities inside a classification loss.'),
  q(46, 'Mechanism', 'How does softmax connect to attention?', 'Attention scores are normalized into weights over visible keys', ['Attention uses softmax to delete queries permanently', 'Softmax replaces key and value vectors'], 'Attention uses softmax to allocate weight across allowed positions.'),
  q(47, 'Mechanism', 'Where should an attention mask be applied relative to softmax?', 'Before softmax, by making disallowed logits effectively impossible', ['After softmax, by adding probabilities above one', 'Only after the value vectors are averaged'], 'Masks alter the logits so normalization ignores illegal choices.'),
  q(48, 'Mechanism', 'What does a very negative masked logit become after softmax?', 'Nearly zero probability', ['The highest probability', 'A negative probability'], 'A very small exponential contributes almost nothing to the denominator.'),
  q(49, 'Mechanism', 'What invariant should you check after any softmax computation?', 'The probabilities should sum to one within numerical tolerance', ['The logits should sum to one', 'The largest probability should always be exactly one'], 'A probability distribution should have total mass one.'),
  q(50, 'Mechanism', 'What mechanism summary should guide debugging?', 'Trace logits, scaled logits, exponentials, denominator, and probabilities together', ['Inspect only the final class name', 'Ignore intermediate values because the formula is simple'], 'Most softmax bugs appear in scaling, axis choice, masking, or normalization.'),

  q(51, 'Application', 'A three-class model outputs equal logits for all classes. What should you expect?', 'A roughly uniform probability distribution', ['A hard prediction for the first class', 'A negative probability for every class'], 'Equal scores mean no class is preferred by softmax.'),
  q(52, 'Application', 'A model raises the logit for class B while all other logits stay fixed. What should the lab show?', 'Class B probability rises and the other probabilities fall', ['Only class B changes while the others stay exactly fixed', 'Every probability rises by the same amount'], 'The shared denominator redistributes probability mass.'),
  q(53, 'Application', 'You add 100 to every logit before softmax. What should a stable implementation return?', 'The same probabilities as before the shift', ['A uniform distribution in every case', 'An overflow error by design'], 'A stable implementation uses translation invariance rather than raw huge exponentials.'),
  q(54, 'Application', 'A naive implementation computes exp(1000) directly. What is the likely issue?', 'Numerical overflow before normalization', ['The formula becomes more calibrated', 'The denominator becomes exactly one'], 'Exponentials can exceed floating-point range without the max-subtraction trick.'),
  q(55, 'Application', 'A classifier gives max softmax probability 0.99 on out-of-distribution data. What is the safe interpretation?', 'High softmax confidence still needs calibration and distribution-shift checks', ['The prediction is certainly correct', 'The model has proved the dataset is in distribution'], 'Softmax confidence can be overconfident under shift.'),
  q(56, 'Application', 'For top-k token sampling, what does softmax provide before sampling?', 'A probability distribution over candidate tokens', ['A new learned vocabulary row for each token', 'The final text without any selection step'], 'Decoding samples or chooses from probabilities derived from logits.'),
  q(57, 'Application', 'In attention, two allowed keys have identical scores. What weights should softmax give them if no other keys exist?', 'Equal weights of 0.5 and 0.5', ['Weights of 1 and 1', 'One positive and one negative weight'], 'Equal logits over two options split mass evenly.'),
  q(58, 'Application', 'An attention key is masked with a very negative score before softmax. What should happen?', 'It receives nearly zero attention weight', ['It receives the largest attention weight', 'It makes all weights negative'], 'Masking before softmax removes illegal positions from meaningful probability mass.'),
  q(59, 'Application', 'A team wants independent labels such as multiple tags per image. What output choice should they reconsider?', 'Use independent sigmoid-style outputs instead of one softmax over mutually exclusive labels', ['Use a single softmax because all tags must compete', 'Remove all output activations'], 'Softmax is for competing options whose probabilities share one mass budget.'),
  q(60, 'Application', 'A model must choose exactly one of several classes. Why is softmax a natural output?', 'It represents a distribution over mutually exclusive choices', ['It lets every class be independently true', 'It avoids all need for a loss function'], 'Multiclass classification often needs one probability distribution across classes.'),
  q(61, 'Application', 'You compare temperatures 0.5 and 2.0 for the same logits. What should happen at 0.5?', 'The top class becomes more dominant', ['The probabilities become more uniform', 'The class order must reverse'], 'Lower temperature sharpens the distribution.'),
  q(62, 'Application', 'You raise temperature for generation because outputs are too repetitive. What are you trying to change?', 'Make the token distribution more diffuse', ['Retrain the model weights during inference', 'Force the top token probability to one'], 'Higher temperature keeps alternatives more alive at sampling time.'),
  q(63, 'Application', 'The softmax entropy is near the maximum for the class count. What does that suggest?', 'The distribution is diffuse with similar probabilities', ['The top class is overwhelmingly dominant', 'The probabilities failed to normalize'], 'Maximum entropy occurs near uniform probability mass.'),
  q(64, 'Application', 'The softmax margin between the top two classes is tiny. What is a reasonable response?', 'Treat the prediction as ambiguous and inspect thresholds or review policies', ['Declare the top class unquestionably correct', 'Delete the runner-up class from the model'], 'A small margin means the leader is barely ahead.'),
  q(65, 'Application', 'A report shows logits but stakeholders ask for probabilities. What should you explain?', 'Softmax converts relative scores into normalized probabilities, not guaranteed truth', ['Logits already sum to one', 'Probability values prove calibration automatically'], 'The conversion is useful but still needs calibration context.'),
  q(66, 'Application', 'A custom implementation normalizes across the batch dimension by mistake. What bug appears?', 'Examples compete with each other instead of classes within each example', ['Each example gets a valid class distribution automatically', 'The logits become more stable by definition'], 'The softmax axis must match the intended distribution.'),
  q(67, 'Application', 'A custom implementation forgets the denominator. What output problem appears?', 'The exponentials no longer sum to one as probabilities', ['The outputs become calibrated probabilities anyway', 'The class ranking always disappears'], 'Exponentiation alone is not normalization.'),
  q(68, 'Application', 'A custom implementation subtracts a different random constant from each class. What is wrong?', 'That changes logit differences and can change probabilities', ['Any subtraction is always invariant', 'It is the required max-subtraction trick'], 'Only the same shift across all logits preserves softmax.'),
  q(69, 'Application', 'A model returns NaN probabilities after a softmax layer. What should you inspect first?', 'Logit scale, temperature, exponentials, and numerical stabilization', ['Only the class display labels', 'Only the final accuracy metric'], 'NaNs often come from unstable numerical operations.'),
  q(70, 'Application', 'A probability vector after softmax sums to 1.08. What should you suspect?', 'A bug in normalization, axis choice, or post-processing', ['A valid high-confidence distribution', 'A normal result of low temperature'], 'Softmax probabilities for one distribution should sum to one.'),
  q(71, 'Application', 'An engineer says the top softmax class is all that matters. What useful diagnostic might they miss?', 'Entropy and margin can reveal uncertainty among alternatives', ['The final argmax class ID', 'The raw number of classes'], 'The full distribution carries more information than argmax alone.'),
  q(72, 'Application', 'A class logit is zero. What should you remember?', 'Zero logit can still receive nonzero probability', ['Zero logit always means zero probability', 'Zero logit is always the highest score'], 'Softmax depends on relative scores, and exp(0) is positive.'),
  q(73, 'Application', 'A very low temperature makes a distribution almost one-hot. What risk follows?', 'Sampling diversity can collapse to the top option', ['The distribution becomes invalid because it sums below one', 'All low-logit classes become negative probabilities'], 'Sharp distributions reduce exploration of alternatives.'),
  q(74, 'Application', 'A very high temperature makes probabilities nearly uniform. What risk follows?', 'The model may ignore meaningful score differences', ['The largest logit becomes infinitely dominant', 'The denominator is removed'], 'Soft distributions can preserve diversity but weaken preference strength.'),
  q(75, 'Application', 'What practical softmax habit prevents many mistakes?', 'Check the axis, temperature, stability shift, sum-to-one property, and calibration context', ['Trust the highest probability without inspection', 'Normalize each score independently and skip tests'], 'Softmax is simple enough to test directly, and its assumptions matter.'),

  q(76, 'Tricky', 'Which softmax claim is false?', 'Softmax rescales each score independently without using the other logits', ['Every class probability uses a shared denominator', 'Changing one logit can change every probability'], 'The shared denominator is the core coupling mechanism.'),
  q(77, 'Tricky', 'Which probability claim is false?', 'Softmax probabilities for one example can sum to more than one', ['Softmax outputs are nonnegative', 'Softmax outputs form a distribution'], 'The normalized probabilities should sum to one.'),
  q(78, 'Tricky', 'Which logit claim is false?', 'A zero logit always gives zero probability', ['A zero logit has exp(0) equal to one', 'Relative logit values determine probabilities'], 'Zero is a score value, not a probability value.'),
  q(79, 'Tricky', 'Which confidence claim is false?', 'A high softmax probability proves the model is calibrated', ['Calibration must be measured separately', 'Softmax can be overconfident'], 'Softmax normalization is not calibration.'),
  q(80, 'Tricky', 'Which invariance claim is false?', 'Adding the same constant to every logit changes the softmax distribution', ['Adding a shared constant preserves probabilities', 'Subtracting the max logit is safe'], 'A shared shift cancels in the softmax ratio.'),
  q(81, 'Tricky', 'Which scaling claim is false?', 'Multiplying all logits by a positive constant is the same invariance as adding a constant', ['Scaling logits usually changes sharpness', 'Temperature changes effective logit scale'], 'Scaling changes score gaps; shifting all logits does not.'),
  q(82, 'Tricky', 'Which temperature claim is false?', 'Temperature retrains the model weights during inference', ['Temperature changes the distribution computed from logits', 'Lower temperature usually sharpens probabilities'], 'Temperature is an inference-time transform unless used inside a training setup explicitly.'),
  q(83, 'Tricky', 'Which numerical-stability claim is false?', 'Subtracting the maximum logit changes the correct probabilities', ['Subtracting the maximum can prevent overflow', 'The common shift cancels out'], 'Max subtraction is the standard stable softmax trick.'),
  q(84, 'Tricky', 'Which masking claim is false?', 'A masked attention position should get high probability because its logit is special', ['A masked position should be driven near zero probability', 'Masks are applied before softmax in attention'], 'Masking should prevent illegal positions from receiving attention mass.'),
  q(85, 'Tricky', 'Which multiclass claim is false?', 'Softmax is ideal when every label is independently true or false', ['Softmax fits mutually exclusive class choices', 'Independent labels often need independent outputs'], 'One softmax forces classes to compete for one probability mass.'),
  q(86, 'Tricky', 'Which argmax claim is false?', 'Softmax reverses the order of logits by default', ['The highest logit has the highest softmax probability', 'Softmax preserves ranking for a fixed temperature'], 'The exponential function is monotonic, so ranking is preserved.'),
  q(87, 'Tricky', 'Which denominator claim is false?', 'Each class uses its own private denominator', ['The denominator is shared across classes', 'The denominator is a sum over exponentials'], 'A private denominator would remove the probability coupling.'),
  q(88, 'Tricky', 'Which implementation claim is false?', 'The softmax axis never matters', ['A wrong axis can normalize across examples', 'Each intended distribution needs its own normalization axis'], 'Axis mistakes are common implementation bugs.'),
  q(89, 'Tricky', 'Which output-range claim is false?', 'Softmax can output negative probabilities for negative logits', ['Negative logits can still yield positive probabilities', 'Exponentials are positive'], 'Exponentiating negative logits gives positive numbers below one.'),
  q(90, 'Tricky', 'Which summary claim is unsafe?', 'Softmax is simple, so it cannot cause production bugs', ['Softmax bugs can involve axis, overflow, temperature, masking, or calibration', 'The distribution should be tested under edge cases'], 'Small mathematical layers can still fail through implementation and interpretation mistakes.'),

  q(91, 'Interview', 'How should you define softmax in one sentence?', 'Softmax maps logits to a normalized categorical distribution using exponentials and a shared denominator', ['Softmax picks a hard class label without probabilities', 'Softmax is feature scaling before logits exist'], 'A strong definition names logits, exponentials, normalization, and distribution.'),
  q(92, 'Interview', 'What formula should you write for softmax?', 'p_i = exp(z_i) / sum_j exp(z_j)', ['p_i = z_i / sum_j z_j for all logits', 'p_i = max(0, z_i)'], 'This is the canonical component-wise formula.'),
  q(93, 'Interview', 'How should you explain why changing one logit affects all probabilities?', 'The denominator is shared, so one exponential changes the normalization for every class', ['The formula directly edits every other logit', 'The probabilities are independent after exponentiation'], 'The coupling comes from the shared denominator.'),
  q(94, 'Interview', 'What numerical-stability detail should you mention?', 'Subtract the maximum logit before exponentiating because softmax is shift-invariant', ['Square every logit before exponentiating', 'Round probabilities before computing them'], 'This is a practical implementation detail interviewers expect.'),
  q(95, 'Interview', 'How should you explain temperature?', 'Temperature rescales logits before softmax, controlling sharpness and entropy', ['Temperature changes the class labels', 'Temperature is always learned automatically'], 'Temperature is a knob on distribution concentration.'),
  q(96, 'Interview', 'What calibration caveat should you include?', 'Softmax probabilities are not guaranteed to be calibrated confidence', ['Softmax probabilities are always empirically correct', 'Calibration is impossible to measure'], 'A production answer distinguishes normalized probability from trustworthy confidence.'),
  q(97, 'Interview', 'How does softmax relate to attention?', 'It normalizes attention scores into weights over visible keys', ['It creates the key vectors from scratch', 'It removes the need for value vectors'], 'Attention reuses softmax as a distribution over positions.'),
  q(98, 'Interview', 'How should you debug a suspicious softmax output?', 'Check logits, axis, temperature, masks, stability shift, and sum-to-one behavior', ['Only inspect the class name with the highest score', 'Assume the largest probability is always reliable'], 'Debugging needs both math checks and interpretation checks.'),
  q(99, 'Interview', 'When might one softmax be the wrong output choice?', 'When labels are not mutually exclusive and need independent probabilities', ['When exactly one class should be selected', 'When probabilities must sum to one'], 'Multi-label tasks usually should not force labels to compete through one softmax.'),
  q(100, 'Interview', 'What production-ready takeaway should you give?', 'Softmax is a coupled normalization from logits to probabilities, useful but sensitive to scale, axis, masking, and calibration', ['Softmax independently rescales scores and removes all uncertainty', 'Softmax makes every high-confidence prediction correct'], 'A strong summary includes both mechanism and operational caveats.'),
];
