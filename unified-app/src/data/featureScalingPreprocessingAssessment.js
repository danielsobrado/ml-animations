function q(id, level, prompt, correct, distractors, explanation) {
  return {
    id,
    level,
    prompt,
    choices: [correct, ...distractors],
    answerIndex: 0,
    explanation,
  };
}

export const FEATURE_SCALING_PREPROCESSING_QUIZ = Object.freeze([
  q('scale-001-purpose', 'Foundation', 'What is the main purpose of feature scaling?', 'Put numeric features on comparable ranges so scale does not dominate learning', ['Create new target labels automatically', 'Guarantee every model has higher accuracy'], 'Scaling changes numeric representation, not the underlying labels.'),
  q('scale-002-standardization', 'Foundation', 'What does standardization usually compute?', 'Subtract the training mean and divide by the training standard deviation', ['Subtract the validation label and divide by test accuracy', 'Replace every value with its rank only'], 'Standardization creates zero-centered, unit-scale features using fitted statistics.'),
  q('scale-003-minmax', 'Foundation', 'What does min-max scaling usually do?', 'Map values using the training minimum and maximum', ['Map labels into one-hot vectors', 'Fit a separate target for every row'], 'Min-max scaling places values into a chosen range such as 0 to 1.'),
  q('scale-004-robust', 'Foundation', 'What does robust scaling usually use?', 'Median and interquartile range from the training data', ['Final test labels and predictions', 'The largest model weight only'], 'Median and IQR are less sensitive to a single extreme point.'),
  q('scale-005-normalization', 'Foundation', 'What is row normalization?', 'Rescaling each example vector by its own norm', ['Fitting a feature mean from all validation rows', 'Turning every feature into a class label'], 'Row normalization is useful when vector direction matters more than magnitude.'),
  q('scale-006-train-only-fit', 'Foundation', 'When should a scaler be fitted?', 'On the training split only', ['On train, validation, and test before splitting', 'On the final test labels after evaluation'], 'Scaler parameters are learned preprocessing state.'),
  q('scale-007-transform-heldout', 'Foundation', 'After fitting a scaler on training data, how should validation and test be handled?', 'Apply the same fitted transform without refitting', ['Fit a new scaler on each held-out split', 'Drop held-out rows from evaluation'], 'Held-out rows should be transformed by training-learned parameters.'),
  q('scale-008-leakage', 'Foundation', 'Why is fitting preprocessing before splitting unsafe?', 'Evaluation statistics can influence the training representation', ['It prevents the model from using numeric features', 'It makes all values categorical'], 'Full-data preprocessing leaks information across the evaluation boundary.'),
  q('scale-009-distance', 'Foundation', 'Why do raw units matter for distance-based models?', 'Large-unit features can dominate Euclidean distances', ['Distances always ignore feature scale', 'Distance-based models use labels as units'], 'KNN and k-means are very sensitive to feature scale.'),
  q('scale-010-gradient', 'Foundation', 'How can scaling help gradient descent?', 'Better-conditioned features can make optimization steps behave more evenly', ['Scaling removes the loss function', 'Scaling guarantees no local minima'], 'Poorly scaled features can create narrow, hard-to-navigate loss surfaces.'),
  q('scale-011-regularization', 'Foundation', 'Why does scaling matter for L1 or L2 penalties?', 'Penalties act on coefficients, so feature scale changes coefficient size', ['Regularization ignores coefficients', 'Scaling deletes the penalty term'], 'Without scaling, the same penalty can affect features unevenly.'),
  q('scale-012-outlier', 'Foundation', 'Which scaler is often least moved by one extreme outlier?', 'Robust scaling based on median and IQR', ['Min-max scaling based on extremes', 'A scaler fitted on validation data'], 'Median and IQR are less sensitive than min, max, mean, and variance.'),
  q('scale-013-minmax-outlier', 'Foundation', 'How can one outlier affect min-max scaling?', 'It can compress most ordinary values into a narrow part of the range', ['It always removes the outlier label', 'It makes every value exactly zero'], 'The min and max define the whole mapping.'),
  q('scale-014-standard-outlier', 'Foundation', 'How can one outlier affect standardization?', 'It can pull the mean and inflate the standard deviation', ['It makes the mean unavailable', 'It changes labels into probabilities'], 'Mean and variance are outlier-sensitive statistics.'),
  q('scale-015-imputation', 'Foundation', 'Why is imputation part of preprocessing?', 'It fills missing feature values using a learned rule', ['It computes final test accuracy', 'It chooses the target class directly'], 'Imputation should follow the same train-only fitting rule.'),
  q('scale-016-one-hot', 'Foundation', 'What does one-hot encoding do?', 'Represent categories as binary indicator columns', ['Scale every numeric column to unit variance', 'Average labels across all rows'], 'Categorical preprocessing changes representation before modeling.'),
  q('scale-017-pipeline', 'Foundation', 'Why put preprocessing in a pipeline?', 'To fit and apply transformations consistently with the model', ['To use the test set for fitting', 'To avoid documenting feature choices'], 'Pipelines reduce accidental leakage and train-serving mismatch.'),
  q('scale-018-cv', 'Foundation', 'Where should preprocessing be fitted during cross-validation?', 'Inside each training fold', ['Once on all rows before folds are made', 'Only on the validation fold'], 'Each fold must preserve its held-out boundary.'),
  q('scale-019-tree-models', 'Foundation', 'Which model family is usually less sensitive to monotonic feature scaling?', 'Tree-based splits such as decision trees and many forests', ['K-nearest neighbors with Euclidean distance', 'K-means with raw distances'], 'Trees split by order thresholds, so simple scaling often matters less.'),
  q('scale-020-core-warning', 'Foundation', 'What is the core warning about preprocessing?', 'Fitted preprocessing is learned from data and must obey evaluation boundaries', ['Preprocessing is harmless bookkeeping in every workflow', 'Scaling must use labels from the final test set'], 'Treat preprocessing as part of model training.'),

  q('scale-021-standard-mechanism', 'Mechanism', 'Why does standardization center a feature?', 'Subtracting the mean moves the training average to zero', ['Subtracting the target removes all errors', 'Subtracting the maximum creates ranks'], 'Centering makes intercepts and optimization easier to reason about.'),
  q('scale-022-standard-spread', 'Mechanism', 'What does dividing by standard deviation change?', 'It expresses values in training-standard-deviation units', ['It creates one category per value', 'It removes all distribution shift'], 'A value of 2 means roughly two training standard deviations above the mean.'),
  q('scale-023-minmax-mechanism', 'Mechanism', 'How does min-max scaling map a training minimum?', 'It maps the training minimum to the lower endpoint', ['It maps the minimum to the label mean', 'It discards the row containing the minimum'], 'The endpoints anchor the linear transformation.'),
  q('scale-024-robust-mechanism', 'Mechanism', 'Why does robust scaling resist a single outlier?', 'Median and quartiles move less than mean and extremes', ['It secretly uses final test labels', 'It clips every value to zero'], 'Robust statistics are designed to reduce outlier influence.'),
  q('scale-025-outlier-choice', 'Mechanism', 'When might robust scaling beat standardization?', 'When a small number of extreme values should not set the scale', ['When every feature is already binary', 'When final test rows must fit preprocessing'], 'Robust scaling is a good default for heavy-tailed numeric features.'),
  q('scale-026-clipping', 'Mechanism', 'What does clipping or winsorizing do before scaling?', 'Limit extreme values so they cannot dominate fitted scale parameters', ['Use validation labels as feature values', 'Guarantee all outliers are errors'], 'Clipping is a modeling choice that should be learned inside the training protocol.'),
  q('scale-027-log-transform', 'Mechanism', 'Why might a log transform help before scaling?', 'It can compress right-skewed positive values', ['It turns classes into probabilities', 'It removes the need for a train split'], 'Log transforms often make multiplicative variation easier to model.'),
  q('scale-028-sparse-centering', 'Mechanism', 'Why can centering sparse matrices be expensive?', 'Subtracting means can turn many implicit zeros into nonzero values', ['Sparse matrices cannot store features', 'Centering removes all rows'], 'Dense matrices may be much larger in memory.'),
  q('scale-029-binary-features', 'Mechanism', 'Why might binary indicator columns not need standard scaling?', 'Their 0/1 scale may already have a clear interpretation', ['They are labels and never features', 'They always cause leakage'], 'Scaling binary features is context-dependent.'),
  q('scale-030-ordinal-features', 'Mechanism', 'Why be careful with ordinal encodings?', 'Numeric codes can imply distances that may not match the category meaning', ['Ordinal features cannot be modeled', 'Scaling makes them perfectly causal'], 'Encoding choices can create artificial geometry.'),
  q('scale-031-imputer-fit', 'Mechanism', 'How should a median imputer be fitted?', 'Compute medians from training rows only', ['Compute medians from final test rows first', 'Compute medians separately on validation labels'], 'Imputer statistics are learned state.'),
  q('scale-032-missing-indicator', 'Mechanism', 'Why add a missingness indicator?', 'Missingness itself may carry predictive signal', ['It removes all missing values from production', 'It leaks labels by definition'], 'A model can use whether a value was missing as information.'),
  q('scale-033-category-unknown', 'Mechanism', 'Why handle unknown categories explicitly?', 'Serving data may contain categories not seen during training', ['Unknown categories always mean positive class', 'Categories vanish after scaling'], 'Preprocessing must define behavior for new values.'),
  q('scale-034-train-serving', 'Mechanism', 'What is train-serving preprocessing parity?', 'The same fitted transformations and feature rules run offline and online', ['Training uses scalers while serving uses labels', 'Serving refits scalers on each request'], 'Mismatch between training and serving can break performance.'),
  q('scale-035-cv-pipeline', 'Mechanism', 'Why must a scaler be inside a CV pipeline?', 'Each fold needs scaler parameters fitted without its validation rows', ['The validation fold should choose the training mean', 'Only the first fold needs scaling'], 'Pipeline CV prevents fold leakage.'),
  q('scale-036-grid-search', 'Mechanism', 'How should scaling interact with grid search?', 'The scaler and model should be refit for each candidate inside the validation protocol', ['The scaler should be fit once on all rows before every candidate', 'The best test score should choose the scaler'], 'Hyperparameter search must include preprocessing safely.'),
  q('scale-037-leakage-stat', 'Mechanism', 'Why can even label-free preprocessing leak?', 'Held-out feature distributions still shape the training transform', ['Only labels can leak information', 'Feature distributions cannot affect models'], 'Evaluation rows can leak through means, variances, minima, or components.'),
  q('scale-038-penalty-geometry', 'Mechanism', 'How does scaling affect regularized linear models?', 'It changes the coefficient size needed for the same feature effect', ['It removes all feature effects', 'It changes the labels into residuals'], 'Penalties operate in coefficient space.'),
  q('scale-039-distance-geometry', 'Mechanism', 'How does scaling affect KNN or k-means?', 'It changes which dimensions dominate distance calculations', ['It makes distances independent of features', 'It turns centroids into labels'], 'Distance geometry is directly tied to feature scale.'),
  q('scale-040-svm-rbf', 'Mechanism', 'Why does scaling matter for RBF-kernel SVMs?', 'The kernel uses distances, so scale changes similarity values', ['The kernel ignores all numeric values', 'The model only reads target labels'], 'Poor scaling can make one feature dominate kernel similarity.'),
  q('scale-041-neural-net', 'Mechanism', 'Why can scaling help neural-network training?', 'Inputs with comparable scale can stabilize gradients and initialization assumptions', ['Scaling replaces backpropagation', 'Scaling prevents all overfitting'], 'Optimization often assumes reasonably scaled inputs.'),
  q('scale-042-tree-exception', 'Mechanism', 'Why are trees often less affected by simple scaling?', 'Threshold splits preserve ordering under many monotonic transforms', ['Trees never use feature values', 'Trees are always immune to preprocessing'], 'This is a useful exception, not a universal rule.'),
  q('scale-043-target-scaling', 'Mechanism', 'When might target scaling be useful?', 'For regression models where rescaling the target improves optimization or interpretation', ['For classification labels that should be averaged with features', 'For using final test targets in training'], 'Target transforms need inverse-transform and evaluation care.'),
  q('scale-044-inverse-transform', 'Mechanism', 'Why track inverse transforms?', 'Predictions may need to be returned to original units', ['The model cannot train without original labels in validation', 'Inverse transforms remove train-serving mismatch automatically'], 'Users often need outputs in real-world units.'),
  q('scale-045-distribution-shift', 'Mechanism', 'What happens when serving values exceed the training min and max?', 'Min-max scaled values can fall outside the original fitted range', ['The scaler must refit on the request', 'The prediction is automatically invalid'], 'Fitted scaling formulas still apply beyond training endpoints.'),
  q('scale-046-drift', 'Mechanism', 'Why monitor feature distributions after deployment?', 'Drift can make fitted preprocessing less representative', ['Monitoring replaces validation', 'Drift means labels are unavailable forever'], 'Preprocessing assumptions can age.'),
  q('scale-047-online-learning', 'Mechanism', 'What is a risk with updating scalers online?', 'Changing scaler state can shift model inputs unless the model is updated consistently', ['Online scaling cannot compute means', 'It always removes leakage'], 'Stateful preprocessing is part of the deployed model.'),
  q('scale-048-numeric-stability', 'Mechanism', 'How can scaling improve numerical conditioning?', 'It reduces extreme differences in feature magnitudes', ['It makes every matrix orthogonal', 'It removes floating point arithmetic'], 'Better conditioning can improve solvers and gradient steps.'),
  q('scale-049-documentation', 'Mechanism', 'What should preprocessing documentation record?', 'Fitted columns, statistics source, missing-value policy, and serving behavior', ['Only the final model score', 'Only the chart colors'], 'Documentation makes preprocessing reviewable and reproducible.'),
  q('scale-050-choice-rule', 'Mechanism', 'What should guide scaler choice?', 'Model sensitivity, feature distribution, outliers, sparsity, and deployment constraints', ['Whichever scaler gives the best final test score after peeking', 'Alphabetical order of feature names'], 'Scaler choice should follow data and model assumptions.'),

  q('scale-051-scenario-knn', 'Application', 'A KNN model uses age in years and income in dollars. What preprocessing is likely needed?', 'Scale numeric features so income does not dominate distance', ['Fit a scaler on final test rows', 'Turn the labels into dollar units'], 'Distance-based models compare coordinate magnitudes.'),
  q('scale-052-scenario-kmeans', 'Application', 'K-means clusters mostly by one large-unit feature. What should you try?', 'Scale features before clustering and reassess cluster meaning', ['Increase k until units stop mattering', 'Use validation labels to fit centroids'], 'K-means minimizes Euclidean distances.'),
  q('scale-053-scenario-outlier', 'Application', 'A single huge transaction compresses min-max scaled values. What scaler may help?', 'Robust scaling or a clipped transform fitted on training data', ['A scaler fitted on test rows', 'No preprocessing because outliers are labels'], 'Robust preprocessing reduces extreme-value dominance.'),
  q('scale-054-scenario-standard', 'Application', 'A linear model with L2 regularization has features with wildly different units. What is safer?', 'Standardize continuous features within the training pipeline', ['Leave units raw and compare coefficients directly', 'Use final test data to choose means'], 'Regularized coefficients need comparable feature scales.'),
  q('scale-055-scenario-cv', 'Application', 'A notebook runs StandardScaler before cross_val_score. What is wrong?', 'Validation folds influenced the scaling statistics', ['StandardScaler cannot be cross-validated', 'The model has no features'], 'The scaler belongs inside the cross-validation pipeline.'),
  q('scale-056-scenario-impute', 'Application', 'A median imputer is fit before train/test split. What should change?', 'Fit the imputer on train only and reuse it for held-out rows', ['Fit a separate imputer on each test batch', 'Drop every row with a missing value after evaluation'], 'Imputation statistics must obey the split boundary.'),
  q('scale-057-scenario-sparse', 'Application', 'A sparse text matrix becomes huge after centering. What should you consider?', 'Use scaling that preserves sparsity or skip centering', ['Fit labels into sparse zeros', 'Convert categories into test scores'], 'Centering sparse inputs can densify them.'),
  q('scale-058-scenario-minmax', 'Application', 'A neural net expects inputs roughly between 0 and 1. What can min-max scaling provide?', 'A bounded training-based mapping for numeric inputs', ['A guarantee against distribution shift', 'A replacement for the loss function'], 'Min-max scaling can match model input expectations.'),
  q('scale-059-scenario-log', 'Application', 'A positive revenue feature is heavily right-skewed. What transform might help?', 'A log transform followed by train-only scaling', ['A test-label average encoding', 'A separate scaler for the validation labels'], 'Log transforms can compress heavy tails.'),
  q('scale-060-scenario-unknown-category', 'Application', 'Serving data contains a category unseen during training. What should preprocessing do?', 'Use a predefined unknown-category handling rule', ['Crash silently and refit on the request', 'Use the final test label as the category'], 'Production preprocessing needs deterministic behavior for new categories.'),
  q('scale-061-scenario-binary', 'Application', 'A dataset has 0/1 indicator features plus continuous lab measurements. What is a reasonable approach?', 'Scale continuous features while deliberately deciding whether indicators should stay 0/1', ['Standardize labels and features together', 'Fit indicators on validation rows only'], 'Not every column needs the same transformation.'),
  q('scale-062-scenario-tree', 'Application', 'A random forest performs the same before and after standardization. Why might that happen?', 'Tree thresholds are often insensitive to simple monotonic rescaling', ['Scaling cannot affect any model ever', 'The validation rows trained the model'], 'Tree models are a common scaling exception.'),
  q('scale-063-scenario-rbf', 'Application', 'An RBF SVM behaves poorly on raw mixed-unit features. What is likely missing?', 'Feature scaling before kernel distances are computed', ['A larger final test set for fitting', 'A categorical label transform'], 'RBF kernels are distance-sensitive.'),
  q('scale-064-scenario-drift', 'Application', 'Production values are far outside the training range. What should you inspect?', 'Feature drift and whether the fitted scaler still represents serving data', ['Whether the chart uses the right color', 'Whether validation labels can update the scaler'], 'Out-of-range scaled values can signal distribution change.'),
  q('scale-065-scenario-online', 'Application', 'A service refits the scaler every hour but the model weights stay fixed. What is risky?', 'The meaning of model inputs changes over time', ['Hourly scaling makes labels unavailable', 'The model will automatically recalibrate'], 'Preprocessing state and model weights should be versioned together.'),
  q('scale-066-scenario-leakage', 'Application', 'A scaler is fit on all data because it does not use labels. What should you say?', 'Feature statistics from held-out rows can still leak', ['Only labels can influence evaluation', 'The scaler is not part of the model pipeline'], 'Label-free preprocessing still learns from data.'),
  q('scale-067-scenario-inverse', 'Application', 'A target is log-transformed for regression. What must reporting do?', 'Convert predictions back to the original target units carefully', ['Report log units as dollars without explanation', 'Use test labels to reverse feature scaling'], 'Target transforms affect interpretation.'),
  q('scale-068-scenario-clip', 'Application', 'A team chooses clipping thresholds after seeing test errors. What is wrong?', 'Clipping became tuned on the final evaluation set', ['Clipping can never be used', 'Thresholds do not affect preprocessing'], 'Preprocessing choices are model choices.'),
  q('scale-069-scenario-serving', 'Application', 'Training uses one-hot columns that serving does not create. What is the issue?', 'Train-serving preprocessing parity is broken', ['One-hot encoding cannot be deployed', 'The model no longer has coefficients'], 'The serving pipeline must reproduce training feature construction.'),
  q('scale-070-scenario-monitoring', 'Application', 'What should you monitor for a deployed scaler?', 'Input distributions, missingness rates, unknown categories, and out-of-range values', ['Only the model filename', 'Only whether training loss was low'], 'Preprocessing health is part of model health.'),
  q('scale-071-scenario-compare', 'Application', 'You compare standard, min-max, and robust scalers. What must stay constant?', 'The split protocol and model-selection metric', ['The final test score after each tweak', 'The validation labels used for fitting scalers'], 'Fair comparison needs a fixed evaluation protocol.'),
  q('scale-072-scenario-pipeline', 'Application', 'A saved model is deployed without its scaler. What should you expect?', 'Predictions can be wrong because inputs are in the wrong representation', ['The model will infer scaling automatically', 'The scaler only mattered for charts'], 'Preprocessing artifacts are part of the model.'),
  q('scale-073-scenario-order', 'Application', 'Should imputation usually happen before scaling?', 'Yes, because most scalers need numeric values after missing data is handled', ['No, scalers should fit missing labels', 'Order never matters in preprocessing'], 'Pipeline order changes fitted statistics and model inputs.'),
  q('scale-074-scenario-columns', 'Application', 'A new training run silently changes column order before scaling. What should prevent this?', 'Column-aware preprocessing with a saved feature schema', ['Using the highest validation fold only', 'Fitting on final test labels'], 'Preprocessing should bind transformations to named columns.'),
  q('scale-075-scenario-choice', 'Application', 'How should you choose among scalers for a production model?', 'Use data distribution, model class, leakage safety, validation evidence, and serving constraints', ['Use whichever scaler wins after repeated test peeking', 'Always choose min-max for every model'], 'Production scaler choice is evidence-based and protocol-safe.'),

  q('scale-076-trap-harmless', 'Tricky', 'Which preprocessing claim is false?', 'Scaling is harmless bookkeeping that can be fit on all rows', ['Fitted preprocessing can leak evaluation information', 'Scaling parameters should be learned inside the training protocol'], 'Preprocessing learns state from data.'),
  q('scale-077-trap-label-free', 'Tricky', 'Which label-free preprocessing claim is false?', 'If a transform ignores labels, it cannot leak anything', ['Feature distributions from held-out rows can leak', 'PCA and scalers still need fold-local fitting'], 'Held-out feature information can still cross boundaries.'),
  q('scale-078-trap-all-models', 'Tricky', 'Which scaling claim is too absolute?', 'Every model family benefits equally from standardization', ['Distance-based models are scale-sensitive', 'Trees are often less sensitive to monotonic scaling'], 'Model class matters because scale sensitivity follows the model geometry and objective.'),
  q('scale-079-trap-minmax', 'Tricky', 'Which min-max claim is false?', 'Min-max scaling guarantees future values stay between 0 and 1', ['Future values can exceed training endpoints', 'The training minimum and maximum define the mapping'], 'Serving values can move outside the fitted range.'),
  q('scale-080-trap-robust', 'Tricky', 'Which robust-scaling claim is misleading?', 'Robust scaling automatically removes every outlier problem', ['Median and IQR reduce sensitivity to extremes', 'Robust scaling still needs validation'], 'Robust scaling is not a cure-all.'),
  q('scale-081-trap-cv', 'Tricky', 'Which cross-validation practice is unsafe?', 'Fit the scaler once before making folds', ['Fit the scaler inside each training fold', 'Use a pipeline for grid search'], 'Full-data fitting leaks fold information.'),
  q('scale-082-trap-coefficients', 'Tricky', 'Which coefficient comparison is unsafe?', 'Compare regularized coefficients from unscaled features as if units matched', ['Scale affects coefficient magnitudes', 'Standardized coefficients can be easier to compare'], 'Raw units change coefficient size.'),
  q('scale-083-trap-outlier', 'Tricky', 'Which outlier response is unsafe?', 'Choose clipping thresholds after inspecting final test errors', ['Set clipping policy inside training validation', 'Document why clipping matches the domain'], 'Test-informed preprocessing is peeking.'),
  q('scale-084-trap-serving', 'Tricky', 'Which deployment practice is unsafe?', 'Refit the scaler independently in serving for each request', ['Ship the fitted scaler with the model', 'Version preprocessing state with model weights'], 'Serving should apply the trained transform.'),
  q('scale-085-trap-one-hot', 'Tricky', 'Which one-hot statement is false?', 'One-hot encoding never needs an unknown-category policy', ['Serving can contain unseen categories', 'Feature schemas should be stable'], 'Unknown categories are common in production.'),
  q('scale-086-trap-binary', 'Tricky', 'Which binary-feature claim is too absolute?', 'All binary indicators must always be standardized', ['Indicator treatment depends on model and interpretation', 'Continuous and binary columns can use different preprocessing'], 'Column-specific preprocessing is normal.'),
  q('scale-087-trap-target', 'Tricky', 'Which target-transform practice is unsafe?', 'Evaluate transformed-target predictions without returning to meaningful units', ['Track inverse transforms for reporting', 'Define the target metric before tuning'], 'Reporting units affect interpretation.'),
  q('scale-088-trap-drift', 'Tricky', 'Which drift claim is false?', 'A fitted scaler from training remains representative forever', ['Feature distributions can shift after deployment', 'Out-of-range rates can be monitored'], 'Preprocessing assumptions can expire.'),
  q('scale-089-trap-schema', 'Tricky', 'Which schema practice is unsafe?', 'Rely on column order changing silently between training and serving', ['Save feature names and transformation mapping', 'Validate serving schema before prediction'], 'Column mismatch can corrupt inputs.'),
  q('scale-090-trap-best-score', 'Tricky', 'Which scaler-selection practice is unsafe?', 'Pick the scaler after repeatedly checking the final test score', ['Choose candidates using validation only', 'Freeze preprocessing before final testing'], 'Scaler choice is model selection.'),

  q('scale-091-interview-define', 'Interview', 'How should you define feature scaling in an interview?', 'A fitted numeric preprocessing step that changes feature magnitudes while preserving evaluation boundaries', ['A way to create labels from features', 'A guarantee that every model improves'], 'A strong definition includes fitted state and boundaries.'),
  q('scale-092-interview-compare', 'Interview', 'How would you compare standard, min-max, and robust scaling?', 'Standard uses mean and standard deviation, min-max uses endpoints, robust uses median and IQR', ['All three use final test labels', 'All three are identical except names'], 'The comparison should name the fitted statistics.'),
  q('scale-093-interview-models', 'Interview', 'Which models should you mention as scale-sensitive?', 'KNN, k-means, SVM kernels, regularized linear models, and many neural nets', ['Only decision trees in every case', 'Only models with categorical labels'], 'Scale sensitivity follows distance, penalties, and optimization geometry.'),
  q('scale-094-interview-leakage', 'Interview', 'What leakage warning belongs in a scaling answer?', 'Fit scalers and imputers only on training data or inside each CV fold', ['Fit once on all data because labels are ignored', 'Use test statistics for stability'], 'Preprocessing leakage is a standard interview follow-up.'),
  q('scale-095-interview-outliers', 'Interview', 'How should you discuss outliers and scaler choice?', 'Explain how mean, variance, min, max, median, and IQR respond to extremes', ['Say outliers never affect scaling', 'Always delete outliers without domain review'], 'Scaler choice should follow robust-statistics reasoning.'),
  q('scale-096-interview-pipeline', 'Interview', 'What makes a preprocessing pipeline production-ready?', 'Saved fitted state, stable schema, train-serving parity, and monitored input distributions', ['A notebook cell that ran once', 'A scaler refit on every request'], 'Production preprocessing needs reproducible artifacts.'),
  q('scale-097-interview-scenario', 'Interview', 'How should you answer a scaling scenario question?', 'Identify the model sensitivity, feature distribution, leakage boundary, and deployment constraint', ['Pick standardization before reading the setup', 'Use final test score to choose'], 'Good answers reason from the setup.'),
  q('scale-098-interview-exception', 'Interview', 'What exception should you know?', 'Tree-based models often need less simple numeric scaling than distance-based models', ['No model ever needs scaling', 'Every model needs identical scaling'], 'Exceptions show mature judgment.'),
  q('scale-099-interview-debug', 'Interview', 'How would you debug a scaling-related production failure?', 'Check schema, fitted scaler version, missing values, unknown categories, drift, and unit changes', ['Only increase model size', 'Only rerun the final test set'], 'Preprocessing bugs often live outside model weights.'),
  q('scale-100-interview-success', 'Interview', 'What is interview-ready preprocessing mastery?', 'Choosing, fitting, validating, shipping, and monitoring transformations without leaking evaluation data', ['Memorizing the standardization formula only', 'Always using one scaler for every column'], 'Mastery connects math, protocol, and operations.'),
]);
