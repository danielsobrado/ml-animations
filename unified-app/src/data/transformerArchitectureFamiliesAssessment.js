function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const rotation = stableHash(`transformer-architecture-families:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + rotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const TRANSFORMER_ARCHITECTURE_FAMILIES_SPECS = [
  ['tfam-001-purpose', 'Foundation', 'What problem does this architecture-family lesson solve?', 'Matching a transformer stack to the way a task consumes and produces text', ['Choosing the prettiest route icon for a model page', 'Replacing attention math with a single universal label'], 'The family choice should follow the task input, output, attention visibility, and training objective.'],
  ['tfam-002-three-families', 'Foundation', 'Which three broad transformer families does the lesson compare?', 'Encoder-only, decoder-only, and encoder-decoder', ['CNN, RNN, and decision tree', 'Tokenizer, optimizer, and data loader'], 'The lesson focuses on BERT-style encoders, GPT-style decoders, and T5-style sequence-to-sequence models.'],
  ['tfam-003-encoder-role', 'Foundation', 'What does the reading-oriented family naturally do?', 'Read a fixed input into contextual representations', ['Generate tokens without any prefix state', 'Skip hidden states and output labels directly'], 'Encoder-style models are a strong fit when the whole input is available before producing a representation or decision.'],
  ['tfam-004-bert-style', 'Foundation', 'Which family best describes a BERT-style model?', 'An encoder stack with bidirectional self-attention', ['A decoder stack with only prefix visibility', 'A pure cross-attention module without self-attention'], 'BERT-style systems encode available input tokens with two-sided context to build contextual representations.'],
  ['tfam-005-decoder-role', 'Foundation', 'What does the generation-oriented family naturally do?', 'Generate from a prefix one token at a time', ['Pool a completed document before any output can change', 'Translate by ignoring the source sequence'], 'Decoder-style models are designed for autoregressive continuation from already visible tokens.'],
  ['tfam-006-gpt-style', 'Foundation', 'Why do GPT-style models fit left-to-right completion?', 'Causal self-attention and next-token prediction match autoregressive decoding', ['Bidirectional masks expose every future token during generation', 'They remove token probabilities from the output head'], 'The model learns to predict the next token from the prefix, then repeats that step during generation.'],
  ['tfam-007-seq2seq-role', 'Foundation', 'What does the source-conditioned family naturally do?', 'Condition target generation on a separate encoded source', ['Classify without reading the input tokens', 'Use only a tokenizer merge table as memory'], 'Encoder-decoder models separate source understanding from target-side generation.'],
  ['tfam-008-t5-style', 'Foundation', 'Which pattern is a T5-style sequence-to-sequence model?', 'An encoder-decoder stack for source-to-target text tasks', ['An encoder-only stack that never decodes tokens', 'A decoder-only stack with no source representation'], 'T5-style models encode an input text and decode an output text while attending back to the encoded source.'],
  ['tfam-009-visibility', 'Foundation', 'What changes most directly across these families?', 'Which tokens each position is allowed to read', ['Whether vectors stop having dimensions', 'Whether training data must be alphabetical'], 'Family behavior is largely defined by attention visibility rules such as bidirectional, causal, and cross-attention access.'],
  ['tfam-010-design-drivers', 'Foundation', 'What decides what a transformer system can naturally do?', 'Objectives and masks along with the block stack', ['Only the file name of the checkpoint', 'Only the color used in the animation'], 'A block design is not enough; the objective and attention masks shape the natural behavior.'],
  ['tfam-011-block-limit', 'Foundation', 'What beginner mistake should this lesson prevent?', 'Treating a transformer block as the whole model design', ['Checking whether the task has an input and output', 'Comparing how masks affect token visibility'], 'The same broad block idea can support different model families when wrapped with different masks and objectives.'],
  ['tfam-012-classification-fit', 'Foundation', 'Which family is the natural starting point for a fixed-input classification task?', 'A bidirectional encoder-style model', ['A source-to-target decoder with no encoder states', 'A causal-only continuation model for unlimited text'], 'Classification usually benefits from reading the whole input before producing a class representation.'],
  ['tfam-013-chat-fit', 'Foundation', 'Which family is the natural starting point for chat completion?', 'A causal decoder-style model', ['A representation-only encoder with no generation loop', 'A cross-attention layer without a target decoder'], 'Chat completion appends tokens to a conversation prefix, matching autoregressive decoding.'],
  ['tfam-014-translation-fit', 'Foundation', 'Which family is the classic fit for translation?', 'A sequence-to-sequence encoder-decoder model', ['A pooled classifier with no target-side generation', 'A model that sees future target words before predicting them'], 'Translation has a source sentence and a target sentence, so separate encoding and decoding are natural.'],
  ['tfam-015-bidirectional', 'Foundation', 'What does bidirectional visibility mean in this lesson?', 'Tokens in the input can use context from both sides when encoding', ['Target tokens can see every future answer while generating', 'The model reads no neighboring positions at all'], 'Bidirectional encoder attention is useful when the full input is known at once.'],
  ['tfam-016-causal', 'Foundation', 'What does causal prefix visibility mean?', 'A position can read only earlier positions and itself', ['A position can read arbitrary future target tokens', 'A position must ignore the entire prompt'], 'Causal masking preserves the left-to-right condition used for next-token prediction.'],
  ['tfam-017-cross-attention', 'Foundation', 'What does decoder cross-attention read in the sequence-to-sequence pattern?', 'Encoded source states produced by the encoder stack', ['Future target labels hidden from the model', 'Optimizer momentum values from training'], 'Cross-attention lets target decoding stay grounded in the source input.'],
  ['tfam-018-output-behavior', 'Foundation', 'How do output behaviors differ at a high level?', 'Encoders produce representations, while decoders produce continuations or target text', ['Every family can only output a single scalar loss', 'All families must emit fixed-length class IDs only'], 'The family should match whether the product needs a representation, free-form continuation, or source-conditioned text.'],
  ['tfam-019-selector', 'Foundation', 'What should the lesson selector help a learner compare?', 'BERT-style, GPT-style, and T5-style visibility and outputs', ['Only three unrelated dashboard color themes', 'Only which optimizer has the shortest name'], 'The selector is meant to connect architecture family with token visibility and task behavior.'],
  ['tfam-020-foundation-summary', 'Foundation', 'What foundation summary is production-safe?', 'Choose the family from input availability, output type, attention visibility, and objective', ['Choose the family from model popularity alone', 'Assume every transformer family behaves identically'], 'The first decision is not brand name; it is matching architecture behavior to the task contract.'],

  ['tfam-021-encoder-mechanism', 'Mechanism', 'What is the core self-attention pattern inside an encoder stack?', 'Bidirectional self-attention over the available input tokens', ['Causal target-only attention with future leakage', 'Cross-attention to labels before encoding'], 'Encoder layers let each input token representation incorporate other visible input positions.'],
  ['tfam-022-encoder-objective', 'Mechanism', 'What objective family commonly pairs with encoder-only representations?', 'Representation, classification, ranking, or masked-token objectives', ['Only open-ended next-token continuation objectives', 'Only image resizing objectives with no text input'], 'Encoders commonly support tasks where a whole input is mapped to a representation or decision.'],
  ['tfam-023-encoder-readout', 'Mechanism', 'How does an encoder-only model usually produce a decision?', 'A task head reads pooled or selected hidden states', ['A sampling loop appends infinite tokens by default', 'A source decoder reads raw HTML routes'], 'The encoder stack creates contextual states, then a head maps those states to the task output.'],
  ['tfam-024-encoder-limitation', 'Mechanism', 'Why is an encoder-only stack not naturally a free-form generator?', 'It lacks the causal decoding loop that appends one predicted token at a time', ['It cannot compute hidden vectors for text', 'It is forbidden from using attention weights'], 'Encoders can be adapted, but their native pattern is representation learning rather than autoregressive continuation.'],
  ['tfam-025-decoder-mechanism', 'Mechanism', 'What self-attention rule defines a decoder-only stack?', 'Masked attention keeps each target position from reading future tokens', ['Every target position reads all later ground-truth tokens', 'All source states are deleted before decoding'], 'The causal mask makes training visibility match generation visibility.'],
  ['tfam-026-decoder-objective', 'Mechanism', 'What training objective naturally pairs with decoder-only language models?', 'Predicting the next token from the prefix', ['Predicting a random class without reading text', 'Reconstructing an encoder memory with no target tokens'], 'The next-token objective directly trains the step used during autoregressive inference.'],
  ['tfam-027-decoder-inference', 'Mechanism', 'What happens during decoder-only inference?', 'The model repeatedly scores the next token and appends the selected token', ['The model encodes a source once and stops before output', 'The model reads future target answers from the dataset'], 'Autoregressive decoding is an iterative loop over growing prefixes.'],
  ['tfam-028-decoder-cache', 'Mechanism', 'Why does serving often cache decoder states?', 'Past key and value states can be reused as the prefix grows', ['Bidirectional future states are required at every step', 'The classifier head must store every validation label'], 'A KV cache avoids recomputing unchanged prefix attention information during generation.'],
  ['tfam-029-encdec-mechanism', 'Mechanism', 'What two processing phases define an encoder-decoder model?', 'Encode the source, then decode the target while attending to source memory', ['Sort tokens, then delete all source context', 'Pool a class label, then stop before generation'], 'The architecture has one stack for source understanding and another for target generation.'],
  ['tfam-030-encdec-masks', 'Mechanism', 'Which masks usually appear in an encoder-decoder text model?', 'Bidirectional source attention, causal target attention, and source-memory attention', ['Only a padding mask after final sampling', 'Only future-token access for every target position'], 'The encoder and decoder have different visibility needs, plus cross-attention into the source states.'],
  ['tfam-031-cross-memory', 'Mechanism', 'What is the memory used by decoder cross-attention?', 'Final or intermediate encoder representations of the source sequence', ['Future target tokens hidden beyond the current step', 'A table of optimizer hyperparameter names'], 'Cross-attention keys and values typically come from encoded source states.'],
  ['tfam-032-source-target', 'Mechanism', 'Why separate source and target streams in sequence-to-sequence modeling?', 'The input text is fully available while the output text must be generated progressively', ['Both streams must always use identical tokens', 'The source must be sampled after the target is complete'], 'This separation matches tasks such as translation, where the source is known and the target is produced left to right.'],
  ['tfam-033-mask-vs-objective', 'Mechanism', 'How do masks and objectives work together?', 'Masks define visibility, while objectives define what prediction is trained', ['Masks replace losses, while objectives choose UI icons', 'Both are irrelevant after embeddings are created'], 'A decoder uses a causal mask because the objective asks it to predict next tokens from prefixes.'],
  ['tfam-034-family-vs-size', 'Mechanism', 'Is architecture family the same as model size?', 'No, small or large models can use any of these family patterns', ['Yes, only billion-parameter models have a family', 'Yes, encoder-decoder always means the largest model'], 'Family describes information flow and task shape, not parameter count.'],
  ['tfam-035-family-vs-brand', 'Mechanism', 'Why should brand names not be the only design rule?', 'Brand examples are shorthand for visibility and objective patterns', ['Brand names determine tensor shapes by themselves', 'Brand names remove the need to test outputs'], 'BERT, GPT, and T5 are examples; the engineering choice should reference the underlying mechanics.'],
  ['tfam-036-output-heads', 'Mechanism', 'How do heads differ across families?', 'Encoders often use task heads, while decoders use token-generation heads', ['All families must use only a binary classifier head', 'No family needs an output head after hidden states'], 'The head should match the expected output: class scores, rankings, or vocabulary logits.'],
  ['tfam-037-training-data-shape', 'Mechanism', 'How does data shape signal the right family?', 'Single completed inputs favor encoders, prefixes favor decoders, paired source-target data favors encoder-decoders', ['All datasets require the same future-visible target format', 'Only the number of files matters for family choice'], 'Look at whether examples are fixed inputs, continuations, or paired transformations.'],
  ['tfam-038-latency-shape', 'Mechanism', 'How can latency differ by family at serving time?', 'Encoder tasks can run once per input, while generation loops may run once per output token', ['All families always finish in one identical matrix multiply', 'Only encoders ever need batching'], 'Autoregressive decoding adds sequential work because each new token depends on the prior prefix.'],
  ['tfam-039-context-flow', 'Mechanism', 'What context flow distinguishes the three families?', 'Whole-input context, prefix-only context, or source-conditioned target context', ['Random context, sorted context, or no context', 'Only validation context for every task'], 'This is the central mechanism distinction learners should carry into task selection.'],
  ['tfam-040-representation-vs-generation', 'Mechanism', 'Why does representation work differ from generation work?', 'Representation work scores or embeds an input, while generation creates new token sequences', ['Representation work never uses tokens', 'Generation work never uses probabilities'], 'The architecture should align with whether the product consumes text or must produce text.'],
  ['tfam-041-bidirectional-training', 'Mechanism', 'Why can bidirectional encoders use both left and right context during encoding?', 'The whole input is available before the model emits its representation or decision', ['The future output has already been generated by the decoder', 'The model is allowed to leak target answers during deployment'], 'This visibility is appropriate for tasks that do not require left-to-right output generation.'],
  ['tfam-042-causal-training', 'Mechanism', 'Why must causal training hide future target tokens?', 'The model will not have future tokens available when generating', ['Future tokens are always available from the user interface', 'The encoder memory contains every target answer'], 'Training should not give the model information it cannot use at inference time.'],
  ['tfam-043-cross-attention-placement', 'Mechanism', 'Where is cross-attention typically placed in a sequence-to-sequence transformer?', 'Inside decoder layers after target self-attention has formed a query state', ['Inside the tokenizer before ids are created', 'Only in the final loss function after sampling'], 'Decoder states query source memory so each target position can condition on the input.'],
  ['tfam-044-padding-source', 'Mechanism', 'What padding rule matters for encoder and source memory attention?', 'Padded source positions should be masked so they are not treated as content', ['Padding must be translated as a real word', 'Padding should always get the highest attention score'], 'Batched source sequences often contain padding that should not influence representations.'],
  ['tfam-045-padding-target', 'Mechanism', 'What padding rule matters for target-side decoding?', 'Target padding should be ignored by loss and masked from attention when appropriate', ['Target padding should become the next-token label', 'Target padding should disable the causal mask'], 'Generation models should learn from real target tokens, not artificial padding slots.'],
  ['tfam-046-fine-tuning', 'Mechanism', 'What changes when adapting a family to a downstream task?', 'The head, prompt format, objective, or data format may change while the visibility pattern remains central', ['The family automatically turns into every other family', 'The tokenizer alone guarantees task success'], 'Fine-tuning can adapt behavior, but it does not erase the underlying attention and output constraints.'],
  ['tfam-047-hybrid-note', 'Mechanism', 'How should you treat hybrid or modern variants in this lesson?', 'Map the variant back to its visibility, objective, and input-output contract', ['Ignore the mechanics and choose the newest acronym', 'Assume variants have no masks or objectives'], 'Real models can blur labels, so the durable reasoning is about information flow.'],
  ['tfam-048-evaluation', 'Mechanism', 'What evaluation question follows from family selection?', 'Does the model receive only the information it will have at inference time?', ['Does the model have the shortest lesson title', 'Does every attention score look identical'], 'A family mismatch often appears as leakage, missing context, or an output format that is hard to produce.'],
  ['tfam-049-lab-justification', 'Mechanism', 'What should a lab justification mention for each task?', 'Attention visibility, training objective, and expected output type', ['Only the family name without any reason', 'Only how many examples are in the batch'], 'The lesson success criteria require explaining why a family fits, not merely naming one.'],
  ['tfam-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide implementation?', 'Use bidirectional encoding, causal decoding, or encoder-to-decoder conditioning according to the task contract', ['Use the same visibility mask for every text task', 'Choose a family before knowing the input and output'], 'The right family follows the available context and required output behavior.'],

  ['tfam-051-search-reranking', 'Application', 'A search system scores a query-document pair after both texts are known. Which family is a strong starting point?', 'Use an encoder-style model to read the pair and score relevance', ['Use a decoder-only chat loop as the default ranking primitive', 'Use a target decoder without providing source text'], 'Reranking is usually a representation or scoring task over fixed available text.'],
  ['tfam-052-sentiment', 'Application', 'A product needs sentiment labels for completed reviews. What family choice is natural?', 'Use a bidirectional reader with a classification head', ['Use a translator that must emit a long target sentence', 'Use a model that cannot see the end of the review'], 'Sentiment classification benefits from reading the entire review before assigning a label.'],
  ['tfam-053-chatbot', 'Application', 'A chatbot must continue a conversation turn by turn. Which family best matches that behavior?', 'Use a decoder-style model trained for next-token continuation', ['Use a representation-only encoder with no decoding loop', 'Use a source encoder that cannot emit target tokens'], 'Conversation responses are generated autoregressively from the visible chat history.'],
  ['tfam-054-autocomplete', 'Application', 'A code editor asks for the next few tokens after a prefix. What should you choose?', 'Choose a causal decoder that predicts continuations from the prefix', ['Choose a pooled encoder that sees both future and past code', 'Choose an encoder-decoder with no source prompt'], 'Autocomplete is a prefix-to-continuation problem.'],
  ['tfam-055-translation-app', 'Application', 'A system converts source sentences into target-language sentences. What is the classic family fit?', 'Choose an encoder-decoder that encodes the source and decodes the target', ['Choose a classifier that emits one fixed label', 'Choose an encoder-only model and expect unlimited output by default'], 'Translation has paired source and target sequences, matching sequence-to-sequence conditioning.'],
  ['tfam-056-summarization', 'Application', 'A summarizer must produce a shorter text grounded in a document. What family is a natural fit?', 'Use source-conditioned generation with an encoder-decoder, or a decoder prompted with the source when designed that way', ['Use a label-only encoder head and never generate text', 'Use a future-leaking target mask to read the summary answers'], 'The safest reasoning is about source grounding and generated output, not a rigid brand rule.'],
  ['tfam-057-embedding-api', 'Application', 'An API returns vectors for documents to support retrieval. What family pattern is natural?', 'Use an encoder-style representation model', ['Use a decoder that must sample every document vector as text', 'Use cross-attention without any source states'], 'Embedding endpoints usually map completed inputs to reusable vector representations.'],
  ['tfam-058-title-generation', 'Application', 'A product generates a title from an article. Which family reasoning is best?', 'It needs source-conditioned text generation from article content', ['It only needs a class head because the output is free-form text', 'It should expose future target tokens during inference'], 'Title generation is not just classification; it creates target text grounded in a source.'],
  ['tfam-059-pair-classification', 'Application', 'A natural-language inference model compares a premise and hypothesis. What is the likely family?', 'An encoder that reads both texts together and predicts a relation', ['A causal decoder that cannot see the hypothesis end', 'A target decoder that ignores the premise'], 'The task is a fixed-input pair judgment, so bidirectional reading is useful.'],
  ['tfam-060-open-ended-writing', 'Application', 'A writing assistant must draft new paragraphs from a prompt. Which fit is most direct?', 'A decoder-only language model for prompt continuation', ['An encoder-only model with only a pooled class output', 'A retriever without a token-generation head'], 'Open-ended drafting is iterative next-token generation conditioned on a prefix.'],
  ['tfam-061-fixed-vs-generated', 'Application', 'How should you decide between a fixed-input labeler and a text generator?', 'Ask whether the output is a label or a sequence of newly generated tokens', ['Ask whether the route name contains more hyphens', 'Assume labels and generated text use the same serving loop'], 'Output type is a primary architecture-family signal.'],
  ['tfam-062-debug-encoder-generation', 'Application', 'An encoder-only prototype struggles to produce multi-sentence answers. What mismatch should you suspect?', 'A representation family is being asked to behave like an autoregressive generator', ['The model has too much source-target separation', 'The reranker is using too many completed inputs'], 'Encoders can support some generative adaptations, but free-form continuation is not their native design.'],
  ['tfam-063-debug-decoder-classification', 'Application', 'A decoder-only model is used for classification by prompting it to emit a label. What should you verify?', 'The prompt, label tokens, calibration, and evaluation match the classification objective', ['That it secretly has bidirectional access to future labels', 'That no logits are produced at any position'], 'Decoder-only models can be used for labels, but the product should validate the objective and output format carefully.'],
  ['tfam-064-debug-leakage', 'Application', 'A target decoder performs too well during training but fails at generation. What is a likely issue?', 'Future target tokens leaked through the attention mask or data shift', ['The encoder saw the source sentence too clearly', 'The classifier head used a pooled state'], 'If training grants information that inference lacks, generation quality can collapse.'],
  ['tfam-065-debug-missing-source', 'Application', 'A translation decoder ignores the input sentence and produces generic output. What should you inspect?', 'Whether cross-attention or source conditioning is wired correctly', ['Whether the task should become sentiment classification', 'Whether future target labels are visible'], 'Sequence-to-sequence generation needs a reliable path from source representations to target decoding.'],
  ['tfam-066-debug-pooled', 'Application', 'A classifier pools only the first token from an encoder. What should you check?', 'Whether the selected representation and task head capture the needed input information', ['Whether the decoder can sample future target words', 'Whether cross-attention reads optimizer states'], 'Encoder family choice is not enough; the readout must also be appropriate.'],
  ['tfam-067-selection-rubric', 'Application', 'What rubric should you apply before choosing a family?', 'Identify available context, required output, objective, and visibility constraints', ['Pick the largest checkpoint before reading requirements', 'Use whichever family has the most famous example'], 'This rubric prevents choosing a model family by habit instead of task mechanics.'],
  ['tfam-068-serving-cost', 'Application', 'Why might a decoder-only writing assistant have different serving costs from an encoder classifier?', 'It may run one forward step for each generated token while maintaining a growing prefix', ['It never uses attention during serving', 'It runs only once regardless of output length'], 'Generation latency and cost scale with generated length in ways a fixed-input classifier may not.'],
  ['tfam-069-batch-design', 'Application', 'What batching issue should you plan for with sequence-to-sequence data?', 'Source and target lengths can differ and need separate masks', ['Every example must have a target identical to the source', 'Padding should be scored as fluent output'], 'Paired text tasks require careful source and target padding, masking, and loss handling.'],
  ['tfam-070-eval-metrics', 'Application', 'How should evaluation reflect the chosen family?', 'Use metrics that match representation quality, generated text quality, or source-conditioned accuracy', ['Use only one metric because all families output the same object', 'Evaluate only the attention color in the visualization'], 'A reranker, chatbot, and translator should not be judged by one identical metric.'],
  ['tfam-071-migration', 'Application', 'A team migrates from an encoder classifier to a generative instruction model. What changes?', 'The task becomes prompt and generation driven, so labels, decoding, and calibration must be retested', ['Nothing changes because transformer blocks guarantee identical behavior', 'The model no longer needs any evaluation set'], 'Changing families changes the product interface and failure modes.'],
  ['tfam-072-rag-reader', 'Application', 'In a RAG pipeline, retrieved passages are placed into a prompt for answer generation. Which family often handles the final answer?', 'A decoder-style generator conditioned on the prompt context', ['A standalone embedding encoder with no output tokens', 'A source padding mask used as the answer head'], 'The retriever may use encoders, while the final answer often uses autoregressive generation.'],
  ['tfam-073-rag-reranker', 'Application', 'In the same pipeline, which family often reranks retrieved passages?', 'An encoder-style scorer over query and passage text', ['A decoder that must write a full essay before scoring', 'A target decoder that cannot read the query'], 'Reranking is a fixed-input relevance decision, so encoder-style scoring is common.'],
  ['tfam-074-multitask', 'Application', 'A product supports classification and generation. What is the best architecture-family stance?', 'Use the family or adaptation that matches each task contract, and validate both workflows', ['Assume one family is automatically optimal for every output type', 'Disable masks so the model can do everything'], 'Multiple workflows may require different heads, prompts, models, or evaluations.'],
  ['tfam-075-application-summary', 'Application', 'What application summary avoids overclaiming?', 'Families are natural fits, not absolute limits; choose by task mechanics and test the result', ['Encoder-only models can never be used outside classification', 'Decoder-only models are always best for every ML product'], 'Production choices should acknowledge adaptations while still respecting information flow.'],

  ['tfam-076-false-same', 'Tricky', 'Which architecture-family claim is false?', 'All transformer families are identical once a block contains attention', ['Family labels summarize different visibility and objective patterns', 'The same task can become easier or harder depending on family fit'], 'The false claim ignores masks, objectives, heads, and inference behavior.'],
  ['tfam-077-false-encoder-generation', 'Tricky', 'Which encoder-only generation belief is wrong?', 'An encoder naturally generates free-form answers by reading future answer tokens', ['An encoder is often useful for fixed-input representations', 'An encoder can be paired with a task head for classification'], 'Reading future answer tokens would be leakage and is not the native encoder generation loop.'],
  ['tfam-078-false-decoder-future', 'Tricky', 'Which decoder-only claim should you reject?', 'A decoder-only model can rely on future target tokens during normal generation', ['A decoder-only model reads a visible prefix', 'A decoder-only model can score next-token probabilities'], 'Future target tokens do not exist yet during deployment generation.'],
  ['tfam-079-false-encdec-mask', 'Tricky', 'Which encoder-decoder claim is false?', 'The decoder target self-attention should freely read later target tokens', ['The decoder can attend to encoded source memory', 'The encoder can read the source sequence bidirectionally'], 'Target-side causal masking prevents leakage from later target positions.'],
  ['tfam-080-false-cross', 'Tricky', 'Which cross-attention misconception is unsafe?', 'Cross-attention mainly reads future target labels instead of source memory', ['Cross-attention can connect decoder states to encoded source states', 'Cross-attention supports source-conditioned generation'], 'Cross-attention is the source-conditioning path, not a license to inspect future answers.'],
  ['tfam-081-false-objective', 'Tricky', 'Which objective claim is false?', 'Training objectives are irrelevant once the model uses transformer layers', ['Next-token prediction supports autoregressive generation', 'Classification heads train representation models for labels'], 'Objectives define what signal the model learns to produce.'],
  ['tfam-082-false-masks', 'Tricky', 'Which mask belief should be rejected?', 'Attention masks only change visualization and not model behavior', ['Masks decide which positions can contribute information', 'Mask mistakes can cause leakage or missing context'], 'Masks are functional constraints in attention computation, not display metadata.'],
  ['tfam-083-false-bert', 'Tricky', 'Which BERT-style claim is wrong?', 'BERT-style models are decoder-only next-token generators by default', ['BERT-style models are commonly encoder-only readers', 'BERT-style models use bidirectional context for available input'], 'BERT-style architecture is associated with encoder-only representation learning.'],
  ['tfam-084-false-gpt', 'Tricky', 'Which GPT-style claim is wrong?', 'GPT-style models are encoder-only bidirectional readers by default', ['GPT-style models commonly use causal decoder stacks', 'GPT-style models generate by extending a prefix'], 'GPT-style systems are decoder-only in the usual architecture-family shorthand.'],
  ['tfam-085-false-t5', 'Tricky', 'Which T5-style claim is wrong?', 'T5-style models are only encoders with no target decoder', ['T5-style models fit source-to-target text transformations', 'T5-style models use encoder-decoder conditioning'], 'T5-style systems are sequence-to-sequence models with both source encoding and target decoding.'],
  ['tfam-086-false-classifier', 'Tricky', 'Which classifier-family claim is false?', 'A fixed-input classifier must always use decoder-only generation', ['An encoder can be a natural fit for completed-input labels', 'The readout head should match the label task'], 'Classification does not inherently require autoregressive text generation.'],
  ['tfam-087-false-chat', 'Tricky', 'Which chat-completion belief should you reject?', 'A bidirectional encoder alone is the natural default for open-ended chat replies', ['Chat replies usually require generating new tokens', 'Causal decoding matches prefix-to-response continuation'], 'An encoder can represent context, but it does not by itself provide the standard generation loop.'],
  ['tfam-088-false-translation', 'Tricky', 'Which translation claim is unsafe?', 'Translation has no source-target separation to model', ['The source sentence is available before target generation', 'The target sequence is produced progressively'], 'Ignoring source-target separation hides the main structure of translation.'],
  ['tfam-089-false-selector', 'Tricky', 'Which lesson-selector claim is false?', 'The selector only changes labels and teaches nothing about visibility', ['The selector should reveal family-specific attention access', 'The selector should connect family choice with output behavior'], 'The selector is meant to make the visibility and output differences concrete.'],
  ['tfam-090-tricky-summary', 'Tricky', 'Which architecture-family trap should you reject?', 'Choose by famous acronym while ignoring input, output, mask, and objective', ['Explain how visibility matches the task', 'Validate the chosen family against product behavior'], 'The common trap is memorizing names without checking the actual task mechanics.'],

  ['tfam-091-interview-definition', 'Interview', 'In an interview, how would you define transformer architecture families?', 'They are recurring stack patterns that match attention visibility and objectives to task input-output shape', ['They are random brand labels with no engineering meaning', 'They are optimizer schedules for unrelated model classes'], 'A strong definition ties family names to information flow and task behavior.'],
  ['tfam-092-interview-three', 'Interview', 'How would you contrast the three main families concisely?', 'Encoders read complete inputs, decoders continue prefixes, and encoder-decoders generate targets from sources', ['Encoders sample images, decoders sort datasets, and encoder-decoders choose metrics', 'All three read future answers in the same way'], 'This contrast is short but captures the decision-critical difference.'],
  ['tfam-093-interview-examples', 'Interview', 'How should you use BERT, GPT, and T5 examples without oversimplifying?', 'Treat them as examples of encoder-only, decoder-only, and encoder-decoder design patterns', ['Treat brand names as proof that evaluation is unnecessary', 'Treat every checkpoint with a similar name as mechanically identical'], 'The examples are useful shorthand only when you remember the underlying mechanics.'],
  ['tfam-094-interview-masks', 'Interview', 'What should you say about masks when comparing families?', 'Bidirectional visibility, causal prefix visibility, or cross-attention create different information access', ['Masks are only UI labels after the model has trained', 'Masks are the same for classification, chat, and translation'], 'Masking is one of the cleanest ways to explain why the families behave differently.'],
  ['tfam-095-interview-task-match', 'Interview', 'How would you choose a family for a new text product?', 'Start from available context, desired output type, training signal, and inference-time visibility', ['Start from whichever acronym is currently popular', 'Start from the largest parameter count and ignore output format'], 'This answer demonstrates engineering judgment instead of memorized taxonomy.'],
  ['tfam-096-interview-debug', 'Interview', 'How would you debug a family mismatch in production?', 'Look for leakage, missing source conditioning, wrong output head, or an inference loop that does not match training', ['Only rename the model family in the UI', 'Only increase the batch size and skip tests'], 'Family mistakes usually surface as information access or output-contract failures.'],
  ['tfam-097-interview-adaptation', 'Interview', 'How would you discuss exceptions and adaptations?', 'Say families are natural fits, while prompting, heads, and fine-tuning can adapt them but must be validated', ['Say no family can ever be adapted beyond its original use', 'Say adaptation removes the need for objective-specific evaluation'], 'This avoids both rigid taxonomy and careless overgeneralization.'],
  ['tfam-098-interview-tests', 'Interview', 'What tests would make the family decision production-ready?', 'Verify masks, data formatting, output contract, leakage resistance, and task-specific metrics', ['Check only that the route loads in the catalog', 'Check only that all answers use the same family name'], 'Production readiness requires behavioral checks around the selected family.'],
  ['tfam-099-interview-tradeoffs', 'Interview', 'What tradeoff answer shows mature architecture reasoning?', 'Encoder scoring can be efficient for fixed inputs, while generation and source-conditioned decoding add output-loop complexity', ['Every family has identical latency and memory behavior', 'Only the model name determines serving cost'], 'A good answer links family structure to serving and evaluation consequences.'],
  ['tfam-100-interview-takeaway', 'Interview', 'What is the production-ready architecture-family takeaway?', 'Pick the transformer family whose visibility, objective, and output behavior match the product task', ['Pick a family once and never revisit evaluation', 'Assume attention alone determines the full system design'], 'This closes the lesson with the durable rule: family choice is an input-output and information-flow decision.'],
];

export const TRANSFORMER_ARCHITECTURE_FAMILIES_QUIZ = TRANSFORMER_ARCHITECTURE_FAMILIES_SPECS.map(
  ([id, level, prompt, correct, distractors, explanation]) => q(id, level, prompt, correct, distractors, explanation),
);
