function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const rotation = stableHash(`positional-encoding:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + rotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const POSITIONAL_ENCODING_SPECS = [
  ['posenc-001-purpose', 'Foundation', 'Why do transformers need a position signal in addition to token embeddings?', 'Self-attention lacks built-in token order', ['Tokenization removes all word identity', 'Softmax cannot normalize without word positions'], 'Self-attention compares token vectors as a set of candidates unless order information is supplied.'],
  ['posenc-002-token-plus-position', 'Foundation', 'What is combined with each token embedding in standard absolute positional encoding?', 'A vector that represents that token slot in the sequence', ['The final answer probability only', 'A random deletion mask for the word'], 'The model receives token identity plus a signal for where that token appears.'],
  ['posenc-003-same-word', 'Foundation', 'Why can the same word need different input representations at different locations?', 'The word identity is the same but its sequence role can change', ['The tokenizer assigns a new vocabulary to each sentence', 'The model dimension changes for every word type'], 'Position information lets one token type participate differently depending on where it occurs.'],
  ['posenc-004-dog-man', 'Foundation', 'What does the "dog bites man" versus "man bites dog" example show?', 'The same tokens can mean different things when their order changes', ['The same words always produce the same sentence meaning', 'Alphabetical sorting is a valid language model input'], 'The lab example isolates why order must be visible to the model.'],
  ['posenc-005-absolute-index', 'Foundation', 'What is an absolute position signal based on?', 'The index of a token slot in the sequence', ['Only the token frequency in the corpus', 'Only the current loss value'], 'Absolute methods attach information for position 0, position 1, and so on.'],
  ['posenc-006-learned-basic', 'Foundation', 'What is a learned absolute position embedding?', 'A trainable table row looked up by position index', ['A fixed rule that never has parameters', 'A mask that hides all future tokens'], 'Learned position embeddings use parameters associated with position ids.'],
  ['posenc-007-sinusoidal-basic', 'Foundation', 'What is the core idea of sinusoidal positional encoding?', 'Use sine and cosine waves to create repeatable position fingerprints', ['Assign every token the same constant vector', 'Sort the sequence before attention starts'], 'Sinusoidal encodings produce deterministic vectors from position and dimension.'],
  ['posenc-008-many-frequencies', 'Foundation', 'Why do sinusoidal encodings use multiple wave frequencies?', 'Different dimensions capture position changes at different scales', ['Every dimension must copy the token id', 'The model needs one frequency per vocabulary item'], 'Short and long wavelength channels together make positions distinct and structured.'],
  ['posenc-009-model-dimension', 'Foundation', 'What does model dimension control in positional encoding?', 'How many positional channels are available to add to each token vector', ['How many sentences the tokenizer deletes', 'The number of answer options in a quiz'], 'The position vector must match the token embedding width.'],
  ['posenc-010-repeatable', 'Foundation', 'What does it mean that sinusoidal position vectors are repeatable?', 'The same position and dimension always compute the same values', ['The same word must appear in every sentence', 'The model randomly changes the table each forward pass'], 'A fixed formula gives stable values without learning a table row.'],
  ['posenc-011-not-token-meaning', 'Foundation', 'Do positional encodings replace token meaning?', 'No, they add order information to token representations', ['Yes, they delete token identity entirely', 'Yes, they are the language modeling objective'], 'Token embeddings still carry word or subword identity; the position signal adds where information.'],
  ['posenc-012-order-vs-identity', 'Foundation', 'Which distinction should a beginner make first?', 'Token identity says what the item is; positional information says where it is', ['Attention masks and tokenization are the same object', 'The output vocabulary determines every input position'], 'This distinction prevents confusing word meaning with sequence order.'],
  ['posenc-013-attention-need', 'Foundation', 'Why is position information especially important for attention?', 'Attention scores compare tokens without a built-in left-to-right scan', ['Attention cannot multiply matrices', 'Attention removes all context before scoring'], 'Unlike recurrence, vanilla self-attention does not inherently step through tokens in order.'],
  ['posenc-014-position-id', 'Foundation', 'What is a position id?', 'A number assigned to a token slot so a position signal can be selected or computed', ['A probability assigned after sampling', 'A replacement vocabulary id for unknown words'], 'Position ids connect sequence slots to learned rows or formula values.'],
  ['posenc-015-addition', 'Foundation', 'How are classic absolute positional encodings commonly combined with token embeddings?', 'They are added elementwise after matching the embedding width', ['They are always concatenated with no projection', 'They are used only after final logits'], 'The original transformer-style setup sums token and position vectors of the same dimension.'],
  ['posenc-016-fixed-vs-learned', 'Foundation', 'What is the basic fixed-versus-learned distinction?', 'Fixed encodings come from a formula; learned embeddings are trainable parameters', ['Both are always random at inference', 'Both replace attention masks'], 'This lesson contrasts deterministic sinusoidal vectors with parameter tables.'],
  ['posenc-017-extrapolation-caveat', 'Foundation', 'What is a common caveat for learned absolute position embeddings?', 'They need trained rows for positions the model is expected to use', ['They cannot be added to token vectors', 'They make attention approximate instead of exact'], 'A learned table has finite rows and does not automatically define unseen positions.'],
  ['posenc-018-lab-controls', 'Foundation', 'Which controls belong to the positional-encoding lab?', 'Sentence order, encoding type, model dimension, and probe position', ['Optimizer momentum, dropout mask, and batchnorm phase', 'Beam width, top-p sampling, and reward model score'], 'Those controls reveal how position information changes the represented sequence.'],
  ['posenc-019-check-understanding', 'Foundation', 'What should you be able to explain after the lab?', 'Why swapping the same words can require different position-aware representations', ['Why all word order can be ignored safely', 'Why tokenizers should alphabetize prompts'], 'The lesson checks whether learners can connect order to meaning.'],
  ['posenc-020-foundation-summary', 'Foundation', 'What foundation summary is correct?', 'Positional encoding gives attention-based models explicit order information', ['Positional encoding is a replacement for token embeddings', 'Positional encoding is only a padding mask'], 'The core idea is order information added to token representations before attention uses them.'],

  ['posenc-021-formula-even', 'Mechanism', 'Which formula gives an even sinusoidal channel?', 'PE(pos, 2i) = sin(pos / 10000^(2i / d_model))', ['PE(pos, 2i) = softmax(token_id)', 'PE(pos, 2i) = random_mask(pos)'], 'Even channels use sine with a wavelength determined by the dimension index.'],
  ['posenc-022-formula-odd', 'Mechanism', 'Which formula gives the paired odd sinusoidal channel?', 'PE(pos, 2i+1) = cos(pos / 10000^(2i / d_model))', ['PE(pos, 2i+1) = argmax(attention)', 'PE(pos, 2i+1) = label - prediction'], 'Odd channels use cosine with the same frequency scale as the neighboring sine channel.'],
  ['posenc-023-d-model', 'Mechanism', 'What does d_model mean in the sinusoidal formula?', 'The width of the model embedding vector', ['The number of training labels in the batch', 'The number of decoder layers already cached'], 'The denominator schedule depends on the embedding dimensionality.'],
  ['posenc-024-dimension-pairs', 'Mechanism', 'Why are dimensions handled in sine-cosine pairs?', 'Each frequency gets a sine coordinate and a cosine coordinate', ['Each token needs two vocabulary ids', 'Each attention head removes two positions'], 'Pairing sine and cosine gives phase information for the same frequency.'],
  ['posenc-025-frequency-schedule', 'Mechanism', 'What does the 10000^(2i / d_model) term control?', 'The wavelength scale assigned to that dimension pair', ['The batch size for every sequence', 'The number of padding tokens to add'], 'Changing the denominator changes how quickly that channel varies with position.'],
  ['posenc-026-low-high-frequency', 'Mechanism', 'What do different sinusoidal frequencies help represent?', 'Both fine local shifts and broader position patterns', ['Only the final token probability', 'Only whether the sequence is padded'], 'Multiple scales let the vector encode nearby and farther-apart positions.'],
  ['posenc-027-table-shape', 'Mechanism', 'What shape does an absolute positional encoding table often have for one maximum length?', 'Maximum sequence length by model dimension', ['Vocabulary size by optimizer step', 'Number of labels by beam width'], 'There is one position vector per slot, each matching the embedding width.'],
  ['posenc-028-broadcast-batch', 'Mechanism', 'How is the same position table usually used across a batch?', 'The position rows are broadcast or indexed for each sequence example', ['Each batch element gets a new vocabulary', 'The table is multiplied by the loss scalar only'], 'Batching reuses the same position scheme while respecting each example shape.'],
  ['posenc-029-add-before-attention', 'Mechanism', 'When are classic absolute position vectors added?', 'Before self-attention builds queries, keys, and values from the input states', ['After all value vectors have already been mixed', 'Only after the final sampled token'], 'The projections then operate on token representations that already include position information.'],
  ['posenc-030-qk-effect', 'Mechanism', 'How can position information affect attention scores?', 'Queries and keys are derived from vectors that include position cues', ['Softmax reads raw text order from the file', 'The value projection ignores every token vector'], 'When Q and K include positional components, their dot products can depend on order.'],
  ['posenc-031-learned-table', 'Mechanism', 'How does a learned position embedding table work?', 'Look up a trainable row for each position id and add it to the token embedding', ['Generate a causal mask after sampling', 'Compute labels from cosine similarity only'], 'The table is trained like other embeddings and selected by position index.'],
  ['posenc-032-position-range', 'Mechanism', 'What happens if a learned absolute table is asked for an out-of-range position?', 'The model lacks a defined learned row unless the implementation extends or remaps it', ['The tokenizer automatically invents semantics', 'The attention mask proves the row is correct'], 'A finite embedding table cannot simply index positions it does not contain.'],
  ['posenc-033-fixed-extrapolate', 'Mechanism', 'Why can fixed sinusoidal encodings be computed beyond the training length?', 'The formula can produce values for larger position ids', ['The model has memorized every possible future prompt', 'The softmax layer stores infinite table rows'], 'The deterministic formula is defined outside the seen range, though model behavior still needs validation.'],
  ['posenc-034-extrapolation-limit', 'Mechanism', 'Why is formula extrapolation not a guarantee of long-context quality?', 'The model may not have learned to use unseen positions reliably', ['The formula stops returning numbers after training', 'All long prompts become padding by definition'], 'Computable encodings and learned generalization are different claims.'],
  ['posenc-035-absolute-vs-relative', 'Mechanism', 'What is the basic absolute-versus-relative distinction?', 'Absolute methods tag each slot; relative methods emphasize offsets between slots', ['Absolute methods remove token embeddings; relative methods remove attention', 'Both methods are only tokenizer merges'], 'Absolute position says where a token is; relative schemes focus more directly on distances.'],
  ['posenc-036-rope-contrast', 'Mechanism', 'How is RoPE different from additive sinusoidal encoding at a high level?', 'RoPE rotates query and key coordinates instead of adding a position vector to input embeddings', ['RoPE deletes keys and values entirely', 'RoPE is a sampling algorithm for logits'], 'The next lesson shifts from added input vectors to rotations inside attention scoring.'],
  ['posenc-037-mask-separate', 'Mechanism', 'How are attention masks related to positional encoding?', 'Masks control visibility; positional encoding supplies order information', ['Masks are the same as position vectors', 'Positions decide which labels count in the loss'], 'A causal mask can block future reads, but it does not by itself encode every token location.'],
  ['posenc-038-padding-separate', 'Mechanism', 'Why do padding masks still matter when positions are present?', 'Padding slots should not become meaningful content just because they have position ids', ['Position ids automatically remove all padding', 'Padding tokens should receive maximum attention'], 'Position and valid-token status are separate pieces of metadata.'],
  ['posenc-039-position-reset', 'Mechanism', 'What position rule matters for packed or segmented examples?', 'Each independent sequence segment should use the intended position numbering', ['All segments must share one final token id', 'Every segment should ignore token embeddings'], 'Packed data can accidentally continue positions across boundaries unless handled deliberately.'],
  ['posenc-040-subword-positions', 'Mechanism', 'How are positions usually assigned after tokenization?', 'Each subword token slot receives its own position id', ['Only whole original words receive vectors', 'Only punctuation receives positions'], 'Transformers operate over tokenized slots, not necessarily original words.'],
  ['posenc-041-left-padding', 'Mechanism', 'Why can left padding make position ids tricky?', 'Real tokens may shift right, so ids must align with the intended content positions', ['Left padding removes the need for embeddings', 'The tokenizer always forbids left padding'], 'Padding side affects where real tokens sit in the tensor.'],
  ['posenc-042-generation-increment', 'Mechanism', 'During autoregressive generation, what should happen to the next token position?', 'It should advance consistently after the existing prefix length', ['It should reset to zero for every generated token', 'It should equal the answer choice index'], 'Generated tokens need position ids that match their location in the growing sequence.'],
  ['posenc-043-kv-cache-position', 'Mechanism', 'Why do KV cache positions need careful alignment?', 'Cached keys and new queries must agree about the positions they represent', ['The cache makes all position ids irrelevant', 'The cache stores only final text strings'], 'Misaligned position ids can make the model attend as if tokens were at the wrong slots.'],
  ['posenc-044-heads-share', 'Mechanism', 'Do all attention heads commonly receive the same input positional signal?', 'Yes, additive input position information is present before head-specific projections', ['No, token positions are hidden from all heads', 'Only the final head receives token embeddings'], 'After addition, every head projects from the same position-aware hidden state.'],
  ['posenc-045-scale-layernorm', 'Mechanism', 'What implementation detail can affect position-token balance?', 'Embedding scaling and normalization can change how strongly the position signal appears', ['The route URL decides the formula', 'The answer letter controls the vector norm'], 'Real models manage vector scales so token and position signals remain usable.'],
  ['posenc-046-learned-update', 'Mechanism', 'How are learned absolute position embeddings trained?', 'Their table rows receive gradient updates with the rest of the model', ['They are edited by the tokenizer after inference', 'They are fixed by the quiz answer order'], 'Learned rows are parameters optimized through backpropagation.'],
  ['posenc-047-sinusoidal-no-params', 'Mechanism', 'Why are sinusoidal encodings described as fixed?', 'Their values come from the formula rather than learned parameters', ['They freeze all transformer weights', 'They force every sentence to the same length'], 'The model can learn to use them, but the encoding table itself is deterministic.'],
  ['posenc-048-distance-structure', 'Mechanism', 'What structure do sinusoidal encodings give nearby positions?', 'Their vectors change smoothly across positions in each frequency channel', ['Their vectors are unrelated random labels', 'Their vectors always become identical after softmax'], 'Smooth waves create predictable changes as the position index moves.'],
  ['posenc-049-debug-values', 'Mechanism', 'What should you inspect when a displayed sinusoidal pattern looks wrong?', 'Position ids, dimension indexing, sine-cosine pairing, and model dimension', ['Only the final sampled text', 'Only the page title'], 'Most formula bugs come from indexing, shape, or pairing mistakes.'],
  ['posenc-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide implementation?', 'Compute or look up one width-matched position vector per token slot, then combine it before attention', ['Replace every token vector with a padding mask', 'Let softmax infer order from option labels'], 'This ties shape, timing, and purpose into one implementation rule.'],

  ['posenc-051-no-position-bug', 'Application', 'A transformer without any position signal treats reordered tokens too similarly. What is likely missing?', 'An order signal before or inside attention', ['A larger output vocabulary only', 'A different color map for attention'], 'Without position information, self-attention has weak access to sequence order.'],
  ['posenc-052-sentence-swap', 'Application', 'In the lab, what should change between "dog bites man" and "man bites dog"?', 'The token-position pairings should change even though the token set is the same', ['The vocabulary should delete one word', 'The model dimension should become zero'], 'The example depends on the same words occupying different slots.'],
  ['posenc-053-disable-position', 'Application', 'If you disable the position signal in the lab, what information is lost?', 'A direct cue telling the model where each token appears', ['The spelling of every token', 'The ability to run matrix multiplication'], 'Token identity remains, but order is no longer explicitly supplied.'],
  ['posenc-054-learned-longer', 'Application', 'A learned absolute-position model receives a prompt longer than its table. What should you check?', 'Whether the implementation has valid rows or a tested extension strategy for those positions', ['Whether top-p sampling is enabled', 'Whether all words are sorted alphabetically'], 'Longer context can fail at the embedding lookup or behave poorly after ad hoc extension.'],
  ['posenc-055-sinusoidal-longer', 'Application', 'A sinusoidal model is evaluated beyond trained lengths. What is the careful claim?', 'The encoding can be computed there, but model quality still needs empirical validation', ['The model is guaranteed perfect at any length', 'The formula becomes a learned table at inference'], 'Fixed encodings avoid missing rows, not every long-context generalization issue.'],
  ['posenc-056-padding-positions', 'Application', 'A left-padded batch gives different outputs than expected. Which metadata should you inspect?', 'Position ids, attention masks, and valid lengths for the real tokens', ['Only the final answer letters', 'Only the optimizer name'], 'Padding shifts tensor slots, so positions and masks must align to content.'],
  ['posenc-057-packed-reset', 'Application', 'Packed training examples concatenate independent sequences. What positional issue can occur?', 'Position numbering may accidentally continue across sequence boundaries', ['Token embeddings must become one-hot labels', 'Attention masks stop using query-key axes'], 'Independent segments often need deliberate resets or segment-aware handling.'],
  ['posenc-058-cache-edit', 'Application', 'A prompt is edited but an old KV cache is reused. What positional risk appears?', 'Cached states may no longer match the token positions in the new prefix', ['The tokenizer will automatically fix every cached key', 'All position vectors become padding masks'], 'Cache reuse is only valid when token content and position alignment still match.'],
  ['posenc-059-generation-off-by-one', 'Application', 'A generator repeats or degrades after the first new token. What positional bug is plausible?', 'The generated token position id is off by one or reset incorrectly', ['The answer choices are not shuffled', 'The route title is too short'], 'Autoregressive decoding needs consistent position increments.'],
  ['posenc-060-classifier', 'Application', 'Why does a classifier built on a transformer encoder still need positions?', 'Word order can affect the representation used for classification', ['Classification deletes all token embeddings', 'Labels supply the missing word order'], 'Even when the output is one label, the input meaning can depend on order.'],
  ['posenc-061-translation', 'Application', 'Why is positional encoding important in machine translation?', 'Source and target word order affect which relationships the model must represent', ['Translation only counts word frequencies', 'The decoder mask replaces all source order'], 'Translation depends on who did what to whom and where phrases appear.'],
  ['posenc-062-code-model', 'Application', 'Why do code models care about position signals?', 'Token order, indentation tokens, and delimiter placement change program meaning', ['Code tokens are unordered sets by definition', 'Position signals make syntax unnecessary'], 'Programming languages are highly order-sensitive.'],
  ['posenc-063-probe-position', 'Application', 'What does changing the probe position in the lab help reveal?', 'Which positional vector or token-position representation is being inspected', ['Which optimizer was used during pretraining', 'Which browser rendered the page'], 'A probe makes one slot concrete so learners can inspect its encoding.'],
  ['posenc-064-dimension-mismatch', 'Application', 'A model crashes when adding token and position embeddings. What should you inspect first?', 'Whether both tensors share the same embedding width and compatible shapes', ['Whether the answer index is balanced', 'Whether every token is a noun'], 'Elementwise addition requires compatible dimensions.'],
  ['posenc-065-visual-sine', 'Application', 'A sinusoidal heatmap shows every dimension as the same flat line. What is suspicious?', 'The frequency schedule or dimension indexing may be wrong', ['The sentence is too grammatical', 'The tokenizer has too many merges only'], 'Different channels should vary according to different sine-cosine frequencies.'],
  ['posenc-066-learned-new-length', 'Application', 'Fine-tuning extends a learned-position model to a longer context. What is a prudent step?', 'Initialize and validate new position rows or use a tested context-extension method', ['Assume unused rows are already optimal', 'Delete attention masks because length increased'], 'New positions need a deliberate strategy and evaluation.'],
  ['posenc-067-library-position-ids', 'Application', 'A library accepts explicit position_ids. Why should you test them?', 'Wrong ids can silently shift the position signal away from the intended tokens', ['They decide the final vocabulary size', 'They replace the loss mask'], 'Explicit position ids are powerful but easy to misalign.'],
  ['posenc-068-relative-migration', 'Application', 'A team migrates from absolute additions to RoPE. What should they not assume?', 'That old position-id behavior transfers unchanged to the new mechanism', ['That query and key geometry may matter', 'That long-context tests are still useful'], 'Different positional mechanisms expose order through different parts of attention.'],
  ['posenc-069-debug-ablation', 'Application', 'What ablation helps prove a task uses order information?', 'Compare behavior with the position signal enabled versus disabled or scrambled', ['Only rename the lesson route', 'Only change the button color'], 'A controlled ablation shows whether order cues matter for the observed behavior.'],
  ['posenc-070-attention-map', 'Application', 'How can attention maps help debug positional behavior?', 'They can show whether attention patterns shift sensibly when token order changes', ['They prove token embeddings are unnecessary', 'They replace unit tests for all tensor shapes'], 'Attention visualization is not complete proof, but it can reveal order-sensitive patterns.'],
  ['posenc-071-padding-not-content', 'Application', 'A model appears to learn from padded positions. What should be checked alongside masks?', 'Whether padding tokens also receive misleading position ids in downstream handling', ['Whether padding should be translated as words', 'Whether every position vector is zero'], 'Padding needs both visibility and representation handling to avoid artifacts.'],
  ['posenc-072-position-interpolation', 'Application', 'Why should position interpolation or scaling be validated carefully?', 'It changes how position ids map onto learned or geometric signals', ['It only changes CSS layout', 'It makes sequence order irrelevant'], 'Context-extension tricks alter the positional coordinate system and can affect behavior.'],
  ['posenc-073-production-tests', 'Application', 'What production tests should cover positional encoding?', 'Shape checks, padding-side cases, cache-position cases, longer-context cases, and order-swap examples', ['Only screenshots of answer choices', 'Only unpositioned bag-of-words inputs'], 'Position bugs appear in boundary cases, not just average prompts.'],
  ['posenc-074-documentation', 'Application', 'What should implementation documentation state?', 'Position convention, maximum length, padding behavior, cache alignment, and extension policy', ['Only the button labels in the UI', 'Only the names of distractor answers'], 'Teams need the convention spelled out because off-by-one and padding mistakes are common.'],
  ['posenc-075-application-summary', 'Application', 'What practical habit prevents positional-encoding bugs?', 'Trace token ids, position ids, masks, and cache positions together on a tiny example', ['Assume order is obvious from the raw string', 'Only inspect the final loss curve'], 'A tiny worked sequence exposes alignment mistakes before they reach production.'],

  ['posenc-076-false-self-attention', 'Tricky', 'Which self-attention order claim is false?', 'Self-attention inherently knows token order without any position signal', ['Position signals give order cues to attention', 'Token embeddings still carry identity'], 'Vanilla self-attention needs an explicit or architectural way to know order.'],
  ['posenc-077-false-replace-token', 'Tricky', 'Which token-meaning claim is false?', 'Positional encodings replace token embeddings and remove word meaning', ['Position vectors add where information', 'Token identity remains part of the input'], 'The lesson explicitly warns that positions add to token meaning rather than replacing it.'],
  ['posenc-078-false-sinusoidal-random', 'Tricky', 'Which sinusoidal claim is false?', 'Sinusoidal encodings are random learned rows updated by gradient descent', ['Sinusoidal values come from a fixed formula', 'Learned tables use trainable rows'], 'This confuses fixed sinusoidal encodings with learned absolute embeddings.'],
  ['posenc-079-false-learned-extrapolate', 'Tricky', 'Which learned-embedding claim is false?', 'Learned absolute position embeddings automatically extrapolate to every longer prompt', ['Learned tables need valid rows', 'Long-context behavior needs validation'], 'A finite learned table does not define arbitrary unseen positions by itself.'],
  ['posenc-080-false-same-vector', 'Tricky', 'Which same-word claim is false?', 'The same token must have an identical final input vector at every sequence position', ['The same token can receive different position additions', 'Order can change a token representation'], 'Position additions make the same token type position-aware.'],
  ['posenc-081-false-mask-replaces', 'Tricky', 'Which mask relationship claim is false?', 'A causal attention mask fully replaces the need for positional information', ['Masks control visibility', 'Position signals supply order cues'], 'Masking and positional encoding solve different parts of the sequence modeling problem.'],
  ['posenc-082-false-position-alone', 'Tricky', 'Which prediction claim is false?', 'The position vector alone decides the next token regardless of token content', ['Token content and model state still matter', 'Position is only one input signal'], 'Positions are not a standalone language model.'],
  ['posenc-083-false-destroy-identity', 'Tricky', 'Which addition claim is false?', 'Adding a position vector necessarily destroys token identity', ['The model can use both token and position components', 'Matching dimensions allow elementwise combination'], 'Addition changes the representation but does not automatically erase token information.'],
  ['posenc-084-false-constant', 'Tricky', 'Which vector claim is false?', 'All positions should use one identical constant vector', ['Different slots need distinguishable position signals', 'Sinusoidal encodings vary by position'], 'A constant vector would not tell positions apart.'],
  ['posenc-085-false-alphabetical', 'Tricky', 'Which ordering claim is false?', 'Sorting tokens alphabetically is a correct substitute for positional encoding', ['Natural order should be represented, not destroyed', 'Order changes can change meaning'], 'Alphabetical sorting discards the sequence structure the model needs.'],
  ['posenc-086-false-padding', 'Tricky', 'Which padding claim is false?', 'Padding positions need no care once positional encodings exist', ['Padding masks and position ids must still align', 'Padding slots are not semantic content'], 'Position signals do not turn padding into valid content.'],
  ['posenc-087-false-cache', 'Tricky', 'Which cache claim is false?', 'Cached position states can always be reused after prompt edits', ['Cache reuse requires matching token and position alignment', 'Prompt edits can invalidate cached keys'], 'A cache is tied to the exact prefix positions used to build it.'],
  ['posenc-088-false-off-by-one', 'Tricky', 'Which position-id claim is false?', 'An off-by-one position id is harmless because attention will fix it automatically', ['Wrong ids can shift the order signal', 'Tiny tests should catch indexing mistakes'], 'Attention consumes the representations it is given; it cannot infer the intended index.'],
  ['posenc-089-false-long-context', 'Tricky', 'Which long-context claim is false?', 'If a formula can compute positions, long-context quality is guaranteed', ['Long-context behavior still needs evaluation', 'Computable encodings do not prove generalization'], 'Being able to produce a vector is not the same as reliable model behavior.'],
  ['posenc-090-tricky-summary', 'Tricky', 'Which belief should you reject?', 'Position signals are optional decoration, token replacements, and automatic long-context guarantees', ['Order signals are part of the model input design', 'Position behavior needs shape and boundary tests'], 'This rejects the main misconceptions while preserving the useful lesson idea.'],

  ['posenc-091-interview-definition', 'Interview', 'How should you define positional encoding in one sentence?', 'A mechanism that injects token order information into transformer representations', ['A tokenizer rule that removes repeated words', 'A sampling method for final logits'], 'A strong definition names order information and where it enters the representation.'],
  ['posenc-092-interview-need', 'Interview', 'How should you explain why transformers need it?', 'Self-attention is permutation-insensitive without an added or built-in order signal', ['Softmax cannot output probabilities without it', 'Tokenization deletes all semantic identity'], 'The interview answer should connect the need to vanilla attention structure.'],
  ['posenc-093-interview-formula', 'Interview', 'What formula detail should you be ready to cite?', 'Even channels use sine and odd channels use cosine at frequency scales set by d_model', ['All channels use one learned constant', 'The formula is the cross-entropy loss'], 'You do not need to derive everything, but you should know the sine-cosine structure.'],
  ['posenc-094-interview-learned-vs-fixed', 'Interview', 'How should you compare learned and sinusoidal absolute encodings?', 'Learned tables are trainable but length-bound; sinusoidal vectors are fixed and computable by formula', ['Both are always identical at every position', 'Both remove attention masks'], 'The key trade-off is parameters and fitted range versus deterministic structure.'],
  ['posenc-095-interview-absolute-relative', 'Interview', 'How should you contrast absolute encodings with relative or rotary schemes?', 'Absolute encodings tag slots, while relative or rotary schemes emphasize relationships inside attention', ['Absolute encodings delete words, while relative encodings delete keys', 'They differ only in answer choice order'], 'This prepares the transition to RoPE without overclaiming.'],
  ['posenc-096-interview-debugging', 'Interview', 'What debugging checklist should you give for position bugs?', 'Check token order, position ids, embedding shapes, padding side, masks, and cache alignment', ['Only inspect final text style', 'Only increase model size'], 'Most real failures are alignment and boundary problems.'],
  ['posenc-097-interview-long-context', 'Interview', 'What should you say about extending context length?', 'Verify the position mechanism supports the range and test quality at the new lengths', ['Assume any larger number works automatically', 'Disable all masks to make room'], 'Long-context changes require both implementation support and empirical validation.'],
  ['posenc-098-interview-masks', 'Interview', 'How should you explain the relationship to masks?', 'Masks decide legal visibility, while positional encodings tell the model where tokens are', ['Masks and positions are the same tensor in every model', 'Positions decide which losses are ignored'], 'This distinction prevents confusing two neighboring transformer concepts.'],
  ['posenc-099-interview-production', 'Interview', 'What production validation should you name?', 'Tiny order-swap tests, padding-side tests, cache-position tests, shape checks, and longer-context evaluations', ['Only one unmasked happy-path prompt', 'Only visual inspection of the homepage'], 'Production readiness needs targeted checks for the ways positions fail.'],
  ['posenc-100-interview-summary', 'Interview', 'What production-ready positional-encoding takeaway should you give?', 'Treat positions as explicit sequence metadata that must stay aligned with tokens, masks, and caches', ['Treat positions as harmless decoration', 'Use alphabetical sorting instead of order signals'], 'The final takeaway is alignment: token slots, position ids, masks, and cached states must agree.'],
];

export const POSITIONAL_ENCODING_QUIZ = POSITIONAL_ENCODING_SPECS.map(([id, level, prompt, correct, distractors, explanation]) => (
  q(id, level, prompt, correct, distractors, explanation)
));
