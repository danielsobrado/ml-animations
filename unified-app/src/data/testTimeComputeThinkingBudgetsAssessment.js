function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) {
    hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  }
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) {
    hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  }
  return hash;
}

function q(number, level, prompt, correct, distractors, explanation) {
  const id = `ttc-${String(number).padStart(3, '0')}`;
  const registryRotation = stableHashNumber(`test-time-compute-thinking-budgets:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return Object.freeze({
    id,
    level,
    prompt,
    choices: Object.freeze(choices),
    answerIndex,
    explanation,
    questionHash: stableHash(`${prompt}\n${choices.join('\n')}`),
  });
}

const SPECS = Object.freeze([
  q(1, 'Foundation', 'What does test-time compute scaling spend?', 'Extra inference compute per query, usually through more samples, more reasoning tokens, tools, or search.', ['More pretraining data before the model is deployed.', 'A larger tokenizer vocabulary with no generation changes.'], 'The lesson contrasts permanent training-time investment with per-request inference-time spending.'),
  q(2, 'Foundation', 'How does training-time scaling differ from inference-time scaling?', 'Training-time scaling changes model weights; inference-time scaling spends additional compute while answering a specific request.', ['Training-time scaling only changes sampling temperature.', 'Inference-time scaling requires training from scratch for every user query.'], 'Training compute buys a new model checkpoint, while test-time compute rents extra work at serving time.'),
  q(3, 'Foundation', 'Why do thinking tokens count as compute?', 'Each generated token requires model inference work and contributes to latency and cost.', ['Tokens are free once the prompt is loaded.', 'Only input tokens are billed or processed.'], 'Longer traces are not just text; they consume decoding steps and serving resources.'),
  q(4, 'Foundation', 'What is the core tradeoff of test-time compute?', 'Higher accuracy on harder tasks can come with higher latency, token cost, and operational complexity.', ['It always improves accuracy without extra cost.', 'It replaces all evaluation with pretraining.'], 'The lesson is about the cost-accuracy frontier, not a free improvement knob.'),
  q(5, 'Foundation', 'What does Best-of-N sampling do?', 'It generates multiple candidate answers and selects one using a verifier or reward signal.', ['It trains N separate base models from scratch.', 'It deletes all but the shortest prompt before decoding.'], 'Best-of-N is parallel sampling plus selection, so the verifier quality matters.'),
  q(6, 'Foundation', 'What is the oracle bound in Best-of-N?', 'The accuracy if selection always chooses the best sampled candidate.', ['The accuracy of greedy decoding with N equal to one.', 'The latency target for a production service.'], 'The oracle bound is theoretical because real systems rarely know which sample is truly best.'),
  q(7, 'Foundation', 'How does Best-of-N compute cost scale with sample count?', 'Roughly linearly with N times average completion length.', ['It stays constant because samples share one forward pass.', 'It falls as N increases because the model becomes smaller.'], 'Batching may reduce wall-clock time, but total generated work still grows with the number of samples.'),
  q(8, 'Foundation', 'What does beam or tree search expand?', 'Partial reasoning paths that are scored and pruned before final answers are complete.', ['Only final answers after all candidates finish.', 'Only cached KV tensors with no text states.'], 'Tree search spends compute where partial paths look promising.'),
  q(9, 'Foundation', 'Why does tree search benefit from process-level scoring?', 'It needs to evaluate intermediate reasoning nodes before deciding which branches to continue.', ['It only needs a final exact-match answer checker.', 'It cannot use any reward model.'], 'A PRM-style signal can score partial steps, while an outcome-only checker is too late for pruning.'),
  q(10, 'Foundation', 'What is an adaptive thinking budget?', 'A budget policy that gives easy queries short traces and hard queries more thinking compute.', ['A fixed maximum token cap applied identically to every query.', 'A rule that always uses the largest context window.'], 'Adaptive policies aim to avoid both truncation and waste.'),
  q(11, 'Foundation', 'What is budget forcing?', 'Constraining or extending the number of reasoning tokens to study how accuracy and latency change.', ['Forcing the model to retrain before each answer.', 'Making every verifier accept all outputs.'], 'The lesson uses budget sweeps to show plateaus and truncation.'),
  q(12, 'Foundation', 'What is tool-augmented compute in this lesson?', 'Using external tools for facts, calculation, or execution instead of spending all effort in internal tokens.', ['Replacing the model with a static lookup table.', 'Turning every answer into a longer hidden prompt.'], 'Tool use trades reasoning tokens for tool latency and grounded observations.'),
  q(13, 'Foundation', 'What is the overthinking plateau?', 'The region where extra thinking tokens add cost but little or no accuracy gain.', ['The point where training compute becomes free.', 'The minimum number of tokens needed for every task.'], 'Past a task-specific threshold, additional trace length can become repetitive waste.'),
  q(14, 'Foundation', 'What is hard budget truncation?', 'A fixed token cap cuts off reasoning before a hard problem is complete.', ['A verifier chooses the best sample perfectly.', 'The model spends too many tokens on a trivial task.'], 'Too small a cap can force premature answers on multi-step tasks.'),
  q(15, 'Foundation', 'What is the verifier-reasoning gap?', 'The selected trace satisfies a verifier proxy without truly solving the intended task.', ['The model refuses to generate more than one token.', 'The GPU runs out of disk space during pretraining.'], 'Verifier gaps are a test-time analogue of optimizing the wrong reward signal.'),
  q(16, 'Foundation', 'What does latency-cost explosion describe?', 'High thinking budgets break serving economics or P99 latency targets at scale.', ['A training loss that becomes negative.', 'A tokenizer that produces only single-character tokens.'], 'More inference compute may be technically possible but operationally unacceptable.'),
  q(17, 'Foundation', 'What is a fixed-cap budget policy?', 'The same hard maximum thinking-token limit is applied to all requests.', ['A classifier routes each request to a custom budget.', 'The model decides when to stop without any cap.'], 'Fixed caps are simple and predictable, but they can waste tokens or truncate hard cases.'),
  q(18, 'Foundation', 'What is a model-controlled budget policy?', 'The model learns when to stop thinking, often through RL length penalties and correctness rewards.', ['A human manually writes every token budget at inference time.', 'The serving layer forbids all intermediate reasoning.'], 'Model-controlled stopping is flexible, but harder to guarantee for strict latency.'),
  q(19, 'Foundation', 'What does speculative or parallel drafting try to reduce?', 'Wall-clock latency by drafting or checking multiple short candidates in parallel.', ['The need for any verifier or selector.', 'The number of parameters in the trained model.'], 'Parallelism can improve responsiveness, although total compute and memory may still be high.'),
  q(20, 'Foundation', 'What does the cost-versus-accuracy table teach?', 'Higher-accuracy strategies often spend many more tokens, dollars, and milliseconds per query.', ['Every strategy has identical cost once deployed.', 'Greedy decoding always dominates search-based methods.'], 'The table makes the Pareto tradeoff explicit.'),

  q(21, 'Mechanism', 'Why can N=32 Best-of-N be much more expensive than N=1?', 'It generates about 32 independent completions before selection.', ['It requires 32 separate model architectures.', 'It retrains the verifier from scratch for each token.'], 'Best-of-N multiplies generation work by the number of candidates.'),
  q(22, 'Mechanism', 'Why does Best-of-N show diminishing returns?', 'Once many samples are similar or the verifier is imperfect, extra candidates add less marginal accuracy.', ['The model becomes less capable after each sample.', 'The context window shrinks when N increases.'], 'The curve rises early and then flattens because selection and sample diversity become bottlenecks.'),
  q(23, 'Mechanism', 'How does verifier quality affect effective Best-of-N accuracy?', 'Weak verifiers fail to select the best candidate, so realized accuracy falls below the oracle bound.', ['Weak verifiers always make N irrelevant by improving every sample.', 'Verifier quality affects training compute only, not inference.'], 'The selection step is the main gap between theoretical and practical Best-of-N.'),
  q(24, 'Mechanism', 'Why can GPU batching lower wall-clock time without lowering total compute?', 'Parallel samples can run together, but each still consumes generation work and memory.', ['Batching removes token generation from the computation graph.', 'Batching makes every sample share identical output tokens.'], 'Throughput and total cost are different operational quantities.'),
  q(25, 'Mechanism', 'What does beam width control in tree search?', 'How many partial paths are kept for further expansion at each depth.', ['The number of model layers activated.', 'The probability that a tool call is allowed.'], 'Wider beams explore more alternatives, increasing compute and memory.'),
  q(26, 'Mechanism', 'Why is a PRM useful for pruning?', 'It can score whether an intermediate reasoning step looks promising before the final answer exists.', ['It only reads the final boxed answer.', 'It compresses every branch into one token.'], 'Tree search needs step-level information to avoid wasting expansion on bad branches.'),
  q(27, 'Mechanism', 'What is the risk of a weak PRM in tree search?', 'It may prune the correct branch or expand convincing but wrong paths.', ['It makes the model unable to tokenize prompts.', 'It guarantees the oracle bound is reached.'], 'Search quality depends on the value signal used to guide expansion.'),
  q(28, 'Mechanism', 'What happens when a thinking budget is too small for the task?', 'Important steps can be omitted, causing truncation and premature answers.', ['The model automatically uses more hidden layers.', 'The verifier becomes perfectly calibrated.'], 'Hard problems often need enough token budget to complete the reasoning chain.'),
  q(29, 'Mechanism', 'What happens when a thinking budget is too large for an easy task?', 'The model may spend tokens on redundant checks with no accuracy benefit.', ['The answer becomes mathematically impossible.', 'The training dataset is rewritten.'], 'Easy tasks often reach the plateau quickly, so extra budget is waste.'),
  q(30, 'Mechanism', 'What does the budget-forcing curve reveal?', 'Accuracy rises with budget at first, then flattens while latency keeps increasing.', ['Latency falls linearly as budget grows.', 'All budgets produce the same cost and accuracy.'], 'The curve is used to find an inflection point on the Pareto frontier.'),
  q(31, 'Mechanism', 'Why is a difficulty classifier useful for adaptive budgets?', 'It routes easy, moderate, and hard queries to different token caps.', ['It replaces the language model decoder.', 'It guarantees every verifier is correct.'], 'Routing is the mechanism that lets adaptive policies allocate compute where it is needed.'),
  q(32, 'Mechanism', 'What failure can a bad difficulty classifier cause?', 'It can under-budget hard queries or over-budget easy ones.', ['It removes the ability to call tools.', 'It changes all model weights permanently.'], 'Misrouting recreates the fixed-cap problems adaptive budgets were meant to solve.'),
  q(33, 'Mechanism', 'How do length penalties train model-controlled budgets?', 'They make unnecessary tokens costly while correctness rewards preserve useful reasoning.', ['They force every answer to be exactly one token.', 'They make the verifier ignore final answers.'], 'Length shaping teaches efficient stopping, but the balance must preserve hard-task accuracy.'),
  q(34, 'Mechanism', 'Why can a strict SLA conflict with model-controlled stopping?', 'The model may decide to think longer than the product latency budget allows.', ['The model cannot generate text under any SLA.', 'Stopping behavior is always exactly predictable.'], 'Self-regulation is flexible, but production systems may need hard caps or routing.'),
  q(35, 'Mechanism', 'What does ReAct add to a reasoning trace?', 'A loop of thoughts, actions, observations, and updated reasoning.', ['Only a larger softmax vocabulary.', 'A hidden critic network for every token.'], 'ReAct interleaves model reasoning with external tool observations.'),
  q(36, 'Mechanism', 'Why can tools reduce internal reasoning tokens?', 'Search, Python, or calculators can supply facts and computations directly.', ['Tools make all prompts shorter before tokenization.', 'Tools eliminate the need to validate outputs.'], 'External actions can replace long guessed derivations with grounded observations.'),
  q(37, 'Mechanism', 'What cost does tool use introduce?', 'Tool-call latency, permissions, reliability checks, and possible external API cost.', ['Zero additional operational complexity.', 'A required full retraining run after every tool call.'], 'Tool use shifts compute outward rather than making it disappear.'),
  q(38, 'Mechanism', 'Why can caching help some test-time compute workloads?', 'Repeated reasoning fragments or tool results may be reused instead of recomputed.', ['Caching proves every cached answer is correct forever.', 'Caching changes the model architecture during inference.'], 'Caching can reduce repeated work, but only when requests and evidence are reusable and fresh.'),
  q(39, 'Mechanism', 'What does speculative decoding contribute to latency control?', 'A faster draft path proposes tokens or candidates that a stronger path can verify.', ['It removes all verifier requirements from Best-of-N.', 'It guarantees higher accuracy than beam search.'], 'Speculation is a latency optimization, not a proof of correctness.'),
  q(40, 'Mechanism', 'Why should budget policies be evaluated by workload mix?', 'A policy that is efficient for easy prompts may fail when hard prompts dominate.', ['Workload mix affects only page styling.', 'All prompt distributions have the same optimal budget.'], 'Average cost and accuracy depend on the distribution of difficulty classes.'),
  q(41, 'Mechanism', 'What does a Pareto frontier show in this lesson?', 'Strategies that trade cost and latency for accuracy without being clearly dominated.', ['Only the route order in the app catalog.', 'The hidden size of every model layer.'], 'The frontier helps choose the cheapest method that reaches the required quality.'),
  q(42, 'Mechanism', 'Why can a high average accuracy hide a latency problem?', 'A few long-budget requests can violate tail latency even if mean accuracy is good.', ['Latency metrics are unrelated to serving systems.', 'Accuracy automatically reduces P99 latency.'], 'Production systems care about P50, P95, and P99 behavior, not only average correctness.'),
  q(43, 'Mechanism', 'What is the difference between serial refinement and parallel Best-of-N?', 'Serial refinement revises one path across rounds; Best-of-N samples many independent paths.', ['Serial refinement never reads prior drafts.', 'Best-of-N modifies the model weights during selection.'], 'Both spend inference compute, but their dependency structure and latency profile differ.'),
  q(44, 'Mechanism', 'Why can sequential self-critique loop?', 'The model can keep finding or inventing revisions without a reliable stopping condition.', ['The context window always expands automatically.', 'The verifier always blocks repeated reasoning.'], 'Refinement needs stopping criteria or it can waste tokens.'),
  q(45, 'Mechanism', 'What makes tool-augmented compute different from longer chain-of-thought?', 'It obtains external observations instead of relying only on internal token reasoning.', ['It disables the model after the first token.', 'It is the same as fixed-cap decoding.'], 'Tools change the source of evidence and computation, not just the number of thoughts.'),
  q(46, 'Mechanism', 'What is the main operational appeal of adaptive budgets?', 'They can lower average cost on mixed workloads while preserving budget for hard cases.', ['They always maximize the token cap.', 'They remove the need for monitoring.'], 'The policy saves on easy requests and spends on hard ones.'),
  q(47, 'Mechanism', 'Why can hidden tests matter for verifier-based selection?', 'Visible checks can be overfit, so hidden evaluations expose whether the selected answer generalizes.', ['Hidden tests make all candidates identical.', 'Hidden tests are only needed during pretraining.'], 'Verifier coverage determines whether test-time selection rewards robust solutions or shortcuts.'),
  q(48, 'Mechanism', 'What does answer selection depend on in Best-of-N?', 'Candidate diversity, verifier quality, and the actual chance that at least one sample is correct.', ['Only the alphabetical order of answers.', 'Only the number of tokens in the prompt.'], 'Sampling more helps only if useful candidates exist and the selector can identify them.'),
  q(49, 'Mechanism', 'Why is context-window size still a constraint?', 'Long reasoning traces, tool observations, and search branches must fit into available context.', ['Context windows are used only during pretraining.', 'More test-time compute removes token limits.'], 'Inference-time strategies still operate within serving and context limits.'),
  q(50, 'Mechanism', 'What should a budget sweep measure besides accuracy?', 'Latency, token count, cost per query, truncation rate, and failure types.', ['Only the active UI tab name.', 'Only the largest possible budget result.'], 'The lesson treats budgets as an engineering tradeoff, so multiple metrics matter.'),

  q(51, 'Application', 'A trivial arithmetic query gets a 4096-token trace. What is the likely failure?', 'Overthinking waste from a budget that is too large for the query difficulty.', ['Hard budget truncation from too few tokens.', 'A successful adaptive router minimizing cost.'], 'The task is easy, so the excess trace raises cost without adding value.'),
  q(52, 'Application', 'A proof answer stops mid-argument because the cap is 128 tokens. What should you diagnose?', 'Hard budget truncation on a task that needs more reasoning steps.', ['Verifier over-selection from too many candidates.', 'Speculative decoding success.'], 'The chain was cut before completion, so the budget is too low.'),
  q(53, 'Application', 'Best-of-N-64 raises cost a lot but accuracy barely moves past N=32. What should you do next?', 'Inspect verifier quality, sample diversity, and whether the curve has reached diminishing returns.', ['Increase N forever because cost does not matter.', 'Conclude greedy decoding is always better.'], 'The plateau suggests the bottleneck may no longer be sample count.'),
  q(54, 'Application', 'A verifier picks polished wrong solutions over terse correct ones. What should you suspect?', 'A verifier-reasoning gap or reward proxy that overvalues style.', ['Perfect oracle selection.', 'A fixed-cap policy working as intended.'], 'Selection quality can fail when the verifier rewards the wrong signals.'),
  q(55, 'Application', 'A coding assistant generates 16 solutions and runs tests on each. Which strategy is this?', 'Best-of-N with a code verifier selecting among sampled candidates.', ['Training-time scaling of model weights.', 'A fixed-cap single greedy decode.'], 'The model samples multiple attempts, then uses execution feedback to pick.'),
  q(56, 'Application', 'A math solver expands partial proofs and prunes low-scoring branches. Which strategy fits?', 'PRM-guided tree or beam search.', ['Pure greedy decoding with no scoring.', 'Training data deduplication.'], 'The key behavior is scoring intermediate paths before final answers.'),
  q(57, 'Application', 'A support bot handles password resets and complex billing disputes. Which budget policy is sensible?', 'Route simple requests to short budgets and complex disputes to longer budgets.', ['Use the maximum budget for every request.', 'Use one tiny budget for every request.'], 'Mixed difficulty is exactly where adaptive routing can save cost.'),
  q(58, 'Application', 'A model needs the current population of a city. What is the safer compute move?', 'Call a search tool and cite or use the observation instead of guessing from memory.', ['Spend thousands of tokens inventing a number.', 'Ignore freshness because reasoning length is enough.'], 'Tool-augmented compute reduces uncertainty when the missing information is external and current.'),
  q(59, 'Application', 'A financial calculation requires exact arithmetic. What tool-augmented choice fits?', 'Use a calculator or code interpreter rather than doing long mental arithmetic in tokens.', ['Increase temperature to get more diverse guesses.', 'Disable all external validation.'], 'Deterministic tools are strong for precise computation.'),
  q(60, 'Application', 'A product has a strict two-second P99 latency SLA. What budget design should you avoid?', 'Unbounded model-controlled thinking with no cap or fallback.', ['A measured fast path for easy requests.', 'A router that sends rare hard cases to a slower tier.'], 'Strict latency needs predictable limits and fallback behavior.'),
  q(61, 'Application', 'A budget sweep shows accuracy improves up to 2048 tokens and then changes by less than one point. What is a reasonable cap?', 'Choose near the inflection point unless the product can justify the extra latency.', ['Always choose 8192 tokens because it is largest.', 'Always choose 64 tokens because it is cheapest.'], 'Pareto selection should stop where extra cost buys little value.'),
  q(62, 'Application', 'A hidden benchmark falls while verifier score rises. What should you investigate?', 'Verifier overfitting, proxy hacking, and whether selected traces truly solve the task.', ['Only the CSS bundle size.', 'Whether all answers use the same option letter.'], 'This is a classic sign that optimized reward and real quality diverged.'),
  q(63, 'Application', 'A ReAct agent makes too many search calls for easy facts already in context. What should you tune?', 'Tool-use gating and stopping rules so calls are made only when they reduce uncertainty enough.', ['Force a tool call before every token.', 'Remove all context from the prompt.'], 'Tools have latency, so the policy should call them when expected benefit exceeds cost.'),
  q(64, 'Application', 'A branch scored low early but later would have solved the proof. What search risk appeared?', 'The PRM pruned a good branch too aggressively.', ['The oracle bound was reached.', 'The fixed cap was too large for a trivial query.'], 'Beam search can fail when intermediate scoring is wrong.'),
  q(65, 'Application', 'A workload is 90% easy queries and 10% hard queries. Why might adaptive budgets beat a fixed max cap?', 'Most requests can use cheap short traces while hard requests still receive enough compute.', ['The model will never need a verifier.', 'The easy queries require the longest traces.'], 'The average cost drops when most traffic avoids the maximum budget.'),
  q(66, 'Application', 'A workload is almost entirely hard contest math. What should you reconsider?', 'Whether a short default budget or easy-query router is appropriate for this distribution.', ['Whether all prompts should be answered greedily with no checks.', 'Whether training-time compute is illegal.'], 'Budget policy should match the actual difficulty mix.'),
  q(67, 'Application', 'A user asks for a fast answer and accepts lower confidence. What service design fits?', 'Offer a fast path with a small budget and optional escalation to deeper reasoning.', ['Always run the slowest beam search.', 'Hide latency tradeoffs from all product decisions.'], 'Tiered serving lets users or products choose a point on the frontier.'),
  q(68, 'Application', 'A legal or medical answer has high stakes. How should test-time compute be treated?', 'More compute may help, but external verification, citations, and safety review remain necessary.', ['Long thinking alone guarantees correctness.', 'A single greedy answer is always enough.'], 'High stakes require evidence and safeguards beyond token count.'),
  q(69, 'Application', 'A model repeats self-checks without changing the answer. What monitoring signal should fire?', 'Token count far above query complexity with flat accuracy or no new evidence.', ['Low total parameter count only.', 'A missing route metadata field.'], 'Overthinking is visible as excessive trace length without useful progress.'),
  q(70, 'Application', 'A system routes hard prompts to tools but tool failures are common. What should production include?', 'Retries, fallbacks, permission checks, and uncertainty reporting.', ['Assume every tool observation is true.', 'Disable all monitoring because tools are deterministic.'], 'Tool-augmented systems need operational reliability controls.'),
  q(71, 'Application', 'A team compares greedy, BoN-16, long thinking, and PRM beam. What should the decision use?', 'Accuracy, latency, cost, failure modes, and the product quality target.', ['Only whichever model name sounds newest.', 'Only average token count with no accuracy check.'], 'The metrics comparison is useful only when tied to requirements.'),
  q(72, 'Application', 'A model answers an easy FAQ with a long hidden trace. What training signal could reduce this?', 'A length penalty that rewards concise correct answers.', ['A reward that pays for every generated token.', 'A verifier that ignores final correctness.'], 'Model-controlled efficiency comes from rewarding correct answers while penalizing unnecessary length.'),
  q(73, 'Application', 'A hard problem needs 1200 tokens but the router labels it easy. What should be fixed?', 'The difficulty classifier or escalation rule.', ['The answer option distribution.', 'The number of static HTML pages.'], 'The adaptive policy failed at the routing step.'),
  q(74, 'Application', 'A code task has flaky tests. What happens to Best-of-N selection?', 'The selector may choose unstable candidates because the verifier signal is noisy.', ['The oracle bound becomes guaranteed.', 'Sample cost becomes constant.'], 'Verifier reliability directly affects which candidate is selected.'),
  q(75, 'Application', 'A generated trace fills context with tool observations and cannot finish. What constraint is binding?', 'The context-window and budget limits still constrain tool-augmented reasoning.', ['Training-time scaling has permanently solved the task.', 'The model has no inference cost.'], 'Tool observations consume context just like generated tokens do.'),

  q(76, 'Tricky', 'Which claim is false about more thinking tokens?', 'More thinking tokens always help, regardless of task difficulty or plateau behavior.', ['More tokens can help hard tasks up to a point.', 'Extra tokens can waste cost after accuracy flattens.'], 'The lesson misconception is that more thinking is automatically better; the plateau disproves that.'),
  q(77, 'Tricky', 'Which Best-of-N claim is wrong?', 'Best-of-N is free if candidates are generated in parallel.', ['Parallelism can reduce wall-clock latency.', 'Total compute still grows with candidate count.'], 'Batching does not erase the work of generating and checking multiple samples.'),
  q(78, 'Tricky', 'Which oracle-bound claim should you reject?', 'The oracle bound is always achieved when N is large enough.', ['A perfect selector would be needed to reach the bound.', 'Weak verifiers leave realized accuracy below the bound.'], 'More candidates do not guarantee perfect selection.'),
  q(79, 'Tricky', 'Which beam-search claim is unsafe?', 'A PRM-guided beam can never prune the correct branch.', ['Intermediate scoring can help allocate search.', 'Bad process scores can misguide pruning.'], 'Search quality depends on the process scorer.'),
  q(80, 'Tricky', 'Which adaptive-budget claim is false?', 'Adaptive routing always beats fixed caps on every workload.', ['Adaptive policies help most when difficulty varies.', 'A bad router can over- or under-budget requests.'], 'No routing policy is universally best without workload evidence.'),
  q(81, 'Tricky', 'Which tool-use claim is a trap?', 'Tool calls make answers automatically true and safe.', ['Tool observations still need relevance and reliability checks.', 'Tools trade internal tokens for external latency and permissions.'], 'External tools can fail, be stale, or be misused, so validation remains necessary.'),
  q(82, 'Tricky', 'Which length-penalty claim is wrong?', 'Stronger length penalties always improve hard reasoning.', ['Length penalties can reduce overthinking.', 'Excessive penalties can truncate necessary steps.'], 'Length shaping has a balance point; too much pressure harms hard tasks.'),
  q(83, 'Tricky', 'Which verifier-gap claim is false?', 'High verifier score proves the selected trace truly solved the intended task.', ['Verifier proxies can be exploited.', 'Hidden tests and process checks can reveal gaps.'], 'Verifier success is evidence, not proof, when the checker is incomplete.'),
  q(84, 'Tricky', 'Which latency claim should you reject?', 'Average accuracy is enough to approve a test-time compute strategy.', ['Tail latency and cost can block deployment.', 'P99 behavior matters for user-facing systems.'], 'A strategy can be accurate but operationally unusable.'),
  q(85, 'Tricky', 'Which fixed-cap claim is dangerous?', 'One fixed cap is optimal for all easy and hard prompts.', ['A fixed cap is simple and predictable.', 'Fixed caps can waste easy-query tokens or truncate hard-query reasoning.'], 'The fixed-cap failure comes from heterogeneous difficulty.'),
  q(86, 'Tricky', 'Which speculation claim is false?', 'Speculative decoding guarantees better reasoning accuracy than deeper search.', ['Speculation can reduce latency.', 'A verifier or stronger model may still need to check drafts.'], 'Speculation is primarily a speed technique.'),
  q(87, 'Tricky', 'Which serial-refinement claim is wrong?', 'Self-critique always converges to the correct answer if given enough rounds.', ['Refinement can correct some mistakes.', 'It can also loop or hallucinate revisions.'], 'More revision rounds can amplify errors without a reliable signal.'),
  q(88, 'Tricky', 'Which cost claim is false?', 'Only output accuracy matters because inference cost is negligible at scale.', ['Serving cost can dominate high-budget reasoning systems.', 'Latency and dollars per query belong in the evaluation.'], 'The lesson treats inference compute as a real product and infrastructure cost.'),
  q(89, 'Tricky', 'Which context claim should be rejected?', 'Longer test-time reasoning removes context-window constraints.', ['Long traces and observations consume context.', 'Budget extension must still fit serving limits.'], 'Test-time compute operates inside finite context and memory constraints.'),
  q(90, 'Tricky', 'Which production-readiness claim is a trap?', 'A single budget sweep on one benchmark proves the policy is ready for deployment.', ['Workload mix and hidden failure modes should be tested.', 'Budget policies need monitoring after launch.'], 'One curve is useful evidence, but production readiness needs broader validation.'),

  q(91, 'Interview', 'How would you summarize test-time compute in an interview?', 'It spends inference-time work such as samples, search, tools, or thinking tokens to improve answers without changing weights.', ['It is only pretraining with a different name.', 'It removes all serving cost from reasoning models.'], 'A concise answer should name per-query compute and the cost-accuracy tradeoff.'),
  q(92, 'Interview', 'How would you compare Best-of-N and beam search?', 'Best-of-N samples complete answers then selects; beam search scores partial paths and expands promising branches.', ['Both are identical fixed-cap greedy decoding.', 'Beam search never needs a value or process signal.'], 'The comparison hinges on when candidates are scored and how compute is allocated.'),
  q(93, 'Interview', 'How would you choose a thinking budget for a product?', 'Run budget sweeps by difficulty class, choose a Pareto point, and enforce latency and cost constraints.', ['Pick the largest context window by default.', 'Choose the budget from a single impressive example.'], 'A strong answer uses empirical curves and product requirements.'),
  q(94, 'Interview', 'What would you monitor after launching adaptive budgets?', 'Difficulty routing errors, token spend, latency percentiles, accuracy by class, truncation, and overthinking.', ['Only total signup count.', 'Only whether the route loads in the browser.'], 'Adaptive systems need both quality and operations monitoring.'),
  q(95, 'Interview', 'How would you debug a verifier-reasoning gap?', 'Inspect winning traces, compare against hidden checks, test adversarial cases, and improve the verifier or process scoring.', ['Trust the highest verifier score without review.', 'Increase N until every issue disappears.'], 'The fix starts by finding how the proxy diverges from intended correctness.'),
  q(96, 'Interview', 'When would you use tools instead of more tokens?', 'When missing facts, exact computation, or external state can be obtained more reliably through a tool.', ['Whenever a tool exists, regardless of latency or permissions.', 'Never, because internal reasoning is always fresher.'], 'Tool use is justified when the expected benefit exceeds latency, cost, and safety risks.'),
  q(97, 'Interview', 'What is an interview-ready answer about overthinking?', 'Extra tokens help until the task is solved or search saturates; after that they increase cost and may loop.', ['Overthinking is impossible in reasoning models.', 'Longer traces are always evidence of better reasoning.'], 'The answer should separate useful deliberation from waste.'),
  q(98, 'Interview', 'How would you defend a tiered reasoning service?', 'Use fast default budgets, escalate hard or high-stakes cases, and measure the cost-quality frontier.', ['Send every query through the slowest path.', 'Never expose latency-quality tradeoffs.'], 'Tiering is a practical way to align compute with user needs.'),
  q(99, 'Interview', 'What tests make a budget policy production-ready?', 'Per-class accuracy, hidden evals, latency percentiles, cost caps, verifier-gap probes, and fallback behavior.', ['Only one happy-path prompt.', 'Only a visual screenshot of the slider.'], 'Production readiness requires behavioral and operational coverage.'),
  q(100, 'Interview', 'What final answer best captures the lesson?', 'Test-time compute is a controllable inference budget that must be allocated, verified, and monitored against accuracy, cost, and latency.', ['More tokens are always the safest answer.', 'Training-time compute and test-time compute have identical economics.'], 'The lesson closes on disciplined budget allocation rather than blind extra thinking.'),
]);

export const TEST_TIME_COMPUTE_THINKING_BUDGETS_QUIZ = Object.freeze(SPECS);
