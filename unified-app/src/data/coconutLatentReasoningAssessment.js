function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) {
    hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  }
  return hash.toString(16).padStart(8, '0');
}

function q(number, level, prompt, correct, distractorA, distractorB, explanation) {
  const id = `coconut-${String(number).padStart(3, '0')}`;
  const choices = [correct, distractorA, distractorB];
  const registryRotation = Number.parseInt(stableHash(`coconut-latent-reasoning:${id}`), 16) % choices.length;
  const targetAnswerIndex = ((number - 1) + registryRotation) % choices.length;
  const balancedChoices = [null, null, null];
  balancedChoices[targetAnswerIndex] = correct;
  const distractors = [distractorA, distractorB];
  let distractorIndex = 0;
  for (let index = 0; index < balancedChoices.length; index += 1) {
    if (balancedChoices[index] === null) {
      balancedChoices[index] = distractors[distractorIndex];
      distractorIndex += 1;
    }
  }

  return Object.freeze({
    id,
    level,
    prompt,
    choices: Object.freeze(balancedChoices),
    answerIndex: targetAnswerIndex,
    explanation,
    questionHash: stableHash(`${prompt}\n${balancedChoices.join('\n')}`),
  });
}

const COCONUT_SPECS = Object.freeze([
  q(1, 'Foundation', 'What is the core idea behind Coconut latent reasoning?', 'Use hidden-state vectors as intermediate reasoning steps before emitting text.', 'Force the model to write every intermediate thought as visible words.', 'Replace transformer layers with a retrieval-only search index.', 'Coconut separates reasoning from explanation by letting some intermediate steps live in continuous hidden-state space.'),
  q(2, 'Foundation', 'How does visible chain-of-thought differ from Coconut in the lesson?', 'CoT writes reasoning as text tokens, while Coconut can reason through hidden-state feedback.', 'CoT bypasses the LM head, while Coconut always writes natural language.', 'CoT uses no compute, while Coconut uses only cached answers.', 'The lesson contrasts spoken scratchpad text with continuous thoughts between tokens.'),
  q(3, 'Foundation', 'During ordinary language mode, what path does a hidden state follow before the next input?', 'It goes through the LM head, becomes a sampled token, and is embedded for the next position.', 'It is copied directly into the next input without token decoding.', 'It is discarded and replaced with a fixed pause vector.', 'Language mode is the normal autoregressive path from hidden state to token to embedding.'),
  q(4, 'Foundation', 'During Coconut latent mode, what becomes the next input embedding?', 'The previous position last hidden state is fed back directly.', 'Only the final answer token is recycled.', 'A static vector shared by every prompt is inserted.', 'Latent mode bypasses token decoding and feeds h_t forward as x_t+1.'),
  q(5, 'Foundation', 'What does Coconut skip when it is in latent mode?', 'Decoding the hidden state through the LM head into a word token.', 'Running the transformer stack on the next position.', 'Scoring future text targets during training.', 'The transformer still runs, but the hidden state is not first converted into a visible token.'),
  q(6, 'Foundation', 'What do <bot> and <eot> mark in the Coconut lesson?', 'The beginning and end of the latent reasoning region.', 'The start and stop of external retrieval calls.', 'The boundary between GPU and CPU execution.', 'Special markers delimit the span where hidden-state thoughts are inserted.'),
  q(7, 'Foundation', 'Why is a continuous thought different from a pause token?', 'A continuous thought is context-dependent hidden state feedback, while a pause token uses a learned token embedding.', 'A continuous thought is always a visible English word.', 'A pause token bypasses the transformer but Coconut does not.', 'Pause tokens spend extra positions with fixed learned embeddings; Coconut reuses the current hidden state.'),
  q(8, 'Foundation', 'What does the lesson mean by reasoning between tokens?', 'The model can spend hidden-state steps before committing to visible output tokens.', 'The model removes all intermediate computation from inference.', 'The model trains only on final answer labels with no transformer states.', 'Coconut can place useful computation in latent positions instead of natural language positions.'),
  q(9, 'Foundation', 'Why does Coconut still cost computation at inference time?', 'Each latent thought is another forward computation step even if it is not visible text.', 'Latent thoughts are free because no characters are printed.', 'Only visible text tokens require transformer computation.', 'The lesson separates visible token count from total compute steps.'),
  q(10, 'Foundation', 'What is the safe way to compare visible tokens and compute steps for Coconut?', 'Count fewer visible text tokens but include latent feedback passes in total compute.', 'Count latent thoughts as zero cost because users cannot read them.', 'Count only final answer tokens and ignore reasoning mode.', 'A latent step can reduce displayed text while still consuming model work.'),
  q(11, 'Foundation', 'What is the purpose of the Coconut curriculum?', 'Gradually replace early text reasoning steps with latent thoughts so hidden states learn to support later text.', 'Hide all chain-of-thought data from the model forever.', 'Train only on question tokens and never on answers.', 'The curriculum starts from text reasoning and moves a growing prefix into latent space.'),
  q(12, 'Foundation', 'In the curriculum view, what happens as the training stage increases?', 'More early reasoning steps become latent positions before the remaining text and answer.', 'More answer tokens are removed from the loss until nothing is trained.', 'The model switches from transformer attention to a convolutional decoder.', 'The staged schedule teaches latent states to carry information that future text depends on.'),
  q(13, 'Foundation', 'Where is training loss applied in the Coconut curriculum?', 'To remaining future text targets after the latent positions.', 'Directly to latent vectors as if each vector had a word label.', 'Only to the question tokens before reasoning begins.', 'Question and latent positions are masked; the objective tests whether latent states help predict later text.'),
  q(14, 'Foundation', 'Why are latent positions masked in the lesson description?', 'They are optimized through their usefulness for later predictions, not by matching a visible token at that position.', 'They are ignored forever and cannot influence answers.', 'They are supervised by a nearest-token probe label.', 'A continuous thought has no direct natural-language target token.'),
  q(15, 'Foundation', 'What does delayed commitment mean for Coconut?', 'Several branch possibilities can remain softly active before one answer path becomes dominant.', 'The model waits to load weights until after the final token.', 'The answer is always postponed until a human writes a proof.', 'The lesson visualizes branch entropy falling as latent steps sharpen the path distribution.'),
  q(16, 'Foundation', 'What does high early branch entropy suggest in the planning view?', 'The model has not yet committed strongly to one branch.', 'The answer has already been written in visible text.', 'The latent state is guaranteed to be interpretable.', 'Entropy measures how spread out the branch distribution is.'),
  q(17, 'Foundation', 'What does low final branch entropy suggest after latent steps?', 'The model has become more committed to one branch path.', 'The model has stopped using transformer computation.', 'The task has no possible distractors.', 'The lesson uses entropy drop as an intuition for delayed commitment.'),
  q(18, 'Foundation', 'Which task type best illustrates the delayed-commitment intuition?', 'Planning with dead ends, where early branch choices can trap the trace.', 'A static image compression task with no reasoning path.', 'A prompt that is already answered by one memorized token.', 'Planning tasks make branch distributions and late commitment easy to visualize.'),
  q(19, 'Foundation', 'What is the main faithfulness caveat for latent thoughts?', 'A useful latent vector may still be opaque, shortcut-driven, or not a faithful reasoning step.', 'Every latent vector automatically decodes to a clear proof sentence.', 'Latent reasoning makes shortcut behavior impossible.', 'The lesson warns that performance and faithful causal reasoning are different claims.'),
  q(20, 'Foundation', 'Which basic intervention can test whether a latent state matters causally?', 'Perturb or remove the latent state and check whether distributions or answers change.', 'Count the characters in the final answer only.', 'Assume the nearest-token probe is always conclusive.', 'Causal interventions are stronger evidence than passive inspection alone.'),

  q(21, 'Mechanism', 'What equation summarizes Coconut latent mode in the lesson?', 'x_t+1 = h_t, so the hidden state becomes the next input.', 'x_t+1 = Embed(sample(LMHead(h_t))), always and without exception.', 'x_t+1 = retrieval_chunk_id, so no transformer state is used.', 'The lesson explicitly contrasts latent feedback with normal LM-head decoding.'),
  q(22, 'Mechanism', 'What equation summarizes ordinary language mode in the lesson?', 'x_t+1 = Embed(sample(LMHead(h_t))).', 'x_t+1 = h_t for every position including visible words.', 'x_t+1 = zero_vector after each reasoning step.', 'Language mode turns hidden states into token choices before feeding the next position.'),
  q(23, 'Mechanism', 'Why can Coconut reduce visible text without reducing all computation?', 'Latent steps replace displayed reasoning tokens with hidden-state positions that still run forward passes.', 'Latent steps are cache hits that never use the model.', 'The final answer is produced without any decoder layers.', 'Visible token count and compute-step count are separate metrics in the panel.'),
  q(24, 'Mechanism', 'How does the pause-token baseline spend extra compute?', 'It inserts learned filler token embeddings at extra positions.', 'It feeds back context-dependent hidden states like Coconut.', 'It removes all intermediate transformer calls.', 'Pause tokens add positions but do not use the current hidden state as the next embedding.'),
  q(25, 'Mechanism', 'Why is pause-token behavior not the same as continuous thought?', 'A pause embedding is fixed by token identity, while a continuous thought changes with the current context.', 'Pause tokens always reveal a full proof to the user.', 'Pause tokens train only with reinforcement learning rewards.', 'The distinction is context-dependent state feedback versus a learned token embedding.'),
  q(26, 'Mechanism', 'What does the timeline show for Coconut mode?', 'Question, <bot>, one or more hidden-state steps, <eot>, then answer.', 'Question, every hidden state decoded as a visible proof, then no answer.', 'Only a final answer token with no intermediate positions.', 'The markers surround the latent reasoning region before text generation resumes.'),
  q(27, 'Mechanism', 'What does the timeline show for No-CoT mode?', 'Question followed directly by answer.', 'Question, <bot>, eight latent vectors, and <eot>.', 'Question, a long visible proof, and then no answer.', 'No-CoT is the direct-answer baseline in the controls.'),
  q(28, 'Mechanism', 'What does the timeline show for visible CoT mode?', 'Question followed by visible reasoning tokens and then answer.', 'Question followed only by hidden vectors.', 'Only fixed pause tokens with no words.', 'Visible CoT communicates intermediate reasoning as text.'),
  q(29, 'Mechanism', 'What does the training-stage slider control?', 'How many early text reasoning steps are replaced by latent positions.', 'How many source links are displayed.', 'How many answer choices the quiz shows.', 'The curriculum stage determines the latent prefix length in the training view.'),
  q(30, 'Mechanism', 'At a later curriculum stage, why can the first text target still receive useful training signal?', 'It is predicted from prior question and latent states that should carry the missing reasoning information.', 'It is copied from a nearest-token probe of the latent vector.', 'It is ignored because all loss is masked after <bot>.', 'Latent states are trained by their effect on future text prediction.'),
  q(31, 'Mechanism', 'What does branch entropy measure in the planning panel?', 'How spread out the active path probabilities are across candidate branches.', 'How many visible characters the answer contains.', 'How much GPU memory the page uses.', 'Entropy is high when several branches remain plausible and low when one dominates.'),
  q(32, 'Mechanism', 'What is a commitment step in the Coconut panel?', 'The first step where one branch probability crosses the chosen dominance threshold.', 'The first source link opened by the learner.', 'The first token that is alphabetically sorted.', 'Commitment step operationalizes when the soft branch distribution becomes decisive.'),
  q(33, 'Mechanism', 'How does the planning task differ from short arithmetic in the lesson?', 'Wrong early branches can lead to dead ends, so delaying commitment is more relevant.', 'It has no branch structure and no distractors.', 'It never benefits from any reasoning steps.', 'The task presets make planning more sensitive to branch choice.'),
  q(34, 'Mechanism', 'What does the nearest-token probe show?', 'A readable token-like interpretation of a hidden state, useful but not definitive.', 'A guaranteed proof that the vector is a faithful reasoning sentence.', 'The exact training loss for every latent dimension.', 'Probes can help inspect vectors but do not prove causal reasoning.'),
  q(35, 'Mechanism', 'What does the vector-norm view show?', 'A toy scalar summary of hidden-state magnitude across the latent path.', 'A complete explanation of every latent feature.', 'The number of source links in the lesson.', 'Vector norms are diagnostics, not interpretations by themselves.'),
  q(36, 'Mechanism', 'What does the path-distribution view show?', 'The probabilities assigned to candidate branches such as lempus, sterpus, and grimpus.', 'The exact words hidden inside each vector.', 'A proof that shortcut datasets are safe.', 'The branch distribution is the lesson vehicle for delayed commitment.'),
  q(37, 'Mechanism', 'What does the remove-latent faithfulness test do?', 'Ablates the continuous thought region and observes output or distribution changes.', 'Adds more visible CoT tokens after the answer.', 'Replaces the transformer with a retrieval index.', 'Removing latent states tests whether they were carrying causal information.'),
  q(38, 'Mechanism', 'What does the perturb-latent faithfulness test do?', 'Adds noise to a hidden state and checks sensitivity.', 'Counts only final-answer punctuation.', 'Makes every latent step visible as a word.', 'Perturbation probes whether small vector changes affect reasoning behavior.'),
  q(39, 'Mechanism', 'What does the shortcut-dataset test ask?', 'Whether the apparent reasoning still works when shortcut cues are removed or shifted.', 'Whether the output string contains fewer than ten characters.', 'Whether the latent vector has the largest norm.', 'Shortcut tests address the risk that latent states exploit spurious cues.'),
  q(40, 'Mechanism', 'Why are interventions stronger than nearest-token probes?', 'They test causal dependence by changing the latent state and measuring behavior.', 'They always translate every vector into a perfect sentence.', 'They remove the need for evaluation datasets.', 'A probe is correlational; an ablation or perturbation can test necessity.'),
  q(41, 'Mechanism', 'What does perturbation sensitivity summarize?', 'How much behavior changes under latent-state interventions.', 'How many prompt tokens are visible.', 'How many answer choices are shuffled.', 'Sensitivity is a faithfulness readout in the panel.'),
  q(42, 'Mechanism', 'Why can a latent vector be useful yet hard to inspect?', 'It is a continuous high-dimensional state, not a discrete explanation token.', 'It is always a one-hot word id.', 'It is never processed by the transformer.', 'Continuous hidden states can carry information without exposing readable steps.'),
  q(43, 'Mechanism', 'What does the lesson mean by reasoning and explanation are not the same operation?', 'The model may search in latent space and communicate only the final result in text.', 'All reasoning must be visible text to exist.', 'Explanation tokens are used as model weights.', 'Coconut separates internal computation from user-facing language.'),
  q(44, 'Mechanism', 'Why can fewer visible tokens be appealing?', 'They may reduce displayed verbosity while preserving some intermediate computation.', 'They prove the model used less total compute in every case.', 'They guarantee the answer is more faithful.', 'The lesson frames visible-token reduction as separate from total compute and quality.'),
  q(45, 'Mechanism', 'What makes planning-heavy tasks a good Coconut demo?', 'They benefit from keeping multiple branches alive before selecting one.', 'They never need uncertainty tracking.', 'They always have a single obvious first token.', 'Delayed commitment is easiest to see when early hard choices can be wrong.'),
  q(46, 'Mechanism', 'What should happen if a reasoning-critical latent state is removed?', 'Branch distributions or answers should change noticeably.', 'Nothing should change if the state was important.', 'Only the CSS theme should change.', 'Ablation impact is evidence that the state mattered.'),
  q(47, 'Mechanism', 'What should happen if a noncritical latent state is perturbed mildly?', 'Behavior may remain similar, suggesting that state was not carrying decisive information.', 'The model must always switch to a different answer.', 'The nearest-token probe must become perfect.', 'Intervention results need to distinguish critical from noncritical states.'),
  q(48, 'Mechanism', 'What is the role of the answer target after <eot>?', 'It resumes visible text generation after the latent region.', 'It marks the start of a retrieval database.', 'It masks every future token from training.', 'Coconut still emits normal text after the latent computation region.'),
  q(49, 'Mechanism', 'How does Coconut relate to transformer token generation prerequisites?', 'It changes what is fed into the next position during selected reasoning steps.', 'It removes autoregressive sequence structure entirely.', 'It uses only convolutional recurrence after the prompt.', 'The method is a modification of the input-feedback loop inside generation.'),
  q(50, 'Mechanism', 'What does the mini-coconut lab reinforce mechanically?', 'Mode switching, latent feedback, masked loss, curriculum, entropy, perturbation, budgets, and probes.', 'Only styling changes for the lesson page.', 'Only retrieval chunk scoring with no hidden states.', 'The exercises mirror the lesson panels with small implementable functions.'),

  q(51, 'Application', 'A model answers directly with no intermediate steps and fails a planning task. Which mode should you compare first?', 'Visible CoT or Coconut, because both add intermediate computation before the answer.', 'A retrieval-only mode that never updates hidden states.', 'A shorter final answer with no extra reasoning.', 'Planning failures often motivate comparing direct answers against reasoning modes.'),
  q(52, 'Application', 'A Coconut run uses four latent steps and only one visible answer sentence. How should compute be reported?', 'Report the visible text reduction but include the four latent forward passes in compute steps.', 'Report zero reasoning cost because no scratchpad was printed.', 'Count only characters in the final sentence.', 'Latent reasoning is hidden from the user, not free to compute.'),
  q(53, 'Application', 'A pause-token baseline improves slightly but less than Coconut on planning. What likely explains the difference?', 'Coconut uses context-dependent hidden-state feedback, while pause tokens use fixed learned embeddings.', 'Pause tokens always bypass the transformer stack.', 'Coconut prints more visible words than CoT.', 'The lesson contrasts fixed filler positions with dynamic continuous thoughts.'),
  q(54, 'Application', 'A curriculum jumps directly from full text CoT to all-latent reasoning and training becomes unstable. What should you try?', 'Use a staged replacement schedule that moves early reasoning steps into latent space gradually.', 'Mask every answer token permanently.', 'Remove all CoT data before training begins.', 'The curriculum exists because learning useful latent thoughts directly is difficult.'),
  q(55, 'Application', 'A latent model performs well on in-distribution shortcut-heavy data but fails when shortcuts are removed. What is the concern?', 'The latent state may encode shortcut behavior rather than robust reasoning.', 'The LM head was never bypassed in latent mode.', 'The model used too many visible explanations.', 'Faithfulness and robustness require shortcut-resistant tests.'),
  q(56, 'Application', 'A nearest-token probe reads a latent state as a plausible branch name. What conclusion is safe?', 'The probe offers suggestive evidence, but interventions are needed before claiming faithful reasoning.', 'The latent vector is guaranteed to be a human-readable proof.', 'The model did not use hidden states at all.', 'Probes are useful but not decisive.'),
  q(57, 'Application', 'You remove the latent region and the answer accuracy drops sharply. What does that support?', 'The latent region was likely carrying behaviorally important information.', 'The latent states were irrelevant to the answer.', 'Visible token count is the only metric that matters.', 'Ablation impact is causal evidence that the region matters.'),
  q(58, 'Application', 'You perturb latent states and branch distributions barely change. What should you consider?', 'Those latent states may be redundant, noncritical, or too mildly perturbed for the task.', 'The perturbation proves all latent reasoning is faithful.', 'The final answer must be wrong by definition.', 'Low sensitivity needs careful interpretation rather than a blanket conclusion.'),
  q(59, 'Application', 'A Coconut model emits a correct answer but no readable reasoning. What user-facing caveat matters?', 'The answer may be useful, but the latent path is not an interpretable explanation.', 'The model must have written the proof internally in English.', 'The visible answer proves every hidden state was faithful.', 'Coconut separates solving from explaining.'),
  q(60, 'Application', 'A product requires auditable step-by-step reasoning. What is a Coconut deployment risk?', 'Hidden-state reasoning may reduce interpretability unless paired with probes, interventions, or visible rationales.', 'It always prints too many scratchpad tokens.', 'It cannot answer planning tasks at all.', 'Opaque latent states can be a poor fit when auditability is mandatory.'),
  q(61, 'Application', 'A planning task has high early entropy and low final entropy after latent steps. How would you describe the behavior?', 'The model delayed hard branch commitment and then focused on one path.', 'The model never considered alternatives.', 'The model skipped every compute step.', 'Entropy drop is the lesson metric for delayed commitment.'),
  q(62, 'Application', 'A task has no branch structure and is solved by a one-step arithmetic rule. What should you expect?', 'Coconut may offer less advantage than on planning-heavy tasks with dead ends.', 'Coconut must always outperform CoT by a fixed amount.', 'Latent states become directly labeled answer tokens.', 'The lesson frames planning tasks as the strongest intuition.'),
  q(63, 'Application', 'A run lowers visible text tokens but also lowers accuracy under perturbation tests. What review is needed?', 'Evaluate whether latent compression is trading away robustness or faithful computation.', 'Ignore accuracy because shorter text dominates every other metric.', 'Remove all intervention tests from evaluation.', 'Production review should balance token savings against reliability.'),
  q(64, 'Application', 'You want to test whether latent steps carry branch information. Which panel readout is most direct?', 'Path distribution across candidate branches over latent steps.', 'The number of static source links.', 'The final answer character count.', 'Branch probabilities are the lesson view of latent path information.'),
  q(65, 'Application', 'A model commits to a dead-end branch on the first latent step. Which metric reveals early commitment?', 'A low commitment step with branch entropy dropping too soon.', 'Only visible token count.', 'Only the training-stage label.', 'The commitment-step metric indicates when one branch becomes dominant.'),
  q(66, 'Application', 'A team claims latent thoughts are interpretable because the nearest token is meaningful. What should you ask next?', 'Whether perturbations, removals, and shortcut tests support the same reasoning story.', 'Whether the final answer uses fewer commas.', 'Whether the source card has a link icon.', 'Interpretability claims need causal and robustness evidence.'),
  q(67, 'Application', 'A Coconut run uses many latent steps and becomes slower than CoT while printing less text. What explains this?', 'Hidden positions reduce displayed tokens but can still increase total forward passes.', 'Latent thoughts are computed by the browser only.', 'The LM head is used twice for each latent step.', 'Compute accounting must include hidden-state feedback steps.'),
  q(68, 'Application', 'A trainer applies loss directly to latent positions as if they were missing words. What is wrong?', 'Latent positions should be masked and trained through future text prediction.', 'Latent positions must always be decoded before training.', 'Only question tokens should receive the answer loss.', 'Continuous thoughts are not supervised by direct word labels in the lesson.'),
  q(69, 'Application', 'A model handles in-distribution planning but fails out of distribution. Which faithfulness test is relevant?', 'Shortcut-dataset evaluation to see whether hidden states rely on spurious cues.', 'Counting only visible text tokens.', 'Increasing source-link count.', 'Shortcut robustness is a core caveat for latent reasoning.'),
  q(70, 'Application', 'A user wants both compact reasoning and a readable explanation. What design may be needed?', 'Use latent reasoning for search but still generate a visible rationale or verification summary when required.', 'Never emit visible tokens after <eot>.', 'Treat nearest-token probes as final explanations.', 'Coconut can separate internal search from external communication, but products may need both.'),
  q(71, 'Application', 'A model performs well until the latent region is removed. Which documentation should mention this?', 'Faithfulness or causal-intervention results, because they show dependence on latent states.', 'Only the CSS audit notes.', 'Only the static route metadata.', 'Ablation outcomes are part of explaining what the model depends on.'),
  q(72, 'Application', 'A hidden-state probe gives different nearest tokens across small perturbations. What should you infer?', 'The probe may be unstable, so avoid overclaiming a precise natural-language interpretation.', 'The final answer is impossible to evaluate.', 'The latent state is guaranteed faithful.', 'Probe stability matters for interpretability claims.'),
  q(73, 'Application', 'When would visible CoT remain preferable to Coconut?', 'When transparent intermediate reasoning is required more than compact hidden computation.', 'Whenever the task has no need for explanations.', 'Whenever compute-step accounting is ignored.', 'Visible CoT provides readable intermediate text that latent states do not.'),
  q(74, 'Application', 'When is Coconut most plausibly useful according to the lesson framing?', 'Planning-heavy reasoning where delayed branch commitment can help before visible output.', 'Static lookup tasks with no uncertainty.', 'Cases where all hidden states must be human-readable.', 'The strongest intuition is branch search before committing to text.'),
  q(75, 'Application', 'Before shipping a Coconut-style feature, what review should be complete?', 'Mode tracing, curriculum behavior, compute accounting, branch metrics, faithfulness interventions, probes, and shortcut tests.', 'Only a count of final answer tokens.', 'Only confirmation that <bot> appears somewhere in the prompt.', 'Production readiness requires both mechanism checks and faithfulness-risk evaluation.'),

  q(76, 'Tricky', 'Which answer rejects the trap: latent thoughts are automatically faithful reasoning traces?', 'False; they may help prediction while remaining opaque, shortcut-driven, or noncausal.', 'True; every hidden state is a faithful proof step.', 'True; faithfulness follows from high accuracy alone.', 'The lesson warns against equating useful latent computation with faithful reasoning.'),
  q(77, 'Tricky', 'Which answer rejects the trap: Coconut makes reasoning free because hidden thoughts are invisible?', 'False; each latent thought still requires a forward computation step.', 'True; invisible states have zero compute cost.', 'True; only printed characters use transformer layers.', 'Invisible to the user does not mean free to compute.'),
  q(78, 'Tricky', 'Which answer rejects the trap: a pause token is the same thing as a continuous thought?', 'False; pause tokens use learned token embeddings while continuous thoughts feed back context-dependent hidden states.', 'True; both are identical fixed embeddings.', 'True; pause tokens bypass all transformer computation.', 'The lesson separates fixed filler tokens from dynamic latent feedback.'),
  q(79, 'Tricky', 'Which answer rejects the trap: nearest-token probes prove what a latent state means?', 'False; probes are suggestive and need interventions or robustness checks.', 'True; the nearest token is always the exact hidden thought.', 'True; probes remove the need for ablations.', 'Probe outputs can be misleading if treated as definitive explanations.'),
  q(80, 'Tricky', 'Which answer rejects the trap: high benchmark accuracy proves latent reasoning is interpretable?', 'False; accuracy can improve even when the internal latent path remains opaque.', 'True; any accurate answer makes every hidden state readable.', 'True; interpretability is identical to final-task score.', 'Performance and interpretability are separate evaluation claims.'),
  q(81, 'Tricky', 'Which answer rejects the trap: the LM head is used inside every Coconut latent step?', 'False; latent mode bypasses LM-head token decoding for those steps.', 'True; every hidden state must be sampled as a word token.', 'True; Coconut is just ordinary visible CoT.', 'The central mechanism is skipping token decoding in the latent region.'),
  q(82, 'Tricky', 'Which answer rejects the trap: latent positions should receive direct word-label loss?', 'False; latent positions are masked and optimized through later text targets.', 'True; every latent vector has a gold word label.', 'True; question tokens should be the only supervised targets.', 'The curriculum trains latent states by their downstream usefulness.'),
  q(83, 'Tricky', 'Which answer rejects the trap: delayed commitment means the model refuses to answer?', 'False; it means branch uncertainty can stay soft before a final path is selected.', 'True; delayed commitment means no answer is produced.', 'True; it only describes model loading time.', 'Delayed commitment is about branch distributions, not refusal.'),
  q(84, 'Tricky', 'Which answer rejects the trap: fewer visible tokens always means better deployment?', 'False; compute, accuracy, robustness, and auditability still need evaluation.', 'True; shorter output automatically dominates every other metric.', 'True; hidden-state reasoning cannot fail.', 'Visible token savings are only one part of the tradeoff.'),
  q(85, 'Tricky', 'Which answer rejects the trap: shortcut tests are unnecessary if ablation changes the answer?', 'False; ablation shows dependence, but shortcut tests ask what information the latent state depends on.', 'True; one ablation result proves robust reasoning.', 'True; shortcut data cannot affect hidden states.', 'Causal dependence and robust reasoning are related but different questions.'),
  q(86, 'Tricky', 'Which answer rejects the trap: Coconut removes the need for visible explanations?', 'False; some applications still require generated rationales or verification summaries.', 'True; hidden states are always enough for users and auditors.', 'True; explanations become impossible after <eot>.', 'Coconut may reduce internal verbosity but does not remove product explanation requirements.'),
  q(87, 'Tricky', 'Which answer rejects the trap: perturbing a latent vector is meaningless because vectors cannot be studied?', 'False; perturbations are useful causal interventions when interpreted carefully.', 'True; continuous states cannot be tested at all.', 'True; only final answer length can be measured.', 'The lesson explicitly includes perturbation as a faithfulness test.'),
  q(88, 'Tricky', 'Which answer rejects the trap: CoT and Coconut differ only in typography?', 'False; CoT emits text tokens while Coconut can feed hidden states directly as inputs.', 'True; both methods use the identical feedback path.', 'True; Coconut is just a different font for scratchpads.', 'The difference is architectural at selected generation steps.'),
  q(89, 'Tricky', 'Which answer rejects the trap: branch entropy drop guarantees correct reasoning?', 'False; entropy drop shows commitment, but the chosen branch can still be wrong or shortcut-based.', 'True; lower entropy always proves correctness.', 'True; entropy replaces answer evaluation.', 'Commitment is not the same as correctness or faithfulness.'),
  q(90, 'Tricky', 'Which answer rejects the trap: a latent thought must correspond to one hidden English sentence?', 'False; a latent state can encode distributed features that do not map cleanly to one sentence.', 'True; every vector is secretly one sentence.', 'True; nearest-token probes decode the full proof exactly.', 'Continuous representations need not be sentence-like.'),

  q(91, 'Interview', 'In an interview, how would you summarize Coconut precisely?', 'Coconut lets selected reasoning steps happen as continuous hidden-state feedback instead of visible text tokens.', 'Coconut is a retrieval method that never changes token generation.', 'Coconut is only a prompt format for longer chain-of-thought.', 'A precise summary names hidden-state feedback and contrasts it with visible CoT.'),
  q(92, 'Interview', 'How would you explain the difference between language mode and latent mode?', 'Language mode decodes h_t through the LM head to a token, while latent mode feeds h_t directly as x_t+1.', 'Language mode uses vectors and latent mode uses only words.', 'Both modes always sample a visible token at every step.', 'The mode distinction is the central mechanism of the lesson.'),
  q(93, 'Interview', 'How would you justify the curriculum design?', 'Start from text reasoning supervision, then gradually replace early steps with latent states so future text remains predictable.', 'Skip all intermediate reasoning data from the beginning.', 'Train only latent vectors with direct word labels.', 'The staged schedule makes the latent task learnable.'),
  q(94, 'Interview', 'How would you evaluate delayed commitment?', 'Track branch distributions, entropy drop, and the step where one path becomes dominant.', 'Only count final-answer punctuation.', 'Only inspect the title of the task preset.', 'The lesson gives branch entropy and commitment step as concrete metrics.'),
  q(95, 'Interview', 'How would you audit faithfulness for a Coconut model?', 'Combine probes with perturbations, ablations, shortcut-resistant data, and answer-behavior checks.', 'Use nearest-token probes alone as proof.', 'Trust hidden states whenever accuracy is high.', 'A strong audit uses causal and robustness tests, not one passive signal.'),
  q(96, 'Interview', 'How would you explain the compute tradeoff?', 'Coconut may reduce visible scratchpad tokens, but latent thoughts still add forward passes.', 'Coconut removes all compute for reasoning.', 'Visible text and compute steps are always identical.', 'Good answers distinguish user-visible length from model work.'),
  q(97, 'Interview', 'What failure mode would you watch for in latent reasoning?', 'Shortcut-driven hidden states that boost in-distribution accuracy but fail robust reasoning tests.', 'Too many readable intermediate proof sentences.', 'A transformer stack that never processes latent positions.', 'The lesson highlights opacity and shortcut behavior as core risks.'),
  q(98, 'Interview', 'When would you prefer visible CoT over Coconut?', 'When human auditability and readable intermediate reasoning are more important than compact hidden computation.', 'When no one needs to understand the output.', 'When every hidden state has already been proven faithful.', 'Visible CoT remains valuable for transparency.'),
  q(99, 'Interview', 'What should a production dashboard for Coconut include?', 'Mode, latent steps, visible tokens, compute steps, branch entropy, commitment step, perturbation sensitivity, and accuracy.', 'Only the final answer text length.', 'Only whether <bot> appears in the prompt.', 'Production monitoring should cover mechanics, cost, reasoning dynamics, and faithfulness signals.'),
  q(100, 'Interview', 'What final review makes this Coconut assessment production-ready?', 'Verify progressive coverage, no answer leakage, balanced choices, misconception traps after setup, and full registry integration.', 'Keep only the original ten questions.', 'Remove explanations to make the quiz shorter.', 'A production assessment needs content quality plus automated invariants.'),
]);

export const COCONUT_LATENT_REASONING_QUIZ = Object.freeze(COCONUT_SPECS);
