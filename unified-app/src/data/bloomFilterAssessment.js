function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash;
}

function q(number, level, prompt, correct, distractors, explanation) {
  const id = `bf-${String(number).padStart(3, '0')}`;
  const registryRotation = stableHashNumber(`bloom-filter:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);
  return Object.freeze({ id, level, prompt, choices: Object.freeze(choices), answerIndex, explanation, questionHash: stableHash(`${prompt}\n${choices.join('\n')}`) });
}

const SPECS = Object.freeze([
  q(1, 'Foundation', 'What problem does a Bloom filter primarily solve?', 'Approximate set membership using much less memory than storing every key.', ['Sorting records by their exact numeric value.', 'Recovering the original object from a compressed code.'], 'The lesson frames Bloom filters as compact membership hints.'),
  q(2, 'Foundation', 'What are the two core ingredients of the data structure?', 'A fixed bit array and several hash functions that map keys to bit positions.', ['A neural network and a labeled training set.', 'A balanced tree plus full copies of each value.'], 'The animation shows a bit array and multiple hash-selected indices.'),
  q(3, 'Foundation', 'What happens when an item is inserted?', 'Each hash position for the item is set to 1 in the bit array.', ['The item text is appended to every empty slot.', 'All unrelated bits are reset to 0 first.'], 'Insertion flips selected bits on and never stores the key itself.'),
  q(4, 'Foundation', 'What answer means the queried item might be present?', 'All queried hash positions are already 1.', ['At least one queried hash position is 0.', 'The filter can print the original stored value.'], 'All ones are ambiguous because other inserts may have set them.'),
  q(5, 'Foundation', 'What answer means the queried item is certainly absent?', 'At least one queried hash position is still 0.', ['Every queried hash position is already 1.', 'The key name appears in a hidden exact list.'], 'A zero bit proves that this key was never inserted with the same hashes.'),
  q(6, 'Foundation', 'What kind of error can the structure make?', 'A false positive, where an absent item looks probably present.', ['A false negative after a normal completed insert.', 'A recovered value that has the wrong type.'], 'Shared bits can mimic membership for absent keys.'),
  q(7, 'Foundation', 'Why should a normal inserted item avoid false negatives?', 'Insertion would have set all of that item hash locations to 1.', ['The bit array stores a checksum for each original key.', 'The query contacts a guaranteed backing database first.'], 'With unchanged bits and hashes, an inserted key cannot find a zero among its own locations.'),
  q(8, 'Foundation', 'What does m represent in the tuning panel?', 'The number of bits available in the bit array.', ['The number of class labels in supervised learning.', 'The maximum character length of each key.'], 'The slider labeled bit array size controls m.'),
  q(9, 'Foundation', 'What does n represent in the probability formula?', 'The expected number of inserted items.', ['The number of answers shown in the quiz.', 'The byte length of the serialized hash seed.'], 'The lesson uses n for planned capacity.'),
  q(10, 'Foundation', 'What does k represent during insert and query?', 'The number of hash functions or hash positions checked per key.', ['The number of exact values returned to the user.', 'The count of databases behind the cache.'], 'The animation highlights k selected bit positions for each word.'),
  q(11, 'Foundation', 'Why can this structure be compact?', 'It stores shared membership evidence as bits instead of storing full keys.', ['It stores two encrypted copies of every item.', 'It learns a separate vector for each document.'], 'Space savings come from representing many keys through overlapping bit patterns.'),
  q(12, 'Foundation', 'What does the filter not provide by itself?', 'The original item value or a proof that a maybe answer is real.', ['A fast negative membership hint.', 'A tunable false-positive probability estimate.'], 'Bloom filters answer membership hints, not value lookup.'),
  q(13, 'Foundation', 'Why are collisions expected instead of surprising?', 'Different inserted items can set some of the same bit positions.', ['Hash functions are forbidden from sharing outputs.', 'Every bit belongs permanently to one key.'], 'The false-positive lab is built around overlapping hash positions.'),
  q(14, 'Foundation', 'In the playground, what does pressing Add do conceptually?', 'It hashes the word and flips the selected positions to 1.', ['It removes the word from a balanced tree.', 'It calculates a gradient update for a classifier.'], 'The first panel visualizes insertion as bit flips.'),
  q(15, 'Foundation', 'In the playground, what does a Check with all ones report?', 'Probably in the set, because those bits could have come from that word.', ['Definitely not in the set, because ones are absence markers.', 'The exact insertion timestamp for the word.'], 'All selected bits being 1 is a maybe-present result.'),
  q(16, 'Foundation', 'In the playground, what does a Check with a zero report?', 'Definitely not in the set, because an inserted word would have set it.', ['Probably present, because zero is the success marker.', 'Unknown until all stored keys are printed.'], 'The animation contrasts maybe positives with certain negatives.'),
  q(17, 'Foundation', 'Why does the false-positive lab use a tiny array?', 'A small array saturates quickly so collisions become easy to observe.', ['A tiny array prevents any false positives from occurring.', 'Small arrays can store complete key strings in each bit.'], 'The lab intentionally makes collisions visible.'),
  q(18, 'Foundation', 'Which knobs does the tuning studio expose?', 'Bit count m, expected items n, and hash count k.', ['Batch size, learning rate, and transformer depth.', 'Precision, recall, and F1 threshold only.'], 'The controls match the standard Bloom filter sizing variables.'),
  q(19, 'Foundation', 'What formula summarizes the approximate false-positive probability?', 'p is approximately (1 - exp(-kn/m)) raised to k.', ['p equals the number of stored keys divided by label count.', 'p is always zero when k is at least two.'], 'The learning card and chart use the standard Bloom filter approximation.'),
  q(20, 'Foundation', 'What foundation takeaway should a learner keep?', 'Bloom filters trade exact positive proof for compact certain negatives.', ['Bloom filters are exact dictionaries with hidden values.', 'Bloom filters guarantee no uncertain answers.'], 'The one-sided error idea is the base for every later tradeoff.'),

  q(21, 'Mechanism', 'How should each hash result be converted into a bit-array location?', 'Map the hash output into the range 0 to m-1, commonly with modulo m.', ['Store the raw hash as a new text key.', 'Round the hash to the nearest probability bucket only.'], 'The code panels use hash(key) % m style indexing.'),
  q(22, 'Mechanism', 'Why is insertion like an OR operation over bits?', 'It only turns selected bits on and leaves existing ones on.', ['It alternates every selected bit between 0 and 1.', 'It clears all positions not touched by the current item.'], 'Setting bits to 1 is monotonic in a plain Bloom filter.'),
  q(23, 'Mechanism', 'What happens if the same key is inserted twice?', 'The second insert is idempotent because the same positions are already 1.', ['The key receives a second exact copy in the bit array.', 'The filter halves the false-positive probability automatically.'], 'Repeating the same bit settings does not change a standard bit array.'),
  q(24, 'Mechanism', 'Why do all-one query positions not prove membership?', 'Other inserted keys may have set the same positions by coincidence.', ['The filter always refuses to use hash functions at query time.', 'Bits with value 1 mean the item was deleted.'], 'False positives come from shared evidence, not from stored key identity.'),
  q(25, 'Mechanism', 'Why does one zero query position prove absence?', 'No completed insert for that key could leave one of its own positions unset.', ['Zero bits store exact item names for later retrieval.', 'The array size m changes during every query.'], 'This is the core no-false-negative argument for ordinary inserts.'),
  q(26, 'Mechanism', 'What creates a false positive in the collision lab?', 'Several earlier words collectively set all positions for an uninserted word.', ['The lab forgets to hash the queried word.', 'The filter decodes a wrong original value from a bit.'], 'An absent item can be covered by bits left by other items.'),
  q(27, 'Mechanism', 'What does the exp(-kn/m) term approximate?', 'The chance a particular bit remains zero after n items with k hashes.', ['The number of labels in a classification head.', 'The exact memory address of the queried key.'], 'The false-positive formula starts by modeling bit fill.'),
  q(28, 'Mechanism', 'Why is the false-positive formula raised to k?', 'An absent key needs all k queried positions to be 1 to look present.', ['Each item is copied into k separate exact tables.', 'The filter performs k gradient steps per query.'], 'The all-ones condition combines k bit events.'),
  q(29, 'Mechanism', 'What does increasing m do when n and k stay reasonable?', 'It lowers saturation by spreading inserts across more bits.', ['It forces all queries to become exact lookups.', 'It guarantees every hash function becomes independent.'], 'More bits reduce overlap pressure.'),
  q(30, 'Mechanism', 'What happens as n grows beyond the planned capacity?', 'More bits become set, so false positives rise above the design target.', ['False positives fall because the filter learns more examples.', 'All bits automatically resize without rebuilding.'], 'Underestimating n is a common sizing failure.'),
  q(31, 'Mechanism', 'Why is there an optimal range for k?', 'Too few hashes under-check; too many hashes saturate the array quickly.', ['Every larger k strictly improves the filter forever.', 'k controls only screen animation speed.'], 'The tuning chart shows a sweet spot rather than a monotone rule.'),
  q(32, 'Mechanism', 'What is the usual approximate optimal k for fixed m and n?', 'k is near (m/n) times ln 2, then rounded to an integer.', ['k is always exactly one regardless of memory.', 'k must equal the number of stored keys.'], 'The tuning panel displays k = (m/n) * ln(2).'),
  q(33, 'Mechanism', 'Why can k too small be ineffective?', 'The query checks too few bits, making accidental coverage easier.', ['It creates false negatives by clearing old bits.', 'It stores too many exact copies of the same key.'], 'A single or tiny number of checks gives weak discrimination.'),
  q(34, 'Mechanism', 'Why can k too large be ineffective?', 'Each insert sets many bits, filling the array and helping absent keys pass.', ['It prevents all inserted keys from setting bits.', 'It converts the structure into a lossless compressor.'], 'More hashes increase strictness but also raise saturation.'),
  q(35, 'Mechanism', 'What is bit-array saturation?', 'The fraction of positions already set to 1 after many inserts.', ['The number of bytes in each original key string.', 'The maximum call stack depth of the hash code.'], 'A saturated array has little remaining absence evidence.'),
  q(36, 'Mechanism', 'Why do independent-looking hashes matter?', 'They spread positions more uniformly and keep the formula useful.', ['They make false positives impossible in every workload.', 'They allow values to be reconstructed from bits.'], 'Correlated hashes clump bits and break the intended distribution.'),
  q(37, 'Mechanism', 'What is a practical way to derive several hash positions?', 'Use multiple seeds or double hashing to produce k index-like values.', ['Use the same unmodified index k times.', 'Sort the key alphabetically and skip hashing.'], 'Implementations often avoid k fully separate hash functions while preserving spread.'),
  q(38, 'Mechanism', 'What is the query-time cost in the basic structure?', 'It is proportional to k hash/index checks.', ['It is proportional to the number of all stored keys.', 'It always requires a full disk scan before answering.'], 'Queries touch a fixed number of bit positions.'),
  q(39, 'Mechanism', 'Why can a query short-circuit on zero?', 'A single zero is enough to return definitely absent.', ['A single zero proves the item is definitely present.', 'The filter must continue to recover the item value.'], 'The query pseudocode can stop at the first failed bit.'),
  q(40, 'Mechanism', 'Why does the structure save memory compared with a hash set?', 'It keeps only m bits and parameters rather than every key.', ['It compresses keys and stores perfect reversible copies.', 'It uses more memory to avoid all collisions.'], 'Bloom filters are chosen when key storage is too costly.'),
  q(41, 'Mechanism', 'Why is deletion unsafe in a plain Bloom filter?', 'Clearing a shared bit can break evidence for other inserted keys.', ['Deletion doubles the number of hash functions.', 'Plain Bloom filters store a deletion log inside each bit.'], 'Zeroing a shared position can create false negatives.'),
  q(42, 'Mechanism', 'What variant supports safer deletion?', 'A counting Bloom filter keeps counts instead of one-bit flags.', ['A plain Bloom filter with random bit clearing.', 'A smaller m with the same number of hashes.'], 'Counting variants can decrement without erasing unrelated evidence prematurely.'),
  q(43, 'Mechanism', 'Why does a library ask for capacity and error rate?', 'It uses them to choose m and k for the desired false-positive budget.', ['It needs labels for supervised model training.', 'It stores the requested keys in a sorted exact table only.'], 'Constructor parameters usually translate design targets into bit and hash counts.'),
  q(44, 'Mechanism', 'What does exceeding the configured capacity do?', 'It degrades the real false-positive rate compared with the requested target.', ['It improves accuracy because more keys give more evidence.', 'It automatically proves every maybe answer exactly.'], 'The planned n is part of the formula.'),
  q(45, 'Mechanism', 'Why must positives often be checked against a source of truth?', 'A maybe-present answer can be a false positive.', ['A definitely-absent answer is always a false negative.', 'The bit array stores encrypted values that need decryption.'], 'Bloom filters are usually prefilters rather than authoritative stores.'),
  q(46, 'Mechanism', 'How do databases use the one-sided answer?', 'They can skip expensive page or disk lookups when the filter says absent.', ['They can delete all source tables when the filter says maybe.', 'They can reconstruct rows directly from the bit array.'], 'Negative answers save work safely.'),
  q(47, 'Mechanism', 'How can cache systems use the filter?', 'They can avoid origin checks for objects known to be absent.', ['They can treat every maybe as a fresh cached object.', 'They can replace TTL validation with bit positions only.'], 'The benefit is reducing negative lookup cost.'),
  q(48, 'Mechanism', 'What must stay consistent when serializing a filter?', 'The bit array, m, k, and hash seeds or hash definitions.', ['Only the user-facing display color.', 'The last queried word but not the bits.'], 'Changing parameters or seeds changes membership answers.'),
  q(49, 'Mechanism', 'Why is the probability formula approximate?', 'It assumes idealized independent hashing and smooth bit-fill behavior.', ['It is approximate because Bloom filters ignore all hashes.', 'It estimates the number of recovered original values.'], 'Real hash families and finite arrays can deviate from the model.'),
  q(50, 'Mechanism', 'What mechanism takeaway connects the panels?', 'Insert sets k bits, query checks k bits, and tuning controls collision risk.', ['Insert sorts keys, query returns values, and tuning picks class labels.', 'Insert deletes zeros, query rewrites hashes, and tuning removes uncertainty.'], 'The three panels cover operation, collision, and parameter choice in sequence.'),

  q(51, 'Application', 'When is a Bloom filter a strong system design choice?', 'When memory is tight and occasional extra positive checks are acceptable.', ['When every positive answer must be a legal proof.', 'When the original value must be returned without another store.'], 'Approximate membership is useful only when false positives are tolerable.'),
  q(52, 'Application', 'Which workload benefits most from a Bloom prefilter?', 'A negative-heavy lookup path where skipping absent keys saves latency.', ['A task that must enumerate all stored values in order.', 'A computation requiring exact counts for every key.'], 'Certain negatives are the operational advantage.'),
  q(53, 'Application', 'How should a service handle a maybe-present response for user access control?', 'Verify against an authoritative source before granting access.', ['Grant access immediately because maybe means guaranteed present.', 'Clear the matching bits to make the answer exact.'], 'Security flows cannot treat false positives as permission.'),
  q(54, 'Application', 'How can an LSM-style storage engine use Bloom filters?', 'Check whether an SSTable might contain a key before reading it.', ['Replace all on-disk key-value blocks with the bit array.', 'Use the filter to sort values by timestamp.'], 'Storage systems use filters to avoid unnecessary disk work.'),
  q(55, 'Application', 'How can a CDN or cache use the structure safely?', 'Skip origin work when the filter proves an object is absent.', ['Serve content directly from the filter bits.', 'Ignore cache validation whenever all bits are 1.'], 'The filter is safe for negative elimination, not content delivery.'),
  q(56, 'Application', 'How can retrieval infrastructure use it as a prefilter?', 'Exclude impossible buckets before doing more expensive index search.', ['Rank documents semantically using only bit values.', 'Generate embeddings for unseen text from hash collisions.'], 'Bloom filters can cheaply rule out candidates in larger pipelines.'),
  q(57, 'Application', 'What should be monitored in a long-lived filter?', 'Bit fill ratio and observed false-positive rate against expectations.', ['Only the number of UI tabs in the lesson.', 'The alphabetical order of inserted keys.'], 'Saturation drift tells you when guarantees are being lost.'),
  q(58, 'Application', 'What is the safest response when observed false positives exceed the budget?', 'Resize or rebuild with a larger planned capacity or lower target rate.', ['Assume the formula has made positives exact.', 'Delete random one bits until the chart improves.'], 'A saturated or undersized filter needs a controlled rebuild strategy.'),
  q(59, 'Application', 'How should m be chosen for a target false-positive rate?', 'Use expected n and target p to allocate enough bits before deployment.', ['Pick the smallest visually pleasing array length.', 'Set m equal to k for every workload.'], 'Sizing starts from capacity and error budget.'),
  q(60, 'Application', 'How should k be handled after computing the continuous optimum?', 'Round to a practical integer and re-evaluate the resulting probability.', ['Use the fractional value directly as half a hash function.', 'Always round to zero to avoid collisions.'], 'Implementations need an integer number of positions.'),
  q(61, 'Application', 'What risk appears if traffic grows beyond expected n?', 'The filter becomes more saturated and lets more absent keys through.', ['The filter starts returning exact stored values.', 'The filter automatically converts to a hash set.'], 'Capacity planning must include peak load and growth.'),
  q(62, 'Application', 'What should be done for a dynamic set with frequent deletes?', 'Use a deletion-capable variant or rebuild instead of clearing bits directly.', ['Zero the selected positions in the plain filter after each delete.', 'Ignore deletes because they never affect answers.'], 'Plain Bloom filters are monotonic.'),
  q(63, 'Application', 'What is the tradeoff versus an exact hash set?', 'Bloom uses less memory but needs tolerance for false positives.', ['Bloom uses more memory but returns full stored objects.', 'Bloom eliminates all hashing work by storing keys plainly.'], 'Exact sets and Bloom filters optimize different constraints.'),
  q(64, 'Application', 'What should an API name communicate about positive results?', 'Positive responses are maybe-present hints, not confirmed membership.', ['Positive responses are cryptographic membership certificates.', 'Positive responses contain the stored value bytes.'], 'Clear naming prevents downstream misuse.'),
  q(65, 'Application', 'How should tests validate the no-false-negative property?', 'Insert known keys and verify queries for those keys never return absent.', ['Query only random absent keys and expect exact counts.', 'Delete known keys from a plain filter and expect certainty.'], 'Inserted keys should always pass under stable parameters.'),
  q(66, 'Application', 'How should false-positive behavior be measured in practice?', 'Query held-out absent keys and estimate the maybe-present rate.', ['Count how many inserted keys return absent after random bit clearing.', 'Ask the filter to list every stored object.'], 'Absent probe sets expose the actual false-positive rate.'),
  q(67, 'Application', 'Why should hash seeds be versioned in distributed services?', 'Mismatched seeds make different services check different bit positions.', ['Seeds only control chart colors in the UI.', 'Changing seeds makes old bits more accurate automatically.'], 'Parameter drift breaks shared filters.'),
  q(68, 'Application', 'What is a good failure plan for corrupted serialized bits?', 'Treat the filter as invalid and fall back to authoritative checks or rebuild.', ['Trust maybe answers more because corruption adds entropy.', 'Clear random bits to recover exact membership.'], 'Corruption can break both accuracy and the no-false-negative guarantee.'),
  q(69, 'Application', 'When should a team avoid a Bloom filter?', 'When false positives are unacceptable or exact enumeration is required.', ['When memory usage matters and negative lookups are common.', 'When an extra authoritative positive check is cheap.'], 'The structure is inappropriate for exact-only semantics.'),
  q(70, 'Application', 'How can a product team explain user-visible impact?', 'Occasional extra work may happen, but absent items can be rejected quickly.', ['Users can recover deleted records from the filter.', 'Every maybe result must be displayed as confirmed.'], 'False positives cost extra checks, while negatives save latency.'),
  q(71, 'Application', 'What makes the structure suitable for edge devices?', 'A small bit array can encode many membership hints under memory limits.', ['It avoids all CPU work by never hashing.', 'It replaces every local database with full text values.'], 'Compactness is often the deciding advantage.'),
  q(72, 'Application', 'What should happen after changing m or k in production?', 'Rebuild or migrate the filter because old bit positions no longer match.', ['Keep old bits and silently reinterpret them with new parameters.', 'Only update the dashboard label.'], 'Parameters define how bits are addressed.'),
  q(73, 'Application', 'How should a design doc state the main guarantee?', 'Negative answers are reliable under stable inserts, while positives need confirmation.', ['Positive answers are exact and negatives are guesses.', 'Every answer includes the original key.'], 'A correct design captures the one-sided error boundary.'),
  q(74, 'Application', 'How should a filter be combined with an authoritative store?', 'Use it as a fast reject path, then verify maybe results in the store.', ['Use it as the only source of truth for grants.', 'Use the store only for negative answers.'], 'Bloom filters reduce unnecessary lookups but do not replace confirmation.'),
  q(75, 'Application', 'What application-level Bloom Filter takeaway should guide deployment?', 'Size for planned capacity, monitor saturation, and verify maybe-present results.', ['Pick the smallest array and trust all positives.', 'Delete by clearing bits and skip all backing checks.'], 'Production use is about controlling the false-positive tradeoff safely.'),

  q(76, 'Tricky', 'Which false claim should be rejected: probably present proves membership?', 'Probably present can be a false positive and must not be treated as proof.', ['Maybe-present answers are exactly why Bloom filters save negative lookups.', 'Authoritative stores can verify uncertain positive responses.'], 'This is the most dangerous misconception.'),
  q(77, 'Tricky', 'Which wrong claim should be rejected: a zero bit is only a weak hint?', 'A zero among the queried positions is a definite absence signal in a valid plain filter.', ['Zeros are the reason negative-heavy workloads benefit.', 'Stable parameters are required for the absence guarantee.'], 'The lesson explicitly separates certain negatives from uncertain positives.'),
  q(78, 'Tricky', 'Why is deleting by clearing bits unsafe in a plain Bloom filter?', 'The cleared bit may be shared by other keys, creating false negatives.', ['It only increases memory usage while preserving correctness.', 'It changes the display color but not membership answers.'], 'Plain filters lack per-key ownership of bits.'),
  q(79, 'Tricky', 'Which trap should be avoided: more hash functions are always better?', 'Too many hashes fill the array faster and can raise false positives.', ['The optimal k depends on m and n.', 'The tuning chart can show a minimum at a finite k.'], 'The k tradeoff has two competing effects.'),
  q(80, 'Tricky', 'Which misconception about m is dangerous?', 'Using too few bits can saturate the filter and make maybe answers common.', ['Larger m can reduce overlap for the same load.', 'm is part of the false-positive formula.'], 'Undersizing the array breaks the intended error budget.'),
  q(81, 'Tricky', 'Why is treating the filter as a key-value store wrong?', 'The bit array does not retain original values or enough information to list keys.', ['It can still answer a membership hint quickly.', 'It may be paired with a separate authoritative store.'], 'Bloom filters intentionally discard value identity.'),
  q(82, 'Tricky', 'Which false statement about hash sets should be rejected?', 'A Bloom filter is just an exact hash table with smaller syntax.', ['A hash set stores keys exactly.', 'A Bloom filter stores compact shared bit evidence.'], 'Exact sets and Bloom filters make different accuracy-memory tradeoffs.'),
  q(83, 'Tricky', 'Why is using one repeated hash position a trap?', 'Repeated or correlated positions reduce independence and weaken the false-positive estimate.', ['Multiple well-spread positions are the intended mechanism.', 'Hash quality affects how uniformly bits are used.'], 'The formula relies on reasonably distributed positions.'),
  q(84, 'Tricky', 'Which security trap should be rejected?', 'Granting privileges from a maybe-present response fails open on false positives.', ['Using the filter only to reject absent candidates can be safe.', 'Positive authorization should come from an exact source.'], 'False positives are unacceptable as standalone security evidence.'),
  q(85, 'Tricky', 'Why is ignoring capacity drift a trap?', 'Inserted items beyond planned n can push the false-positive rate above target.', ['Capacity is one of the sizing assumptions.', 'Monitoring fill ratio can reveal drift.'], 'A filter sized for yesterday may be wrong for today.'),
  q(86, 'Tricky', 'Which false belief about the formula should be rejected?', 'The formula is a useful approximation, not an exact promise for poor hashes.', ['Idealized hashing assumptions matter.', 'Empirical checks can validate the deployed rate.'], 'Math guidance still depends on implementation quality.'),
  q(87, 'Tricky', 'Why is reusing old bits after changing hash seeds unsafe?', 'Old one bits no longer correspond to the new queried positions.', ['Seed consistency is part of serialized filter compatibility.', 'Rebuilds are normal after parameter changes.'], 'Changing the addressing scheme invalidates the stored evidence.'),
  q(88, 'Tricky', 'Which UI interpretation trap should be avoided?', 'A green maybe-present result should not be shown as confirmed presence.', ['A red definitely-absent result can safely stop a negative lookup.', 'Labels should expose uncertainty clearly.'], 'User-facing wording must preserve the one-sided semantics.'),
  q(89, 'Tricky', 'Why is random bit clearing a bad repair for high false positives?', 'It can introduce false negatives by erasing evidence for inserted items.', ['A planned rebuild can restore the intended false-positive budget.', 'Increasing m requires a coherent migration.'], 'Lowering saturation manually breaks correctness.'),
  q(90, 'Tricky', 'Which final trap should be rejected before deployment?', 'Bloom filters do not remove the need for authoritative positive checks.', ['They can reduce negative lookup cost.', 'They are useful when false positives are bounded.'], 'The tool is a prefilter, not a complete truth source.'),

  q(91, 'Interview', 'How would you define a Bloom filter in one interview-ready sentence?', 'It is a compact probabilistic set-membership structure with no false negatives and possible false positives.', ['It is a lossless dictionary that returns stored values.', 'It is a neural ranking model for approximate nearest neighbors.'], 'A strong definition names compactness, membership, and one-sided error.'),
  q(92, 'Interview', 'How would you explain insert and query succinctly?', 'Insert hashes a key to k positions and sets bits; query checks whether those k bits are all set.', ['Insert stores the key text; query scans every stored key.', 'Insert trains weights; query samples a generated token.'], 'This captures the operational mechanism without extra detail.'),
  q(93, 'Interview', 'How would you justify the no-false-negative guarantee?', 'If a key was inserted, the same hash positions were set and should still be 1.', ['The filter keeps a perfect backup copy of every key.', 'False negatives disappear because positives are always exact.'], 'The argument depends on stable hashes and no unsafe deletion.'),
  q(94, 'Interview', 'How would you derive the false-positive intuition?', 'After many inserts bits become 1 with some fill probability, and an absent key passes only if all k checked bits are 1.', ['The filter counts exact absent keys and divides by m.', 'The probability is unrelated to m, n, or k.'], 'This explains why p depends on saturation and the number of checks.'),
  q(95, 'Interview', 'How would you choose parameters for a service?', 'Estimate capacity n and target p, compute m and near-optimal k, then validate empirically.', ['Choose k as large as possible and ignore memory.', 'Start with a tiny array and trust the UI result.'], 'Parameter choice is an engineering budget decision.'),
  q(96, 'Interview', 'How would you compare Bloom filters with exact hash sets?', 'Hash sets cost more memory but answer exactly; Bloom filters save memory with false positives.', ['Bloom filters always use more memory and return values.', 'Hash sets cannot answer membership queries.'], 'The comparison is memory versus exactness.'),
  q(97, 'Interview', 'How would you handle deletions in an interview design?', 'Use a counting Bloom filter, periodic rebuilds, or another structure instead of clearing bits.', ['Clear the k positions for the removed key in the plain filter.', 'Ignore deletion requirements and keep claiming exactness.'], 'Deletion is the classic plain Bloom filter limitation.'),
  q(98, 'Interview', 'How would you diagnose unexpected high false positives?', 'Check capacity overrun, bit saturation, hash quality, and parameter or seed mismatches.', ['Assume inserted keys are now false negatives.', 'Disable the backing store and trust maybe answers.'], 'Operational debugging maps directly to the model assumptions.'),
  q(99, 'Interview', 'When would you refuse to use a Bloom filter?', 'When positive answers must be exact, deletions are core without variants, or enumeration is required.', ['When memory is scarce and negative lookups dominate.', 'When an extra positive confirmation step is acceptable.'], 'A senior answer includes non-use cases.'),
  q(100, 'Interview', 'What interview-ready Bloom Filter mastery statement ties the lesson together?', 'Use it as a sized, monitored, one-sided prefilter: certain no, uncertain maybe, never standalone proof.', ['Use it as a tiny exact database: every yes is certain and every value is stored.', 'Use it as a deletion-friendly counter by clearing bits whenever keys leave.'], 'The final lesson point combines semantics, tuning, and safe deployment.'),
]);

export const BLOOM_FILTER_QUIZ = SPECS;
