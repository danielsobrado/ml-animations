function q(id, level, prompt, correct, distractors, explanation) {
  const number = Number(id.match(/\d+/)?.[0] || 1);
  const desiredFinalIndex = (number - 1) % 3;
  const rotation = stableHash(`residual-stream:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + rotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    choices,
    answerIndex,
    prompt,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

const RESIDUAL_STREAM_SPECS = [
  ['resstream-001-purpose', 'Foundation', 'What problem does the residual stream solve in a transformer?', 'It carries token information through many components without forcing each layer to rewrite everything', ['It stores every training example in a database', 'It replaces the tokenizer with a cache'], 'The residual stream is the running hidden representation that survives across transformer blocks.'],
  ['resstream-002-definition', 'Foundation', 'Which description best matches the residual stream?', 'The evolving token representation flowing through the model', ['A separate retrieval memory bank', 'The final vocabulary probability table'], 'Each token has a current hidden vector that components read from and write back into.'],
  ['resstream-003-additive-update', 'Foundation', 'What does a transformer block usually do to the residual stream?', 'Adds attention and MLP outputs into the running representation', ['Deletes the previous representation at every layer', 'Stores only final sampled tokens'], 'Residual connections add component writes instead of replacing the whole stream.'],
  ['resstream-004-not-replacement', 'Foundation', 'What is the key difference between a residual write and full replacement?', 'A write is added to the current representation', ['A write erases every earlier feature', 'A write changes the vocabulary size'], 'The stream accumulates information because new component outputs are added to the prior state.'],
  ['resstream-005-token-local', 'Foundation', 'What does one residual-stream vector represent?', 'The current hidden state for one token position', ['All model parameters at once', 'Only the learning rate schedule'], 'Transformers maintain a hidden vector per token slot as computation proceeds.'],
  ['resstream-006-components', 'Foundation', 'Which components commonly write into the stream?', 'Attention and MLP blocks', ['Only the browser renderer', 'Only the tokenizer after inference'], 'The lesson cards describe attention and MLP outputs as vector writes into the shared stream.'],
  ['resstream-007-shared-workspace', 'Foundation', 'Why is the residual stream often described as a shared workspace?', 'Different components add useful features into the same running representation', ['It is shared with the training database', 'It replaces all layer weights'], 'Attention, MLP, and later layers can build on information already present in the stream.'],
  ['resstream-008-layer-flow', 'Foundation', 'How does information move from one transformer layer to the next?', 'The updated stream becomes the input state for the next layer', ['Only final logits are passed forward', 'The tokenizer reruns on every layer'], 'Residual updates make layer-to-layer information flow explicit.'],
  ['resstream-009-subject-relation-syntax', 'Foundation', 'What kinds of features might accumulate in the stream?', 'Subject, relation, syntax, and prediction-related features', ['Only file paths and CSS classes', 'Only random answer labels'], 'The lab asks learners to inspect how several feature types can build up together.'],
  ['resstream-010-before-write-after', 'Foundation', 'What does the lab mean by before, write, and after vectors?', 'The stream state before a component, the component output, and the summed result', ['The tokenizer, optimizer, and deployment log', 'The question, answer, and score letter'], 'Tracing these three states makes residual addition concrete.'],
  ['resstream-011-scale-control-basic', 'Foundation', 'Why do normalization and scaling matter around residual writes?', 'Large writes can dominate or destabilize the running representation', ['They make token order unnecessary', 'They remove the need for embeddings'], 'Many additions into one stream require scale control so useful information remains available.'],
  ['resstream-012-not-memory-bank', 'Foundation', 'What is the residual stream not?', 'It is not a separate memory bank or KV cache', ['It is the current hidden representation', 'It is read by transformer components'], 'The lesson warns against confusing the stream with external memory or cached key-value tensors.'],
  ['resstream-013-attention-read', 'Foundation', 'How does attention use the residual stream?', 'It reads current token states to produce an attention write', ['It ignores hidden states entirely', 'It only reads final probabilities'], 'Attention projections are computed from the current stream state.'],
  ['resstream-014-mlp-write', 'Foundation', 'How does an MLP block interact with the stream?', 'It transforms the current token state and adds a new write back', ['It appends a new training example', 'It deletes attention masks'], 'MLPs produce per-token updates that are added through residual connections.'],
  ['resstream-015-deep-model', 'Foundation', 'Why are residual streams important in deep transformers?', 'They let information and gradients move through many layers more easily', ['They prevent all matrix multiplication', 'They make every layer identical'], 'Residual paths help preserve signals across depth.'],
  ['resstream-016-feature-accumulation', 'Foundation', 'What does feature accumulation mean here?', 'Later stream states can contain information contributed by many earlier components', ['Every component stores a separate database row', 'All features are reset after each block'], 'The stream becomes a running sum of original state plus component writes.'],
  ['resstream-017-write-strength', 'Foundation', 'What does changing write strength in the lesson reveal?', 'How strongly one component update changes the after state', ['How many tokens the tokenizer knows', 'How the route list is sorted'], 'Increasing a write makes its contribution to the summed stream more visible.'],
  ['resstream-018-success-criterion', 'Foundation', 'What should you be able to distinguish after the lab?', 'Adding a component write versus replacing the whole token representation', ['KV cache size versus dataset size only', 'Top-p sampling versus beam search only'], 'The success criterion is recognizing additive residual updates.'],
  ['resstream-019-beginner-trap', 'Foundation', 'What beginner trap should you avoid?', 'Treating the residual stream as a separate memory bank', ['Calling it an evolving hidden state', 'Tracing before and after vectors'], 'The stream is inside the forward pass, not a standalone retrieval system.'],
  ['resstream-020-foundation-summary', 'Foundation', 'What foundation summary is correct?', 'The residual stream is the running token representation updated by additive component writes', ['The residual stream is a tokenizer vocabulary table', 'The residual stream is only a final output probability'], 'This summary captures state, token scope, and additive updates.'],

  ['resstream-021-formula', 'Mechanism', 'Which formula matches the residual-stream update shown in the lesson?', 'x_{l+1} = x_l + Attn(x_l) + MLP(x_l)', ['x_{l+1} = tokenizer(labels)', 'loss = cache - route'], 'The lesson formula shows the next stream as the current stream plus component outputs.'],
  ['resstream-022-current-state', 'Mechanism', 'In the formula, what does x_l represent?', 'The residual stream state at layer l', ['The final sampled token id', 'The number of answer choices'], 'x_l is the hidden representation that the layer reads and updates.'],
  ['resstream-023-attn-output', 'Mechanism', 'What does Attn(x_l) represent in the update?', 'The attention block write computed from the current stream', ['The tokenizer vocabulary', 'The browser layout'], 'Attention reads the current stream and returns a vector update.'],
  ['resstream-024-mlp-output', 'Mechanism', 'What does MLP(x_l) represent in the update?', 'The feed-forward block write added to the stream', ['The external database lookup', 'The final softmax label only'], 'The MLP is another component that writes into the shared representation.'],
  ['resstream-025-sequential-blocks', 'Mechanism', 'Do real transformer blocks always add attention and MLP at the exact same formula line?', 'No, implementations may sequence sublayers while preserving residual additions', ['Yes, every architecture must use one literal expression', 'No, residuals are never additive'], 'The compact formula is a teaching summary; real blocks can be pre-norm, post-norm, or separated.'],
  ['resstream-026-pre-norm', 'Mechanism', 'What is a pre-norm residual block pattern?', 'Normalize before a sublayer, then add the sublayer output to the stream', ['Normalize only after final text sampling', 'Delete the stream before every sublayer'], 'Pre-norm designs feed normalized states to components while keeping an additive residual path.'],
  ['resstream-027-post-norm', 'Mechanism', 'What is a post-norm residual block pattern?', 'Add the sublayer output, then normalize the result', ['Normalize the tokenizer vocabulary only', 'Skip residual addition entirely'], 'Post-norm places normalization after the residual update.'],
  ['resstream-028-scale-balance', 'Mechanism', 'Why does scale balance matter when many writes accumulate?', 'A very large write can drown out earlier useful features', ['The route path controls hidden vector length', 'Softmax fixes every internal scale issue'], 'Residual addition preserves information only if update magnitudes stay usable.'],
  ['resstream-029-gradient-path', 'Mechanism', 'Why do residual connections help gradient flow?', 'They provide a shorter additive path through depth', ['They remove backpropagation entirely', 'They make gradients unrelated to layers'], 'The identity-like path helps signals move across many blocks.'],
  ['resstream-030-identity-path', 'Mechanism', 'What is meant by an identity path in residual learning?', 'Part of the previous state can pass forward directly through addition', ['The model copies the raw training file', 'Each token becomes its own vocabulary'], 'The old stream remains present in the sum rather than being fully overwritten.'],
  ['resstream-031-information-mixing', 'Mechanism', 'How can one token stream receive information from other tokens?', 'Attention writes can mix information from other positions into the token state', ['The tokenizer randomly swaps words after each layer', 'MLP blocks read the training database directly'], 'Attention is the cross-token communication mechanism that writes results into each token stream.'],
  ['resstream-032-mlp-local', 'Mechanism', 'How is an MLP write different from an attention write?', 'An MLP usually transforms each token position independently after attention has mixed context', ['An MLP always reads future tokens directly', 'An MLP is a separate KV cache'], 'MLPs act positionwise on the current representation.'],
  ['resstream-033-layernorm-role', 'Mechanism', 'What role does layer normalization often play around residual streams?', 'It stabilizes the scale of states read by or produced around sublayers', ['It changes token order by itself', 'It stores external memory'], 'Normalization helps components interact with a predictable hidden-state scale.'],
  ['resstream-034-residual-width', 'Mechanism', 'What shape constraint must residual writes satisfy?', 'They must match the stream width so they can be added elementwise', ['They must match the vocabulary size exactly', 'They must be scalar losses only'], 'Residual addition requires compatible vector shapes.'],
  ['resstream-035-projection-needed', 'Mechanism', 'When might a projection be needed before a residual add?', 'When a component output must be mapped back to the model width', ['When answer choices are shuffled', 'When the browser has a dark theme'], 'Attention and MLP internals can use different shapes but must return the residual width.'],
  ['resstream-036-residual-write', 'Mechanism', 'What is a residual write?', 'A vector update produced by a component and added into the stream', ['A saved copy of the full dataset', 'A token that replaces all context'], 'Calling it a write emphasizes contribution into the shared hidden state.'],
  ['resstream-037-composition', 'Mechanism', 'Why can residual streams support compositional features?', 'Multiple components can add different feature directions into the same representation', ['Every feature must live in a separate model', 'The stream clears itself after each update'], 'The running vector can carry several superposed pieces of information.'],
  ['resstream-038-superposition-caveat', 'Mechanism', 'What is a careful way to describe superposed features?', 'Features may share dimensions and need analysis rather than one-dimension-one-feature assumptions', ['Every neuron maps to exactly one human concept', 'No hidden feature can overlap'], 'Residual representations are distributed, so interpretation requires care.'],
  ['resstream-039-logit-readout', 'Mechanism', 'How do final logits relate to the residual stream?', 'A final readout maps the last stream state to vocabulary scores', ['The stream is ignored after the first layer', 'Logits are stored in the tokenizer'], 'The final hidden representation is converted into output scores.'],
  ['resstream-040-unembedding', 'Mechanism', 'What does an unembedding matrix usually read?', 'The final token residual stream state', ['The raw browser event log', 'Only the padding mask'], 'Language models project final hidden states into vocabulary logits.'],
  ['resstream-041-layer-probing', 'Mechanism', 'What does probing intermediate stream states help reveal?', 'How information changes as layers add component writes', ['Whether CSS classes are sorted', 'Whether the tokenizer has a logo'], 'Layer probes can show where features become available.'],
  ['resstream-042-activation-patching', 'Mechanism', 'What is activation patching in residual-stream analysis?', 'Replacing or copying internal states to test causal effects on behavior', ['Changing the answer choice labels only', 'Sorting tokens before tokenization'], 'Patching tests whether a specific internal representation matters for an output.'],
  ['resstream-043-residual-dropout', 'Mechanism', 'Where might dropout appear around residual writes during training?', 'On sublayer outputs before they are added to the stream', ['On final answer letters after inference', 'Inside the route table only'], 'Some training setups regularize residual updates before addition.'],
  ['resstream-044-parallel-blocks', 'Mechanism', 'What is a parallel residual block variant?', 'Attention and MLP writes can be computed from a shared input and both added', ['Every layer runs in a different browser tab', 'The model deletes the identity path'], 'Some architectures use parallel writes while keeping the residual-stream idea.'],
  ['resstream-045-residual-branch', 'Mechanism', 'Why call attention or MLP a residual branch?', 'It is a side computation whose output is added back to the main stream', ['It is a branch in a database index', 'It replaces the main hidden state with labels'], 'The main path carries x while branches compute updates.'],
  ['resstream-046-norm-growth', 'Mechanism', 'What can happen if residual norms grow unchecked?', 'Hidden states can become unstable or dominated by recent writes', ['The tokenizer gains new words automatically', 'Attention masks become unnecessary'], 'Scale growth can harm trainability and interpretability.'],
  ['resstream-047-feature-erasure', 'Mechanism', 'Can additive writes still suppress earlier information?', 'Yes, later writes can interfere with or counteract earlier feature directions', ['No, addition guarantees perfect preservation of every feature', 'No, components cannot produce negative directions'], 'Residual addition helps preservation but does not make interference impossible.'],
  ['resstream-048-depth-accumulation', 'Mechanism', 'Why does depth make residual-stream management harder?', 'Many components contribute updates that must remain balanced over time', ['Deeper models stop using hidden states', 'Every layer uses a new tokenizer'], 'More layers mean more opportunities for useful accumulation or harmful domination.'],
  ['resstream-049-visual-debug', 'Mechanism', 'What visual debugging view fits this lesson?', 'Before, component write, and after vectors for a selected token', ['Only final generated text', 'Only package version strings'], 'That view directly shows additive residual behavior.'],
  ['resstream-050-mechanism-summary', 'Mechanism', 'What mechanism summary should guide implementation?', 'Keep a width-matched token state and add attention or MLP writes while controlling scale', ['Replace the stream with a memory database', 'Let softmax infer every internal feature'], 'This covers shape, additive update, and stability.'],

  ['resstream-051-write-too-large', 'Application', 'A block output overwhelms all earlier token features. What should you inspect?', 'Residual write scale, normalization, and initialization', ['Only the route name', 'Only the final answer shuffle'], 'Dominating writes usually point to scale-control problems.'],
  ['resstream-052-shape-crash', 'Application', 'A model crashes when adding an MLP output to the stream. What is the likely issue?', 'The write does not match the residual stream width', ['The tokenizer has too many words', 'The attention mask is too visible'], 'Residual addition requires compatible shapes.'],
  ['resstream-053-feature-trace', 'Application', 'You want to know when a subject feature appears. What should you inspect?', 'Intermediate residual stream states across layers', ['Only the final CSS bundle', 'Only the dataset file name'], 'Layer-by-layer probes can reveal when a feature becomes represented.'],
  ['resstream-054-attention-write', 'Application', 'A token needs information from a previous token. Which write path can add it?', 'An attention write into that token residual stream', ['A browser cache write', 'A final sampling-only write'], 'Attention mixes information across positions and writes the result into each stream.'],
  ['resstream-055-mlp-feature', 'Application', 'A positionwise nonlinear feature appears after context has been mixed. Which component may add it?', 'The MLP write for that token state', ['The padding mask alone', 'The route table'], 'MLPs transform the token representation after attention has made context available.'],
  ['resstream-056-ablation', 'Application', 'What ablation tests whether a component write matters?', 'Remove or scale that write and observe the output change', ['Rename the lesson title', 'Sort the answer choices'], 'Changing a write while holding other factors fixed tests its causal contribution.'],
  ['resstream-057-patching', 'Application', 'A clean prompt works but a corrupted prompt fails. How can residual patching help?', 'Copy a suspected clean residual state into the corrupted run and test recovery', ['Change the homepage color', 'Delete the tokenizer vocabulary'], 'Activation patching can identify internal states that carry needed information.'],
  ['resstream-058-skip-connection-bug', 'Application', 'A refactor accidentally removes the residual add around a sublayer. What symptom is plausible?', 'Information and gradients become harder to preserve across depth', ['The model gains infinite context', 'The tokenizer becomes bidirectional'], 'Without the skip path, every layer must rewrite the representation from scratch.'],
  ['resstream-059-post-norm-instability', 'Application', 'Training a very deep post-norm transformer is unstable. What concept is relevant?', 'Residual scale and normalization placement affect gradient flow', ['Output fonts affect hidden states', 'The KV cache replaces training'], 'Norm placement changes how signals move through deep residual stacks.'],
  ['resstream-060-residual-scaling', 'Application', 'Why might a model use residual scaling factors?', 'To keep many accumulated writes from becoming too large', ['To choose the final answer letter', 'To remove all attention heads'], 'Scaling can keep residual updates in a trainable range.'],
  ['resstream-061-long-depth', 'Application', 'A 100-layer model shows exploding residual norms. What should you check?', 'Initialization, normalization, residual scaling, and write magnitudes', ['Only final text punctuation', 'Only data file extensions'], 'Deep residual networks need deliberate scale management.'],
  ['resstream-062-interpretability', 'Application', 'Why do interpretability tools often inspect the residual stream?', 'It is the shared representation many components read from and write to', ['It is an external database of examples', 'It contains only answer choices'], 'The residual stream is a central place to trace model computation.'],
  ['resstream-063-logit-lens', 'Application', 'What does a logit-lens style probe use?', 'Intermediate residual states mapped through a readout to estimate token preferences', ['Only the training loss curve', 'Only the token count'], 'Such probes ask what a partial stream state already implies for output logits.'],
  ['resstream-064-lab-write-strength', 'Application', 'In the lab, increasing one attention write changes the after vector. What should you explain?', 'The after state changes by adding that stronger write to the before state', ['The before state is deleted from memory', 'The tokenizer changes the sentence'], 'The lab is designed to make additive composition visible.'],
  ['resstream-065-negative-write', 'Application', 'Can a component write reduce a feature direction already present in the stream?', 'Yes, a write can add an opposing direction', ['No, every write only increases every feature', 'No, vectors cannot have negative components'], 'Vector addition can reinforce or counteract existing directions.'],
  ['resstream-066-residual-diff', 'Application', 'How can you compare two prompts at a specific layer?', 'Subtract or inspect their residual stream states at the same token position', ['Compare only their route URLs', 'Compare only final page scroll'], 'Residual-state differences can reveal where representations diverge.'],
  ['resstream-067-token-position', 'Application', 'Why must you specify a token position when tracing the stream?', 'Each token slot has its own residual vector', ['There is only one vector for the whole batch', 'Positions are used only after softmax'], 'Residual streams are per token position across the sequence.'],
  ['resstream-068-batch-axis', 'Application', 'A diagnostic mixes stream states from two examples in a batch. What failed?', 'Batch-axis indexing or logging alignment', ['The residual stream became a database', 'The MLP stopped being a vector function'], 'State inspection must keep batch and token axes aligned.'],
  ['resstream-069-checkpoint-mismatch', 'Application', 'A checkpoint expects pre-norm blocks but code loads post-norm blocks. What is the risk?', 'The residual-stream computation no longer matches training', ['The vocabulary automatically repairs the layer order', 'The attention mask becomes a loss mask'], 'Norm placement is part of the architecture contract.'],
  ['resstream-070-quantization', 'Application', 'Why can quantization affect residual-stream behavior?', 'Rounding can change accumulated hidden-state directions and scales', ['Quantization only changes CSS', 'Quantization deletes all residual paths'], 'The stream carries many small contributions that precision changes can disturb.'],
  ['resstream-071-serving-cache', 'Application', 'How is the residual stream different from the KV cache during serving?', 'The stream is the current hidden state; the KV cache stores projected past keys and values', ['They are the same external memory bank', 'The stream is only a prompt string'], 'This distinction prevents confusing forward activations with cached attention tensors.'],
  ['resstream-072-debug-report', 'Application', 'What should a residual-stream debug report include?', 'Layer, token position, before state, component write, after state, norm, and scale controls', ['Only the page title', 'Only the final generated answer'], 'Useful reports make the residual update trace explicit.'],
  ['resstream-073-unit-test', 'Application', 'What unit test fits a residual add implementation?', 'A tiny tensor case where after equals before plus the component write', ['Only one random full model sample', 'Only a screenshot comparison'], 'A small deterministic add catches replacement and shape mistakes.'],
  ['resstream-074-safety-monitoring', 'Application', 'Why might residual activations be monitored in production research?', 'Activation norms or directions can reveal distribution shifts or unstable internals', ['They prove every answer is safe', 'They replace all output evaluation'], 'Internal monitoring can supplement, not replace, behavioral checks.'],
  ['resstream-075-application-summary', 'Application', 'What practical habit prevents residual-stream bugs?', 'Trace a tiny before-write-after example with shapes and norms before trusting deeper behavior', ['Assume the skip path is always correct', 'Only inspect the final loss'], 'Small traces expose whether addition, shape, and scale are correct.'],

  ['resstream-076-false-memory', 'Tricky', 'Which residual-stream claim is false?', 'The residual stream is a separate memory bank outside the forward pass', ['The stream is an evolving hidden representation', 'Components read from and write into the stream'], 'The named misconception is treating the stream as external memory.'],
  ['resstream-077-false-replace', 'Tricky', 'Which update claim is false?', 'Each residual write fully replaces the previous token representation', ['Residual writes are added', 'The previous state can pass forward through the sum'], 'Residual connections preserve an additive path for the old state.'],
  ['resstream-078-false-final-only', 'Tricky', 'Which layer claim is false?', 'Only the final layer has a residual stream', ['Every transformer layer updates hidden states', 'Intermediate streams can be inspected'], 'The stream flows through the stack, not just the last layer.'],
  ['resstream-079-false-scale', 'Tricky', 'Which scale claim is false?', 'Residual write scale never matters because addition is always stable', ['Large writes can dominate', 'Normalization and scaling can matter'], 'Many additions can destabilize or drown out information.'],
  ['resstream-080-false-no-interference', 'Tricky', 'Which preservation claim is false?', 'Residual addition guarantees every earlier feature is perfectly preserved', ['Later writes can interfere', 'Feature directions can be counteracted'], 'Residual paths help but do not eliminate interference.'],
  ['resstream-081-false-shape', 'Tricky', 'Which shape claim is false?', 'A component write can have any width and still be added to the stream', ['Residual writes need compatible width', 'Projection can map outputs back to model width'], 'Elementwise addition requires compatible tensor shapes.'],
  ['resstream-082-false-attention', 'Tricky', 'Which attention claim is false?', 'Attention never reads the residual stream', ['Attention projections are computed from stream states', 'Attention can write mixed context back'], 'Attention uses the current hidden representation as its input.'],
  ['resstream-083-false-mlp', 'Tricky', 'Which MLP claim is false?', 'MLP blocks are external databases attached to the residual stream', ['MLPs transform token states', 'MLP outputs are added as residual writes'], 'MLPs are neural sublayers, not retrieval stores.'],
  ['resstream-084-false-cache', 'Tricky', 'Which cache claim is false?', 'The residual stream and KV cache are the same object', ['The stream is a hidden state', 'The KV cache stores projected past attention tensors'], 'They are related during inference but have different roles.'],
  ['resstream-085-false-norm', 'Tricky', 'Which normalization claim is false?', 'Layer normalization is only a display feature with no effect on stream computation', ['Normalization changes states read by sublayers', 'Norm placement is architectural'], 'Layer norm is part of the model computation.'],
  ['resstream-086-false-gradients', 'Tricky', 'Which gradient-flow claim is false?', 'Residual connections remove the need for backpropagation', ['Residual paths can help gradients flow', 'Training still uses gradients'], 'Skip connections support optimization but do not replace learning.'],
  ['resstream-087-false-logits', 'Tricky', 'Which readout claim is false?', 'Final logits are unrelated to the final residual stream', ['The final stream is mapped to vocabulary scores', 'Readouts use hidden states'], 'The language-model head reads from the last hidden representation.'],
  ['resstream-088-false-probing', 'Tricky', 'Which interpretability claim is false?', 'Intermediate residual states cannot affect model behavior', ['Patching can test causal roles', 'Components build on intermediate states'], 'Internal states matter because later components read them.'],
  ['resstream-089-false-parallel', 'Tricky', 'Which architecture claim is false?', 'Any block ordering is compatible with any checkpoint because residuals are additive', ['Norm placement and block order must match the checkpoint', 'Architecture details affect stream computation'], 'Residual addition does not make architecture contracts interchangeable.'],
  ['resstream-090-tricky-summary', 'Tricky', 'Which belief should you reject?', 'Residual streams are external memory, full replacement, shape-free addition, and scale-proof storage', ['They are running hidden states with additive writes', 'Shapes, norms, and architecture conventions still matter'], 'This rejects the main traps while preserving the central residual idea.'],

  ['resstream-091-interview-definition', 'Interview', 'How should you define the residual stream in one sentence?', 'The running per-token hidden representation that transformer components read from and add writes into', ['A database of all examples seen during training', 'A sampler for final tokens'], 'A strong definition names per-token state, reads, and additive writes.'],
  ['resstream-092-interview-formula', 'Interview', 'What formula should you be ready to cite?', 'x_{l+1} = x_l plus attention and MLP writes, often with normalization around sublayers', ['loss equals tokenizer minus cache', 'softmax replaces hidden states'], 'The formula anchors the additive update without overcommitting to one block variant.'],
  ['resstream-093-interview-why', 'Interview', 'Why are residual streams useful in deep transformers?', 'They preserve information paths and let many components accumulate features across layers', ['They remove all need for attention', 'They store the training set externally'], 'The answer should connect depth, information flow, and feature accumulation.'],
  ['resstream-094-interview-normalization', 'Interview', 'How should you discuss normalization around the stream?', 'Norm placement and residual scaling control the state seen by sublayers and the size of writes', ['Normalization only changes display formatting', 'Scale never matters for residual addition'], 'Scale control is central to stable residual computation.'],
  ['resstream-095-interview-components', 'Interview', 'How should you compare attention and MLP writes?', 'Attention can mix cross-token context, while MLPs transform each token state positionwise', ['Both are external databases', 'Neither writes into the stream'], 'This shows the different jobs of the two main residual branches.'],
  ['resstream-096-interview-debugging', 'Interview', 'What debugging checklist should you give?', 'Check before state, component write, after state, shape, norm, layer, token position, and checkpoint block order', ['Only inspect generated text', 'Only increase model size'], 'Most residual bugs are addition, shape, scale, or architecture-mismatch issues.'],
  ['resstream-097-interview-interpretability', 'Interview', 'How should you explain residual-stream interpretability?', 'Probe or patch intermediate states to test where information appears and whether it causes behavior', ['Assume every dimension has one obvious feature', 'Ignore intermediate layers'], 'Good analysis separates observation from causal tests.'],
  ['resstream-098-interview-cache', 'Interview', 'How should you distinguish the stream from the KV cache?', 'The stream is the current hidden representation; the KV cache stores projected past keys and values for attention', ['They are identical memory banks', 'The stream is only final logits'], 'This distinction matters in serving and debugging.'],
  ['resstream-099-interview-production', 'Interview', 'What production validation should you name?', 'Tiny residual-add tests, shape checks, norm monitoring, checkpoint architecture checks, and activation parity tests', ['Only route smoke tests', 'Only one final response sample'], 'Production readiness requires targeted internal and behavior checks.'],
  ['resstream-100-interview-summary', 'Interview', 'What production-ready residual-stream takeaway should you give?', 'Treat the stream as the central hidden-state bus whose additive writes must keep shape, scale, and architecture aligned', ['Treat it as an external database', 'Assume residuals make every write safe'], 'The final takeaway is disciplined alignment of writes, norms, and block conventions.'],
];

export const RESIDUAL_STREAM_QUIZ = RESIDUAL_STREAM_SPECS.map(([id, level, prompt, correct, distractors, explanation]) => (
  q(id, level, prompt, correct, distractors, explanation)
));
