function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash;
}

function q(number, level, prompt, correct, distractors, explanation) {
  const id = `w2v-${String(number).padStart(3, '0')}`;
  const registryRotation = stableHashNumber(`word2vec:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);
  return Object.freeze({ id, level, prompt, choices: Object.freeze(choices), answerIndex, explanation, questionHash: stableHash(`${prompt}\n${choices.join('\n')}`) });
}

const SPECS = Object.freeze([
  q(1, 'Foundation', 'What problem does Word2Vec address first?', 'It learns dense numeric word vectors from text so words can be compared and modeled.', ['It hand-writes grammar rules for every sentence.', 'It counts vocabulary terms into sparse document rows only.'], 'The lesson starts by moving from symbolic words to learned dense embeddings.'),
  q(2, 'Foundation', 'What is the distributional hypothesis behind Word2Vec?', 'Words used in similar neighborhoods tend to have related meanings.', ['Words with the same spelling always have identical roles.', 'Words near punctuation should be removed from the model.'], 'Word2Vec uses surrounding words as training signal for meaning-like geometry.'),
  q(3, 'Foundation', 'Why is Word2Vec self-supervised?', 'It creates prediction targets from raw text windows instead of needing manual labels.', ['It requires human-written sentiment labels for every word.', 'It never uses examples during training.'], 'Center and context words provide supervision directly from the corpus.'),
  q(4, 'Foundation', 'How does a Word2Vec vector differ from a one-hot vector?', 'It is a compact learned vector where coordinates can support similarity.', ['It has one coordinate for every document in the corpus.', 'It stores only whether a token appeared in one review.'], 'Dense embeddings replace sparse one-hot identity vectors with learned representations.'),
  q(5, 'Foundation', 'What dimensionality does the lesson present as typical for Word2Vec?', 'Roughly 100 to 300 learned dimensions per word.', ['Exactly one dimension per character.', 'One million dimensions for every token.'], 'The intro contrasts vocabulary-sized one-hot vectors with compact 100-300D embeddings.'),
  q(6, 'Foundation', 'What do the numbers in a learned embedding coordinate mean?', 'They are learned parameters shaped by context prediction, not hand-named features.', ['They are alphabetical positions from the dictionary.', 'They are exact counts from one sentence.'], 'Individual embedding coordinates are not usually directly interpretable labels.'),
  q(7, 'Foundation', 'What does Word2Vec try to make true for words with similar usage?', 'Their vectors should land near each other in embedding space.', ['Their spellings should become identical strings.', 'Their document labels should be deleted.'], 'The central intuition is geometric closeness for similar contexts.'),
  q(8, 'Foundation', 'In Skip-gram, what is predicted from what?', 'The model uses a center word to predict surrounding context words.', ['The model uses context words to predict the center word.', 'The model uses full documents to predict punctuation.'], 'Skip-gram points from one center token outward to nearby tokens.'),
  q(9, 'Foundation', 'In CBOW, what is predicted from what?', 'The model uses surrounding context words to predict the center word.', ['The model uses a center word to predict each context word.', 'The model uses analogies to predict document length.'], 'CBOW reverses Skip-gram by pooling context before predicting the target token.'),
  q(10, 'Foundation', 'What does a context window define?', 'Which nearby tokens count as context for a selected center word.', ['Which class labels should be merged together.', 'Which model checkpoints should be saved.'], 'Window size controls the local neighborhood used for training pairs.'),
  q(11, 'Foundation', 'What is a Skip-gram training pair?', 'One observed center word paired with one real neighboring context word.', ['One document paired with its author name.', 'One vector dimension paired with a color.'], 'The Skip-gram panel builds positive center-context examples from a sentence.'),
  q(12, 'Foundation', 'After training, where do word embeddings come from?', 'Rows of a learned embedding weight matrix become word vectors.', ['The final loss curve becomes the vocabulary.', 'The punctuation table becomes the model output.'], 'The lesson explicitly extracts the trained weight matrix as embeddings.'),
  q(13, 'Foundation', 'What probability does the Skip-gram objective maximize?', 'High probability for actual context words given the center word.', ['High probability for random labels given the document title.', 'High probability that every word has the same vector.'], 'The formal objective is log probability of context conditioned on center.'),
  q(14, 'Foundation', 'Why does Word2Vec use negative sampling?', 'It avoids scoring the entire vocabulary for every training update.', ['It creates negative product reviews for sentiment tasks.', 'It removes all rare words from the corpus.'], 'Negative sampling makes the expensive full softmax training step practical.'),
  q(15, 'Foundation', 'What is a positive pair in negative sampling?', 'A center word and an actual context word observed in the training window.', ['Two words selected only because they are alphabetically adjacent.', 'A random word pair known to be unrelated.'], 'Positive examples are real local co-occurrences from the corpus.'),
  q(16, 'Foundation', 'What is a negative sample in this lesson?', 'A vocabulary word treated as not being the observed context for that center word.', ['A word with a negative emotional tone.', 'A token that has been removed by lowercasing.'], 'Negative samples contrast the true neighbor with sampled non-neighbors.'),
  q(17, 'Foundation', 'Which similarity measure does the lesson use for embeddings?', 'Cosine similarity between word vectors.', ['Exact string edit distance only.', 'Raw document length difference.'], 'The embeddings panel measures vector angle to compare words.'),
  q(18, 'Foundation', 'What should learners remember about famous analogies?', 'They are approximate relationship patterns that may appear in trained spaces.', ['They are guaranteed dictionary laws for every corpus.', 'They prove every vector dimension has a name.'], 'The lesson warns that analogy demos are model- and corpus-dependent.'),
  q(19, 'Foundation', 'What is a static-vector limitation of Word2Vec?', 'A word like bank gets one vector even when context changes its sense.', ['Every word receives a new vector for every sentence automatically.', 'The model cannot store any word vector at all.'], 'Classic Word2Vec is not contextual; one vocabulary entry maps to one embedding.'),
  q(20, 'Foundation', 'What is the foundation-level takeaway for Word2Vec?', 'It learns dense word vectors by predicting local context from raw text.', ['It is only a sparse count table with no training objective.', 'It is a supervised classifier that requires human labels.'], 'The first learning milestone is the connection between local prediction and embeddings.'),
  q(21, 'Mechanism', 'With window size two, what context is considered?', 'Up to two tokens on each side of the center word.', ['Only the first two tokens in the entire corpus.', 'All words in every document regardless of distance.'], 'The animation highlights a symmetric local window around the center token.'),
  q(22, 'Mechanism', 'Why can one Skip-gram center word create multiple examples?', 'Each neighboring context word becomes its own center-context pair.', ['Each embedding dimension becomes a new document.', 'Each punctuation mark creates a class label.'], 'Skip-gram makes separate predictions for the nearby words around the center.'),
  q(23, 'Mechanism', 'How does CBOW combine multiple context words?', 'It averages or pools their context embeddings before predicting the target.', ['It sorts them alphabetically and keeps only the first one.', 'It converts them into TF-IDF document rows.'], 'The CBOW panel shows an averaged hidden representation from context vectors.'),
  q(24, 'Mechanism', 'Why is CBOW often faster than Skip-gram?', 'CBOW makes one center-word prediction per window position.', ['CBOW uses no parameters and never trains.', 'CBOW scores every sentence with a grammar parser.'], 'The lesson contrasts one CBOW prediction with multiple Skip-gram predictions.'),
  q(25, 'Mechanism', 'Why can Skip-gram be useful for rarer words?', 'It gives each center occurrence several context-prediction updates.', ['It deletes rare words before any learning happens.', 'It stores rare words as punctuation marks.'], 'More outward predictions can give sparse words more training signal.'),
  q(26, 'Mechanism', 'Why is a full vocabulary softmax expensive?', 'It must compare the center word against every possible output word.', ['It requires every document to be labeled by hand.', 'It can only run on already sorted vocabularies.'], 'The vocabulary-size output layer is the bottleneck negative sampling avoids.'),
  q(27, 'Mechanism', 'How does negative sampling reframe the training task?', 'It trains a binary distinction between observed pairs and sampled non-pairs.', ['It turns word vectors into character-level spelling rules.', 'It forces all words to share one coordinate.'], 'The objective rewards true context pairs and penalizes selected negative pairs.'),
  q(28, 'Mechanism', 'What should happen to the dot product for a positive pair?', 'It should increase so the center and real context vectors align more strongly.', ['It should always become exactly zero.', 'It should be ignored during training.'], 'Positive examples push related vectors toward higher compatibility.'),
  q(29, 'Mechanism', 'What should happen to sampled negative words?', 'Their compatibility with the center word should be pushed lower.', ['They should become the only words updated as positives.', 'They should be copied into the center word string.'], 'Negative examples teach the model which sampled words were not the observed context.'),
  q(30, 'Mechanism', 'How many vectors are updated in the negative-sampling picture?', 'The center, the positive context, and the selected negatives.', ['Every vocabulary row on every update.', 'Only punctuation vectors from the sentence.'], 'The panel emphasizes updating a small selected subset rather than the whole vocabulary.'),
  q(31, 'Mechanism', 'What does vector_size control in the Gensim example?', 'The number of learned embedding dimensions.', ['The number of raw documents in the corpus.', 'The maximum number of CPU workers.'], 'The code uses vector_size to set the embedding dimensionality.'),
  q(32, 'Mechanism', 'What does window control in the Gensim example?', 'The maximum distance used to collect context tokens.', ['The number of analogy answers returned.', 'The file format used for saving vectors.'], 'The training window determines which neighbors create examples.'),
  q(33, 'Mechanism', 'What does min_count control during training?', 'Words below the frequency threshold are ignored by the vocabulary.', ['All words are forced to have identical vectors.', 'The model switches from Skip-gram to CBOW.'], 'min_count is a vocabulary-pruning parameter, not an embedding dimension.'),
  q(34, 'Mechanism', 'What does sg=1 mean in the Gensim Word2Vec call?', 'Train the Skip-gram architecture.', ['Train only a TF-IDF vectorizer.', 'Disable all context windows.'], 'The lesson code marks sg=1 as Skip-gram and sg=0 as CBOW.'),
  q(35, 'Mechanism', 'What does sg=0 mean in the Gensim Word2Vec call?', 'Train the CBOW architecture.', ['Use a contextual transformer encoder.', 'Load only precomputed analogy files.'], 'Gensim toggles between the two Word2Vec architectures with sg.'),
  q(36, 'Mechanism', 'What do epochs control in Word2Vec training?', 'How many passes the model makes over the training data.', ['How many dimensions are shown in t-SNE.', 'How many words are printed in an analogy.'], 'More epochs mean more training iterations over the corpus.'),
  q(37, 'Mechanism', 'What does model.wv expose in the lesson code?', 'The trained keyed word vectors used for similarity and lookup.', ['The raw HTML for the animation panel.', 'The full list of unprocessed sentences only.'], 'Gensim separates the model object from its learned word-vector lookup.'),
  q(38, 'Mechanism', 'What does most_similar return?', 'Nearest words by vector similarity to a query or vector expression.', ['The longest words in the corpus alphabetically.', 'The documents with the most punctuation.'], 'The code uses most_similar for neighbors and analogy-style queries.'),
  q(39, 'Mechanism', 'How are Gensim analogy queries expressed?', 'Positive words are added and negative words are subtracted in vector space.', ['All words are multiplied by their raw counts.', 'Only the first letter of each word is compared.'], 'The lesson demonstrates vector arithmetic through positive and negative lists.'),
  q(40, 'Mechanism', 'What does the Google News example represent?', 'A pre-trained Word2Vec model with 300-dimensional vectors.', ['A tiny model trained on one toy sentence.', 'A supervised classifier for movie reviews.'], 'The code panel loads word2vec-google-news-300 as a ready embedding table.'),
  q(41, 'Mechanism', 'Why save a full Word2Vec model instead of only vectors?', 'The full model can be loaded for continued training.', ['The full model stores fewer parameters than one vector.', 'The full model deletes the vocabulary mapping.'], 'The save/load section distinguishes full models from standalone KeyedVectors.'),
  q(42, 'Mechanism', 'What is a KeyedVectors file useful for?', 'Serving or querying trained vectors without the full training state.', ['Changing sg from Skip-gram to CBOW after training.', 'Recovering labels that were never provided.'], 'KeyedVectors are smaller when only lookup and similarity are needed.'),
  q(43, 'Mechanism', 'Why do TensorFlow and PyTorch examples use embedding layers?', 'They learn lookup tables for target and context word IDs.', ['They manually store English dictionary definitions.', 'They render the t-SNE plot inside the loss.'], 'Embedding layers are trainable matrices indexed by vocabulary IDs.'),
  q(44, 'Mechanism', 'After custom Skip-gram training, what can be extracted?', 'The target embedding weights as learned word vectors.', ['Only the random negative labels as final features.', 'A list of grammar productions from the corpus.'], 'The TensorFlow code takes weights from the target embedding layer.'),
  q(45, 'Mechanism', 'What does a 2D t-SNE plot show in the code panel?', 'A projection for visualization, not the original embedding geometry.', ['The exact two dimensions used by the trained model.', 'The final classification labels for every word.'], 'The visualization compresses high-dimensional vectors to a viewable plane.'),
  q(46, 'Mechanism', 'What does analogy evaluation measure?', 'Whether vector arithmetic retrieves the expected held-out word.', ['Whether every word has a unique spelling.', 'Whether raw counts match a BoW matrix.'], 'Analogy tests score relational patterns in the embedding space.'),
  q(47, 'Mechanism', 'What does semantic similarity analysis compute?', 'Cosine scores for selected word pairs.', ['Manual grammar parses for each sentence.', 'The number of epochs used by each word.'], 'The lesson code prints pairwise similarity values from trained vectors.'),
  q(48, 'Mechanism', 'How does Word2Vec differ from bag of words?', 'Word2Vec learns dense word-level geometry from context prediction.', ['Word2Vec is only a sparse document count matrix.', 'Word2Vec preserves exact sentence order by default.'], 'BoW counts terms in documents; Word2Vec trains embeddings for words.'),
  q(49, 'Mechanism', 'How does Word2Vec differ from GloVe at a high level?', 'Word2Vec trains on local prediction windows instead of first factorizing a global co-occurrence matrix.', ['Word2Vec requires subword n-grams for every token.', 'Word2Vec is a transformer with attention masks.'], 'The surrounding lessons contrast local prediction with global count objectives.'),
  q(50, 'Mechanism', 'What is the mechanism-level Word2Vec story?', 'Build local context examples, optimize predictions, and reuse learned weight rows as embeddings.', ['Count full documents once and skip model training entirely.', 'Attach a dictionary definition to each vector dimension.'], 'A correct mechanism trace connects windows, objectives, updates, and extracted vectors.'),
  q(51, 'Application', 'When is Word2Vec a useful baseline for text features?', 'When semantic similarity between individual words can help a downstream model.', ['When exact phrase order is the only acceptable signal.', 'When no text corpus or pretrained vectors are available.'], 'Word embeddings can feed classifiers, retrieval, clustering, and exploratory analysis.'),
  q(52, 'Application', 'How might a document classifier use Word2Vec?', 'Average or pool word vectors before feeding another model.', ['Treat every word as a separate class label.', 'Replace all tokens with punctuation counts only.'], 'A simple downstream baseline can aggregate token embeddings into document features.'),
  q(53, 'Application', 'When should you train Word2Vec on an in-domain corpus?', 'When you have enough domain text and specialized vocabulary matters.', ['Whenever the corpus has only a few short sentences.', 'Only when manual labels are already perfect.'], 'Domain corpora can teach meanings that generic pretrained vectors miss.'),
  q(54, 'Application', 'When are pretrained Word2Vec vectors attractive?', 'When broad language coverage is useful and local data is limited.', ['When every token is a private product code unseen online.', 'When the task requires contextual word senses.'], 'Pretrained embeddings can be a practical starting point with small datasets.'),
  q(55, 'Application', 'What production problem appears with out-of-vocabulary words?', 'Classic Word2Vec cannot look up words missing from its trained vocabulary.', ['Every unseen word automatically gets a perfect vector.', 'OOV words always become correct analogy answers.'], 'Whole-word Word2Vec has no subword composition for unknown forms.'),
  q(56, 'Application', 'Which model family helps with OOV morphology beyond classic Word2Vec?', 'FastText-style subword embeddings.', ['A full softmax over punctuation only.', 'A random lookup table with no training.'], 'The lesson sequence points to FastText when word pieces matter.'),
  q(57, 'Application', 'Which model family addresses context-dependent word senses?', 'Contextual embeddings such as ELMo or BERT.', ['Static one-vector-per-word lookup alone.', 'A larger min_count threshold only.'], 'Contextual models can give bank different representations in different sentences.'),
  q(58, 'Application', 'Why should Word2Vec embeddings be audited for bias?', 'They can encode social biases present in the training corpus.', ['They are mathematically unable to store associations.', 'They remove all demographic language automatically.'], 'The embeddings panel lists bias as a limitation of learned word vectors.'),
  q(59, 'Application', 'What is the risk with polysemous words like bank?', 'One static vector blends multiple senses into a single representation.', ['The word is always removed by negative sampling.', 'The model creates a separate vector for every sentence.'], 'Static embeddings cannot adapt the word vector to the current sentence.'),
  q(60, 'Application', 'How should analogy results be interpreted in practice?', 'As suggestive diagnostics, not guaranteed proof of understanding.', ['As formal guarantees that the model is unbiased.', 'As a replacement for all downstream evaluation.'], 'The lesson frames analogies as approximate patterns from some trained spaces.'),
  q(61, 'Application', 'How can window size change what embeddings emphasize?', 'Smaller windows tend to capture local syntax, while larger windows can capture broader topical association.', ['Window size changes only the file extension.', 'Larger windows always delete frequent words.'], 'The context definition controls what relationships create training signal.'),
  q(62, 'Application', 'What is the tradeoff in raising min_count?', 'It reduces vocabulary noise but can drop rare useful domain terms.', ['It increases the number of context windows automatically.', 'It turns Skip-gram into a transformer.'], 'Vocabulary pruning is useful but risky for rare terminology.'),
  q(63, 'Application', 'How should the number of negative samples be treated?', 'As a training-efficiency hyperparameter to tune with corpus size and quality.', ['As the number of bad reviews in the dataset.', 'As a guarantee that all words become synonyms.'], 'The panel gives typical ranges rather than a universal constant.'),
  q(64, 'Application', 'What is the tradeoff in choosing embedding dimension?', 'More dimensions can add capacity but also cost and overfitting risk.', ['More dimensions always make evaluation unnecessary.', 'Fewer dimensions always preserve every relation perfectly.'], 'Vector size is a practical modeling choice, not a magic quality switch.'),
  q(65, 'Application', 'Why does tokenization matter before Word2Vec training?', 'It decides which word IDs and contexts the model will actually see.', ['It changes cosine similarity into edit distance.', 'It removes the need for a corpus.'], 'Preprocessing determines the vocabulary and local windows available for learning.'),
  q(66, 'Application', 'How should a Word2Vec model be evaluated for a task?', 'Use held-out downstream metrics or targeted similarity tests relevant to the use case.', ['Rely only on whether a 2D plot looks tidy.', 'Assume analogy demos cover every application.'], 'Useful evaluation must match the intended retrieval, classification, or analysis task.'),
  q(67, 'Application', 'When is plain Word2Vec a poor fit?', 'When exact word order, negation, or sentence-level composition is central.', ['When individual word similarity is relevant.', 'When pretrained vectors are available.'], 'Static word vectors alone do not model full sentence structure.'),
  q(68, 'Application', 'Why might BoW still beat Word2Vec for an audit?', 'BoW features map directly to visible vocabulary counts.', ['BoW always captures synonymy better than embeddings.', 'BoW automatically fixes polysemy.'], 'Interpretability can matter more than semantic smoothness in some workflows.'),
  q(69, 'Application', 'What memory choice is sensible for inference-only similarity lookup?', 'Save and load keyed vectors rather than the full trainable model.', ['Keep optimizer state when no future training is planned.', 'Store only the t-SNE coordinates.'], 'The code panel shows smaller vector-only artifacts for serving.'),
  q(70, 'Application', 'When do you need the full saved Gensim model?', 'When you plan to continue training the Word2Vec model.', ['When you only need nearest-neighbor lookup.', 'When you want to discard all vocabulary data.'], 'The full model preserves training state beyond just the vector table.'),
  q(71, 'Application', 'What should you check before using a pretrained embedding file?', 'Domain, language, tokenization, license, coverage, and evaluation fit.', ['Only whether the file has a short name.', 'Only whether the first analogy looks famous.'], 'Pretrained vectors can mismatch the deployment corpus and constraints.'),
  q(72, 'Application', 'How can embeddings support clustering?', 'Cluster words or documents using distances between learned vectors.', ['Cluster model files by their extensions.', 'Cluster punctuation marks by sentence length only.'], 'The code panel includes clustering over selected word vectors.'),
  q(73, 'Application', 'Why normalize or use cosine for word similarity?', 'It focuses on vector direction rather than raw magnitude alone.', ['It restores original sentence order.', 'It makes all OOV words available.'], 'Cosine is commonly used because direction carries similarity information.'),
  q(74, 'Application', 'What is a common failure when averaging word vectors?', 'The average can lose word order and compositional meaning.', ['The average creates a contextual transformer layer.', 'The average prevents any downstream classifier.'], 'Simple pooling is useful but discards syntax and negation structure.'),
  q(75, 'Application', 'What is the application-level takeaway?', 'Use Word2Vec when local-context semantic geometry helps, but validate coverage, bias, and task fit.', ['Use Word2Vec as a universal replacement for all NLP models.', 'Use analogy examples as the only production acceptance test.'], 'A practical answer balances the embedding benefit with known limitations.'),
  q(76, 'Tricky', 'Which word-order claim is false for classic Word2Vec?', 'Classic Word2Vec does not preserve exact sentence order in the final word vector.', ['Training windows use nearby tokens as learning signal.', 'Window size affects which neighbors create examples.'], 'The model learns from local order-adjacent contexts, but each word still has one static vector.'),
  q(77, 'Tricky', 'Why is the claim "Word2Vec solves polysemy" wrong?', 'A whole-word Word2Vec vocabulary gives each word form one static vector.', ['The bank example shows why context can matter.', 'Contextual embeddings were introduced after static embeddings.'], 'One vector cannot cleanly separate financial bank from river bank by sentence context.'),
  q(78, 'Tricky', 'Why is it unsafe to treat analogy success as proof of fairness?', 'Analogy behavior can expose patterns while embeddings may still encode harmful bias.', ['Bias is listed as a Word2Vec limitation.', 'Analogy queries use vector addition and subtraction.'], 'A few impressive analogies do not replace bias and downstream audits.'),
  q(79, 'Tricky', 'Which negative-sampling interpretation is a trap?', 'Negative samples are not negative-sentiment reviews; they are sampled non-context words.', ['Positive pairs come from observed local windows.', 'The objective separates true pairs from sampled alternatives.'], 'The word negative refers to the training label, not emotional polarity.'),
  q(80, 'Tricky', 'Which CBOW versus Skip-gram statement is false?', 'CBOW and Skip-gram use the same prediction direction.', ['CBOW predicts the center from context.', 'Skip-gram predicts context from the center.'], 'The architectures are mirror images in the lesson panels.'),
  q(81, 'Tricky', 'Why is "full softmax is always cheap" a dangerous claim?', 'A full softmax scales with vocabulary size and can dominate training cost.', ['Negative sampling is introduced as an efficiency method.', 'Vocabulary size is a central training concern.'], 'The lesson motivates negative sampling because full-vocabulary scoring is expensive.'),
  q(82, 'Tricky', 'Why is "more embedding dimensions always win" wrong?', 'Extra dimensions can add cost, noise, and overfitting without task gains.', ['vector_size controls embedding width.', 'Typical dimensions are often around 100 to 300.'], 'Dimension is a hyperparameter that needs validation, not a monotonic quality guarantee.'),
  q(83, 'Tricky', 'Why is "pretrained vectors are always superior" false?', 'Pretrained vectors can mismatch the deployment domain, language, or vocabulary.', ['Pretrained vectors can be useful with limited local data.', 'The code panel demonstrates loading Google News vectors.'], 'Generic coverage helps only when it fits the task and text distribution.'),
  q(84, 'Tricky', 'Why reject "nearest neighbors look good, so evaluation is done"?', 'Qualitative neighbors do not replace held-out task metrics or targeted tests.', ['Nearest-neighbor inspection can be a useful diagnostic.', 'Cosine similarity is a common comparison tool.'], 'Production quality depends on the actual downstream behavior.'),
  q(85, 'Tricky', 'Which supervision claim is false for Word2Vec?', 'Word2Vec requires manually labeled examples for each training pair.', ['The corpus supplies center and context targets automatically.', 'Self-supervision is one reason the method scales.'], 'The training targets are created from raw text windows.'),
  q(86, 'Tricky', 'Why is "OOV words automatically get vectors" wrong for classic Word2Vec?', 'Unseen whole words are missing from the trained lookup table.', ['FastText-style subwords can address many OOV cases.', 'The vocabulary is fixed after training.'], 'Classic Word2Vec lacks the subword composition used by FastText.'),
  q(87, 'Tricky', 'Which cosine-similarity claim is false?', 'Cosine similarity compares only raw vector length.', ['Cosine similarity measures vector direction.', 'Embedding neighbors often use cosine scores.'], 'Cosine normalizes magnitude and focuses on angle.'),
  q(88, 'Tricky', 'Why reject treating a 2D plot as the original embedding space?', 'The plot is a projection that can distort high-dimensional distances.', ['t-SNE is useful for visualization.', 'Real vectors may have hundreds of dimensions.'], 'Visualization helps intuition but is not the original training space.'),
  q(89, 'Tricky', 'Why is "Word2Vec understands language like a human" a trap?', 'It learns statistical context patterns, not grounded human understanding.', ['It can capture useful semantic associations.', 'It can support analogy-like vector operations.'], 'The lesson shows powerful geometry while still naming limitations.'),
  q(90, 'Tricky', 'Which update-cost claim is false for negative sampling?', 'Negative sampling updates every vocabulary vector on each training pair.', ['It updates a selected positive pair and sampled negatives.', 'The efficiency gain comes from avoiding all-output scoring.'], 'The panel emphasizes only updating the small chosen set of vectors.'),
  q(91, 'Interview', 'How would you define Word2Vec in one interview answer?', 'A self-supervised method that learns static dense word embeddings from local context prediction.', ['A supervised parser that labels every dependency edge.', 'A sparse document-term counter with no learned parameters.'], 'A concise definition should include self-supervision, static vectors, and context prediction.'),
  q(92, 'Interview', 'How would you explain the Skip-gram pipeline?', 'Choose a center token, form nearby context pairs, predict those contexts, then keep the learned embeddings.', ['Average all context words first and predict the center only.', 'Count document words once and sort columns alphabetically.'], 'The ordered explanation should follow the Skip-gram animation steps.'),
  q(93, 'Interview', 'How would you compare CBOW and Skip-gram?', 'CBOW predicts a center from pooled context; Skip-gram predicts contexts from a center.', ['Both are contextual transformers with attention masks.', 'Both ignore context windows and train only on labels.'], 'The key contrast is prediction direction and number of examples per window.'),
  q(94, 'Interview', 'How would you explain negative sampling clearly?', 'Train real center-context pairs against a few sampled non-context words instead of a full softmax.', ['Generate bad reviews so sentiment classifiers have more data.', 'Delete all frequent words before fitting the vocabulary.'], 'A good answer explains both the binary contrast and the efficiency reason.'),
  q(95, 'Interview', 'How should an interview answer handle analogies?', 'Say they reveal approximate relational geometry, then mention corpus dependence and bias risk.', ['Claim they prove the model has complete language understanding.', 'Avoid mentioning their limitations because the demo is famous.'], 'The lesson presents analogies as useful but not universal or fairness-proof.'),
  q(96, 'Interview', 'Which parameters would you discuss when training Word2Vec?', 'Architecture, vector size, window, min_count, negatives, epochs, and corpus preprocessing.', ['Only the color palette of the t-SNE plot.', 'Only the order of multiple-choice answers.'], 'The code panel highlights these knobs as practical training choices.'),
  q(97, 'Interview', 'How would you diagnose weak Word2Vec results?', 'Check corpus size, tokenization, vocabulary coverage, window, dimensions, negatives, and task evaluation.', ['Assume all embeddings are correct if training finished.', 'Only increase dimensions without inspecting data.'], 'A rigorous diagnosis starts from data and objective before tuning blindly.'),
  q(98, 'Interview', 'How would you compare Word2Vec with nearby methods?', 'BoW is sparse counts, GloVe uses global co-occurrence, FastText adds subwords, and BERT is contextual.', ['All four methods are identical dense lookup tables.', 'Only BERT can ever be used for word similarity.'], 'This contrast places Word2Vec in the broader NLP representation timeline.'),
  q(99, 'Interview', 'What production risks should you name for Word2Vec?', 'OOV coverage, domain mismatch, bias, static meanings, preprocessing drift, and downstream validation.', ['Only the number of visible points in a 2D chart.', 'Only whether the analogy king-man+woman works once.'], 'Production use requires monitoring the assumptions that embeddings depend on.'),
  q(100, 'Interview', 'What demonstrates interview-ready Word2Vec mastery?', 'Connecting local-context objectives to dense geometry, efficient training, practical use, and limitations.', ['Repeating that Word2Vec is always the best NLP representation.', 'Memorizing one analogy while ignoring how training works.'], 'Mastery means explaining the mechanism and knowing when the representation is appropriate.'),
]);

export const WORD2VEC_QUIZ = SPECS;
