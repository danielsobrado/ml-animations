function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  return hash;
}

function q(number, level, prompt, correct, distractors, explanation) {
  const id = `tool-${String(number).padStart(3, '0')}`;
  const registryRotation = stableHashNumber(`tool-using-reasoning-models:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);
  return Object.freeze({ id, level, prompt, choices: Object.freeze(choices), answerIndex, explanation, questionHash: stableHash(`${prompt}\n${choices.join('\n')}`) });
}

const SPECS = Object.freeze([
  q(1, 'Foundation', 'What defines a tool-using reasoning model?', 'It interleaves internal reasoning with external actions and observations.', ['It only retrieves one passage before answering.', 'It disables all structured outputs.'], 'The core lesson loop is reason, choose an action, observe the result, and revise.'),
  q(2, 'Foundation', 'What is the basic think-act-observe loop?', 'Plan what is missing, call a tool, read the observation, then update the plan.', ['Train new weights after each user prompt.', 'Always answer before seeing tool results.'], 'Tool use is a control loop rather than a single static context addition.'),
  q(3, 'Foundation', 'How does this differ from ordinary RAG?', 'RAG often retrieves before generation, while tool reasoning can choose actions mid-solution.', ['RAG is the only method that can read documents.', 'Tool reasoning cannot use search results.'], 'The important distinction is dynamic action selection during the reasoning process.'),
  q(4, 'Foundation', 'What is a tool observation?', 'The returned result from an executed tool call that the model can inspect.', ['The tool schema before any action is executed.', 'A hidden token that replaces the final answer.'], 'Observations are external feedback, such as search snippets, code output, or file text.'),
  q(5, 'Foundation', 'What does a search tool mainly solve?', 'Fresh, external, or detailed facts not reliable in frozen parameters.', ['Exact arithmetic inside attention layers.', 'Permission approval for destructive actions.'], 'Search is valuable when current evidence or web-scale context is needed.'),
  q(6, 'Foundation', 'What does a Python tool mainly solve?', 'Calculation, simulation, plotting, and executable verification.', ['Current legal facts without browsing.', 'User identity authorization.'], 'Python turns fragile mental computation into reproducible executable checks.'),
  q(7, 'Foundation', 'What does file analysis mainly solve?', 'Grounding answers in user-provided documents, tables, or code files.', ['Changing model weights permanently.', 'Avoiding every prompt-injection risk automatically.'], 'File tools connect the answer to local evidence that may not exist online.'),
  q(8, 'Foundation', 'What does browser or computer use mainly solve?', 'Interactive UI tasks that require observing and acting on a screen.', ['Static document retrieval without interaction.', 'Removing the need for user permissions.'], 'Computer-use tools let agents navigate stateful interfaces with higher side-effect risk.'),
  q(9, 'Foundation', 'What does a function call provide?', 'A structured way to invoke a known API with arguments.', ['A guarantee that the plan is safe.', 'A replacement for all observations.'], 'Function calling is a tool invocation format, not the whole agent policy.'),
  q(10, 'Foundation', 'What is agent planning?', 'Choosing, ordering, and revising actions over multiple steps based on feedback.', ['Emitting a single JSON object and stopping.', 'Ignoring observations to keep the first plan unchanged.'], 'Planning is the control logic around possible tool calls.'),
  q(11, 'Foundation', 'What question should precede a tool call?', 'What uncertainty would this action reduce, and is it worth the cost and risk?', ['Which tool was used most often in past traces?', 'Can the model hide the observation from logs?'], 'The lesson models tool policy as benefit minus latency, cost, and safety risk.'),
  q(12, 'Foundation', 'What does tool-call precision measure?', 'The share of made tool calls that were actually useful or necessary.', ['The byte length of a function schema.', 'The number of model layers exposed to APIs.'], 'Low precision indicates unnecessary tool use and wasted latency.'),
  q(13, 'Foundation', 'What does tool-call recall measure?', 'Whether needed tool calls were made when uncertainty required them.', ['How many optional tools were exposed.', 'Whether every available tool was called.'], 'Low recall means the model guessed when it should have grounded or verified.'),
  q(14, 'Foundation', 'What is tool overuse?', 'Calling tools repeatedly when direct reasoning or context would suffice.', ['Refusing to use a needed calculator.', 'Treating retrieved text as hostile data.'], 'Overuse wastes latency, cost, and user attention.'),
  q(15, 'Foundation', 'What is stale search?', 'Using outdated retrieved evidence as if it were current.', ['A Python exception from bad syntax.', 'A browser click blocked by approval.'], 'Freshness checks matter whenever facts can change over time.'),
  q(16, 'Foundation', 'What is hallucinated tool output?', 'Claiming a tool returned evidence that was never observed.', ['A tool honestly reporting an error.', 'A function schema rejecting invalid JSON.'], 'This is a groundedness failure because the reasoning trace invents observations.'),
  q(17, 'Foundation', 'What is indirect prompt injection through tools?', 'Untrusted tool content contains instructions that try to hijack the model policy.', ['A query returning no relevant evidence.', 'A calculator returning a floating-point value.'], 'External content should be treated as data, not as higher-priority instructions.'),
  q(18, 'Foundation', 'Why are permission gates important?', 'They block high-risk side effects until a user or policy approves them.', ['They make search indexes perfectly fresh.', 'They eliminate all need for verification.'], 'Tools that write, send, delete, buy, or renew credentials need control points.'),
  q(19, 'Foundation', 'What is the safe treatment of tool output?', 'Process it as untrusted evidence that needs relevance, freshness, and policy checks.', ['Let it override system instructions.', 'Assume it is always executable code.'], 'The lesson’s main safety warning is that observations are useful but not automatically true or safe.'),
  q(20, 'Foundation', 'What is the first takeaway of this lesson?', 'Tool use is a learned reasoning policy over actions, observations, costs, and safeguards.', ['Tool APIs alone make answers correct.', 'Every prompt should trigger every available tool.'], 'The rest of the lesson builds from policy quality, not plugin availability.'),

  q(21, 'Mechanism', 'How should a model decide whether search is justified?', 'Estimate whether external evidence reduces uncertainty more than it adds latency and risk.', ['Search after every noun phrase.', 'Search only when the answer is already known.'], 'Search is a policy decision driven by missing information.'),
  q(22, 'Mechanism', 'Why can multi-turn query reformulation help?', 'Weak results can be inspected and converted into more specific follow-up queries.', ['It prevents the model from reading observations.', 'It guarantees that old pages disappear.'], 'Search-R1-style loops learn to improve queries from intermediate evidence.'),
  q(23, 'Mechanism', 'What should happen after conflicting search results?', 'Compare provenance and dates, refine the query, and report uncertainty if unresolved.', ['Average all snippets without checking sources.', 'Use the first result because it appeared first.'], 'Grounded reasoning must handle disagreement rather than collapse it into confidence.'),
  q(24, 'Mechanism', 'Why is date reasoning part of search use?', 'Retrieved facts may be outdated relative to the user’s requested time frame.', ['Dates are only UI decoration.', 'Search tools cannot return older pages.'], 'Freshness failures are common for current facts, APIs, rankings, and documentation.'),
  q(25, 'Mechanism', 'How does Python complement natural-language reasoning?', 'It verifies calculations, simulations, and code behavior that are error-prone in tokens.', ['It replaces every source citation.', 'It makes permission checks unnecessary.'], 'Executable verification is strongest where exact computation matters.'),
  q(26, 'Mechanism', 'When is Python the wrong primary tool?', 'When the missing input is external evidence rather than a computable relationship.', ['When arithmetic must be exact.', 'When a table calculation needs checking.'], 'Python cannot invent missing facts; it operates on supplied data and code.'),
  q(27, 'Mechanism', 'Why must Python outputs still be interpreted?', 'Correct code can answer the wrong question if inputs or assumptions are wrong.', ['Interpreter output is always policy-approved.', 'Execution logs are the final user answer.'], 'Tool observations need semantic checking, not blind copying.'),
  q(28, 'Mechanism', 'What should file-grounded answering preserve?', 'Claims should trace to the provided document or table evidence.', ['Plausible facts from model memory should override missing files.', 'The model should ignore file boundaries.'], 'File tools are used to distinguish supported claims from guesses.'),
  q(29, 'Mechanism', 'Why is chunk selection risky in file analysis?', 'The retrieved chunk may omit the relevant clause, row, or surrounding context.', ['Files cannot contain adversarial text.', 'Every PDF chunk has identical meaning.'], 'Wrong or incomplete context can produce confident but unsupported answers.'),
  q(30, 'Mechanism', 'How should untrusted document instructions be handled?', 'As content to analyze, not instructions that can change the agent’s policy.', ['As system messages with higher priority.', 'As automatic authorization for tool calls.'], 'This is the core defense against prompt injection in retrieved content.'),
  q(31, 'Mechanism', 'What distinguishes function calling from planning?', 'Function calling formats one action; planning manages a sequence of decisions and observations.', ['Planning is only JSON validation.', 'Function calling forbids external APIs.'], 'A valid function call can still be part of a bad plan.'),
  q(32, 'Mechanism', 'Why do schemas help tool use?', 'They constrain arguments so APIs receive predictable structured inputs.', ['They prove the chosen action is necessary.', 'They make all returned facts correct.'], 'Schemas reduce invocation errors but do not solve policy or truthfulness alone.'),
  q(33, 'Mechanism', 'What is an agent trajectory?', 'A sequence of thoughts, tool calls, observations, and final responses.', ['A static list of model hyperparameters.', 'Only the final answer string.'], 'Trajectories are what training, evaluation, and debugging inspect.'),
  q(34, 'Mechanism', 'What does tool-result masking do in RL training?', 'It keeps tool returns as context while excluding them from model-generation loss.', ['It hides tool output from the model at inference.', 'It deletes all final-answer tokens.'], 'Masking prevents the model from being trained to imitate observations it did not generate.'),
  q(35, 'Mechanism', 'Why mask observation tokens from the policy loss?', 'Tool outputs are environment data, not actions produced by the policy.', ['Observation tokens are never useful context.', 'All losses must ignore model queries too.'], 'The model should learn when and how to query, not claim credit for retrieved text.'),
  q(36, 'Mechanism', 'Which tokens should remain trainable in a search trajectory?', 'Model-generated queries, reasoning decisions, and final answer tokens.', ['Raw search-result snippets only.', 'Browser screenshots converted to pixels only.'], 'The policy learns its actions and answer behavior while observations condition the next step.'),
  q(37, 'Mechanism', 'How can reward shaping discourage tool overuse?', 'Penalize unnecessary calls while rewarding correct grounded answers.', ['Reward the largest number of calls.', 'Ignore latency and cost in all tasks.'], 'A useful reward balances answer quality with efficient action choice.'),
  q(38, 'Mechanism', 'How can reward shaping cause tool underuse?', 'A latency penalty can become too strong, making the model avoid needed tools.', ['Search always has zero cost.', 'Python always changes the model weights.'], 'Bad penalties can push the policy into confident guessing.'),
  q(39, 'Mechanism', 'Why log observations explicitly?', 'Auditable logs let verifiers compare claims against actual tool returns.', ['Logs make stale pages current.', 'Logs remove all privacy concerns automatically.'], 'Explicit observations help catch hallucinated tool output.'),
  q(40, 'Mechanism', 'What does a quote verifier check?', 'Whether cited text actually appears in the retrieved or read source.', ['Whether the final answer sounds confident.', 'Whether the tool schema is compact.'], 'Quote checks are a concrete guard against fabricated evidence.'),
  q(41, 'Mechanism', 'Why limit max tool calls?', 'To prevent loops, runaway cost, and repeated unproductive actions.', ['To make every task use the same trajectory length.', 'To stop the model from reading any observation.'], 'Budgets are part of safe operational control.'),
  q(42, 'Mechanism', 'What is loop progression tracking?', 'Checking whether repeated actions are adding new evidence or just cycling.', ['Counting only prompt characters.', 'Deleting the agent memory after every step.'], 'Repeated identical searches or clicks should trigger a stop or strategy change.'),
  q(43, 'Mechanism', 'Why are side-effecting tools special?', 'Their actions can change external state, so failures may be costly or irreversible.', ['They cannot return observations.', 'They are always read-only.'], 'Write, send, delete, renew, purchase, and deploy actions need stricter controls.'),
  q(44, 'Mechanism', 'What does least privilege mean for tools?', 'Expose only the tools and permissions needed for the current task.', ['Give every agent admin access.', 'Let retrieved text choose permissions.'], 'Limited capability reduces blast radius when the policy is tricked.'),
  q(45, 'Mechanism', 'Why use read-only tools before write tools?', 'They gather evidence with lower risk before any state-changing action.', ['Read-only tools always answer every task.', 'Write tools never need approval.'], 'Risk should rise only when the plan justifies it.'),
  q(46, 'Mechanism', 'How should memory be treated as a tool?', 'As persistent context that may be stale or privacy-sensitive.', ['As a perfect current database.', 'As an authorization system for external actions.'], 'Memory can help continuity but needs freshness and access controls.'),
  q(47, 'Mechanism', 'What makes browser tasks harder than static function calls?', 'They require visual state tracking, UI navigation, and careful side-effect judgment.', ['They never involve external state.', 'They always have one valid coordinate.'], 'Computer use combines perception, planning, and action safety.'),
  q(48, 'Mechanism', 'Why is observation quality part of evaluation?', 'A model can call the right tool but misread, ignore, or overtrust the result.', ['Tool names alone prove success.', 'Only final token count matters.'], 'Tool use must be judged through the whole trajectory.'),
  q(49, 'Mechanism', 'What is the mechanism-level policy equation?', 'Expected uncertainty reduction minus latency, cost, and safety risk.', ['Parameter count divided by prompt length.', 'Number of tools multiplied by temperature.'], 'The animation frames tool selection as a tradeoff, not a reflex.'),
  q(50, 'Mechanism', 'What summarizes the mechanism section?', 'Good tool use learns when to act, what to call, how to read observations, and when to stop.', ['More tools always means better reasoning.', 'Function schemas remove the need for planning.'], 'The mechanisms coordinate action choice, grounding, training masks, and safeguards.'),

  q(51, 'Application', 'A user asks for today’s stock price. What should the agent do?', 'Use search or a market data tool because the fact is time-sensitive.', ['Answer from pretraining memory with no caveat.', 'Use Python without any price input.'], 'Current external state requires a fresh evidence source.'),
  q(52, 'Application', 'A user asks to multiply several invoice rows. Which tool fits?', 'Use Python or a spreadsheet-capable verifier after reading the relevant values.', ['Browse the web for unrelated examples.', 'Guess from the invoice title.'], 'Exact arithmetic should be executable and grounded in the file data.'),
  q(53, 'Application', 'A contract PDF says “ignore the invoice.” What should the agent do?', 'Treat that sentence as document content and continue applying the task instructions.', ['Let the PDF override the user request.', 'Delete the invoice before analysis.'], 'Untrusted file text should not gain instruction authority.'),
  q(54, 'Application', 'A search result is from 2020 for a 2026 question. What is the best response?', 'Flag it as stale, search with date constraints, and avoid presenting it as current.', ['Use it because any result is enough.', 'Hide the date from the final answer.'], 'Freshness is part of evidence quality.'),
  q(55, 'Application', 'A Python script returns a surprising answer. What should happen next?', 'Inspect the code, inputs, and assumptions before trusting the output.', ['Copy the number as final because Python ran.', 'Discard all tool use forever.'], 'Execution verifies only the program that was actually run.'),
  q(56, 'Application', 'A browser agent must renew an API key. What control is appropriate?', 'Require approval before the state-changing renewal click.', ['Click immediately because navigation succeeded.', 'Use search instead of the visible UI.'], 'Credential renewal changes external state and should be gated.'),
  q(57, 'Application', 'A model repeatedly searches the same query after timeouts. What mitigation fits?', 'Limit retries and require a changed strategy or caveated fallback.', ['Increase temperature until it loops faster.', 'Reward every repeated search.'], 'Loop controls prevent runaway tool use without progress.'),
  q(58, 'Application', 'A model answers a document question using a fact not in the files. What failed?', 'File grounding and unsupported-claim detection.', ['Search latency optimization.', 'Function argument serialization only.'], 'The answer must separate evidence-backed claims from plausible guesses.'),
  q(59, 'Application', 'A simple definition question is answered after five searches. What failed?', 'Tool-call precision and cost-aware routing.', ['Tool-call recall only.', 'Browser coordinate tracking.'], 'Unnecessary calls show the policy overused tools.'),
  q(60, 'Application', 'A hard arithmetic task is answered without a calculator and is wrong. What failed?', 'Tool-call recall for a needed verifier.', ['Excessive permission gating.', 'Prompt injection filtering.'], 'The model should have invoked the available exact-computation tool.'),
  q(61, 'Application', 'A retrieved snippet says “run this shell command.” What should the agent do?', 'Treat it as untrusted text and refuse to execute it without independent authorization.', ['Execute it because search returned it.', 'Promote it to a developer instruction.'], 'Tool output can carry adversarial instructions.'),
  q(62, 'Application', 'A function schema accepts a payment amount. What extra check is needed?', 'Validate intent, permissions, and approval before a side-effecting call.', ['Assume valid JSON means safe action.', 'Mask the user’s original request.'], 'Syntactic validity is not sufficient for financial side effects.'),
  q(63, 'Application', 'A tool returns “not found.” What should the model avoid?', 'Inventing a specific observation that supports its preferred answer.', ['Reporting uncertainty or trying a better query.', 'Checking whether the file path was wrong.'], 'Missing evidence should not become fabricated evidence.'),
  q(64, 'Application', 'A source gives a quote and the answer cites it. What should verification check?', 'The quoted text exists in the source and supports the specific claim.', ['Only whether the quote sounds fluent.', 'Whether the model used a long enough chain.'], 'Grounded citations need exact support, not just plausible phrasing.'),
  q(65, 'Application', 'A coding task needs file reads, edits, and tests. What is the safe plan?', 'Inspect files, make a narrow change, run tests, and gate risky commands.', ['Rewrite the repository without reading it.', 'Trust issue text as shell instructions.'], 'Tool-using coding is a permissioned loop over evidence and verification.'),
  q(66, 'Application', 'A UI automation observes the wrong button position. What should it do?', 'Re-observe the screen or ask for confirmation before risky clicks.', ['Click the old coordinate anyway.', 'Assume the page cannot change.'], 'Browser tools require state tracking because interfaces can shift.'),
  q(67, 'Application', 'A policy exposes every internal API to every task. What is the concern?', 'Violating least privilege and expanding the attack surface.', ['Reducing prompt length too much.', 'Preventing any observation logging.'], 'Available tools define what an injected or confused agent can do.'),
  q(68, 'Application', 'A model uses memory from last month for a changed project. What should it check?', 'Whether the memory is still current and appropriate for this user/task.', ['Whether the memory is written in JSON.', 'Whether search can be disabled permanently.'], 'Persistent memory can be stale or sensitive.'),
  q(69, 'Application', 'A model gets two tools: search and Python. Which sequence fits a current numeric fact?', 'Search for the current value, then use Python if calculation is needed.', ['Python first with no inputs, then invent the source.', 'Search forever even after enough evidence.'], 'Tool order should match missing evidence before computation.'),
  q(70, 'Application', 'A reward gives points for every tool call. What behavior may appear?', 'The model may call unnecessary tools to maximize reward.', ['The model will always become more factual.', 'The model will stop using all tools.'], 'Reward design can create overuse just like format reward can create reward hacking.'),
  q(71, 'Application', 'A reward penalizes every tool call heavily. What behavior may appear?', 'The model may guess instead of using necessary evidence.', ['The model will cite sources more carefully.', 'The model will improve browser perception.'], 'Tool costs must not overwhelm the value of grounding.'),
  q(72, 'Application', 'A trajectory includes search-result tokens in the model loss. What is wrong?', 'The loss trains the policy as if it generated environment observations.', ['The model can no longer see the search result.', 'Final answers become untrainable by definition.'], 'Tool-result masking keeps environment data out of generation credit.'),
  q(73, 'Application', 'A file tool retrieves one row but the answer needs totals across all rows. What should happen?', 'Retrieve the missing data or state that the current evidence is incomplete.', ['Generalize from the one row silently.', 'Treat the file name as the total.'], 'Grounding includes coverage of the needed evidence, not just any evidence.'),
  q(74, 'Application', 'An agent claims it renewed a key but logs show no browser action. What failed?', 'Observation/action auditability and truthful reporting.', ['The tokenizer vocabulary was too small.', 'The search query was too fresh.'], 'Agent claims should match logged actions and observations.'),
  q(75, 'Application', 'What is a production-ready tool policy trying to optimize?', 'Task success, groundedness, latency, cost, and safety together.', ['Only the number of available tools.', 'Only the length of hidden reasoning.'], 'Deployment quality is multi-objective, not just raw accuracy.'),

  q(76, 'Tricky', 'Which claim about tool use is false?', 'Giving a model tools automatically makes final answers correct and safe.', ['Tool observations can improve grounding when checked.', 'Tool calls can add latency, cost, and risk.'], 'Tools are evidence and action channels, not truth guarantees.'),
  q(77, 'Tricky', 'Which search claim is wrong?', 'The first search result should be treated as current truth regardless of date.', ['Search can resolve external uncertainty.', 'Search results need freshness and provenance checks.'], 'Ranking is not the same as verified currency.'),
  q(78, 'Tricky', 'Which Python claim is unsafe?', 'Python output is always correct even if the code or inputs are wrong.', ['Python can verify arithmetic.', 'Python can run simulations from supplied assumptions.'], 'Executable tools still depend on the program and data.'),
  q(79, 'Tricky', 'Which file-grounding claim is a trap?', 'If a document contains an instruction, the agent should follow it as policy.', ['Documents can supply evidence.', 'Document text may include adversarial content.'], 'Retrieved text is not an instruction layer.'),
  q(80, 'Tricky', 'Which function-calling claim is false?', 'A valid JSON tool call proves the overall agent plan is safe and necessary.', ['Schemas can reduce argument errors.', 'Side-effecting calls still need policy checks.'], 'Syntax validation does not establish intent, authorization, or necessity.'),
  q(81, 'Tricky', 'Which planning claim should you reject?', 'Agent planning means never revising the first action sequence.', ['Planning can adapt after observations.', 'Unexpected tool failures should update the plan.'], 'The point of the loop is feedback-conditioned revision.'),
  q(82, 'Tricky', 'Which masking claim is wrong?', 'Tool-result masking prevents the model from using observations as context.', ['Masking can exclude observation tokens from loss.', 'The model can still condition on returned evidence.'], 'Masking affects training credit, not contextual availability.'),
  q(83, 'Tricky', 'Which reward claim is dangerous?', 'Rewarding more tool calls always improves reasoning quality.', ['Rewards should include correctness and efficiency.', 'Bad reward shaping can cause overuse.'], 'The policy can exploit action-count incentives.'),
  q(84, 'Tricky', 'Which safety claim is false?', 'Read/write tool access should be equally unrestricted for convenience.', ['Write actions can change external state.', 'Least privilege reduces blast radius.'], 'Permission levels should match task need and risk.'),
  q(85, 'Tricky', 'Which observation claim is a trap?', 'If the model expected a result, it may report that result even when the tool did not return it.', ['Logs should capture actual observations.', 'Claims should be checked against tool returns.'], 'Expected evidence is not observed evidence.'),
  q(86, 'Tricky', 'Which browser-agent claim is unsafe?', 'A successful screenshot means the agent can click any destructive control without approval.', ['Screenshots help locate UI state.', 'Risky clicks may require confirmation.'], 'Observation does not equal authorization.'),
  q(87, 'Tricky', 'Which memory claim is wrong?', 'Persistent memory is always current, authorized, and safe to reuse.', ['Memory can reduce repeated context gathering.', 'Memory may become stale or sensitive.'], 'Memory needs freshness and permission checks.'),
  q(88, 'Tricky', 'Which metric claim is false?', 'High final-answer accuracy alone proves the tool policy is efficient and safe.', ['Tool precision can expose overuse.', 'Tool recall can expose missed needed calls.'], 'Trajectory metrics catch failures that final accuracy hides.'),
  q(89, 'Tricky', 'Which loop claim is a trap?', 'Repeating the same failed search indefinitely is a valid persistence strategy.', ['Retries should have limits.', 'A changed query or fallback may be needed.'], 'Persistence without new information becomes an infinite loop.'),
  q(90, 'Tricky', 'Which deployment claim should be rejected?', 'Accuracy on a static QA set is sufficient for deploying tool-using agents.', ['Deployment evals should include tools and permissions.', 'Prompt injection and side effects require testing.'], 'Tool agents need workflow and safety evaluation, not only answer scoring.'),

  q(91, 'Interview', 'How would you summarize tool-using reasoning in an interview?', 'It is a feedback loop where the model chooses external actions to reduce uncertainty under cost and safety constraints.', ['It is just a plugin menu attached to chat.', 'It is ordinary RAG with a longer prompt.'], 'A strong answer names action choice, observations, tradeoffs, and safeguards.'),
  q(92, 'Interview', 'How would you explain search as a learned action?', 'The policy learns when to query, how to reformulate, and when evidence is sufficient.', ['Search is mandatory before every token.', 'Search replaces the need to evaluate sources.'], 'Modern training can shape query generation and stopping behavior.'),
  q(93, 'Interview', 'How would you explain Python’s role?', 'Python is a deterministic verifier for calculations, simulations, code behavior, and data checks.', ['Python supplies all current world facts automatically.', 'Python authorizes side effects by running.'], 'The key is executable verification, bounded by correct inputs and assumptions.'),
  q(94, 'Interview', 'How would you distinguish function calls from agents?', 'Function calls are structured invocations; agents use planning loops to decide and revise invocations.', ['Agents cannot use function schemas.', 'Function calls automatically ensure safety.'], 'This distinction prevents confusing mechanism with control policy.'),
  q(95, 'Interview', 'How would you describe Search-R1-style masking?', 'Keep retrieved text in context, but mask observation tokens from the policy loss.', ['Delete search results before the model reads them.', 'Train the model to claim it generated search snippets.'], 'The model should learn actions and answers, not receive credit for environment tokens.'),
  q(96, 'Interview', 'How would you debug tool overuse?', 'Inspect trajectories, measure tool precision, add cost penalties, and test direct-answer cases.', ['Add more tools and reward every call.', 'Remove all logging to reduce overhead.'], 'Diagnosis needs action logs and counterexamples where tools are unnecessary.'),
  q(97, 'Interview', 'How would you debug stale search?', 'Check dates, source provenance, query wording, and whether the final answer preserves uncertainty.', ['Assume ranking implies freshness.', 'Ignore timestamps because search was used.'], 'Freshness failures are visible in evidence and final claim handling.'),
  q(98, 'Interview', 'How would you defend against prompt injection through tools?', 'Treat external content as data, preserve instruction hierarchy, restrict tools, and validate actions.', ['Let retrieved pages define new policies.', 'Disable all source display and trust the answer.'], 'Defense is layered across prompts, permissions, parsing, and monitoring.'),
  q(99, 'Interview', 'How would you evaluate a tool-using agent?', 'Score task success plus trajectories, precision, recall, latency, cost, grounding, and unsafe actions.', ['Only count whether any final answer was produced.', 'Only inspect hidden model weights.'], 'Tool-agent evaluation must include behavior over tools and side effects.'),
  q(100, 'Interview', 'What is the production-ready takeaway?', 'Deploy tool agents with calibrated action policies, audited observations, least privilege, and workflow tests.', ['Expose every tool and trust final fluency.', 'Use static QA accuracy as the only gate.'], 'Strong deployment decisions require evidence that the system acts correctly and safely.'),
]);

export const TOOL_USING_REASONING_MODELS_QUIZ = SPECS;
