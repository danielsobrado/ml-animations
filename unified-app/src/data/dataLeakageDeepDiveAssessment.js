function q(id, level, prompt, correct, distractors, explanation) {
  const questionNumber = Number(id.match(/^leak-(\d{3})-/)?.[1] || 1);
  const desiredFinalIndex = (questionNumber - 1) % 3;
  const registryRotation = stableHash(`data-leakage-deep-dive:${id}`) % 3;
  const answerIndex = (desiredFinalIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return {
    id,
    level,
    prompt,
    choices,
    answerIndex,
    explanation,
  };
}

function stableHash(value) {
  return [...String(value)].reduce((hash, char) => ((hash * 31) + char.charCodeAt(0)) >>> 0, 0);
}

export const DATA_LEAKAGE_DEEP_DIVE_QUIZ = Object.freeze([
  q('leak-001-definition', 'Foundation', 'What is data leakage?', 'Information unavailable at prediction time or evaluation time influences training or model selection', ['A model has fewer features than expected', 'A validation score is lower than training score'], 'Leakage makes evaluation easier than the real prediction problem.'),
  q('leak-002-boundary', 'Foundation', 'What boundary does leakage usually cross?', 'The boundary between allowed training information and held-out or future information', ['The boundary between two color themes', 'The boundary between batch size and learning rate'], 'A leakage audit asks what information was allowed to move where.'),
  q('leak-003-optimism', 'Foundation', 'Why is leakage dangerous?', 'It creates offline scores that are too optimistic for deployment', ['It always makes training slower', 'It prevents the model from fitting parameters'], 'The model can appear strong because it used information it will not have later.'),
  q('leak-004-target-derived', 'Foundation', 'What is a target-derived feature?', 'A feature built from the label, outcome, or post-outcome event', ['A raw feature observed before prediction', 'A feature scaled from training rows only'], 'Target-derived columns can encode the answer.'),
  q('leak-005-post-outcome', 'Foundation', 'Why is post-outcome information suspicious?', 'It may not exist when the prediction must be made', ['It always has missing values', 'It is never correlated with labels'], 'Availability time matters as much as predictive power.'),
  q('leak-006-duplicate-entities', 'Foundation', 'How can duplicate entities leak?', 'Rows from the same entity appear in both fitting and evaluation splits', ['The entity id is removed from every row', 'The validation set has no labels'], 'Entity overlap can turn memorization into a fake generalization score.'),
  q('leak-007-time-order', 'Foundation', 'How can time order create leakage?', 'Training uses future data to evaluate earlier predictions', ['Rows are sorted before display', 'The model uses a calendar feature observed before prediction'], 'Forecasting protocols must respect chronology.'),
  q('leak-008-preprocessing', 'Foundation', 'How can preprocessing leak?', 'A transform learns statistics from evaluation rows before model fitting', ['The transform is fit inside each training split', 'The validation rows receive the training-fitted transform'], 'Scalers, imputers, encoders, and reducers can all carry information.'),
  q('leak-009-feature-selection', 'Foundation', 'How can feature selection leak?', 'Features are selected using labels from rows that should be held out', ['Constant columns are dropped using training rows only', 'A pipeline records selected columns after fitting'], 'Label-informed selection is part of model selection.'),
  q('leak-010-test-peeking', 'Foundation', 'What is repeated test peeking?', 'Changing the model after looking at final test performance', ['Training without a validation set but never opening test', 'Saving the test score in a report once'], 'Repeated test feedback turns the test set into development data.'),
  q('leak-011-aggregation', 'Foundation', 'Why can aggregate features leak?', 'An aggregate may include the row being predicted or future rows', ['Averages are never predictive', 'Aggregates cannot be computed in production'], 'Aggregates need a clear cutoff and exclusion rule.'),
  q('leak-012-target-encoding', 'Foundation', 'Why is target encoding leak-prone?', 'Category values can encode label averages from held-out rows', ['It removes all categorical signal', 'It can only be used after deployment'], 'Target-dependent encoders need fold-local fitting.'),
  q('leak-013-near-duplicates', 'Foundation', 'Why are near-duplicates a leakage risk?', 'Very similar copies can cross splits and inflate evaluation', ['They make labels impossible to store', 'They guarantee the model underfits'], 'Split related originals together before augmentation or deduplication.'),
  q('leak-014-leaderboard', 'Foundation', 'How can a public leaderboard leak signal?', 'Repeated submissions adapt model choices to leaderboard feedback', ['Leaderboards never use labels', 'More submissions always improve honesty'], 'Leaderboard feedback is a limited test signal.'),
  q('leak-015-availability', 'Foundation', 'What is feature availability?', 'Whether a feature is known at the moment the model must predict', ['Whether a feature has a short column name', 'Whether a feature is stored as a float'], 'Prediction-time availability is central to leakage audits.'),
  q('leak-016-proxy', 'Foundation', 'What is a leakage proxy?', 'A seemingly allowed feature that indirectly encodes unavailable outcome information', ['A weak feature with no correlation', 'A feature used only in training rows'], 'Leakage can hide behind harmless-looking operational fields.'),
  q('leak-017-split-unit', 'Foundation', 'What is the split unit?', 'The entity or event level that must not cross evaluation boundaries', ['The number of answers in a quiz', 'The file extension of the dataset'], 'The split unit should match the deployment generalization target.'),
  q('leak-018-pipeline-rule', 'Foundation', 'What is the safe pipeline rule?', 'Fit every learned step on training data only, then apply it to held-out data', ['Fit every step on all rows for stability', 'Fit validation transforms separately and compare'], 'Learned state must not use held-out rows.'),
  q('leak-019-symptom', 'Foundation', 'What score pattern often hints at leakage?', 'Validation is implausibly strong and collapses under a cleaner protocol', ['Training loss decreases gradually', 'A baseline is weaker than a tuned model'], 'Leakage often appears as suspiciously easy offline performance.'),
  q('leak-020-audit-question', 'Foundation', 'What is the first audit question for leakage?', 'Would this information be available in the same form at prediction time?', ['Does the feature have the highest correlation', 'Can the chart fit on one screen'], 'Availability and boundary questions catch many leaks early.'),

  q('leak-021-mechanism-boundary', 'Mechanism', 'How should you reason about a leakage mechanism?', 'Name the information source, the boundary crossed, and the decision it influenced', ['Only compare the final accuracy numbers', 'Only remove the most predictive feature'], 'Leakage analysis should be causal and procedural.'),
  q('leak-022-post-event-path', 'Mechanism', 'Why can a post-event field be harmful even if it is accurate?', 'It predicts by using evidence created after the prediction deadline', ['Accurate features are never allowed', 'It reduces the number of model parameters'], 'The issue is timing, not whether the field is truthful.'),
  q('leak-023-label-normalization', 'Mechanism', 'How can normalizing by a label-derived total leak?', 'The denominator may contain the answer or future outcome', ['Normalization always removes signal', 'The label becomes a string column'], 'Ratios and rates can smuggle target information.'),
  q('leak-024-user-history', 'Mechanism', 'How can user history features leak?', 'History may include events after the prediction timestamp', ['History features are always forbidden', 'User ids cannot appear in features'], 'History windows need cutoff-aware construction.'),
  q('leak-025-entity-overlap', 'Mechanism', 'Why does entity overlap inflate validation?', 'The model can learn entity-specific patterns that reappear in held-out rows', ['Entities make cross-validation impossible', 'The validation labels become unlabeled'], 'The evaluation no longer tests unseen entities.'),
  q('leak-026-household-overlap', 'Mechanism', 'Why can household or device grouping matter?', 'Related accounts can share hidden information even without identical user ids', ['Groups always remove all labels', 'Devices cannot produce features'], 'The grouping key may be broader than one row id.'),
  q('leak-027-time-window', 'Mechanism', 'What makes rolling features leakage-safe?', 'They use only events before the prediction cutoff', ['They use all future rows for smoother estimates', 'They average validation labels separately'], 'Window boundaries must align with serving time.'),
  q('leak-028-backfill', 'Mechanism', 'Why can backfilled data create leakage?', 'A dataset snapshot may contain values corrected after prediction time', ['Backfilled values are always random noise', 'Backfilling removes target columns'], 'Historical snapshots can know more than the model knew then.'),
  q('leak-029-imputation', 'Mechanism', 'How can imputation leak?', 'Fill values are computed from held-out rows before evaluation', ['Missing values are always target labels', 'Imputation cannot be part of a pipeline'], 'Even simple medians are learned preprocessing state.'),
  q('leak-030-scaling', 'Mechanism', 'Why can full-data scaling leak?', 'Evaluation distribution statistics shape the training representation', ['Scaling makes every model deterministic', 'Validation rows lose their feature values'], 'Evaluation rows should not help choose means, variances, or ranges.'),
  q('leak-031-pca', 'Mechanism', 'How can dimensionality reduction leak?', 'Components are fit using held-out row structure before validation', ['PCA cannot be used without labels', 'Components are always target values'], 'Unsupervised transforms can still leak distribution information.'),
  q('leak-032-feature-search', 'Mechanism', 'Why is searching features on all data risky?', 'The chosen feature set has already adapted to held-out labels or rows', ['Search removes the need for validation', 'All selected features become unavailable'], 'Feature discovery belongs inside the development protocol.'),
  q('leak-033-target-encoder-fold', 'Mechanism', 'What is the fold-safe target encoding pattern?', 'Compute encodings inside folds so each held-out row uses training-only label aggregates', ['Compute one full-data category mean before splitting', 'Replace categories with final test labels'], 'Out-of-fold encoding prevents each row from seeing its own label.'),
  q('leak-034-count-encoding', 'Mechanism', 'Can count encoding leak?', 'Yes, if counts include future or held-out population information unavailable at serving', ['No, because counts never use labels', 'Only if the column is numeric'], 'Label-free aggregates still need availability rules.'),
  q('leak-035-augmentation', 'Mechanism', 'How can data augmentation leak?', 'Augmented variants of the same original appear across train and validation', ['Augmentation always removes duplicates', 'Validation images are never evaluated'], 'Split originals before producing related variants.'),
  q('leak-036-neighbor-features', 'Mechanism', 'Why can graph neighbor features leak?', 'Neighbors may include validation or future nodes whose labels or outcomes influence features', ['Graphs cannot be split', 'Neighbor features are never predictive'], 'Graph construction needs boundary-aware sampling.'),
  q('leak-037-cross-validation', 'Mechanism', 'How does leakage interact with cross-validation?', 'Every fold must refit learned steps without using that fold as input', ['CV automatically removes all leakage', 'Only the first fold needs a boundary'], 'Fold-local fitting is the core defense.'),
  q('leak-038-hyperparameter-loop', 'Mechanism', 'How can hyperparameter search leak into final evaluation?', 'Repeated choices are made after inspecting the final test score', ['Hyperparameters are never model choices', 'Validation cannot compare models'], 'Final evaluation should not be part of the search loop.'),
  q('leak-039-multiple-comparisons', 'Mechanism', 'Why do many modeling attempts raise leakage-like optimism?', 'Selection can overfit noise in the validation or leaderboard feedback', ['Trying many models removes variance', 'The final model stops needing data'], 'The feedback loop itself can become overused.'),
  q('leak-040-data-cleaning', 'Mechanism', 'How can cleaning decisions leak?', 'Rules are chosen after looking at held-out labels or future errors', ['Cleaning raw duplicates before splitting by entity is always leakage', 'Cleaning never affects evaluation'], 'Manual decisions can encode evaluation information.'),
  q('leak-041-label-delay', 'Mechanism', 'Why does label delay matter?', 'A label may be known offline but unavailable when the model must act', ['Delayed labels cannot be evaluated later', 'Delay makes every feature safe'], 'Offline datasets often hide real-world timing constraints.'),
  q('leak-042-survivorship', 'Mechanism', 'What is survivorship leakage?', 'Training only on cases that survived to be observed later than prediction time', ['Training on rows with missing values', 'Using a small validation set'], 'Selection into the dataset can reveal future outcomes.'),
  q('leak-043-censoring', 'Mechanism', 'Why can censoring cause leakage?', 'Rows may be included or excluded using future outcome knowledge', ['Censoring means all features are strings', 'It removes the need for time splits'], 'Dataset construction can leak before modeling starts.'),
  q('leak-044-randomization', 'Mechanism', 'Why does random splitting not guarantee honesty?', 'Random rows can still share entities, time information, or generated variants', ['Randomness always makes evaluation invalid', 'Randomness removes all correlations'], 'The right split depends on dependence structure.'),
  q('leak-045-baseline', 'Mechanism', 'How can a simple baseline help find leakage?', 'If a trivial feature gives extreme performance, it may expose a shortcut', ['Baselines cannot detect data issues', 'A weak baseline proves all features are safe'], 'Suspiciously strong simple models deserve feature audits.'),
  q('leak-046-ablation', 'Mechanism', 'How does ablation help diagnose leakage?', 'Removing suspect features shows whether performance depends on unavailable information', ['Ablation only changes chart colors', 'Ablation always improves production'], 'Leakage features often cause large offline drops when removed.'),
  q('leak-047-permutation', 'Mechanism', 'What can permutation tests reveal in leakage audits?', 'Unexpected signal after label shuffling can indicate pipeline contamination', ['They prove every feature is causal', 'They replace deployment validation'], 'A model should not predict shuffled labels unless something leaks.'),
  q('leak-048-manifest', 'Mechanism', 'Why keep a split manifest?', 'It records which entities or rows belong to each split for reproducibility', ['It stores model weights only', 'It chooses the highest score automatically'], 'Stable split definitions prevent accidental reshuffling.'),
  q('leak-049-serving-parity', 'Mechanism', 'What is serving parity?', 'Training features are computed the same way and with the same timing as serving features', ['The UI uses the same font in training and serving', 'The model trains on final test labels'], 'Parity checks close the offline-online gap.'),
  q('leak-050-fix-taxonomy', 'Mechanism', 'What should a leakage fix specify?', 'The blocked information path and the new split, pipeline, or cutoff rule', ['Only the improved score', 'Only the name of the model class'], 'A real fix names the prevention mechanism.'),

  q('leak-051-scenario-readmission', 'Application', 'A hospital readmission model uses discharge codes assigned after the stay. What is wrong?', 'The feature may be created after the prediction decision point', ['Discharge codes are never correlated with outcomes', 'Hospitals cannot use validation data'], 'The prediction deadline defines allowed information.'),
  q('leak-052-scenario-fraud', 'Application', 'A fraud model uses chargeback status to predict fraud at purchase time. What is wrong?', 'Chargeback status is an outcome observed after the transaction', ['Fraud labels must never be evaluated', 'Purchase-time features are always leakage'], 'Post-outcome fields can encode the label.'),
  q('leak-053-scenario-customer', 'Application', 'A churn model randomly splits monthly records from the same customer. What is safer?', 'Group records by customer or validate on future periods', ['Scatter each customer across every split', 'Tune on final test until churn improves'], 'The split should test new customers or future behavior, not memorized history.'),
  q('leak-054-scenario-time', 'Application', 'A sales forecast shuffles all weeks before validation. What should you change?', 'Use chronological validation with training before held-out weeks', ['Keep shuffling and remove the metric', 'Fit the scaler on all future weeks'], 'Forecasting must avoid future-to-past training.'),
  q('leak-055-scenario-scaler', 'Application', 'A StandardScaler is fit before train/test split. What is the issue?', 'Test distribution statistics influenced the training features', ['Scaling cannot be used for ML', 'The test labels are deleted'], 'Fit scaling on train only and apply the learned transform.'),
  q('leak-056-scenario-imputer', 'Application', 'A median imputer uses all rows before cross-validation. What is the leak?', 'Validation fold values contribute to the fill statistic', ['Median imputation requires final test labels', 'The model cannot train on imputed features'], 'Imputers belong inside the fold pipeline.'),
  q('leak-057-scenario-feature-select', 'Application', 'A team selects top features by correlation with all labels before CV. What happened?', 'Held-out labels shaped the selected feature set', ['Correlation cannot compare features', 'CV no longer has training rows'], 'The feature-selection step must be inside each fold.'),
  q('leak-058-scenario-target-encoding', 'Application', 'A category encoder uses full-data target means. What should replace it?', 'Out-of-fold or train-only target encoding with smoothing', ['Encoding every category with the final test label', 'Dropping the validation boundary'], 'The held-out row should not contribute to its own encoded value.'),
  q('leak-059-scenario-augmentation', 'Application', 'Image crops from one original land in train and validation. What is the fix?', 'Split by original image before creating augmented variants', ['Augment validation more aggressively', 'Use the best validation crop only'], 'Related images should stay on one side of the boundary.'),
  q('leak-060-scenario-duplicates', 'Application', 'Near-identical product listings cross splits. What risk appears?', 'The model may match copies instead of learning transferable product signals', ['Duplicates make labels unavailable', 'Product text cannot be modeled'], 'Deduplicate or group related listings before splitting.'),
  q('leak-061-scenario-aggregate', 'Application', 'A feature is user average spend computed over the whole dataset. What should you audit?', 'Whether the average includes future or held-out purchases', ['Whether the column name is short', 'Whether the feature is stored as text'], 'Aggregates need cutoff-aware construction.'),
  q('leak-062-scenario-backfill', 'Application', 'Training data contains account status corrected months after prediction. What is the issue?', 'Offline snapshots contain knowledge unavailable at decision time', ['Corrected data is always less predictive', 'Status fields cannot be stored'], 'Use point-in-time feature snapshots when possible.'),
  q('leak-063-scenario-leaderboard', 'Application', 'A competition team submits 200 times and tunes to public score. What should they expect?', 'The public score may be overfit and private performance can drop', ['Every submission proves deployment generalization', 'Leaderboard feedback is not information'], 'Repeated leaderboard feedback behaves like a noisy validation signal.'),
  q('leak-064-scenario-test-peek', 'Application', 'A test score is disappointing, so the team changes preprocessing and retests. What happened?', 'The test set became part of the development loop', ['The test set became larger', 'The labels were no longer labels'], 'Any test-guided change weakens final-estimate validity.'),
  q('leak-065-scenario-baseline', 'Application', 'A one-feature baseline almost perfectly predicts the label. What should you do?', 'Inspect whether the feature is post-outcome or target-derived', ['Assume deployment is solved', 'Delete the train split'], 'Simple shortcuts often reveal leakage.'),
  q('leak-066-scenario-production-drop', 'Application', 'Offline AUC is 0.99 but production AUC is near random. What is a plausible cause?', 'Training used unavailable or future information', ['The metric was too hard to calculate', 'The model had too many validation rows'], 'A large offline-online gap is a classic leakage symptom.'),
  q('leak-067-scenario-fold-leak', 'Application', 'PCA is fit once on all rows before CV. What is the safer pattern?', 'Fit PCA inside each training fold within the pipeline', ['Fit PCA on each validation fold separately', 'Use final test data to choose components'], 'Unsupervised fitted transforms still need fold-local training.'),
  q('leak-068-scenario-graph', 'Application', 'A graph feature aggregates labels from neighboring validation nodes. What is wrong?', 'Held-out label information flows through the graph feature', ['Graphs cannot use validation rows at all', 'Neighbors always remove leakage'], 'Graph features need careful message-passing boundaries.'),
  q('leak-069-scenario-cleaning', 'Application', 'A researcher removes rows after reviewing test errors. What is wrong?', 'Data-cleaning choices are now influenced by final evaluation', ['Cleaning never changes metrics', 'Test errors cannot be logged'], 'Test-set feedback should not reshape the dataset.'),
  q('leak-070-scenario-split-manifest', 'Application', 'A team rerandomizes splits until validation looks stable. What should they use?', 'A saved split manifest and predefined rerun policy', ['Unlimited reshuffling until the score peaks', 'Only the easiest validation subset'], 'The split protocol should not be tuned silently.'),
  q('leak-071-scenario-label-delay', 'Application', 'A loan model uses delinquency status from 90 days later at application time. What is the leak?', 'Future outcome status is being used as an input', ['Loan applications cannot have features', 'Delayed labels are never useful for evaluation'], 'Outcome timing must match the decision moment.'),
  q('leak-072-scenario-survivorship', 'Application', 'A dataset includes only customers active six months later. What should you worry about?', 'Inclusion may depend on future survival information', ['The sample is guaranteed unbiased', 'Active customers cannot be modeled'], 'Who appears in the dataset can leak future outcomes.'),
  q('leak-073-scenario-monitoring', 'Application', 'A leakage fix ships. What should monitoring compare?', 'Offline validation assumptions against live feature availability and performance', ['Only the training notebook cell count', 'Only the final model filename'], 'Monitoring should verify the prevention rule remains true.'),
  q('leak-074-scenario-document', 'Application', 'What should the leakage audit note for a suspect feature?', 'Source table, timestamp, allowed cutoff, and whether labels or held-out rows influence it', ['Only whether the feature improved accuracy', 'Only the feature display name'], 'Documentation turns leakage checks into reviewable evidence.'),
  q('leak-075-scenario-decision', 'Application', 'If a feature is highly predictive but unavailable at serving time, what should you do?', 'Remove or replace it with a point-in-time available proxy', ['Keep it because offline metrics matter most', 'Use it only on final test rows'], 'Predictive power does not override availability.'),

  q('leak-076-trap-correlation', 'Tricky', 'Which leakage claim is false?', 'A feature is safe whenever it is highly correlated with the label', ['A predictive feature still needs availability review', 'Post-outcome fields can inflate validation'], 'Correlation does not prove the signal is allowed.'),
  q('leak-077-trap-unsupervised', 'Tricky', 'Which preprocessing claim is false?', 'Unsupervised transforms cannot leak because they ignore labels', ['Full-data fitting can leak distribution information', 'Fold-local fitting protects held-out rows'], 'Label-free does not mean boundary-free.'),
  q('leak-078-trap-random', 'Tricky', 'Which random-split claim is false?', 'A random row split is always honest for repeated users', ['Entity-level dependence can break random splits', 'Grouped splits can test unseen entities'], 'Randomness does not remove dependence.'),
  q('leak-079-trap-time', 'Tricky', 'Which time-series claim is false?', 'Shuffling is safe when future rows share the same schema as past rows', ['Future information can make validation too easy', 'Chronological validation better matches forecasting'], 'Schema similarity does not fix temporal leakage.'),
  q('leak-080-trap-test', 'Tricky', 'Which test-set behavior is unsafe?', 'Changing features after each final test result', ['Opening the final test once after choices are frozen', 'Recording the final protocol before testing'], 'Repeated test-guided edits are development.'),
  q('leak-081-trap-target-encoding', 'Tricky', 'Which target-encoding claim is misleading?', 'Full-data target means are fine because they are just category metadata', ['Out-of-fold encodings reduce self-label leakage', 'Smoothing can help rare categories'], 'The means are computed from labels.'),
  q('leak-082-trap-duplicates', 'Tricky', 'Which duplicate handling is unsafe?', 'Creating augmented variants before splitting originals', ['Grouping variants from the same original', 'Checking near-duplicate overlap across splits'], 'Generate or group related examples after defining the boundary.'),
  q('leak-083-trap-aggregates', 'Tricky', 'Which aggregate feature is suspicious?', 'Lifetime spend computed using events after the prediction date', ['Spend before the cutoff computed from training history', 'A count window ending before prediction time'], 'The word lifetime often hides future events.'),
  q('leak-084-trap-cleaning', 'Tricky', 'Which cleaning practice is unsafe?', 'Choosing row-removal rules after inspecting final test mistakes', ['Applying predefined quality rules before splitting', 'Documenting duplicate removal criteria'], 'Manual cleanup can become test tuning.'),
  q('leak-085-trap-baseline', 'Tricky', 'Which interpretation of an extremely strong simple baseline is unsafe?', 'It proves production performance will be equally strong', ['It may indicate a target-derived shortcut', 'It deserves feature availability review'], 'Unexpectedly easy prediction should trigger suspicion.'),
  q('leak-086-trap-proxy', 'Tricky', 'Which proxy statement is false?', 'A feature is safe if its name does not mention the target', ['Operational fields can indirectly encode outcomes', 'Feature source and timestamp matter'], 'Names do not determine availability.'),
  q('leak-087-trap-leaderboard', 'Tricky', 'Which leaderboard claim is false?', 'A public leaderboard can be queried indefinitely without overfitting risk', ['Repeated submissions can adapt to public feedback', 'A private holdout can expose public-score overfit'], 'Feedback loops can overfit even without direct labels.'),
  q('leak-088-trap-ablation', 'Tricky', 'Which ablation conclusion is too strong?', 'If removing one suspect feature barely changes score, no leakage exists anywhere', ['Ablation can test one suspected path', 'Multiple leakage paths may coexist'], 'One negative check does not clear the full pipeline.'),
  q('leak-089-trap-snapshot', 'Tricky', 'Which snapshot claim is false?', 'A current warehouse snapshot always represents what was known historically', ['Point-in-time snapshots reduce backfill leakage', 'Corrected values can include future knowledge'], 'Warehouses often overwrite history.'),
  q('leak-090-trap-monitoring', 'Tricky', 'Which production claim is unsafe?', 'A clean validation protocol means serving checks are unnecessary', ['Feature availability can drift after deployment', 'Monitoring can catch pipeline regressions'], 'Leakage prevention must survive operational change.'),

  q('leak-091-interview-define', 'Interview', 'How should you define data leakage in an interview?', 'Forbidden information crosses into training, model selection, or evaluation and makes results optimistic', ['Any feature with high importance', 'Any model that performs worse online'], 'A strong definition names forbidden information and optimism.'),
  q('leak-092-interview-taxonomy', 'Interview', 'What leakage taxonomy should you be ready to explain?', 'Target-derived, temporal, entity overlap, preprocessing, aggregation, and repeated-test leakage', ['Only train loss and validation loss', 'Only missing values and outliers'], 'Interviewers expect multiple modes, not one example.'),
  q('leak-093-interview-audit', 'Interview', 'How would you audit a new feature for leakage?', 'Check source, timestamp, prediction cutoff, target dependence, split overlap, and serving availability', ['Keep it if correlation is high', 'Reject it if its name is long'], 'A systematic audit beats intuition.'),
  q('leak-094-interview-scenario', 'Interview', 'How should you answer a leakage scenario question?', 'Identify the forbidden path, predict the metric symptom, and propose a boundary-safe fix', ['Guess the model class first', 'Only say the score is high'], 'Good answers connect cause, evidence, and remedy.'),
  q('leak-095-interview-cv', 'Interview', 'How do you connect leakage to cross-validation?', 'Every learned pipeline step and feature search must be refit inside each fold', ['CV automatically prevents all leakage', 'Only final test rows need boundaries'], 'Fold-local state is a central interview point.'),
  q('leak-096-interview-time', 'Interview', 'What should you say about leakage in time-dependent systems?', 'Use point-in-time features, chronological splits, and cutoffs that match serving latency', ['Shuffle rows to make more random folds', 'Use future corrections for cleaner labels at prediction time'], 'Temporal leakage is about information availability over time.'),
  q('leak-097-interview-entities', 'Interview', 'How do you handle repeated users or related samples?', 'Split by the entity or group that must generalize independently', ['Randomize every row independently', 'Keep only the easiest repeated users'], 'The split unit should match the claim being tested.'),
  q('leak-098-interview-fix', 'Interview', 'What makes a leakage fix convincing?', 'It blocks the information path and is verified with a recreated leakage-safe evaluation', ['It keeps the old score unchanged', 'It hides the suspect feature name'], 'The fix must change the protocol, not just the explanation.'),
  q('leak-099-interview-review', 'Interview', 'What design-review statement should you challenge?', 'The validation score is high, so leakage is impossible', ['This feature needs a timestamp audit', 'The test set should be opened once after freezing choices'], 'High scores can be evidence for suspicion, not proof of safety.'),
  q('leak-100-interview-success', 'Interview', 'What is interview-ready leakage mastery?', 'Reasoning from prediction-time availability to split design, pipeline boundaries, diagnostics, and fixes', ['Memorizing one example of duplicate users', 'Always deleting the most predictive feature'], 'Mastery means tracing information flow under changed conditions.'),
]);
