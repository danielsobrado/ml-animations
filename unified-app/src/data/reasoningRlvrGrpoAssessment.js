function stableHash(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) {
    hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  }
  return hash.toString(16).padStart(8, '0');
}

function stableHashNumber(text) {
  let hash = 0;
  for (let index = 0; index < text.length; index += 1) {
    hash = (hash * 31 + text.charCodeAt(index)) >>> 0;
  }
  return hash;
}

function q(number, level, prompt, correct, distractors, explanation) {
  const id = `rlvr-grpo-${String(number).padStart(3, '0')}`;
  const registryRotation = stableHashNumber(`reasoning-rlvr-grpo:${id}`) % 3;
  const targetAnswerIndex = (number - 1) % 3;
  const answerIndex = (targetAnswerIndex + registryRotation) % 3;
  const choices = [...distractors];
  choices.splice(answerIndex, 0, correct);

  return Object.freeze({
    id,
    level,
    prompt,
    choices: Object.freeze(choices),
    answerIndex,
    explanation,
    questionHash: stableHash(`${prompt}\n${choices.join('\n')}`),
  });
}

const SPECS = Object.freeze([
  q(1, 'Foundation', 'What broad shift does the reasoning RLVR lesson focus on?', 'Moving from next-token imitation toward training models to search, verify, and improve multi-step solutions.', ['Replacing policy optimization with static answer tables.', 'Compressing every solution trace into one scalar before decoding.'], 'The lesson is about post-training behavior: the model samples solution traces, receives verifier feedback, and changes its policy.'),
  q(2, 'Foundation', 'What is the base pre-training stage in the pipeline?', 'A language-modeling stage that learns facts and syntax from raw text but lacks structured reasoning behavior.', ['A verifier-only stage that executes code without a model.', 'A distillation stage that copies a finished teacher into a small student.'], 'Pre-training gives broad language ability, but it does not by itself enforce readable reasoning traces or verifier-aligned solving.'),
  q(3, 'Foundation', 'Why does the pipeline add cold-start SFT before heavy RL?', 'It seeds readable trace structure, answer formatting, and basic planning so early RL does not start from unreadable chaos.', ['It removes the need for any reward signal later.', 'It makes the final student larger than the teacher model.'], 'Cold-start data gives the policy a stable initial behavior before the noisier reinforcement loop begins.'),
  q(4, 'Foundation', 'What does RLVR mean in this lesson?', 'Reinforcement learning with verifiable rewards from checkers such as math parsers, tests, or structured validators.', ['Reinforcement learning with visual rendering as the only metric.', 'A supervised loss that ignores generated answers.'], 'RLVR uses rewards that can be checked mechanically when the task has an objective answer or contract.'),
  q(5, 'Foundation', 'What is the core role of a verifier reward?', 'It scores whether a generated solution satisfies an externally checkable target.', ['It stores rollout logs for faster dashboards.', 'It chooses the number of transformer layers.'], 'The verifier supplies the training signal that says which sampled outputs solved the task.'),
  q(6, 'Foundation', 'What does GRPO sample for a single prompt?', 'A group of candidate responses that can be scored and compared against sibling responses.', ['Exactly one gold response written by a human for every update.', 'A fixed set of optimizer hyperparameters without generated text.'], 'GRPO needs a group so rewards can be normalized within that prompt instead of using a learned critic.'),
  q(7, 'Foundation', 'In GRPO, what acts as the baseline for a candidate response?', 'The reward statistics of the sampled group for the same prompt.', ['The length of the original user prompt.', 'The longest word in the final answer.'], 'The group mean and spread replace the value baseline used by PPO-style actor-critic training.'),
  q(8, 'Foundation', 'What does a positive group-relative advantage mean?', 'The candidate scored above its sibling baseline and should receive reinforcing pressure.', ['The candidate used the shortest possible answer regardless of correctness.', 'The candidate must be copied into the cold-start dataset.'], 'Positive advantage is relative, not absolute: it says the sample was better than the group reference.'),
  q(9, 'Foundation', 'What does a negative group-relative advantage mean?', 'The sampled behavior scored below its sibling baseline and should become less likely.', ['The optimizer should delete the prompt from the dataset.', 'The reference model has no parameters.'], 'Below-average samples create suppressing pressure inside the guarded policy update.'),
  q(10, 'Foundation', 'What does rejection sampling SFT do after an RL-trained teacher generates many traces?', 'It filters generated candidates and fine-tunes on accepted traces with supervised learning.', ['It updates the policy online after every token with a critic.', 'It replaces answer checking with random sampling only.'], 'Rejection sampling is an offline curation step: generate, check, keep, and train on the selected examples.'),
  q(11, 'Foundation', 'What is an outcome reward model in the ORM versus PRM comparison?', 'A scorer that judges the final answer or final outcome of a whole reasoning trace.', ['A model that assigns a separate reward to each hidden attention head.', 'A parser that always explains every intermediate error.'], 'ORMs are coarse: they see whether the final output is good, but can miss where the reasoning went wrong.'),
  q(12, 'Foundation', 'What is a process reward model in the lesson?', 'A step-level scorer that tries to assign credit or blame to intermediate reasoning steps.', ['A trace compressor used only after distillation.', 'A proof pretty-printer that never scores correctness.'], 'PRMs give more granular feedback, which can help identify a bad step even before the final answer is produced.'),
  q(13, 'Foundation', 'Why can format rewards be useful?', 'They encourage parseable traces, answer boxes, and consistent structure needed by downstream filters.', ['They prove the mathematical reasoning is correct.', 'They remove the need for correctness checks.'], 'Format matters operationally, but it is only a supporting reward and must not dominate task success.'),
  q(14, 'Foundation', 'Why add a small length penalty to reasoning rewards?', 'To discourage circular overthinking and control inference cost while preserving enough room for hard problems.', ['To force every answer to be one token long.', 'To make the verifier ignore final answers.'], 'Length shaping counters unbounded traces, but it must be tuned because difficult tasks may require real intermediate work.'),
  q(15, 'Foundation', 'What is the purpose of a KL penalty during policy optimization?', 'It limits drift from a reference policy so the model does not collapse into strange or unsafe behavior.', ['It computes the exact final answer for math problems.', 'It increases reward by repeating formatting tags.'], 'KL regularization keeps the optimized policy near a known distribution while still allowing useful updates.'),
  q(16, 'Foundation', 'What behavior did the lesson associate with pure RL starts such as R1-Zero?', 'Emergent self-correction can appear, but readability, formatting, and language consistency may be unstable.', ['The model cannot ever improve without human annotations for every token.', 'The model immediately becomes a small distilled student.'], 'Pure RL can discover useful search behaviors, but the lesson highlights instability without cold-start structure.'),
  q(17, 'Foundation', 'What does the cold-start plus RL variant aim to improve over a pure RL start?', 'It keeps the benefits of reinforcement learning while making traces more readable and stable.', ['It prevents all exploration during training.', 'It removes verifiers from the pipeline.'], 'Cold-start SFT supplies a scaffold; RL then optimizes against verifiable outcomes and other constraints.'),
  q(18, 'Foundation', 'What is student distillation in this reasoning pipeline?', 'Training a smaller model on curated teacher traces so it can imitate useful reasoning patterns at lower serving cost.', ['Training the teacher to be larger at inference time.', 'Deleting all supervised traces after RL.'], 'Distillation transfers behavior and format from a stronger teacher, but not necessarily the full reasoning capacity.'),
  q(19, 'Foundation', 'Why is an all-negative GRPO group risky?', 'The least bad wrong response can still look relatively good and receive positive pressure.', ['The model is guaranteed to reset to pre-training weights.', 'The verifier stops producing valid scores.'], 'Group-relative normalization can reinforce a flawed trace when every sibling is wrong and one is merely less wrong.'),
  q(20, 'Foundation', 'What is reward hacking in the lesson context?', 'Optimizing visible reward features such as tags or style without actually solving the task.', ['Reducing batch size to fit GPU memory.', 'Using a validation set after training finishes.'], 'Reward hacking happens when the policy exploits a misspecified reward instead of learning the intended capability.'),

  q(21, 'Mechanism', 'Which pipeline order matches the lesson map?', 'Base pre-training, cold-start SFT, reasoning RL, rejection sampling SFT, final alignment, then distillation.', ['Distillation, final alignment, base pre-training, then cold-start SFT.', 'Rejection sampling, reward tuning, prompt sorting, then deployment only.'], 'The map shows a staged post-training flow from broad base ability to RL-trained reasoning and smaller students.'),
  q(22, 'Mechanism', 'What makes a cold-start trace useful for this lesson?', 'It shows structured planning, verification, clear tags, and a parseable final answer.', ['It is always the longest possible answer.', 'It hides every intermediate result from the verifier.'], 'The cold-start examples bootstrap a readable trace format that later rewards can refine.'),
  q(23, 'Mechanism', 'Why is a wrong but well-formatted trace dangerous?', 'A weak reward mix may overvalue structure and underweight the incorrect final result.', ['It cannot be tokenized by a language model.', 'It always receives zero from every possible reward component.'], 'The trace can look polished while solving the wrong problem, so correctness must dominate cosmetic rewards.'),
  q(24, 'Mechanism', 'Why can nested or repeated thinking tags reveal a reward design bug?', 'They show the policy exploiting a format parser instead of adding useful reasoning content.', ['They prove the answer checker is perfectly calibrated.', 'They are required by GRPO to compute the mean reward.'], 'If repeated tags earn too much, the model can collect reward by gaming the parser.'),
  q(25, 'Mechanism', 'What does the reward tuner demonstrate when correctness has the largest weight?', 'Correct answers should dominate the total reward even when format and language bonuses are present.', ['Formatting alone should outrank all wrong answers.', 'Longer traces should always be preferred.'], 'A balanced reward gives small auxiliary bonuses without letting them override the objective outcome.'),
  q(26, 'Mechanism', 'How does the rejection-sampling strictness control affect candidate filtering?', 'Higher strictness keeps fewer candidates and demands stronger quality and formatting.', ['Higher strictness disables all verifier checks.', 'Higher strictness makes wrong answers pass automatically.'], 'The lesson filter becomes more selective as strictness increases, especially against format failures.'),
  q(27, 'Mechanism', 'Why can a correct answer without required answer formatting still fail curation?', 'The downstream parser may need the expected structure to verify and reuse the trace reliably.', ['Correct answers are never useful for SFT.', 'The model is penalized for being concise in every setting.'], 'Offline curation often requires both correctness and a stable output contract.'),
  q(28, 'Mechanism', 'What limitation of an ORM is shown by step errors with a correct final answer?', 'It can reward the whole chain even when intermediate reasoning contains false steps.', ['It can only score language mixing and never final answers.', 'It always needs step-level human annotation to run.'], 'An outcome-only checker may miss brittle or accidental reasoning if the final output happens to match.'),
  q(29, 'Mechanism', 'What advantage does PRM-style feedback provide in the polynomial example?', 'It can flag the exact arithmetic step that first goes wrong.', ['It removes the need to inspect intermediate steps.', 'It guarantees the model never overthinks.'], 'Step-level scoring helps diagnose where reasoning quality failed instead of judging only the final box.'),
  q(30, 'Mechanism', 'What components form the simple reward sum in the GRPO workbench?', 'Correctness, format, language coherence, and a length penalty.', ['Only GPU memory and optimizer state.', 'Only dashboard colors and panel titles.'], 'The workbench exposes a deliberately simple weighted reward to show how design choices shift policy pressure.'),
  q(31, 'Mechanism', 'What does the group mean do in the GRPO advantage formula?', 'It centers each candidate reward relative to siblings from the same prompt.', ['It sets the maximum response length for every prompt.', 'It stores the old policy logits.'], 'Subtracting the group mean turns raw rewards into above-baseline or below-baseline signals.'),
  q(32, 'Mechanism', 'What does the group standard deviation do in the advantage formula?', 'It scales the centered reward by the amount of contrast in the group.', ['It counts how many tokens were generated before the answer.', 'It measures the number of training epochs completed.'], 'Reward spread matters because a tiny difference should not be treated like a large separation.'),
  q(33, 'Mechanism', 'Why include a small epsilon in the GRPO denominator?', 'It prevents numerical instability when reward spread is extremely small.', ['It adds a hidden supervised target to every token.', 'It forces every group to have exactly eight candidates.'], 'The stabilizer avoids division problems and keeps low-contrast groups from exploding numerically.'),
  q(34, 'Mechanism', 'What happens when every candidate reward is almost identical?', 'The group provides little useful relative signal because advantages are near zero or unstable.', ['The verifier automatically creates new solutions.', 'The KL penalty is no longer computed.'], 'GRPO needs contrast; identical rewards make it unclear which behavior should be reinforced.'),
  q(35, 'Mechanism', 'What does the clipping parameter control in the workbench?', 'It limits the update strength from very large positive or negative advantages.', ['It decides which lesson panel is active.', 'It converts a wrong answer into a correct one.'], 'Clipping is a guardrail that stops one sampled advantage from creating an excessive policy move.'),
  q(36, 'Mechanism', 'Why does group size matter for GRPO?', 'It changes the chance of seeing useful reward contrast among sampled responses.', ['It changes the number of transformer layers in the model.', 'It removes the need for any verifier.'], 'Larger groups can find successes on harder prompts, but they cost more rollout compute.'),
  q(37, 'Mechanism', 'Why can too little sampling diversity weaken GRPO?', 'The group may contain near-duplicate traces with too little contrast to teach the policy.', ['The reward checker starts scoring hidden states directly.', 'The student model becomes larger than the teacher.'], 'Sibling samples need meaningful variation so relative rewards identify better behavior.'),
  q(38, 'Mechanism', 'Why can too much sampling randomness be wasteful?', 'It may produce mostly incoherent or all-wrong samples with poor learning signal.', ['It makes every answer pass the parser.', 'It disables cold-start SFT traces.'], 'Exploration must be high enough to discover alternatives but not so high that rollouts become useless noise.'),
  q(39, 'Mechanism', 'What does the all-negative indicator warn about?', 'Every sampled candidate is effectively failing, so relative reinforcement may point at flawed traces.', ['Every sampled candidate is a verified solution.', 'The batch contains no prompts.'], 'The warning is about relative learning when no sample is actually successful.'),
  q(40, 'Mechanism', 'What is the training value of a mixed-success group?', 'It contains both successful and failed traces, giving a clear contrast for relative updates.', ['It proves all future prompts will be easy.', 'It removes the need for answer normalization.'], 'Mixed groups are useful because the policy can increase winners and suppress losers for the same problem.'),
  q(41, 'Mechanism', 'How does the KL reference model constrain RL training?', 'It measures how far the active policy has moved from a stable baseline distribution.', ['It executes the code unit tests used as rewards.', 'It stores the curated rejection-sampling dataset.'], 'KL compares policy distributions and adds pressure against excessive drift.'),
  q(42, 'Mechanism', 'What can happen if KL pressure is too weak?', 'The policy may drift into language mixing, formatting collapse, or narrow reward exploits.', ['The model is forced to copy every cold-start trace exactly.', 'The verifier gains perfect process supervision.'], 'Weak regularization can let reward optimization damage general language behavior.'),
  q(43, 'Mechanism', 'What can happen if KL pressure is too strong?', 'The policy may stay too close to the reference and fail to learn new reasoning behavior.', ['The group standard deviation becomes infinite by definition.', 'The answer parser stops reading boxed answers.'], 'KL is a tradeoff: enough stability without preventing useful optimization.'),
  q(44, 'Mechanism', 'Why is language consistency included as a possible reward?', 'It discourages traces that switch languages and become difficult to read or filter.', ['It proves the numerical answer is correct.', 'It replaces all math parsers in RLVR.'], 'Language coherence is a secondary quality signal for production-readable reasoning traces.'),
  q(45, 'Mechanism', 'Why should format rewards usually be capped or kept small?', 'They support parsing but should not overpower correctness or verifier success.', ['They are the only signal GRPO can optimize.', 'They automatically eliminate all reward hacking.'], 'A format bonus is useful only as a helper signal; oversized bonuses invite parser gaming.'),
  q(46, 'Mechanism', 'What does rejection sampling contribute after RL improves the teacher?', 'It converts many sampled teacher solutions into a curated supervised dataset.', ['It changes the training loop into a static lookup table.', 'It removes every incorrect sample by rewriting failed traces as successes.'], 'The accepted traces become data for another SFT pass or for student distillation.'),
  q(47, 'Mechanism', 'What does the distillation curation step filter out?', 'Verbose loops, format failures, language mixing, and incorrect traces.', ['All short correct traces by default.', 'Every example that came from a strong teacher.'], 'The pipeline keeps dense, useful traces and rejects artifacts that would teach bad behavior.'),
  q(48, 'Mechanism', 'What loss usually trains the smaller student on curated traces?', 'Standard supervised token prediction on the teacher-generated trace dataset.', ['A live online policy gradient with no target text.', 'A static table over reward names only.'], 'Student distillation commonly uses SFT-style cross-entropy on selected teacher outputs.'),
  q(49, 'Mechanism', 'Why are math and code natural RLVR domains?', 'Their answers can often be checked by parsers, unit tests, or deterministic execution.', ['They never require long reasoning.', 'They cannot suffer from reward hacking.'], 'Verifiable tasks are attractive because automated feedback can scale beyond manual preference labels.'),
  q(50, 'Mechanism', 'What is a verifier gap?', 'A mismatch where passing the checker does not fully imply the intended solution quality.', ['A larger context window used only during distillation.', 'A missing heading in the training map.'], 'Verifiers are useful but imperfect; they must match the real task contract closely.'),

  q(51, 'Application', 'A reward dashboard shows wrong answers with perfect tags beating correct concise answers. What should you change first?', 'Increase correctness dominance and reduce or cap the format bonus.', ['Remove all verifier checks and score style only.', 'Reward repeated tags because they are easy to parse.'], 'The scenario indicates format reward hacking, so the objective should restore correctness as the primary signal.'),
  q(52, 'Application', 'A model solves simple arithmetic but writes thousands of repetitive tokens. What reward adjustment fits the lesson?', 'Add or strengthen a modest length penalty while monitoring hard-problem accuracy.', ['Remove all limits because longer is always better.', 'Give bonus reward for every repeated verification sentence.'], 'Overthinking is controlled by length shaping, but the penalty must not truncate necessary reasoning.'),
  q(53, 'Application', 'A reasoning trace alternates between English and another language mid-solution. What lesson mechanism addresses it?', 'Add language consistency scoring or filter mixed-language traces during curation.', ['Increase only the format tag reward.', 'Treat every mixed-language trace as proof of correctness.'], 'The lesson lists language mixing as a failure mode and suggests a consistency reward or curation rule.'),
  q(54, 'Application', 'During GRPO, all eight completions for a hard prompt are wrong. What is the safest diagnosis?', 'The group lacks a true positive example, so relative advantages may reinforce the least bad failure.', ['The prompt is automatically solved because one candidate must receive positive advantage.', 'The model should skip KL penalties forever.'], 'All-negative groups are a known trap because relative normalization does not know absolute correctness.'),
  q(55, 'Application', 'How might you respond to repeated all-negative groups on frontier prompts?', 'Adjust difficulty, sampling diversity, group size, or data mix so some groups contain successful traces.', ['Force every wrong sample to become the reference answer.', 'Disable verification because failures are inconvenient.'], 'The goal is to create useful contrast rather than blindly reinforcing wrong siblings.'),
  q(56, 'Application', 'A group has all candidates correct with nearly identical rewards. What training issue appears?', 'The update has weak contrast because every sibling is already successful.', ['Every candidate must receive a negative advantage.', 'The answer checker is unusable for any future task.'], 'All-correct groups can waste rollout budget because they provide little information about which behavior is better.'),
  q(57, 'Application', 'A candidate is correct but has no boxed final answer. How should a strict curation filter treat it?', 'It may reject the trace if the output contract requires a boxed answer for parsing.', ['It must always keep it because correctness ignores deployment format.', 'It should mark the trace unusable even when the value is right.'], 'Curated reasoning datasets need reliable final-answer extraction, not just informal correctness.'),
  q(58, 'Application', 'A step-by-step proof gets the right final answer after two cancelling mistakes. Which reward type best catches the issue?', 'Process-level scoring that evaluates intermediate steps.', ['Outcome-only scoring that sees only the final box.', 'A format-only regex that counts tags.'], 'A PRM-style signal can mark the flawed steps even when the final answer happens to be correct.'),
  q(59, 'Application', 'An ORM gives zero to a long trace whose final answer is wrong but whose first four steps were valid. What limitation is visible?', 'Outcome-only reward can discard useful partial reasoning signal.', ['The final answer must be ignored in all training.', 'The trace cannot be used for any diagnostic review.'], 'ORMs are simple and scalable, but they do not naturally assign credit to individual steps.'),
  q(60, 'Application', 'The GRPO workbench shows a high positive advantage for a verbose correct candidate. What extra check matters before reinforcing it?', 'Whether the reward includes enough length pressure to avoid teaching unnecessary verbosity.', ['Whether the candidate has the largest trace id.', 'Whether every wrong candidate has the same token count.'], 'Correct but bloated traces may still need shaping so the policy does not learn expensive loops.'),
  q(61, 'Application', 'A verifier accepts answers that repeat empty thinking tags. What is the production fix?', 'Tighten the parser and require meaningful content or cap tag-related rewards.', ['Raise the format reward until the exploit disappears.', 'Remove correctness scoring from the objective.'], 'The exploit targets the reward definition, so the checker and reward weights need hardening.'),
  q(62, 'Application', 'A code RLVR task passes public tests but fails hidden tests. What risk does that expose?', 'The verifier is incomplete and may reward overfitting to visible checks.', ['GRPO cannot train on code tasks at all.', 'The model has no sampled group.'], 'Verifiable rewards are only as good as the checker coverage and hidden evaluation design.'),
  q(63, 'Application', 'A team wants to use RLVR for creative writing quality. What should you warn?', 'Open-ended quality may lack an objective verifier, so preference or rubric methods may be more appropriate.', ['RLVR always works best for subjective taste.', 'Math parsers can judge every narrative style issue.'], 'RLVR is strongest when task success can be checked mechanically.'),
  q(64, 'Application', 'A small distilled model copies the teacher format but fails new hard math problems. What does this show?', 'Distillation transferred style better than full problem-solving capacity.', ['The teacher traces were necessarily useless.', 'The student must have the same number of parameters as the teacher.'], 'Students can imitate patterns from curated traces while lacking the teacher capacity for harder generalization.'),
  q(65, 'Application', 'A reward curve rises while independent accuracy is flat. What should you suspect?', 'The policy may be optimizing the measured reward without improving true task success.', ['The model is guaranteed to be reasoning better.', 'The validation set should be deleted.'], 'Reward improvements need external checks because reward hacking and verifier gaps can fool the dashboard.'),
  q(66, 'Application', 'A deployment requires short answers for easy questions and longer traces for hard ones. What policy is aligned with the lesson?', 'Use length shaping or routing that adapts budget to difficulty instead of one fixed hard cap.', ['Reward every response for maximum length.', 'Forbid all intermediate reasoning for every task.'], 'The length tradeoff is task-dependent: easy prompts should stay concise, hard prompts need enough compute.'),
  q(67, 'Application', 'You are curating rejection-sampling outputs. Which trace should be filtered out?', 'A trace that is correct only because it exploits a parser bug or malformed answer pattern.', ['A concise trace with valid reasoning and a parseable final answer.', 'A trace with a useful self-check that stays within the contract.'], 'Curation should reject artifacts that would teach reward exploits or brittle formatting.'),
  q(68, 'Application', 'A GRPO run becomes less helpful in normal chat after math RL. What control should you inspect?', 'KL strength and final alignment data that preserve general behavior.', ['Only the title of the training map.', 'Only the number of panels in the lesson.'], 'Policy drift can damage general capabilities, so reference constraints and alignment passes matter.'),
  q(69, 'Application', 'A candidate has reward 1.2 while the group mean is 0.9 and spread is nonzero. What is the sign of its advantage?', 'Positive, because it is above the group baseline.', ['Negative, because all rewards above zero are suppressed.', 'Undefined, because mean rewards cannot be used in GRPO.'], 'The sign follows reward minus group mean before scaling by the group spread.'),
  q(70, 'Application', 'A candidate has reward 0.4 while the group mean is 0.9. What update direction does GRPO imply?', 'Suppress that sampled response relative to stronger siblings.', ['Reinforce it because every nonzero reward is good.', 'Clone it into the cold-start SFT set automatically.'], 'Below-baseline reward creates negative advantage and lowers the likelihood of that behavior.'),
  q(71, 'Application', 'A group has rewards 1, 1, 1, and 1. What practical issue does the advantage calculation face?', 'There is no reward spread, so the relative signal is weak after stabilization.', ['Every answer must be treated as wrong.', 'The verifier has proven process correctness for each step.'], 'Identical rewards make centered advantages uninformative.'),
  q(72, 'Application', 'What should you log when auditing GRPO health?', 'Group success mix, reward spread, KL drift, length, format failures, and independent accuracy.', ['Only the highest reward from one hand-picked prompt.', 'Only the color assigned to the active tab.'], 'A production audit needs multiple signals to distinguish real learning from reward exploitation.'),
  q(73, 'Application', 'You need a dataset for student SFT after teacher RL. What examples should be preferred?', 'Verified, concise, readable traces that solve the task and follow the output contract.', ['All generated traces, including wrong and malformed outputs.', 'Only the longest traces regardless of answer quality.'], 'Distillation data quality determines what behavior the student imitates.'),
  q(74, 'Application', 'A model finds creative solution paths during pure RL but outputs messy mixed-language traces. What pipeline step helps make the behavior usable?', 'Cold-start or follow-up SFT with structured, language-consistent traces.', ['Removing every curation filter.', 'Increasing format reward until correctness is irrelevant.'], 'The lesson frames R1-style training as combining RL discovery with supervised readability and alignment cleanup.'),
  q(75, 'Application', 'A team asks whether GRPO removes all memory cost from RL training. What should you answer?', 'It avoids a separate critic network, but rollouts, reference policy, optimizer state, and activations still cost memory.', ['It makes training free because no tensors are stored.', 'It requires two full critic networks for every prompt.'], 'GRPO reduces critic-related overhead compared with PPO, but it is not costless.'),

  q(76, 'Tricky', 'Which claim is false about post-training reasoning capability?', 'Adding chain-of-thought examples alone fully explains RLVR-trained reasoning behavior.', ['Verifier feedback and policy optimization can change sampled behavior.', 'Cold-start traces can improve readability before RL.'], 'The common misconception is that reasoning post-training is just imitation; the lesson emphasizes active optimization under verifier constraints.'),
  q(77, 'Tricky', 'Which reward claim is false and unsafe?', 'Verifiable rewards guarantee there is no reward hacking or verifier exploitation.', ['Verifiers can scale feedback when task success is objectively checkable.', 'Checker design must be audited for gaps and exploits.'], 'Mechanical rewards reduce some subjectivity, but bad parsers, weak tests, and proxy objectives can still be exploited.'),
  q(78, 'Tricky', 'Which domain claim should you reject?', 'Compilers and math parsers can verify every open-ended task equally well.', ['RLVR works best when a task has objective checks.', 'Subjective outputs may need preference or rubric evaluation.'], 'Not every target quality has a deterministic checker, so RLVR is not universal.'),
  q(79, 'Tricky', 'Which all-negative group claim is a trap?', 'It is always safe to reinforce the best-looking candidate from an all-wrong group.', ['All-negative groups can create misleading positive relative advantages.', 'Sampling or data changes may be needed to find successful traces.'], 'Relative advantage can rank wrong traces, so absolute verifier failure still matters.'),
  q(80, 'Tricky', 'Which format-reward claim is wrong?', 'A very high format reward is safe because readable tags necessarily mean correct reasoning.', ['Format bonuses help parsers when kept subordinate to correctness.', 'Malformed or empty tags should be filtered or penalized.'], 'Readable structure is not the same as solving the task; over-weighted format invites reward hacking.'),
  q(81, 'Tricky', 'Which length-penalty claim is false?', 'A larger length penalty always improves reasoning quality on every prompt.', ['Length penalties can reduce overthinking and serving cost.', 'Too much length pressure can truncate hard-problem reasoning.'], 'Length shaping is a tradeoff, not a monotonic improvement knob.'),
  q(82, 'Tricky', 'Which distillation claim is a misconception?', 'A small student trained on teacher traces automatically has the teacher model capability.', ['The student may learn format and common patterns.', 'Hard unseen tasks can expose a capacity gap.'], 'Distillation transfers behavior through data, but model capacity and generalization remain limiting factors.'),
  q(83, 'Tricky', 'Which advantage interpretation is false?', 'Positive group-relative advantage means the reasoning is necessarily correct in an absolute sense.', ['Positive advantage means better than the sampled group baseline.', 'A wrong candidate can be positive if all siblings are worse.'], 'Advantage is relative to the group, which is why all-negative groups are dangerous.'),
  q(84, 'Tricky', 'Which PRM claim should you reject?', 'A PRM is always deterministic, free, and available without training or labels.', ['PRM-style feedback can localize bad reasoning steps.', 'Step-level supervision may be harder to obtain than outcome checks.'], 'Process feedback is useful but usually more complex and expensive than simple final-answer checking.'),
  q(85, 'Tricky', 'Which KL claim is unsafe?', 'Zero KL pressure is always best because it lets the policy chase reward fastest.', ['KL can prevent excessive drift from the reference model.', 'Too much KL can also slow useful learning.'], 'Removing drift control can accelerate collapse, reward exploits, or loss of general helpfulness.'),
  q(86, 'Tricky', 'Which overthinking claim is false?', 'Longer traces are always better evidence of deeper reasoning.', ['Some hard tasks need more tokens than easy tasks.', 'Circular repetition can waste compute without improving correctness.'], 'Trace length is not a proof of quality; the lesson treats overthinking as a failure mode.'),
  q(87, 'Tricky', 'Which group-composition claim is wrong?', 'All-correct groups always provide the strongest GRPO training signal.', ['Mixed-success groups usually give clearer contrast.', 'All-correct groups can have weak relative information.'], 'If every sibling succeeds similarly, the update may not know which behavior to prefer.'),
  q(88, 'Tricky', 'Which rejection-sampling claim is false?', 'Rejection sampling fine-tuning is the same as online RL with a live policy-gradient update.', ['RFT filters generated traces offline.', 'Accepted traces can train a model with supervised token prediction.'], 'Rejection sampling is a generate-filter-train loop, not the same operation as online policy optimization.'),
  q(89, 'Tricky', 'Which final-answer claim is a trap?', 'A correct final answer proves every intermediate reasoning step was valid.', ['Outcome checks can miss cancelling mistakes.', 'Process checks can expose an invalid step despite the final box.'], 'The lesson contrasts ORM and PRM because final outcomes alone can hide flawed reasoning.'),
  q(90, 'Tricky', 'Which evaluation claim should be rejected?', 'A rising training reward alone proves the deployed reasoning system is ready.', ['Independent accuracy and failure-mode audits are still required.', 'Reward curves can improve while true task quality stagnates.'], 'Production readiness requires external evaluation, not only the optimized reward metric.'),

  q(91, 'Interview', 'How would you summarize the full lesson pipeline in one answer?', 'Start with a base model, seed readable reasoning with cold-start SFT, optimize with verifier-guided GRPO, curate traces, align, then distill.', ['Train only a formatting parser, skip rewards, and deploy the raw base model.', 'Use only a neural judge and never inspect generated traces.'], 'A strong interview answer names the staged flow and why each stage exists.'),
  q(92, 'Interview', 'How should you compare GRPO with PPO for reasoning post-training?', 'GRPO keeps guarded policy optimization but replaces the learned critic baseline with group reward normalization.', ['GRPO is pure supervised learning with no sampled rewards.', 'GRPO requires a larger critic than PPO for every prompt.'], 'The key contrast is critic removal through within-group statistics, not absence of policy updates.'),
  q(93, 'Interview', 'What would you include in a production reward design review?', 'Correctness dominance, parser hardening, length shaping, KL control, language checks, and hidden verifier tests.', ['Only a format bonus and a panel title.', 'Only the mean reward from one easy prompt.'], 'A production review must test the whole reward contract and the ways the policy can exploit it.'),
  q(94, 'Interview', 'How would you debug reward hacking in an RLVR reasoning run?', 'Compare high-reward traces against independent correctness checks and inspect whether style features are being exploited.', ['Trust the reward number and avoid reading traces.', 'Increase the exploited bonus until the curve stabilizes.'], 'Reward hacking is found by looking at actual samples and testing whether reward aligns with intended task success.'),
  q(95, 'Interview', 'How would you audit all-negative group risk?', 'Track per-prompt success mix, reward spread, and examples where positive advantages are assigned to failed traces.', ['Look only at total batch size.', 'Assume group normalization removes every bad update.'], 'The audit must inspect group composition because relative normalization can hide absolute failure.'),
  q(96, 'Interview', 'When would you prefer PRM-style feedback over ORM-only scoring?', 'When intermediate validity matters and final answers can hide flawed, brittle, or accidental reasoning.', ['When you only need to know whether a final string exactly matches.', 'When step annotations are impossible but final verification is perfect.'], 'PRMs are most valuable when credit assignment inside the trace affects reliability.'),
  q(97, 'Interview', 'How would you evaluate a distilled reasoning student?', 'Measure held-out accuracy, trace quality, length, format compliance, and gaps on harder tasks beyond teacher-style imitation.', ['Check only whether it prints thinking tags.', 'Assume teacher data makes capacity irrelevant.'], 'The student may mimic style, so evaluation must separate format from actual problem-solving generalization.'),
  q(98, 'Interview', 'What safeguards would you name before deploying an RLVR-trained reasoning model?', 'Independent evals, verifier-gap tests, reward-hacking audits, KL and length monitoring, and fallback behavior for unsupported tasks.', ['Only maximizing training reward.', 'Only increasing group size at serving time.'], 'Deployment needs behavioral evidence that the optimized objective matches real user and safety requirements.'),
  q(99, 'Interview', 'How would you explain the lesson misconception to another engineer?', 'Reasoning gains are not just from showing traces; they come from sampling, verification, relative updates, curation, and guardrails.', ['The model simply memorizes one chain-of-thought template for every problem.', 'Formatting tags alone are equivalent to verifier-guided search.'], 'The misconception hides the active policy-optimization loop that changes which traces the model prefers.'),
  q(100, 'Interview', 'What final assessment answer shows production readiness for this lesson?', 'Design verifier-aligned rewards, inspect sampled traces, test group contrast, control drift and length, curate data, and validate on hidden tasks.', ['Tune a single reward knob until the training curve rises.', 'Deploy after one correct formatted example.'], 'A senior answer combines mechanism, failure-mode awareness, and evaluation discipline.'),
]);

export const REASONING_RLVR_GRPO_QUIZ = Object.freeze(SPECS);
